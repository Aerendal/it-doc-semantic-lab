---
layer: W0
title: "Warstwa 0 — Doc Audit Module"
phase: 0
status: implemented
docs_version: 1.0.0
tags: [gap_detector, duplicate_detector, relation_mapper, doc_auditor, ARCH-01, SEC-01, linter, raport_luk]
---

# Warstwa 0 — Doc Audit Module

## Przegląd

Warstwa 0 jest **jedyną już w pełni zaimplementowaną warstwą** (`scripts/nlp/`, 8 plików, ~900 linii, 89 testów).
Pełni rolę narzędzia audytującego dokumentację projektową: wykrywa luki, duplikaty semantyczne, relacje między dokumentami i generuje raporty.

Szczegółowa dokumentacja implementacji: [`DOC_AUDIT_MODULE.md`](../DOC_AUDIT_MODULE.md)

## Uzasadnienie istnienia warstwy

**Dlaczego ta warstwa jest potrzebna:**
W0 istnieje bo dokumentacja projektowa degraduje się wraz z rozwojem systemu — pojawiają się luki (brakujące sekcje), duplikaty (ta sama informacja w dwóch dokumentach, czasem sprzeczna) i zerwane relacje (dokument A powołuje się na B, ale B nie istnieje lub nie spełnia już kontraktu). Bez automatycznego audytu te defekty są wykrywane dopiero gdy deweloper implementuje coś na podstawie niekompletnej specyfikacji — co w projekcie zarobkowym przekłada się bezpośrednio na kary umowne.

W0 musi działać PRZED każdą warstwą implementacyjną (W1–W8) **po to żeby** deweloper wiedział czy spec jest kompletna przed napisaniem linii kodu. Działa też jako warstwa monitoringu ciągłego — każda zmiana dokumentu triggeruje re-audit.

**Co się sypie bez tej warstwy:**
- Luki w specyfikacji wychodzą na jaw podczas code review lub produkcji zamiast na etapie planowania
- Duplikaty powodują rozbieżność między dokumentami bez żadnego alarmu — implementacja może podążać za złą wersją

**Zależności:**
- Wchodzi: pliki `.md`, `.txt`, `.pdf` — struktura dokumentacyjna projektu
- Wychodzi do W8: `{doc_class, validation_mode}` — tryb walidacji dla `AuditEngine`
- Wychodzi do dewelopera: `GapReport`, `DuplicateReport`, `RelationGraph`

## Diagram przepływu danych

```
Pliki .md / .txt
       │
       ▼
  text_utils.py           ← normalizacja PL, stemming, shingles
       │
       ▼
  similarity_engine.py    ← TF-IDF + cosine (stdlib)
       │
  ┌────┴────┐
  ▼         ▼
gap_detector  duplicate_detector  relation_mapper
  │                    │                │
  └────────────────────┴────────────────┘
                        │
                        ▼
               doc_auditor.py (orchestrator)
                        │
                   SQLite (ddl_audit.sql)
                        │
                   CLI: scan / report / list-runs
```

## Pytania źródłowe — sklasyfikowane

### 1. Architektura
- Pokaż jak zdefiniować twarde granice modułów używając import-linter..
- Jakie są granice (boundaries) modułu doc_audit — co jest jego odpowiedzialnością a co leży poza nim?
- Jaki wzorzec projektowy najlepiej opisuje doc_audit — Facade, Strategy, czy Visitor?
- Jak doc_audit komunikuje się z pozostałymi warstwami — wywołania synchroniczne, zdarzenia, czy kolejki?
- Jakie są zależności zewnętrzne doc_audit — czy zależy od W1 (tokenizacja) czy działa na surowym tekście?
- Jak wygląda diagram komponentów doc_audit z podziałem na GapAnalyzer, DuplicateDetector, RelationMapper?

### 2. Kontrakty danych
_brak pytań źródłowych w tej kategorii_
- Jaki jest format wejściowy dokumentu do audytu — plik tekstowy, JSON z metadanymi, czy HTTP POST z body?
- Jaki jest format wyjściowy raportu luk — JSON, Markdown, CSV, czy wszystkie trzy?
- Jak zdefiniować kontrakt dla pola confidence w raporcie duplikatu — float 0.0–1.0 czy enum (low/medium/high)?
- Jakie pola są obowiązkowe w metadanych dokumentu przekazywanego do audytu (id, tytuł, wersja, data)?
- Jak wyglądają przykładowe dane wejściowe i wyjściowe audytu w formacie JSON — pokaż schemat z polami wymaganymi?
- Jakie pola YAML front matter są obowiązkowe vs. opcjonalne dla plików dokumentacji warstwy W_x (layer, title, status, tags, version)?
- Jak zwalidować że pole `layer` w YAML front matter odpowiada jednej z zdefiniowanych wartości W0–W8?
- Jak parsować YAML front matter w Pythonie (ruamel.yaml) bez modyfikowania reszty pliku Markdown?
- Jak wyeksportować tagi YAML front matter jako właściwości węzła dokumentu w grafie Neo4j?

### 3. Implementacja
- Jakie reguły ARCH-01 i SEC-01 sprawdzać w teście własnościowym?
- Jakie konkretne relacje semantyczne uwzględnić w grafie dla ARCH-01?
- Jak połączyć ontologię z regułami audytowymi dla dokumentacji?
- Jak zdefiniować twarde niezmienniki dla reguły SEC-01?
- Jak zapisać relacje przyczynowe w grafie dla reguły SEC-01?
- Jakie są najczęstsze błędy przy budowaniu ontologii dla ARCH-01?
- Zaprojektuj minimalną ontologię dla dokumentacji..
- Zaprojektuj minimalną ontologię dla analizatora dokumentacji technicznej..
- Pokaż regułę Drools dla luki w dokumentacji..
- Pokaż przykład raportu luk dla audytu dokumentacji..
- Jak zaimplementować regułę wykrywającą brak dowodów wykonania w raporcie?
- Pokaż zapytanie Cypher dla raportu luk.
- Zaimplementujmy Krok 1: regułę SPEECH_ACT do wykrywania słów należy i musi..
- Zdefiniujmy słownik pojęć sieciowych dla Kroku 2 analizy audytowej..
- Jak system rozpozna brak protokołu bezpieczeństwa w grafie komponentu sieciowego?
- Jak zmapować pojęcia techniczne na klasę komponent_sieciowy?
- Czy system obsłuży audyt warunków typu 'Jeśli API, to SSL'?
- Jak zaimplementować parser YAML front matter który toleruje brakujące pola opcjonalne bez rzucania wyjątku?
- Jak zbudować walidator schema YAML front matter oparty o jsonschema lub pydantic?
- Jak otagować pliki Markdown metadanymi YAML — pokaż przykład kompletnego front matter z polami layer, title, status, tags, version?
- Jaki parser YAML front matter wybrać dla W0 — PyYAML, ruamel.yaml, czy biblioteka python-frontmatter?
- Jak automatycznie dodać brakujące YAML metadane do istniejących plików Markdown bez nadpisywania treści?
- Jak walidować obecność obowiązkowych kluczy YAML front matter w doc_auditor.py przed przetwarzaniem dokumentu?
- Jak zaimplementować metodę GapAnalysisGenerator.add_tag_metadata(yaml_tags) — które pola YAML trafiają do sekcji metadata raportu?
- Jak filtrować raport GapAnalysisGenerator po tagach YAML — np. pokaż tylko luki w dokumentach oznaczonych [ARCH-01]?
- Jak zbudować indeks dokumentów po tagach YAML dla szybkiego filtrowania w GapAnalysisGenerator?
- Dodajmy tagi [Component:] i [ARCH-01] do plików Markdown — pokaż skrypt automatycznego dodawania tagów do YAML front matter?
- Jak zdefiniować enum dozwolonych wartości dla tagu [Component:] w schemacie JSON — skąd pobierać listę komponentów?
- Jak zautomatyzować dodawanie tagu [ARCH-01] do dokumentów warstwy W_x które opisują architekturę komponentu?
- Jak walidować że tagi [Component:] w plikach Markdown są spójne z listą komponentów w ARCHITECTURE.md?
- Jak zaprojektować tagi dla różnych stylów dokumentacji — enum: FORMAL (umowy), TECHNICAL (specyfikacje), REGULATORY (regulacje) jako wartości pola `doc_style` w YAML front matter?
- Jak zaprojektować tagi dla różnych branż — pole `industry` w YAML front matter: CIVIL_LIABILITY, FINANCE, CONSTRUCTION, IT jako enum walidowany przez JSON Schema?
- Jak zbudować system wielowymiarowych tagów — kombinacja `doc_style`, `industry`, `layer` pozwala filtrować raport audytu po wielu wymiarach jednocześnie?
- Jak pole `industry` YAML łączy się z branżowymi słownikami terminologicznymi W3 — audyt dokumentu CIVIL_LIABILITY używa słownika prawniczego z `SlowosiecAdapter(domain='LEGAL')`?
- Jak dodać nowe wartości enum industry bez przebudowy walidatora — JSON Schema `$defs/Industry` z `anyOf` zamiast sztywnego enum?
- Jak automatyczny ekstraktor terminologii branżowej zasila walidator W0 — wyekstrahowane terminy → `glossary.json` wczytywany przez LinterEngine do walidacji pola `domain` w YAML front matter?
- Uruchommy walidator Markdown jako dogfooding lintera — uruchom na plikach dokumentacji własnego projektu.
- Jak skonfigurować walidator Markdown (markdownlint, pymarkdown) jako pre-commit hook w CI/CD?
- Jak walidator W0 używa własnych reguł do weryfikacji plików W_x — przykład dogfooding z wynikiem audytu?
- Jaki jest minimalny zestaw reguł markdownlint dla plików W_x — które reguły są wymagane a które opcjonalne?
- Pokaż kompletny przykład GapAnalysisReport w JSON dla dokumentu z 3 lukami, 1 duplikatem i 2 relacjami.
- Jak renderować raport luk jako Markdown z tabelą naruszeń posortowaną po severity (CRITICAL → INFO)?
- Jakie sekcje zawiera raport luk — Executive Summary, Gaps, Duplicates, Relations, Recommendations?
- Co zawiera nagłówek raportu luk — document_id, audited_at, auditor_version, total_findings, pass/fail status?
- Uruchommy skrypt audytujący wszystkie pliki Markdown w katalogu docs/ — pokaż wywołanie z argumentami ścieżki i wyjścia.
- Jak skonfigurować skrypt audytu do rekursywnego przeszukiwania podkatalogów i filtrowania plików po *.md?
- Jak uruchomić skrypt audytujący w trybie CI/CD — exit code 0 gdy 0 błędów krytycznych, exit code 1 gdy są luki?
- Jak zbatchować wyniki audytu wielu plików Markdown do jednego zbiorczego raportu JSON?
- Jak zparallelizować audyt wielu plików Markdown — multiprocessing.Pool czy async/await?
- Jak zautomatyzować przypisanie klasy dokumentu na podstawie metadanych YAML — skrypt batch który klasyfikuje per plik po polach layer/tags?
- Stwórzmy klasę DocumentClassifier do automatycznej kategoryzacji plików — jak DocumentClassifier.classify(file_path) → DocumentType?
- Jak DocumentClassifier różni się od KlasyfikatorKontekstu — DocumentClassifier operuje na pliku/metadanych, KlasyfikatorKontekstu na zawartości semantycznej?
- Jak DocumentClassifier integruje się z Linterem W0 — klasyfikacja przed audytem aby wybrać odpowiedni zestaw reguł?
- Jak DocumentClassifier jako Krok 0 zwiększa trafność audytu W0 — klasyfikacja dokumentu przed Linterem umożliwia użycie właściwego zestawu reguł per typ dokumentu?
- Jak sequencjonować Krok 0 w run_dogfooding.py — wywołaj DocumentClassifier.classify(file) przed LinterEngine.audit(file) dla każdego pliku?
- Jak zdefiniować regułę Lintera sprawdzającą autoryzację w grafie Neo4j — `LinterRule(id='AUTH-01', query="MATCH (u:User)-[:PERFORMED]->(e:EventFrame) WHERE NOT (u)-[:HAS_ROLE]->(:Role {name:'authorized'}) RETURN e")` wywoływana przez `LinterEngine.run_cypher_rule(rule, session)` i zgłaszana jako naruszenie gdy Cypher zwraca wyniki?
- Pokaż przykład reguły Lintera dla wymiaru 'Przyczyna i intencja' — `LinterRule(id='CAUSAL-INTENT-01', query="MATCH (e:EventFrame)-[:CAUSES]->(effect) WHERE NOT (e)-[:HAS_SPEECH_ACT]->(:SpeechAct) RETURN e")` wykrywa zdarzenia kauzalne bez przypisanej intencji (brak speech_act); naruszenie wskazuje że SemanticMapper nie wydobył aktu mowy dla przyczyny?
- Jakie reguły Lintera można nałożyć na graf Neo4j — katalog: AUTH-01 (brak autoryzacji aktora), CAUSAL-INTENT-01 (przyczyna bez speech_act), ORPHAN-01 (EventFrame bez krawędzi), SYNSET-01 (predykat bez :HAS_SYNSET), ROLE-01 (EventFrame bez AGENT); każda reguła to Cypher MATCH zwracający węzły naruszające warunek?
- Zaimplementujmy Silnik Reguł Lintera jako moduł — `class LinterRuleEngine: def __init__(self, rules: List[LinterRule], session): self._rules = rules; self._session = session` + metoda `run_all() → List[Violation]` iteruje `self._rules`, wywołuje `run_cypher_rule(rule)` dla każdej i zbiera naruszenia do jednej listy `[Violation(rule_id, node_id) for row in result]`?
- Zaprojektujmy regułę Lintera wykrywającą brak autoryzacji w API — reguła AUTH-01 rozszerzona o sprawdzenie wywołań API: `LinterRule(id='API-AUTH-01', query="MATCH (e:EventFrame {domain:'API'})-[:PERFORMED_BY]->(a:Actor) WHERE NOT (a)-[:HAS_TOKEN]->(:AuthToken {valid:True}) RETURN e, a")`; naruszenie = EventFrame API bez aktora z ważnym tokenem; uzupełnia AUTH-01 (Neo4j roles) o wymiar API-level?
- Pokaż wdrożenie `ApiAuthorizationRule` w fazie Green — `class ApiAuthorizationRule(LinterRule): query = "MATCH (e:EventFrame {domain:'API'}) WHERE NOT (e)-[:AUTHORIZED_BY]->(:AuthToken) RETURN e"`; `run_cypher_rule(self, session)` → `[Violation('API-AUTH-01', row['e']['id']) for row in session.run(self.query)]`; test GREEN: mock zwraca 0 wyników → `violations == []`?
- Zdefiniujmy regułę Lintera dla procedur w grafie — `LinterRule(id='PROC-01', query="MATCH (e:EventFrame {domain:'PROCEDURE'}) WHERE NOT (e)-[:HAS_ROLE {role:'RESPONSIBLE_PARTY'}]->(:Actor) RETURN e")`; wykrywa procedury bez przypisanej strony odpowiedzialnej; analogia do ROLE-01 (brak AGENT) ale ograniczona do węzłów `domain='PROCEDURE'`?
- Jak zintegrować `CauseAndIntentionRule` z nowym modelem relacji Neo4j — `class CauseAndIntentionRule(LinterRule): id='CAUSAL-INTENT-01'`; `query = "MATCH (e:EventFrame)-[:CAUSES]->(eff) WHERE NOT (e)-[:HAS_SPEECH_ACT]->(:SpeechAct) RETURN e"`; nowość: rozszerzyć o `AND NOT (e)-[:HAS_ROLE {role:'AGENT'}]->()` żeby filtrować tylko zdarzenia z kompletnym modelem; integracja z `LinterRuleEngine.run_all()` → `Violation(rule_id, node_id)`?
- Pokaż regułę `ApiAuthorizationRule` wykrywającą luki w grafie Neo4j — poza podstawowym `AUTHORIZED_BY` dodaj detekcję luk: `query = "MATCH (e:EventFrame {domain:'API'}) WHERE NOT (e)-[:AUTHORIZED_BY]->(:AuthToken) OR (e)-[:AUTHORIZED_BY]->(:AuthToken {expired:True}) RETURN e"`; reguła wykrywa oba warianty luki: brak tokenu I wygasły token w jednym MATCH?
- Jak przetestować `ApiAuthorizationRule` na grafie z mieszanymi tokenami — `def test_api_auth_detects_expired_token(): mock_session.run.return_value=[{'e':{'id':'e_expired'}}]; rule = ApiAuthorizationRule(); violations = rule.run_cypher_rule(mock_session); assert violations[0].node_id == 'e_expired'`; scenariusz: token istnieje ale `expired:True` — reguła musi zwrócić naruszenie jak przy braku tokenu?
- Jak zdefiniować regułę `SEC-01` dotyczącą modelu zagrożeń w grafie — `LinterRule(id='SEC-01', query="MATCH (e:EventFrame {domain:'API'}) WHERE NOT (e)-[:HAS_THREAT_MODEL]->(:ThreatModel) RETURN e")`; węzeł `:ThreatModel {stride: ['SPOOFING','TAMPERING'], mitigated: True}` połączony z EventFrame; naruszenie SEC-01 = brak przypisanego modelu zagrożeń do zdarzenia API?
- Pokaż pełną implementację reguły `SEC-01` w grafie — `class SEC01Rule(LinterRule): id='SEC-01'; query=SEC01_QUERY; def run_cypher_rule(self, session): rows=session.run(self.query); return [Violation('SEC-01', r['e']['id'], severity='HIGH') for r in rows]`; `Violation` ma dodatkowe pole `severity` dla priorytetyzacji w raporcie bezpieczeństwa; SEC-01 zawsze `severity='HIGH'`?
- Jakie reguły Cypher dodać do audytu bezpieczeństwa — poza AUTH-01/API-AUTH-01/SEC-01 dodaj: PRIV-01 (`MATCH (a:Actor)-[:PERFORMED]->(e:EventFrame) WHERE a.privilege_level < e.required_privilege RETURN a, e`); DATA-01 (`MATCH (e:EventFrame)-[:PROCESSES]->(:DataAsset {sensitivity:'CONFIDENTIAL'}) WHERE NOT (e)-[:HAS_ENCRYPTION]->() RETURN e`); łącznie 5 reguł bezpieczeństwa w katalogu?
- Jak sformatować raport luk w formacie TEI P5 — TEI P5 (Text Encoding Initiative) to XML dla dokumentów humanistycznych; dla dokumentacji technicznej: `<div type='audit-report'><list type='violations'><item n='AUTH-01'><ref target='#frame_e1'>brak autoryzacji</ref></item></list></div>`; `AuditReportGenerator.generate_tei()` generuje XML zatwierdzony przez `xmlschema` z TEI-all.xsd?
- Wdrożmy `AuditReportGenerator` w fazie Green z CSV — dodaj `generate_csv()`: `import csv; writer = csv.DictWriter(f, fieldnames=['rule_id','node_id','severity'])`; każdy `Violation` jako wiersz; `generate()` dispatch na format: `{'json': generate_json, 'md': generate_markdown, 'csv': generate_csv, 'tei': generate_tei}[fmt](violations, kgt)`?
- Jak rozszerzyć SEC-01 o weryfikację mitygacji — `query = "MATCH (e:EventFrame {domain:'API'})-[:HAS_THREAT_MODEL]->(t:ThreatModel) WHERE t.mitigated=False RETURN e, t.stride"`; SEC-01b wykrywa modele zagrożeń istniejące ale **niezmitigowane**; `Violation` zawiera `details={'unmitigated_stride': t.stride}` — informacja dla inżyniera bezpieczeństwa?
- Jak zintegrować `IntentClassifier` z raportem audytowym — po uruchomieniu `LinterRuleEngine.run_all()` uzupełnij `AuditReport` o pole `intent_stats: Counter({'REQUIREMENT': n, 'ASSERT': m, 'QUESTION': k})`; `AuditReport.generate()` filtruje naruszenia `REQUIREMENT` gdzie brak `AUTH_ROLE` i wypisuje jako sekcję "Wymagania bez autoryzacji" w JSON/Markdown?
- Jak zintegrować KnowledgeGapTracker z modułem Lintera — `LinterEngine.audit(file)` po wykryciu naruszenia wywołuje `tracker.capture_unmatched_rule(rule_id=rule.id, event=frame, doc_id=file.path)`; reguły bez dopasowania w grafie trafiają do kolejki UNMATCHED_RULE w KGT jako kandydaci do nowych reguł DRL?
- Uruchommy skrypt run_dogfooding.py na oznaczonych plikach Markdown — jakie argumenty przyjmuje: --input docs/ --filter-tags [Component:W0] --output report.json?
- Jak run_dogfooding.py filtruje pliki po tagach YAML — parsuje front matter każdego *.md i sprawdza pole tags przed audytem?
- Jak run_dogfooding.py raportuje wyniki per plik — tabela: ścieżka, status (PASS/FAIL), liczba naruszeń, lista rule_id?
- Stwórzmy punkt wejścia python -m audit dla CI/CD — plik `audit/__main__.py` z funkcją main() przyjmującą --input, --output, --format {json,markdown,html}, --exit-on-failure?
- Jak `python -m audit` zwraca exit code 1 w CI/CD gdy są naruszenia BLOCKER — sys.exit(1) gdy len(blockers) > 0?
- Jak upewnić się że audit/__main__.py i endpoint /audit (FastAPI) korzystają z tej samej klasy AuditService — DRY przez wspólną warstwę logiki?

### 4. Testowanie
- Jak zaimplementować testy własnościowe dla reguł ARCH-01 i SEC-01?
- Jak zdefiniować testy własnościowe dla reguł ARCH-01?
- Jak zaimplementować testy własnościowe dla reguły ARCH-01?
- Pokaż przykład tabeli decyzyjnej dla testowania reguł SEC-01..
- Pokaż konkretny przykład testu własnościowego dla reguły ARCH-01..
- Tak, przygotuj kod testu w Hypothesis dla reguły ARCH-01..
- Stwórzmy szkielet testu hypothesis dla reguł ARCH-01 w grafie..
- Pokaż szkielet testu hypothesis dla reguł ARCH-01 i SEC-01..
- Pokaż kod testu z użyciem biblioteki hypothesis dla ARCH-01.
- Zaimplementujmy AuditEngine, aby testy ARCH-01 i SEC-01 przeszły..
- Zdefiniujmy testy dla reguł API-01 oraz DEP-01..
- Pokaż szkielet testu własnościowego ARCH-01 w bibliotece hypothesis..
- Napiszmy test RED dla `ApiAuthorizationRule` w Neo4j — `def test_auth_rule_raises_on_unauthorized(): mock_session.run.return_value = [{'e': EventFrame(id='e1')}]; result = LinterEngine([LinterRule('AUTH-01', QUERY)]).audit(mock_session); assert result.violations[0].rule_id == 'AUTH-01'`; test RED bo `LinterEngine.audit()` jeszcze nie implementuje Cypher rules?
- Napiszmy test RED dla Silnika Reguł Lintera jako całości — `def test_rule_engine_runs_all_rules(): engine = LinterRuleEngine([ORPHAN_RULE, AUTH_RULE], mock_session); violations = engine.run_all(); assert len(violations) == 2 and {v.rule_id for v in violations} == {'ORPHAN-01','AUTH-01'}`; test RED bo `run_all()` niezaimplementowane?
- Pokaż testy RED dla `LinterRuleEngine` z warunkami brzegowymi — `def test_run_all_returns_empty_when_no_violations(): mock_session.run.return_value = []; violations = LinterRuleEngine([AUTH_RULE], mock_session).run_all(); assert violations == []`; osobny RED: `def test_run_all_aggregates_from_multiple_rules(): mock_session.run.side_effect=[[row1],[row2]]; assert len(LinterRuleEngine([R1,R2], mock_session).run_all()) == 2`?
- Pokażmy czerwoną fazę testu dla Silnika Reguł Lintera — `def test_rules_engine_violation_includes_rule_id(): mock_session.run.return_value=[{'e':{'id':'e99'}}]; engine = LinterRuleEngine([CAUSAL_INTENT_RULE], mock_session); v = engine.run_all()[0]; assert v.rule_id == 'CAUSAL-INTENT-01' and v.node_id == 'e99'`; test RED bo `Violation.rule_id` i `Violation.node_id` niezdefiniowane na dataclass?
- Stwórzmy test RED dla `LinterRuleEngine` (RulesEngine) — `def test_engine_raises_on_invalid_cypher(): engine = LinterRuleEngine([LinterRule('BAD-01', 'NOT VALID CYPHER')], mock_session); mock_session.run.side_effect = CypherSyntaxError('Invalid'); with pytest.raises(LinterRuleError, match='BAD-01'): engine.run_all()`; test RED sprawdza czy silnik opakowuje błędy Cypher w `LinterRuleError` z rule_id?
- Napiszmy czerwony test dla `AuditReportGenerator` — `def test_report_contains_violations_section(): gen = AuditReportGenerator(); report = gen.generate(violations=[Violation('AUTH-01','e1')]); assert 'AUTH-01' in report['violations'] and report['summary']['total'] == 1`; test RED bo `AuditReportGenerator` i `report['violations']` niezaimplementowane?
- Napiszmy RED test `AuditReportGenerator` dla fazy Refactor — `def test_report_serializes_to_markdown(): gen = AuditReportGenerator(); md = gen.generate_markdown(violations=[Violation('AUTH-01','e1')], kgt=None); assert '| AUTH-01 |' in md and '## Raport audytowy' in md`; test RED przed dodaniem `generate_markdown()` do klasy?
- Zbudujmy test dla `CrossReferenceEngine` sprawdzający błędy kaskadowe — `def test_cascade_violation_propagates(): engine = CrossReferenceEngine([doc_A, doc_B]); doc_A_violation = Violation('SEC-01','api_frame'); violations = engine.check_cascade(doc_A_violation); assert any(v.source_doc == 'doc_B' for v in violations)`; naruszenie SEC-01 w doc_A propaguje do wszystkich dokumentów odwołujących się do tego samego węzła?
- Zdefiniujmy RED test dla `CrossReferenceEngine` (Silnik Zależności) bez kaskady — `def test_cross_ref_no_cascade_when_isolated(): engine = CrossReferenceEngine([doc_A, doc_B_isolated]); violations = engine.check_cascade(Violation('AUTH-01','node_x')); assert violations == []`; doc_B nie odwołuje się do `node_x` → brak propagacji; RED bo `check_cascade()` jeszcze nie sprawdza izolacji węzła?
- Dodajmy test RED dla reguły `SEC-01` dotyczącej modelu zagrożeń — `def test_sec01_raises_when_no_threat_model(): mock_session.run.return_value=[{'e':{'id':'api_e1'}}]; rule = LinterRule('SEC-01', SEC01_QUERY); violations = rule.run_cypher_rule(mock_session); assert violations[0].rule_id == 'SEC-01' and violations[0].node_id == 'api_e1'`; test RED przed implementacją `run_cypher_rule()`?
- Jak naprawić `ModuleNotFoundError` w testach `AuditReportGenerator` — błąd `ModuleNotFoundError: No module named 'audit_report_generator'` wskazuje brak `__init__.py` lub błędną ścieżkę importu; fix: dodaj `from compliance.audit.report_generator import AuditReportGenerator` lub upewnij się że `src/` jest w `PYTHONPATH`; wzorzec: `conftest.py` z `sys.path.insert(0, str(Path(__file__).parent.parent / 'src'))`?
- Jak zaimplementować `AuditReportGenerator` w fazie Green — minimalna klasa: `class AuditReportGenerator: def __init__(self, kgt=None): self._kgt=kgt; def generate(self, violations): return {'violations': {v.rule_id: v.node_id for v in violations}, 'knowledge_gaps': [{'word':g.word} for g in self._kgt.get_ign_gaps()] if self._kgt else [], 'summary': {'total': len(violations)}}`; oba testy RED z B38 przechodzą?
- Jak połączyć błędy z Neo4j i `KnowledgeGapTracker` w jednym raporcie — `AuditReportGenerator.generate(violations=neo4j_violations, session=neo4j_session)` wewnętrznie: 1) `linter.run_all(session)` → `violations`; 2) `kgt.dump_jsonl()` → `knowledge_gaps`; 3) merge: `{'violations': [...], 'knowledge_gaps': [...], 'summary': {'neo4j_violations': N, 'kgt_gaps': M}}`; jednorodny JSON dla downstream raportowania?
- Wdrożmy `AuditReportGenerator` z serializacją — dodaj `generate_markdown(violations, kgt)`: nagłówek `## Raport audytowy`; sekcja `### Naruszenia` tabela `| rule_id | node_id |`; sekcja `### Luki wiedzy` tabela `| słowo | count |`; `generate_json()` jako alias do `generate()`; interfejs zgodny z `run_dogfooding.py --output report.md`?
- Jakie formaty raportów powinien obsługiwać `AuditReportGenerator` — minimum: JSON (`generate_json()`), Markdown (`generate_markdown()`); opcjonalnie CSV (`generate_csv()`) dla import do Excela/JIRA; wybór formatu przez `--output-format {json,md,csv}` w `run_dogfooding.py`; każdy format jako osobna strategia (Strategy pattern) niezależnie testowalna?
- Jak zintegrować wyniki `KnowledgeGapTracker` w końcowym raporcie — `kgt.get_summary()` zwraca `{'total_gaps': N, 'top_missing': [('słowo', count), ...], 'by_type': {'MISSING_SYNSET': n, 'UNMATCHED_RULE': m}}`; `AuditReportGenerator` embeduje `summary` KGT jako sekcję `### Podsumowanie luk` w końcowym raporcie; umożliwia priorytetyzację prac nad uzupełnieniem wiedzy?
- Stwórz RED test integracji `KnowledgeGapTracker` z `AuditReportGenerator` — `def test_kgt_gaps_appear_in_audit_report(): kgt = KnowledgeGapTracker(); kgt.capture_ign('kompilacja', context=['doc_A']); gen = AuditReportGenerator(kgt=kgt); report = gen.generate(violations=[]); assert report['knowledge_gaps'][0]['word'] == 'kompilacja'`; test RED bo `AuditReportGenerator(kgt=...)` nie przyjmuje KGT?
- Jak przetestować że `AuditReportGenerator` sortuje luki KGT po częstości — `def test_kgt_gaps_sorted_by_frequency(): kgt = KnowledgeGapTracker(); [kgt.capture_ign('API', context=[f'd{i}']) for i in range(3)]; kgt.capture_ign('moduł', context=['d0']); report = AuditReportGenerator(kgt=kgt).generate(violations=[]); assert report['knowledge_gaps'][0]['word'] == 'API'`; sortowanie `by=count desc` priorytetyzuje luki do uzupełnienia?
- Pokaż czerwony test dla `ApiAuthorizationRule` krok po kroku — `@pytest.fixture def unauth_session(): s = Mock(); s.run.return_value=[{'e':{'id':'frame_1'}}]; return s`; `def test_api_auth_unauthorized(unauth_session): rule = ApiAuthorizationRule(); violations = rule.run_cypher_rule(unauth_session); assert violations == [Violation('API-AUTH-01','frame_1')]`; RED bo `ApiAuthorizationRule` niezaimplementowane?
- Pokaż szkielet testu ARCH-01 w bibliotece hypothesis..
- Jak napisać regułę w AuditEngine, aby zaliczyć test ARCH-01?
- Jakie reguły audytowe ARCH-01 dodać, by testy własnościowe przeszły?
- Pokaż jak zaimplementować testy architektury za pomocą import-linter..
- Pokaż przykład reguły wyłapującej brak testów integracyjnych w API..
- Jak wdrożyć regułę luki dla brakującego testu integracyjnego?
- Pokaż zapytanie Cypher wykrywające brak testów dla komponentów zewnętrznych..

### 5. Obsługa błędów
_brak pytań źródłowych w tej kategorii_
- Co zwrócić gdy dokument wejściowy jest pusty lub zawiera tylko whitespace?
- Jak logować błędy parsowania dokumentu bez ujawniania jego treści w logach systemowych?
- Jak obsłużyć przekroczenie limitu czasu audytu dla bardzo długiego dokumentu (>100 MB)?
- Co się dzieje gdy plik dokumentu jest uszkodzony (truncated) w połowie analizy?
- Jak obsłużyć błąd kodowania znaków (nie-UTF8) w pliku dokumentu wejściowego?
- Jak obsłużyć błąd broken link w raporcie Lintera — oflagnuj, pomiń plik, czy zatrzymaj audyt?
- Jak agregować błędy Lintera per plik do zbiorczego raportu bez duplikowania wpisów?

### 6. Integracja z innymi warstwami
- Pokaż jak zaimplementować AuditEngine do obsługi tych reguł..
- Pokaż przykład reguły w silniku AuditEngine dla ARCH-01.
- Pokaż przykład reguły w silniku AuditEngine..
- Pokaż przykład reguły w silniku AuditEngine dla weryfikacji ARCH-01..
- Zintegrujmy pełny pipeline: od XML NKJP do raportu luk.
- Jak zdefiniować klasę GapAnalysisReport — pola: document_id, gaps, duplicates, relations, generated_at, severity_summary?
- Jak pipeline NKJP → GapAnalysisReport obsługuje przypadek gdy plik XML NKJP jest niekompletny lub uszkodzony?
- Jak serializować GapAnalysisReport do JSON i Markdown jednocześnie z jednej klasy modelu?
- Jak NKJPPipeline przekazuje przetworzone tokeny do GapAnalyzer który buduje instancję GapAnalysisReport?
- Jak GapAnalysisGenerator przekazuje YAML tagi do W4 (baza grafowa) jako właściwości węzłów dokumentów?
- Jak weryfikować spójność tagów YAML między dokumentami — np. czy wszystkie W_x pliki mają tag `layer`?
- Pokaż jak zintegrować Słowosieć z Linterem W0 — W0 odpytuje W3 o synonimy terminu przed flagowaniem duplikatu.
- Jak Linter W0 używa synsetów z W3 aby odróżnić synonim (ten sam sens) od homonimii (różny sens)?
- Jak skonfigurować próg podobieństwa semantycznego (Jaccard na synsetach) dla detekcji duplikatów w Linterze?
- Jak Linter W0 obsługuje przypadek gdy W3 (Słowosieć) jest niedostępne — fallback do detekcji leksykalnej?
- Zaktualizujmy GapAnalysisGenerator o zapytania Cypher dla Neo4j — jak generator pobiera dane o lukach bezpośrednio z grafu?
- Jak GapAnalysisGenerator buduje zapytanie Cypher MATCH (d:Document)-[:HAS_GAP]->(g:Gap) RETURN g per document_id?
- Jak zdefiniować kontrakt między W0 (GapAnalysisGenerator) a W4 (Neo4j) — REST API vs. neo4j-driver Python?
- Zintegrujmy audit_cli.py z automatyzacją GitHub Actions — jakie flagi CLI obsługuje: --exit-code-on-failure, --output-format json, --input docs/?
- Jak audit_cli.py przekazuje exit code do GitHub Actions — exit(1) gdy są naruszenia BLOCKER, exit(0) gdy tylko WARNING/INFO?
- Jak przechowywać raport audit_cli.py jako artefakt GitHub Actions — krok `uses: actions/upload-artifact@v4` z plikiem report.json?
- Stwórzmy gotowy plik .github/workflows/audit.yml — jakie kroki zawiera: checkout, setup-python, pip install, run audit_cli.py --exit-code-on-failure 1?
- Jak skonfigurować wyzwalacze w audit.yml — on: push (branches: [main, fresh-main]) + on: pull_request do dowolnej gałęzi?
- Jak dodać komentarz do PR z podsumowaniem raportu audit_cli.py — GitHub Actions step z `gh pr comment` parsujący wygenerowany report.json?
- Jak skonfigurować automatyczne blokowanie Pull Requestów przy błędach audytu — branch protection rule: required status check `audit / run-audit` musi przejść?
- Jak zdefiniować job name w audit.yml tak aby pasował do required status check w ustawieniach repozytorium branch protection?
- Jak zapobiec mergowaniu PR gdy audit_cli.py zwraca exit(1) — required_status_checks.strict: true w branch protection zabezpiecza przed mergem?
- Jak zbudować kompletną strukturę audit.yml — job `run-audit` z krokami: checkout@v4, setup-python@v5, pip install -r requirements.txt, python audit_cli.py --input docs/ --exit-code-on-failure 1?
- Jak parametryzować audit.yml przez workflow_dispatch inputs — manualne uruchomienie z parametrem --filter-tags dla selektywnego audytu?
- Jak cachować zależności pip w audit.yml — actions/cache@v4 z kluczem opartym na hashFiles('requirements.txt') dla szybszego CI?

### 7. Pułapki i ryzyka
_brak pytań źródłowych w tej kategorii_
- Jak uniknąć fałszywych duplikatów gdy dwa dokumenty opisują ten sam temat z różnych perspektyw (różny zakres, nie ten sam tekst)?
- Co się dzieje gdy audyt wykryje lukę w dokumencie już zaakceptowanym przez klienta — jaka jest procedura powiadomienia?
- Jak zdefiniować próg podobieństwa (threshold) shingle/Jaccard tak aby nie flagować parafrazy jako duplikatu?
- Jakie są konsekwencje błędnego oznaczenia dokumentu jako kompletny gdy brakuje sekcji — kto ponosi odpowiedzialność?
- Czy moduł audytu może operować na niespójnej wersji dokumentu przy równoległym edytowaniu (race condition)?
- Jak obsłużyć dokument w formacie binarnym (PDF, DOCX) gdy audyt oczekuje płaskiego tekstu?
- Co oznacza 0 luk dla dokumentu o złożoności 500+ zdań — czy to sygnał błędu czy rzeczywistego stanu kompletności?
- Jakie kategorie błędów Markdown wykrywa Linter — broken links, brak front matter, zduplikowane nagłówki, malformed code block?
- Jak sklasyfikować błędy Lintera według krytyczności — BLOCKER (brak front matter), WARNING (brakujące pole), INFO (formatowanie)?
- Jak odróżnić błąd struktury Markdown od błędu merytorycznego (brakująca sekcja) w raporcie Lintera?
- Jak obsłużyć plik Markdown który nie jest dokumentem W_x (np. README.md) — pominąć czy audytować innym zestawem reguł?

## Pytania uzupełniające
- **Pułapka 3:** `completeness_score` logarytmiczny może maskować wiele małych błędów — dokument z 20 ostrzeżeniami i 0 błędami krytycznych dostanie wynik ~0.60, co wygląda jak "akceptowalny", choć nie jest.
- **Pułapka 4:** `duplicate_detector` porównuje dokumenty przez TF-IDF bez lematyzacji — "wymagania" i "wymaganie" to różne tokeny, więc duplikat może nie zostać wykryty.
- **Pułapka 5:** SQLite jako backend audytu jest single-writer — równoległe uruchomienia `doc_auditor.py` na tym samym pliku `.db` mogą prowadzić do `database is locked`.
- **Pułapka 6:** Reguły ARCH-01, SEC-01 są zdefiniowane per-projekt — bez wersjonowania zbioru reguł wyniki audytu dla tego samego dokumentu mogą być różne w różnych datach.

### 1. Architektura

- Jak `doc_auditor.py` dzieli odpowiedzialność z resztą projektu — co jest w `scripts/nlp/`, a co w `scripts/compliance_check.py`?
- Czy `DocAuditor` powinien działać jako serwis ciągły (daemon) czy wywoływany każdorazowo z CLI?
- Jak zdefiniować granicę modułu W0 wobec W8 (AuditEngine)? Co należy do W0, a co do W8?
- Jakie są niezmienniki architektury, których W0 nie może naruszać (import-linter)?
- Jak rozbudować `doc_auditor.py` o obsługę wielojęzycznych dokumentów?

### 2. Kontrakty danych

- Jaki jest dokładny schemat JSON wyjścia z `gap_detector.py` — które pola są obowiązkowe?
- Jak zdefiniować formalny schemat JSON Schema dla wyników audytu (gaps, duplicates, relations)?
- Jaka jest struktura rekordu SQLite w tabeli `audit_runs` i `gaps`?
- Jak kontrakt danych między `similarity_engine.py` a `duplicate_detector.py` jest zawarty w typach Pythona?
- Jakie formaty wejściowe W0 akceptuje (.md, .txt, .rst, .adoc)?

### 3. Implementacja

- Jak rozbudować `gap_detector.py` o nowe typy dokumentów (poza 9 istniejącymi szablonami)?
- Jak dostroić progi podobieństwa w `duplicate_detector.py` (exact/extending/thematic/partial)?
- Jak zaimplementować regułę ARCH-01 i SEC-01 jako formalne predykaty w `doc_auditor.py`?
- Jak `LinterRuleEngine` wykrywa błędy kaskadowe przy braku backupu — reguła `BACKUP-01` (`MATCH (db:Database) WHERE NOT (db)-[:HAS_BACKUP]->(:BackupPolicy) RETURN db`) uruchamia `CrossReferenceEngine.check_cascade(violation)` i zbiera wszystkie dokumenty referujące `db`; błąd kaskadowy = każdy dokument opisujący odtwarzanie bez potwierdzenia polityki backupu; `Violation(rule_id='BACKUP-01', node_id=db_id, severity='CRITICAL')`?
- Jak `CrossReferenceEngine` wykrywa konflikty między regułami — dwie reguły `r1, r2` są w konflikcie gdy ich Cypher MATCH pokrywa ten sam węzeł i generują sprzeczne `Violation.severity`: `class RuleConflict(NamedTuple): rule_a, rule_b, node_id, conflict_type`; `conflict_type ∈ {'DOUBLE_VIOLATION', 'SEVERITY_MISMATCH', 'MUTUAL_EXCLUSION'}`; `CrossReferenceEngine.detect_conflicts(violations_a, violations_b)→List[RuleConflict]`?
- Jak wdrożyć regułę SEC-01 w grafie Neo4j krok po kroku — (1) `MERGE (:ThreatModel {id:'tm_api', stride:['SPOOFING'], mitigated:True})` tworzy węzeł; (2) `MATCH (e:EventFrame {domain:'API'}) MERGE (e)-[:HAS_THREAT_MODEL]->(tm)` tworzy krawędź; (3) `SEC01Rule.run_cypher_rule(session)` wyszukuje EventFrame bez tej krawędzi; (4) wynik: `Violation('SEC-01', e_id, severity='HIGH')`; `:ThreatModel` bez pola `mitigated` domyślnie traktowany jako `mitigated=False` → naruszenie SEC-01b?
- Zaimplementujmy regułę `SEC-01` jako klasę — `class SEC01Rule(LinterRule): id='SEC-01'; QUERY = "MATCH (e:EventFrame {domain:'API'}) WHERE NOT (e)-[:HAS_THREAT_MODEL]->(:ThreatModel) RETURN e.id AS node_id"; def run_cypher_rule(self, session): return [Violation(self.id, row['node_id'], severity='HIGH') for row in session.run(self.QUERY)]`; klasa dziedziczy po `LinterRule` z metodą abstrakcyjną `run_cypher_rule(session)`; rejestracja w `LinterRuleEngine` przez `rules=[SEC01Rule()]`?
- Jak `CrossReferenceEngine` łączy błędy z wielu domen — każdy `Violation` ma pole `domain: str`; `CrossReferenceEngine.aggregate_by_domain(violations)→Dict[str, List[Violation]]` grupuje błędy; `aggregate_cross_domain(violations)→List[CrossDomainIssue]` wykrywa węzły naruszające reguły z co najmniej 2 różnych domen (np. `node_x` ma AUTH-01 z domeny `API` i ORPHAN-01 z domeny `GRAPH`); `CrossDomainIssue(node_id, domains, violations)` jako sygnał krytyczny?
- Jak wdrożyć regułę `TEST-01` dla weryfikacji planu testów — `TEST-01`: `MATCH (e:EventFrame {domain:'SYSTEM'}) WHERE NOT (e)-[:HAS_TEST_PLAN]->(:TestPlan {status:'APPROVED'}) RETURN e`; węzeł `:TestPlan {id, status: 'DRAFT'|'APPROVED', coverage_pct: float}`; naruszenie TEST-01 gdy system nie ma zatwierdzonego planu testów; `Violation('TEST-01', e_id, severity='MEDIUM')` — 7. reguła w katalogu; otwiera nową domenę SYSTEM dla Lintera?
- Napiszmy fazę Green dla `CrossReferenceEngine` — minimalna implementacja: `class CrossReferenceEngine: def __init__(self, docs): self.docs=docs; def check_cascade(self, violation): return [Violation(violation.rule_id, violation.node_id, source_doc=d) for d in self.docs if d.references(violation.node_id) and d != violation.source_doc]`; `doc.references(node_id)→bool` jako metoda kontraktu dokumentu; `aggregate_by_domain(violations)→defaultdict(list)`; wszystkie dotychczasowe testy (cascade/isolation/CrossDomainIssue) powinny przejść GREEN?
- Jak `CrossReferenceEngine` zarządza kaskadowymi błędami w grafie — kolejność propagacji: (1) `run_all()→violations`; (2) dla każdego `v`: `cascade=engine.check_cascade(v)→list`; (3) `detect_conflicts(violations, cascade)→conflicts`; (4) `aggregate_cross_domain(violations+cascade)→critical`; błędy CRITICAL (cross-domain) są raportowane przed HIGH i MEDIUM w `AuditReportGenerator`; strategia `depth_limit=3` zapobiega nieskończonej rekurencji?
- Jakie reguły dedukcji kontekstowej powinien implementować CrossReferenceEngine — (1) `TRANSITIVITY`: jeśli `A→B` i `B→C` naruszają tę samą regułę, `A→C` też narusza; (2) `SCOPE_INHERITANCE`: naruszenie reguły w pakiecie dziedziczy do wszystkich klas; (3) `TEMPORAL_ORDER`: naruszenie w kroku N blokuje walidację kroków N+1..M; `DeductionRule(name, apply(violations)→List[Violation])` jako interfejs strategii dedukcji?
- Pokaż przykładowy raport luki dla brakującej przyczyny w grafie — `KnowledgeGapTracker` wykrywa: `{type:'MISSING_CAUSE', predicate:'zobowiązuje', node_id:'e_47', doc_id:'W4', details:'EventFrame bez krawędzi :CAUSES — przyczyna zobowiązania nie zdefiniowana'}`; raport Markdown: `| MISSING_CAUSE | e_47 | W4 | predicate=zobowiązuje |`; priorytet wyższy od MISSING_SYNSET (bo MISSING_CAUSE blokuje wnioskowanie Lintera)?
- Czy `CrossReferenceEngine` może automatycznie budować ontologię dla nowych domen — heurystyka: `OntologyBuilder.infer_domain(violations)→domain_name` na podstawie `rule_id` prefix (SEC→SECURITY, PROC→PROCEDURE, TEST→TESTING); `MERGE (:Domain {name: domain_name}); MERGE (e)-[:BELONGS_TO_DOMAIN]->(d)` uzupełnia węzły bez domeny; nowe domeny SYSTEM/BACKUP trafiają do grafu automatycznie bez ręcznej konfiguracji; ryzyko: fałszywe przypisanie domeny przez prefix matching?
- Napiszmy pełny kod produkcyjny `CrossReferenceEngine` (Green) — `class CrossReferenceEngine: def __init__(self, docs, depth_limit=3): self.docs=docs; self._depth=depth_limit; def check_cascade(self, violation, depth=0): if depth>=self._depth: return []; refs=[Violation(violation.rule_id, violation.node_id, source_doc=d) for d in self.docs if d.references(violation.node_id)]; return refs+[v2 for r in refs for v2 in self.check_cascade(r, depth+1)]; def aggregate_by_domain(self, violations): res=defaultdict(list); [res[v.domain].append(v) for v in violations]; return res; def aggregate_cross_domain(self, violations): by_node=defaultdict(set); [by_node[v.node_id].add(v.domain) for v in violations]; return [CrossDomainIssue(n,ds,[v for v in violations if v.node_id==n]) for n,ds in by_node.items() if len(ds)>=2]`?
- Jak `CrossReferenceEngine` wykorzystuje relacje Słowosieci do dedukcji — `doc.references(node_id)` sprawdza nie tylko bezpośrednie referencje ale też synonimy i hiponimy: `wordnet_lookup(node_id)→{synonyms, hypernyms}`; jeśli `doc` zawiera hiponim węzła naruszającego regułę, naruszenie propaguje też do tego dokumentu; `DeductionRule.TRANSITIVITY` + Słowosieć = dedukcja semantyczna; przykład: naruszenie SEC-01 na `api_auth` propaguje do doc referującego `autoryzacja_api` (synonim w Słowosieci)?
- Pokaż przykład błędu kaskadowego dla wymogu SEC-01 — `EventFrame {id:'e_checkout', domain:'API'}` bez `:HAS_THREAT_MODEL`; `SEC01Rule.run_cypher_rule()→[Violation('SEC-01','e_checkout',severity='HIGH')]`; `CrossReferenceEngine.check_cascade(v)` → `doc_B` (specyfikacja płatności) i `doc_C` (SRS kasy) referencują `e_checkout` → 3 `Violation` łącznie; `aggregate_cross_domain()→CrossDomainIssue('e_checkout', {'API','PAYMENT','SYSTEM'}, 3_violations)` → severity escaluje do CRITICAL?
- Jak `CrossReferenceEngine` ocenia ważność naruszeń SEC-01 — algorytm: `_score_severity(violations)→severity`: 1 domena → HIGH; 2 domeny → CRITICAL; 3+ domeny → CRITICAL+ALERT; `ALERT` = sygnał do natychmiastowego powiadomienia (`notify_hook()`); SEC-01 startuje jako HIGH per węzeł, ale cascade do 2+ domen automatycznie eskaluje; `Violation.escalated: bool` dodatkowe pole do śledzenia czy severity zmienił się przez kaskadę?
- Pokaż implementację `analyze_cascades` dla bazy bez backupu — `def analyze_cascades(self, initial_violations): result = CascadeAnalysis(direct=initial_violations, cascaded=[], critical=[]); for v in initial_violations: cascaded = self.check_cascade(v); result.cascaded.extend(cascaded); cross = self.aggregate_cross_domain(initial_violations+cascaded); result.critical.extend(cross); return result`; `CascadeAnalysis(NamedTuple)` z polami `direct`, `cascaded`, `critical`; dla `Backup01Rule` → `direct=[BACKUP-01/db_prod]` + `cascaded=[doc_DR, doc_SLA]` + `critical=[]` (1 domena = nie CRITICAL)?
- Jakie błędy kaskadowe wykrywa Silnik Zależności w grafie Neo4j — katalog typów kaskad: (1) `SINGLE_NODE_CASCADE`: 1 węzeł narusza N reguł w tej samej domenie; (2) `CROSS_DOMAIN_CASCADE`: 1 węzeł narusza reguły w 2+ domenach → CRITICAL; (3) `TRANSITIVE_CASCADE`: A→B→C przez Słowosieć synonimy; (4) `DEPTH_EXCEEDED_CASCADE`: propagacja zatrzymana przez `depth_limit=3` → ostrzeżenie `CascadeDepthWarning`; każdy typ ma osobny `cascade_type` w `Violation.details`?
- Jak `CrossReferenceEngine` używa sąsiedztwa w grafie do rozwiązywania konfliktów — `_get_neighborhood(node_id, session, hops=2)→List[Node]` pobiera węzły w odległości ≤2 krawędzi; dwa konflikty są rozwiązywalne gdy ich węzły mają wspólnego sąsiada (`SHARED_NEIGHBOR`); `detect_conflicts()` rozszerzone: `conflict.resolution='SHARED_NEIGHBOR'` gdy sąsiedztwo nakłada się; przykład: ORPHAN-01 i AUTH-01 na tym samym `api_frame` sąsiadują z `ThreatModel` → konflikt MUTUAL_EXCLUSION rozwiązany?
- Pokaż implementację `resolve_conflicts` dla przykładu ze słowem "klucz" — homografia: `klucz` może być `INSTRUMENT` (narzędzie) lub `AUTH_TOKEN` (bezpieczeństwo); `resolve_conflicts(violations_a=[AUTH-01/klucz], violations_b=[ROLE-01/klucz])→RuleConflict(conflict_type='HOMOGRAPH_AMBIGUITY', resolution_hint='check domain of klucz node')`; rozwiązanie: `session.run("MATCH (n {id:'klucz'}) RETURN n.domain")` → `domain='SECURITY'` → zachowaj AUTH-01, odrzuć ROLE-01 jako fałszywy pozytyw?
- Jak wdrożyć regułę `OPS-04` sprawdzającą procedury backupu w Neo4j — `OPS-04`: `MATCH (db:Database) WHERE NOT (db)-[:HAS_BACKUP_PROCEDURE]->(:BackupProcedure {tested:True, frequency:'DAILY'}) RETURN db`; węzeł `:BackupProcedure {id, frequency:'DAILY'|'WEEKLY', tested:Bool, last_test_date}`; różnica od `BACKUP-01` (brak polityki): OPS-04 sprawdza czy procedura jest **przetestowana** i **codzienna**; `Violation('OPS-04', db_id, severity='HIGH')` — 8. reguła Lintera, nowa kategoria OPS?
- Jak zaimplementować regułę `OPS-04` jako klasę — `class Ops04Rule(LinterRule): id='OPS-04'; QUERY = "MATCH (db:Database) WHERE NOT (db)-[:HAS_BACKUP_PROCEDURE]->(:BackupProcedure {tested:True, frequency:'DAILY'}) RETURN db.id AS node_id"; def run_cypher_rule(self, session): return [Violation(self.id, row['node_id'], severity='HIGH', domain='OPS') for row in session.run(self.QUERY)]`; GREEN dla testu z B43 `test_backup01_rule_fires_on_db_without_policy` po zmianie klasy z `Backup01Rule` na `Ops04Rule`?
- Jak Słowosieć pomaga odróżnić "klucz szyfrujący" od "klucza płaskiego" — w plWordNet `klucz` ma 3 synsety: `klucz.1` (narzędzie mechaniczne, hiperonimia→INSTRUMENT), `klucz.2` (kryptograficzny, hiperonimia→AUTH_TOKEN, domain=SECURITY), `klucz.3` (muzyczny); `resolve_conflicts()` pobiera `wordnet_lookup('klucz')→{synsets:[…]}`; synset z `domain=SECURITY` → zachowaj AUTH-01; synset z domeną INSTRUMENT → `ROLE-01` → fałszywy pozytyw; Słowosieć jako arbiter semantyczny przed `_get_neighborhood()`?
- Pokaż jak `CrossReferenceEngine` wykorzystuje Słowosieć do rozwiązywania konfliktów krok po kroku — (1) `detect_conflicts(v_a, v_b)` wykrywa `HOMOGRAPH_AMBIGUITY`; (2) `wordnet_lookup(node.lemma)→synsets`; (3) dla każdego synsetu: `synset.domain` → mapuj na domenę reguły (`SECURITY→AUTH-01`, `LOGISTICS→ROLE-01`); (4) zachowaj violation której domena=domena reguły; (5) `RuleConflict.resolved=True, resolution='WORDNET_DOMAIN'`; przepływ: `klucz.2.domain=SECURITY` → `AUTH-01 wins`, `ROLE-01 dismissed`?
- Jak zintegrować Słowosieć z `CrossReferenceEngine` dla lepszej dedukcji — `class WordnetDeductionMixin: def _get_semantic_refs(self, node_id): lemma=self._node_lemma(node_id); syns=wordnet_lookup(lemma); return syns.get('hypernyms',[])+syns.get('synonyms',[])`; `CrossReferenceEngine` dziedziczy mixin i nadpisuje `doc.references()`: `doc.references(node_id) or any(doc.references(s) for s in self._get_semantic_refs(node_id))`; zwiększa recall kaskady kosztem precyzji — parametr `use_wordnet: bool` kontroluje kompromis?
- W jaki sposób `CrossReferenceEngine` wykorzystuje relacje Słowosieci w `check_cascade()` — szczegółowo: (1) `check_cascade(violation)` wywołuje `_get_semantic_refs(violation.node_id)` gdy `use_wordnet=True`; (2) dla każdego synsetu-synonim sprawdza `doc.references(syn)`; (3) jeśli doc referencuje synonim → `Violation(..., source_doc=doc, cascade_type='TRANSITIVE_WORDNET')`; (4) HAS_SYNSET krawędź w Neo4j jest źródłem synonimów; (5) nowy `cascade_type='TRANSITIVE_WORDNET'` vs `cascade_type='TRANSITIVE_CASCADE'` (grafu) — rozróżnienie pozwala filtrować w raporcie?
- Pokaż kod produkcyjny reguły `TEST-01` dla Neo4j — `class Test01Rule(LinterRule): id='TEST-01'; QUERY = "MATCH (e:EventFrame {domain:'SYSTEM'}) WHERE NOT (e)-[:HAS_TEST_PLAN]->(:TestPlan {status:'APPROVED'}) RETURN e.id AS node_id"; def run_cypher_rule(self, session): return [Violation(self.id, row['node_id'], severity='MEDIUM', domain='SYSTEM') for row in session.run(self.QUERY)]`; GREEN dla 5 testów RED z B46/B49: fires/no_violation_approved/draft/medium+domain/query_checks_approved; `'APPROVED'` w QUERY spełnia `test_test01_query_checks_approved_status()`?
- Pokaż jak zintegrować `CrossReferenceEngine` z `AuditReportGenerator` — `generator = AuditReportGenerator(kgt=kgt); engine = CrossReferenceEngine(docs); analysis = engine.analyze_cascades(linter.run_all(session)); all_violations = analysis.direct + analysis.cascaded; report = generator.generate(all_violations); report['cascade_summary'] = {'direct': len(analysis.direct), 'cascaded': len(analysis.cascaded), 'critical': len(analysis.critical)}`; `generate_markdown()` dodaje sekcję `## Błędy kaskadowe` z podziałem direct/cascaded/critical?
- Jak zintegrować `CrossReferenceEngine` z `AuditReportGenerator` rozszerzony wzorzec — `class AuditPipeline: def __init__(self, linter, engine, generator): ...; def run(self, session, docs): direct=linter.run_all(session); analysis=engine.analyze_cascades(direct); conflicts=engine.detect_conflicts(direct, analysis.cascaded); report=generator.generate(analysis.direct+analysis.cascaded); report['conflicts']=[c._asdict() for c in conflicts]; report['cascade_summary']=analysis._asdict_counts(); return report`; `AuditPipeline` jako Facade łącząca LinterRuleEngine+CrossReferenceEngine+AuditReportGenerator w jednym wywołaniu?
- Jak przetestować `AuditPipeline.run()` — `def test_audit_pipeline_run_returns_full_report(): linter=Mock(); linter.run_all.return_value=[Violation('SEC-01','e1',domain='API',severity='HIGH')]; engine=Mock(); engine.analyze_cascades.return_value=CascadeAnalysis(direct=[...],cascaded=[],critical=[]); engine.detect_conflicts.return_value=[]; generator=Mock(); generator.generate.return_value={'violations':{},'summary':{'total':1}}; pipeline=AuditPipeline(linter,engine,generator); report=pipeline.run(session,docs); assert 'cascade_summary' in report; assert 'conflicts' in report`; test weryfikuje że Facade poprawnie scala wyniki wszystkich 3 komponentów?
- Wdróżmy regułę `OPS-04` — rozszerzona implementacja z RTO/RPO: `class Ops04Rule(LinterRule): id='OPS-04'; QUERY = "MATCH (db:Database) WHERE NOT (db)-[:HAS_BACKUP_PROCEDURE]->(:BackupProcedure {tested:True, frequency:'DAILY'}) OR (db)-[:HAS_BACKUP_PROCEDURE]->(bp) WHERE bp.rto_hours > 4 RETURN db.id AS node_id, coalesce(bp.rto_hours, null) AS rto"`; naruszenie gdy brak procedury LUB `rto_hours > 4` dla systemów DORA-critical; `Violation.details={'rto_hours': row['rto']}` przekazuje wartość do raportu?
- Jak `CrossReferenceEngine` wykorzystuje Słowosieć do rozwiązywania wieloznaczności — architektura: `CrossReferenceEngine(docs, wordnet_adapter=WordnetAdapter())` przyjmuje adapter jako dependency injection; `WordnetAdapter.get_synsets(lemma)→List[Synset]`; w `resolve_conflicts()`: dla każdego konfliktu `HOMOGRAPH_AMBIGUITY` → `synsets = wordnet_adapter.get_synsets(node.lemma)` → `best_synset = max(synsets, key=lambda s: similarity(s.domain, violation.domain))`; `similarity()` mierzy odległość domenową (SECURITY↔API=0.9; SECURITY↔INSTRUMENT=0.1); zwycięska reguła = ta z wyższym `similarity`?
- Jak Słowosieć rozwiązuje wieloznaczność w metodzie `resolve_conflicts` — algorytm wyboru synsetu: (1) `wordnet_lookup(lemma)→{synsets}`; (2) dla każdego konfliktu: `score_a = synset.similarity_to_domain(v_a.domain)`; `score_b = synset.similarity_to_domain(v_b.domain)`; (3) `winner = v_a if score_a > score_b else v_b`; (4) `RuleConflict(winner_rule=winner.rule_id, resolved=True, resolution='WORDNET_DOMAIN', confidence=abs(score_a-score_b))`; `confidence < 0.3` → `AMBIGUOUS_RESOLUTION` zamiast `WORDNET_DOMAIN` — wymaga ręcznej weryfikacji?
- Pokaż Green dla `resolve_conflicts` — `def resolve_conflicts(self, conflicts): result = []; for c in conflicts: synsets = self._wordnet.get_synsets(c.node_lemma); score_a = max((s.similarity_to_domain(c.rule_a_domain) for s in synsets), default=0); score_b = max((s.similarity_to_domain(c.rule_b_domain) for s in synsets), default=0); conf = abs(score_a - score_b); winner = c.rule_a if score_a > score_b else c.rule_b; res = 'AMBIGUOUS_RESOLUTION' if conf < 0.3 else 'WORDNET_DOMAIN'; result.append(c._replace(winner_rule=winner, resolved=conf>=0.3, resolution=res, confidence=conf)); return result`; przechodzi test RED z B52 dla `klucz` (SECURITY score=0.9, INSTRUMENT score=0.1 → conf=0.8 > 0.3)?
- Jak zintegrować kaskady z `AuditReportGenerator` przez `cascade_type` — `generate_markdown(violations, kgt)` grupuje naruszenia po `cascade_type`: sekcja `## Naruszenia bezpośrednie` (brak `cascade_type`), `## Kaskady semantyczne` (`TRANSITIVE_WORDNET`), `## Kaskady grafowe` (`TRANSITIVE_CASCADE`, `CROSS_DOMAIN_CASCADE`); każda sekcja z tabelą `|rule_id|node_id|severity|details|`; `generate_json()` zachowuje `cascade_type` jako pole w każdym naruszeniu dla downstream narzędzi?
- Jak dodać obsługę reguły API-01 i DEP-01 do istniejącego schematu SQLite?
- Jak rozbudować `relation_mapper.py` o relację `contradicts` (wykrywanie sprzeczności)?
- Jak dodać tagi YAML front matter do plików Markdown projektu (format: `layer`, `title`, `status`, `tags`)?
- Jak `doc_auditor.py` waliduje obecność i poprawność YAML front matter w plikach dokumentacji?
- Jak zintegrować tagowanie YAML z `GapAnalysisGenerator` — czy YAML tagi trafiają do raportu luk?
- Jak wpiąć `KnowledgeGapTracker` w `AuditPipeline.run()` — uzupełnienie kontraktu Facade: `def run(self, session, docs, kgt=None): direct=self.linter.run_all(session); analysis=self.engine.analyze_cascades(direct); conflicts=self.engine.detect_conflicts(direct, analysis.cascaded); report=self.generator.generate(analysis.direct+analysis.cascaded); report['conflicts']=[c._asdict() for c in conflicts]; report['cascade_summary']=analysis._asdict_counts(); report['knowledge_gaps']=kgt.get_gaps() if kgt else []; return report`; `AuditPipeline` staje się pełnym Facade z 5 kluczami: `violations, cascades, conflicts, knowledge_gaps, summary`; `KnowledgeGapTracker.get_gaps()→List[KnowledgeGap]` zawiera `{type, node_id, doc_id, details}`?
- Faza Green dla `CrossDomainIssue.severity_escalated` (zamknięcie RED z B49) — `@dataclass class CrossDomainIssue: node_id: str; domains: Set[str]; violations: List[Violation]; severity_escalated: str = field(init=False)` + `def __post_init__(self): order=['MEDIUM','HIGH','CRITICAL']; self.severity_escalated = 'CRITICAL' if len(self.domains) >= 2 else max(self.violations, key=lambda v: order.index(v.severity)).severity`; `aggregate_cross_domain()` konstruuje `CrossDomainIssue(node_id, domains, violations)` bez jawnego przekazywania `severity_escalated` — auto-obliczane przez `__post_init__`; test z B49 `assert cross[0].severity_escalated == 'CRITICAL'` przechodzi GREEN; domyka cykl TDD #9?
- Jak rozszerzyć `OPS-04` o wymóg RPO ≤1h zgodnie z DORA art.12 — trzecia gałąź UNION: `UNION MATCH (db:Database)-[:HAS_BACKUP_PROCEDURE]->(bp:BackupProcedure) WHERE bp.rpo_hours > 1 RETURN db.id AS node_id, bp.rpo_hours AS rpo`; `Violation.details={'rpo_hours': row['rpo']}` dla naruszeń RPO; pełny DORA-compliance = OPS-04 z 3 gałęziami (brak procedury + RTO > 4h + RPO > 1h); nowy RED test: `def test_ops04_fires_when_rpo_exceeds_1h(): session.run.return_value=[{'node_id':'db_prod','rto':None,'rpo':4}]; assert Ops04Rule().run_cypher_rule(session)[0].details=={'rpo_hours':4}`; czyni OPS-04 pełną implementacją DORA Pillar 4 Recovery Objectives?
- Jak deduplikować naruszenia w `AuditPipeline.run()` gdy kaskady pokrywają te same węzły co Linter — strategia: `seen=set(); unique=[v for v in direct+cascaded if (v.rule_id,v.node_id) not in seen and not seen.add((v.rule_id,v.node_id))]`; `AuditPipeline` zachowuje najwyższe `severity` dla duplikatów `(rule_id, node_id)`; raport zawiera `report['violations_deduplicated_count']` vs `report['violations_total_count']` — różnica = miara efektu kaskadowego; dzięki temu `AuditReportGenerator` nie wyświetla tego samego naruszenia dwa razy?
- Zaimplementujmy metodę `evaluate` dla `SEC01Rule` — wyższa warstwa nad `run_cypher_rule`: `def evaluate(self, session, engine=None): violations=self.run_cypher_rule(session); all_v=engine.analyze_cascades(violations).direct+engine.analyze_cascades(violations).cascaded if engine else violations; return EvaluationResult(violations=all_v, is_clean=not all_v, summary=f'SEC-01: {len(violations)} bezpośrednich, {len(all_v)-len(violations)} kaskadowych')`; `EvaluationResult(NamedTuple)` z polami `violations, is_clean, summary`; `LinterRule` base class dostaje domyślną `evaluate(session, engine=None)` — każda reguła dziedziczy bezpłatnie; otwiera TDD cycle #10?
- Jak ujednolicić kontrakt `Test01Rule` przez metodę `evaluate` — deleguje do `run_cypher_rule` + opcjonalnie `engine.analyze_cascades()`; `summary=f'TEST-01: {len(all_v)} systemów bez zatwierdzonego TestPlan'`; `AuditPipeline.run()` może teraz wywoływać `[r.evaluate(session, engine) for r in rules]` zamiast `linter.run_all(session)` → per-reguła `EvaluationResult` z `summary` zamiast płaskiej listy naruszeń?
- Jak `AuditReportGenerator.generate_markdown()` sortuje naruszenia w sekcjach cascade_type — domyślna kolejność: CRITICAL > HIGH > MEDIUM; implementacja: `sorted(violations, key=lambda v: ['MEDIUM','HIGH','CRITICAL'].index(v.severity), reverse=True)`; sekcja `## Kaskady grafowe` zawiera najpierw CROSS_DOMAIN_CASCADE (CRITICAL) potem TRANSITIVE_CASCADE (HIGH); sekcja `## Podsumowanie` zawiera licznik per-severity `CRITICAL: N, HIGH: M, MEDIUM: K`; `generate_json()` zachowuje oryginalną kolejność dla downstream narzędzi?
- Jak zaimplementować regułę hiperonimii w `resolve_conflicts` — zasada specyficzności: reguła dotycząca bezpośrednio lematu węzła wygrywa nad regułą dotyczącą jego hipernima; implementacja: `if any(is_hypernym_of(s.id, c.rule_b_domain) for s in synsets): return c._replace(winner_rule=c.rule_a, resolved=True, resolution='HYPERNYMY_SPECIFICITY', confidence=0.95)`; `is_hypernym_of(synset_id, domain)` sprawdza `MATCH (:Synset {id:synset_id})-[:HYPERNYM*1..3]->(:Domain {name:domain})`; przykład: AUTH-01 wygrywa nad ROLE-01 bo `klucz.2→AUTH_TOKEN→SECURITY_OBJECT` jest hiperonimem domeny ROLE; `HYPERNYMY_SPECIFICITY` jako 3. typ `resolution` obok `WORDNET_DOMAIN` i `AMBIGUOUS_RESOLUTION`?
- Jak zoptymalizować integrację Słowosieci z `CrossReferenceEngine` dla hiperonimii — `is_hypernym_of()` wywołuje `session.run()` per konflikt; optymalizacja: `@lru_cache(maxsize=512)` na `is_hypernym_of(synset_id, domain)`; `WordnetAdapter` z `_cache: Dict[str, List[Synset]]` buforuje `get_synsets(lemma)` przez czas życia sesji; dla `resolve_conflicts(10_conflicts)` cache redukuje wywołania Cypher 10→1 per unikatowy lemmat; `WordnetAdapter(cache_size=512, ttl_seconds=3600)` jako konfigurowalna opcja przy DI?
- Jak wdrożyć parser Markdown do ekstrakcji relacji semantycznych — `MarkdownRelationExtractor`: parsuje nagłówki → `MERGE (:Topic {name:heading.text, level:heading.level})`; parsuje bullet `- podmiot predykat dopełnienie` → NLP pipeline (Morfeusz+IntentClassifier) → EventFrame; parsuje bloki kodu → `MERGE (:CodeSnippet {class_name, method_name, language})`; implementacja: `import mistune; ast=mistune.create_markdown(renderer=ASTRenderer())(content)`; każdy `list_item` przechodzi przez `SemanticMapper` zanim trafi do grafu; `MarkdownRelationExtractor` jako input adapter przed potokiem W1→W5?
- Jakie reguły wnioskowania (Event Reasoning) dodać do `CrossReferenceEngine` — rozszerzenie katalogu `DeductionRule`: (4) `CAUSAL_CHAIN`: `A→[:CAUSES]→B→[:CAUSES]→C` → naruszenie A propaguje do C (max `depth_limit`); (5) `OBLIGATION_TRANSFER`: `COMMITMENT` w EventFrame propaguje do wszystkich EventFrame z tym samym AGENT — naruszenie zobowiązania A dotyka B gdy `B.agent==A.agent`; (6) `BLOCKED_SEQUENCE`: naruszenie w kroku N blokuje walidację kroków N+1..M; każda reguła jako `DeductionRule(name, apply(violations)→List[Violation])`; rejestracja: `engine.add_deduction_rule(CausalChainRule())`?
- Jak zautomatyzować ekstrakcję relacji semantycznych z dokumentów technicznych — `pre-commit` hook: `.pre-commit-config.yaml` z `hooks: [{id: extract-semantics, entry: python -m extractor.markdown_relation_extractor, files: \.md$}]`; GitHub Actions trigger: `on: push: paths: ['dokumentacja/**/*.md']` → krok `python -m extractor.run_batch --changed $CHANGED_FILES`; `run_batch(changed_files)` przetwarza tylko zmienione pliki (delta extraction); wynik trafia do Neo4j przez `GraphQueryAdapter`; MERGE zamiast CREATE gwarantuje idempotentność?
- Jak zintegrować Słowosieć z parserem NLP (DependencyParserAdapter) dla hiperonimii — po `DependencyParserAdapter.parse(tokens)→DependencyTree`: dla każdego tokena `wordnet_adapter.get_synsets(t.lemma)→synsets`; wzbogacenie: `t.hypernyms = [s.id for s in synsets if s.relation=='HYPERNYM']`; `EnrichedToken.hypernyms` trafia do `SemanticMapper`; `SemanticMapper` tworzy `MERGE (tok:Token)-[:HAS_SYNSET]->(syn)-[:HYPERNYM*1..3]->(:Domain {name:domain})`; `is_hypernym_of()` w `resolve_conflicts` używa tego grafu zamiast zewnętrznego API — zero dodatkowych `session.run()` po initial plWordNet import?
- Przejdźmy do projektowania parsera NLP dla warstwy ekstrakcji danych — architektura warstwy: `RawText→Tokenizer(Morfeusz2)→DependencyParser(spaCy+pl_core_news_lg)→SemanticMapper→Neo4j`; klasy: `TokenizerAdapter.tokenize(text)→List[MorphToken]`, `DependencyParserAdapter.parse(tokens)→DependencyTree`, `SemanticMapper.map(tree)→List[EventFrame]`; punkt integracji: `ExtractionPipeline(tokenizer, parser, mapper, graph_adapter).run(doc)` jako Facade; każda warstwa testowalnie niezależna — `DependencyParser` testowany na złotym zbiorze 50 zdań technicznych z precyzją >0.85?
- Jak wyciągnąć podmiot i dopełnienie ze zdania przy użyciu spaCy — `doc = nlp("Wykonawca dostarczył dokumentację techniczną z opóźnieniem")`; podmiot (nsubj): `subj = [t for t in doc if t.dep_=='nsubj'][0]` → `Token('Wykonawca', pos='NOUN', dep='nsubj')`; dopełnienie bliższe (obj): `obj = [t for t in doc if t.dep_ in ('obj','dobj')][0]` → `Token('dokumentację', pos='NOUN')`; mapowanie: `subj→EventFrame.agent=subj.lemma_`, `obj→EventFrame.patient=obj.lemma_`; `DependencyParserAdapter.extract_roles(doc)→{'agent':'Wykonawca','patient':'dokumentacja','action':'dostarczyć'}` — wynik bezpośrednio jako `HAS_ROLE` edges w Neo4j?
- Jak stworzyć `SlowosiecProxy` z funkcją hiperonimii — wzorzec Proxy: `class SlowosiecProxy: def __init__(self, adapter:WordnetAdapter, cache_ttl:int=3600): self._adapter=adapter; self._cache:dict={}; self._timestamps:dict={}`; metoda: `def get_hypernyms(self, lemma:str, depth:int=3)->List[str]: key=(lemma,depth); if key in self._cache and time()-self._timestamps[key]<self._ttl: return self._cache[key]; result=self._adapter.get_hypernyms(lemma, depth); self._cache[key]=result; self._timestamps[key]=time(); return result`; `invalidate(lemma)` usuwa klucze z prefixem; `SlowosiecProxy` wstrzykiwany do `SemanticMapper` — `adapter=None` → None-safe fallback `return []`?
- Pokaż jak zaprogramować parser dokumentów technicznych w Pythonie — `class TechnicalDocParser: def parse(self, path:Path)->ParsedDoc: text=path.read_text('utf-8'); sections=self._split_sections(text); return ParsedDoc(sections=[Section(heading=h, sentences=self._extract_sentences(body)) for h,body in sections])`; `_split_sections`: `re.split(r'^#{1,3}\s', text, flags=re.M)`; `_extract_sentences`: `[s.strip() for s in re.split(r'(?<=[.?!])\s+(?=[A-ZŁÓŚĄĘĆŹ])', body) if len(s)>10]`; `ExtractionPipeline.run(doc:ParsedDoc)` → dla każdego `Section.sentences` wywołuje `DependencyParserAdapter.extract_roles()` → `SemanticMapper.map()` → `GraphQueryAdapter.merge_event_frame()`; pipeline przetwarza 100 zdań/s na CPU?
- Jak zintegrować Morfeusz 2 z naszym pipeline do ekstrakcji lematów — `import morfeusz2; morf = morfeusz2.Morfeusz()`; analiza: `results = morf.analyse("dostarczył")`; wynik: `[(0,1,('dostarczyć','dostarczyć:verb:sg:past:m1:perf','dostarczyć'))]`; `MorphToken.from_morfeusz(r)`: `lemma=r[2][1].split(':')[0]`, `pos=r[2][1]`, `offset=r[0]`; `TokenizerAdapter.tokenize(text)→List[MorphToken]` wywołuje `morf.analyse_text(text)` (jedno wywołanie na cały tekst); filtr: `[t for t in tokens if not t.pos.startswith('interp')]` usuwa interpunkcję; `:MorphToken {lemma, pos, offset, form}` jako węzeł wejściowy dla `DependencyParserAdapter`?
- Stwórzmy klasę `SlowosiecProxy` do obsługi relacji hiperonimii — rozszerzenie o `get_synonyms`: `def get_synonyms(self, lemma:str)->List[str]: key=('syn',lemma); if key in self._cache and time()-self._timestamps[key]<self._ttl: return self._cache[key]; result=self._adapter.get_synonyms(lemma); self._cache[key]=result; self._timestamps[key]=time(); return result`; `get_all_relations(lemma)→RelationSet(hypernyms, synonyms, hyponyms)` — jedno wywołanie pobiera wszystkie relacje; `cache_stats()→{hits:int, misses:int, size:int}` do monitoringu; leniwa inicjalizacja: `@classmethod from_neo4j(uri, auth)→SlowosiecProxy` tworzy `WordnetAdapter(Neo4jSession)` + `SlowosiecProxy` w jednym kroku?
- Jak zintegrować sterownik Neo4j z `AuditReportGenerator` — `AuditReportGenerator.__init__(self, adapter:GraphQueryAdapter)`: dependency injection sterownika; `generate(session)→Report`: `violations=self._adapter.run("MATCH (v:Violation) RETURN v")`; `cascades=self._adapter.find_cascades()`; `conflicts=self._adapter.run("MATCH (c:Conflict) RETURN c")`; `Report` jako `TypedDict` z kluczami `violations/cascades/conflicts/summary`; `AuditPipeline(linter, engine, generator).run(session)` → `generator.generate(session)` jako finalny krok; `Neo4jAdapter.from_env()` wstrzykiwany przez `AuditPipeline.from_env()` fabrykę — zero konfiguracji w testach produkcyjnych?
- Jak zintegrować `SlowosiecProxy` z `CrossReferenceEngine` dla lepszej dedukcji — `CrossReferenceEngine._resolve_semantic(node_a, node_b)`: `rels_a=proxy.get_all_relations(lemma_a)`, `rels_b=proxy.get_all_relations(lemma_b)`; overlap score: `len(set(rels_a.hypernyms)&set(rels_b.hypernyms))/max(len(rels_a.hypernyms),1)`; jeśli score>0.5 → `resolution_type='WORDNET_DOMAIN', confidence=score`; synonimia: `lemma_b in rels_a.synonyms → confidence=0.99`; wynik: `resolve_conflicts` ogranicza false-positive kaskady przez odfiltrowanie kolizji semantycznie pokrewnych węzłów (np. `klucz_szyfrujący`↔`klucz_uwierzytelniający` → 1 violation zamiast 2)?
- Jak `SlowosiecProxy` obsługuje ścieżki hiperonimii w grafie relacji — proxy deleguje do: `WordnetAdapter.get_hypernyms(lemma, depth=3)` → `MATCH (s:Synset {lemma:$l})-[:HYPERNYM_OF*1..3]->(h) RETURN h.lemma, length(path) AS depth`; wynik posortowany po depth: `[('urządzenie',1),('artefakt',2),('obiekt',3)]`; `SlowosiecProxy._build_hypernym_path(lemma)→List[Tuple[str,int]]` buforuje ścieżkę; `is_ancestor(lemma_a, lemma_b)` sprawdza `lemma_b in {h for h,_ in path_a}` — O(1) dzięki `set`; `CrossReferenceEngine` używa `is_ancestor` zamiast pełnego przeszukiwania grafu — 10× szybciej dla depth=3?
- Jak zintegrować Słowosieć z `CrossReferenceEngine` do obsługi hiperonimii — `CRE.resolve_hypernymy(violation_a, violation_b)`: `path_a=proxy.get_hypernym_path(node_a.domain_lemma)`; `path_b=proxy.get_hypernym_path(node_b.domain_lemma)`; wspólny przodek: `lca=next((h for h,d in path_a if h in {x for x,_ in path_b}), None)`; gdy `lca` istnieje i `depth<=2` → `ResolutionType.HYPERNYMY_SPECIFICITY, confidence=0.95`; gdy `lca` na depth=3 → `ResolutionType.AMBIGUOUS_RESOLUTION, confidence=0.3`; integracja z `resolve_conflicts`: nowy branch w `if score>0.5` → `elif proxy.is_ancestor(domain_a, domain_b)`; zamknięcie 3. ścieżki rezolucji HYPERNYMY_SPECIFICITY jako pełna implementacja?
- Jak wdrożyć parser Markdown do automatycznego zasilania grafu Neo4j — tryb ciągły: `FileWatcher(path='dokumentacja/', pattern='*.md', callback=MarkdownRelationExtractor().extract_and_merge)`; callback: `extract(path)→nodes+rels` → `BatchWriter.write(nodes,rels)` → `ExtractionRun` status COMPLETE; alternatywa: `MarkdownRelationExtractor` jako APOC trigger: `CALL apoc.trigger.add('md_import', 'MATCH (d:Document {status:\"PENDING\"}) CALL extractor.process(d) YIELD result RETURN result', {phase:'after'})`; idempotentność: `MERGE` + `ExtractionRun.status='COMPLETE'` blokuje podwójne przetwarzanie tego samego pliku (hash check)?
- Pokaż jak zintegrować parser logiki dokumentu z Neo4j — `DocumentLogicParser.parse(section:Section)→List[LogicRule]`; wzorzec: zdanie z `musi`/`należy` → `LogicRule(type='OBLIGATION', subject=agent, predicate=action, object=patient, source_section=heading)`; `MERGE (:LogicRule {id:hash(text), type:$type, source_doc_id:$did})-[:DERIVED_FROM]->(:Section {heading:$h})-[:BELONGS_TO]->(:Document {id:$did})`; `LogicRule` staje się `EventFrame` z `domain='CONTRACT'` gdy `section.heading` zawiera słowo kluczowe z `DOMAIN_WHITELIST`; cały pipeline: `TechnicalDocParser→DocumentLogicParser→SemanticMapper→BatchWriter`; `LogicRule` walidowany przez wszystkie reguły Lintera (ROLE-01, OPS-04, TEMPORAL-02)?
- Wybierzmy opcję spaCy dla pełnych zdań — porównanie: `spacy.load('pl_core_news_sm')` (12MB, F1=0.82) vs `pl_core_news_lg` (541MB, F1=0.89) vs Morfeusz2-only (bez dep-parse); wybór `pl_core_news_lg` dla dokumentacji technicznej: lepsza precyzja NER dla `Wykonawca`/`Zamawiający` (PERSON+ORG), dep-parse F1=0.87 dla zdań prawniczych; konfiguracja: `nlp = spacy.load('pl_core_news_lg'); nlp.add_pipe('sentencizer', before='parser')`; `sentencizer` dzieli tekst na pełne zdania zanim dep-parser je przetworzy; `DependencyParserAdapter(nlp)` jako Facade — model jest wstrzykiwalny w testach przez `nlp=Mock()`?
- Jak spaCy radzi sobie z polską fleksją w grafie — spaCy `pl_core_news_lg` lemmatyzuje przez wbudowany model (`Token.lemma_`), ale dla form nieznanych używa reguł morfologicznych; wzbogacenie przez Morfeusz2: `MorphToken.lemma` z Morfeusz zastępuje `Token.lemma_` gdy `Token.is_oov==True`; scalenie: `DependencyParserAdapter.parse()` uruchamia spaCy dep-parse → dla każdego tokenu OOV podmienia lematę z `TokenizerAdapter`; wynik: `EnrichedToken(lemma=morfeusz_lemma, dep=spacy_dep, pos=spacy_pos)` łączy precyzję Morfeusza z dep-rel spaCy — zero ponownych wywołań API?
- Pokaż jak zintegrować Słowosieć z ekstraktorem spaCy — po `doc=nlp(text)`: dla każdego tokenu `t` z `t.pos_ in ('NOUN','VERB','ADJ')`: `synsets=wordnet_proxy.get_hypernym_path(t.lemma_)`; wzbogacenie: `t._.hypernyms=synsets` przez spaCy extension `Token.set_extension('hypernyms',default=[])`; `SemanticMapper` czyta `t._.hypernyms` zamiast wywoływać `WordnetAdapter` osobno; pipeline: `spaCy(text)→[Token+hypernyms]→SemanticMapper→EventFrame→Neo4j`; jedno przejście po tokenach zastępuje N osobnych `proxy.get_hypernym_path()` wywołań — `batch_enrichment()` dla całego `Doc` w jednym cache lookup?
- Zaprojektuj strukturę parsera Markdown dla dokumentacji technicznej — moduły: `MarkdownSplitter` (sekcje po nagłówkach `re.split(r'^#{1,3}')`), `SentenceExtractor` (zdania po `[.?!]`), `ObligationDetector` (pattern matching `musi|należy|zobowiązuje`), `EventFrameBuilder` (spaCy extract_roles), `GraphBatchWriter` (UNWIND MERGE); klasa główna: `TechnicalDocPipeline(splitter, extractor, detector, builder, writer).process(path:Path)→ExtractionReport`; `ExtractionReport(docs_processed, frames_created, violations_found, duration_ms)`; każdy moduł testowalny niezależnie — `ObligationDetector` testowany 50 wzorcami z precyzją >0.9?
- Jak spaCy wspiera integrację z polskim grafem semantycznym — `token.morph` (spaCy) zwraca `MorphAnalysis` z `Case, Gender, Number`; `EnrichedToken(lemma=morfeusz_lemma, dep=spacy_dep, pos=spacy_pos, morph_case=token.morph.get('Case',''))` → `MERGE (:Token {lemma:$l, pos:$p, morph_case:$mc})`; `Token._.hypernyms` (extension via `set_extension`) ładuje ścieżkę hiperonimii ze `SlowosiecProxy.get_hypernym_path()` podczas jednego przejścia przez `Doc`; `SemanticMapper` czyta `t._.hypernyms` i `t.morph_case` zamiast wywoływać osobne adaptery — jeden `MERGE` na token z pełnymi atrybutami morfologicznymi i semantycznymi?
- Jak zintegrować Słowosieć z `CrossReferenceEngine` przez indeks semantyczny — `CRE.build_semantic_index(docs)`: dla każdego węzła `n` w `docs` → `proxy.get_all_relations(n.lemma)→RelationSet`; wynik: `semantic_index: Dict[str, RelationSet]` w pamięci; `check_cascade(violation)` sprawdza `semantic_index[violation.node_id].synonyms + hypernyms` zamiast wywołania API per krok kaskady; LCA grouping: naruszenia z LCA depth≤2 grupowane w jeden `CrossDomainIssue` jako klaster semantyczny; `build_semantic_index()` wywołany raz przed `run_all()` — O(1) lookup zamiast O(n) wywołań `proxy` podczas propagacji?
- Pokaż jak za pomocą spaCy wyciągnąć relacje agent-akcja-obiekt — pełna implementacja z obsługą wielu zdań i błędów: `def extract_aro(text:str, nlp)->Optional[RoleSet]: doc=nlp(text); for sent in doc.sents: nsubj=[t for t in sent if t.dep_=='nsubj' and t.head.pos_=='VERB']; root=[t for t in sent if t.dep_=='ROOT' and t.pos_=='VERB']; obj=[t for t in sent if t.dep_ in('obj','dobj','iobj') and t.head.pos_=='VERB']; if not root: continue; return RoleSet(agent=nsubj[0].lemma_ if nsubj else None, action=root[0].lemma_, patient=obj[0].lemma_ if obj else None); return None`; `t.head.pos_=='VERB'` filtruje fałszywe nsubj w zdaniach nominalnych; `RoleSet(agent=None)` → `Violation('ROLE-01')`; `for sent in doc.sents` obsługuje dokumenty wielozdaniowe?
- Napiszmy kod `SlowosiecProxy` z rekurencyjnym przechodzeniem po grafie hiperonimii — `def get_hypernyms(self, lemma:str, depth:int=3, _visited:set=None)->List[str]: if _visited is None: _visited=set(); if depth==0 or lemma in _visited: return []; _visited.add(lemma); key=f'{lemma}:{depth}'; if key in self._cache: return self._cache[key]; direct=self._adapter.get_hypernyms(lemma); result=list(direct); for h in direct: result+=self.get_hypernyms(h,depth-1,_visited); self._cache[key]=result; return result`; `_visited` zapobiega cyklom w plWordNet; `depth=3` pokrywa `klucz→urządzenie→artefakt→obiekt_fizyczny`; `key=f'{lemma}:{depth}'` — różne głębokości mają osobne wpisy TTL w cache?
- Jak użyć spaCy do ekstrakcji podmiotu i dopełnienia z dokumentacji technicznej — wzorzec dla zdań proceduralnych: `def extract_subject_object(doc:Doc)->List[Tuple[str,str,str]]: results=[]; for sent in doc.sents: subjs=[(t.lemma_,t.dep_) for t in sent if t.dep_ in('nsubj','nsubj:pass')]; objs=[(t.lemma_,t.dep_) for t in sent if t.dep_ in('obj','dobj','obl','iobj')]; verb=next((t.lemma_ for t in sent if t.dep_=='ROOT'),None); if verb: results+=[(s[0],verb,o[0]) for s in subjs for o in objs]; return results`; `nsubj:pass` obsługuje stronę bierną; `obl` wychwytuje dopełnienia przyimkowe; wynik `[('Wykonawca','dostarczyć','dokumentacja')]` bezpośrednio do `SemanticMapper.map_batch()`?
- Jak stworzyć `SlowosiecProxy` do rekurencyjnego pobierania hiperonimów — pełna klasa: `class SlowosiecProxy: def __init__(self, adapter:WordnetAdapter, cache_ttl:int=300): self._adapter=adapter; self._cache:Dict[str,Tuple[List,float]]={}`; `get_hypernyms(lemma,depth=3,_visited=None)` — implementacja z `_visited` i TTL: `if key in self._cache and time()-self._cache[key][1]<self._ttl: return self._cache[key][0]`; `@classmethod def from_neo4j(cls, uri, auth): adapter=Neo4jWordnetAdapter(GraphDatabase.driver(uri,auth=auth)); return cls(adapter)`; `cache_stats()→{'hits':self._hits,'misses':self._misses,'size':len(self._cache)}`; `None`-safe: gdy `adapter=None` → `get_hypernyms` zwraca `[]` bez wyjątku?
- Pokaż jak zintegrować spaCy ze Słowosiecią w parserze NLP — `class NLPPipeline: def __init__(self, nlp, proxy:SlowosiecProxy): self.nlp=nlp; self.proxy=proxy; Token.set_extension('hypernyms',default=[],force=True)`; metoda `enrich(text:str)->Doc`: `doc=self.nlp(text); [setattr(t._,'hypernyms',self.proxy.get_hypernyms(t.lemma_)) for t in doc if t.pos_ in('NOUN','VERB','ADJ') and not t.is_stop]; return doc`; downstream: `SemanticMapper(doc).map()` czyta `t._.hypernyms` z każdego tokenu — zero wywołań `proxy` poza `enrich()`; `NLPPipeline(nlp, SlowosiecProxy.from_neo4j(uri,auth))` jako punkt wejścia całego stacku?
- Pokaż kod `SlowosiecProxy` z funkcją rekurencyjnej hiperonimii przez Neo4j — `Neo4jWordnetAdapter` używa Cypher zamiast Python-recursion: `def get_hypernyms(self, lemma:str)->List[str]: result=self.session.run("MATCH (s:Synset {lemma:$l})-[:HYPERNYM*1..3]->(h:Synset) RETURN h.lemma AS lemma", l=lemma); return [r['lemma'] for r in result]`; `[:HYPERNYM*1..3]` = Cypher-native rekurencja do głębokości 3; `SlowosiecProxy(Neo4jWordnetAdapter(session))` — Python `get_hypernyms()` z `_visited` guard potrzebne tylko gdy adapter jest in-memory (plWordNet XML); dla Neo4j backend: delegacja do `[:HYPERNYM*1..depth]` path query jest szybsza niż Python rekurencja (index scan vs heap)?
- Rozszerz `SlowosiecProxy` o typowany wynik `HypernymChain` — `HypernymChain = NamedTuple('HypernymChain', [('root', str), ('lemmas', List[str]), ('depths', List[int])])`; `get_hypernym_path(lemma, depth=3)->HypernymChain`: pobiera `raw=get_hypernyms(lemma, depth)`, odpytuje głębokość każdego: `levels={h: adapter.get_depth(lemma, h) for h in raw}`; zwraca `HypernymChain(root=lemma, lemmas=sorted(raw, key=lambda h: levels[h]), depths=[levels[h] for h in sorted(raw, key=lambda h: levels[h])])`; `__iter__` na `HypernymChain` zwraca `zip(lemmas, depths)`; `path_to_root(lemma)` = najdłuższa gałąź jako `List[str]`; `CrossReferenceEngine.lca(chain_a, chain_b)` = `next(h for h in chain_a.lemmas if h in set(chain_b.lemmas), None)` — O(1) dla obu chainów bez dodatkowych wywołań proxy?
- Jak zintegrować spaCy z Neo4j do zapisu ról semantycznych — `extract_subject_object(doc)→List[Tuple[str,str,str]]` zwraca `(subj_lemma, verb_lemma, obj_lemma)`; batch Cypher: `UNWIND $roles AS r MERGE (a:Token {lemma:r.agent}) MERGE (v:Token {lemma:r.action, pos:'VERB'}) MERGE (p:Token {lemma:r.patient}) MERGE (e:EventFrame {id:r.id}) MERGE (e)-[:HAS_ROLE {role:'AGENT', dep:'nsubj'}]->(a) MERGE (e)-[:HAS_ROLE {role:'ACTION', dep:'ROOT'}]->(v) MERGE (e)-[:HAS_ROLE {role:'PATIENT', dep:'obj'}]->(p)`; `SemanticMapper.map_batch(doc)→List[RoleSet]` → `session.run(UNWIND_CYPHER, {'roles': [r._asdict() for r in rolesets]})`; zero N+1 queries — cały dokument jako jedno `session.run()` przez `UNWIND $roles`; `nsubj:pass` obsługuje bierną `Wykonawca` jako PATIENT, nie AGENT?
- Jak wstrzyknąć hierarchię pojęć ze spaCy do Neo4j jako ścieżkę domenową — `NLPPipeline._inject_hypernym_hierarchy(doc, session)`: dla każdego tokenu `t` z `t._.hypernyms` → `chain=SlowosiecProxy.get_hypernym_path(t.lemma_)`; Cypher: `UNWIND $chains AS c UNWIND c.lemmas AS domain MERGE (:Domain {name:domain}) MERGE (tok:Token {lemma:c.root})-[:BELONGS_TO_DOMAIN {depth:c.depths[index(c.lemmas,domain)]}]->(:Domain {name:domain})`; `doc.pipe(texts, batch_size=32)` przetwarza dokumenty batchowo — `disable=['ner','senter']` wyłącza komponenty nieużywane w pipeline'u semantycznym; `NLPPipeline.fast_mode()` = `spacy.load('pl_core_news_lg', disable=['ner','senter','attribute_ruler'])` + `batch_size=64`; 3× szybszy niż full pipeline dla dokumentacji technicznej bez Named Entity Recognition?
- Jak wdrożyć moduł NLPExtractor z Morfeuszem dla bezbłędnej lematyzacji — `TokenizerAdapter.tokenize_bulk(text)->List[MorphToken]`: `morf.analyse_text(full_text)` = jedno wywołanie dla całego dokumentu vs `morf.analyse()` per token = 40× szybciej; `MorphToken.from_morfeusz(r)`: `lemma=r[2][1].split(':')[0], pos=r[2][1], offset=r[0]`; OOV merge z spaCy: `EnrichedToken.lemma = morfeusz_token.lemma if spacy_token.is_oov else spacy_token.lemma_`; `TokenizerAdapter` wstrzykiwany do `DependencyParserAdapter.parse(tokens)→DependencyTree` jako osobna warstwa — spaCy robi dep-parse, Morfeusz dostarcza lematy dla OOV; `NLPExtractor = NLPPipeline(spacy.load('pl_core_news_lg'), SlowosiecProxy(adapter))` jako public API modułu?
- Jak SlowosiecProxy identyfikuje właściwy synset dla wieloznacznych słów — `disambiguate(lemma, context_tokens:List[str])->str`: dla każdego kandydackiego synsetu `S` lemmy: `score(S) = |hypernyms(S) ∩ {h for t in context_tokens for h in get_hypernyms(t)}| / max(len(hypernyms(S)),1)`; najwyższy `score` → `best_synset_id`; przykład: `klucz` ma synset HARDWARE i CRYPTOGRAPHY; kontekst `['baza','szyfrowanie','AES']` → hypernymy wskazują domenę CRYPTOGRAPHY → `score(CRYPTOGRAPHY)=0.8 > score(HARDWARE)=0.1`; minimum kontekstu: 3 tokeny; fallback gdy `len(context)<3`: zwraca wszystkie synsety bez disambiguacji; `disambiguate()` zastępuje `get_hypernyms()` w `NLPPipeline.enrich()` gdy `doc` ma >5 tokenów — precyzja rośnie z 71% do ~89% na corpus prawniczym?
- Jak zaprogramować resolver koreferencji dla polskich zaimków w dokumentacji — `PolishCoreferenceResolver.resolve(doc)->Dict[int,int]`: mapuje indeks tokenu-zaimka → indeks antecedenta; polskie zaimki: `{'on','ona','ono','oni','one','go','mu','jej','im','ich','je','się'}`; algorytm: w zdaniu → `nsubj` poprzedniej klauzuli; między zdaniami → ostatni `EventFrame.agent` przed zdaniem bez agenta; scalenie z `SemanticMapper`: `if frame.agent is None and sent_has_pronoun: frame.agent = resolver.resolve(pronoun_token)`; `CrossSentenceReferenceChain(frames:List[EventFrame])` grupuje zdarzenia z tym samym resolved agent — `MERGE (e1)-[:SAME_AGENT]->(e2)` gdy `e1.agent == e2.agent` po resolver; `Violation('COREREF-01')` gdy resolver nie może ustalić antecedenta dla zaimka w zdaniu wymagającym?
- Które moduły zbudować najpierw dla szybkiego prototypu — kolejność implementacji (każdy krok daje działający increment): (1) `TokenizerAdapter(Morfeusz2)` → testy lematyzacji; (2) `DependencyParserAdapter(spaCy)` → dep-tree; (3) `SemanticMapper.map(tree)→EventFrame` z AGENT/ACTION/PATIENT; (4) `LinterRuleEngine([ROLE-01,TEMPORAL-01])` → pierwsze naruszenia; (5) `AuditReportGenerator.generate_json()` → działający raport w 5 krokach; (6) `Neo4jAdapter` → persystencja; (7) `CrossReferenceEngine` → kaskady; (8) `SlowosiecProxy` + `DeductionEngine` → pełna semantyka; etap 1-5 = prototyp w ~2 tygodnie bez Neo4j i Słowosieci; etap 6-8 = produkcja; `NLPPipeline.fast_mode()` zastępuje etap 1+2 na początku — zero konfiguracji?
- Jak spaCy identyfikuje tematyzację i remat w zdaniu — temat (tema) = dana informacja, zdaniowo-inicjalny `nsubj`; remat (remat) = nowa informacja, predykat+obiekt; implementacja: `def extract_theme_rheme(sent:Span)->Tuple[str,str]: nsubj=[t for t in sent if t.dep_=='nsubj']; theme=nsubj[0].lemma_ if nsubj else sent[0].lemma_; rheme=' '.join(t.lemma_ for t in sent if t.dep_ in('ROOT','obj','dobj','obl'))` ; `EventFrame.theme_topic=theme; EventFrame.rheme_focus=rheme`; spójność dyskursu: gdy dwa kolejne zdania mają ten sam `theme_topic` → `MERGE (e1)-[:TOPICALLY_RELATED]->(e2)`; polskie cleft: `'To Wykonawca dostarczył SRS'` → marker `'to'` przed `nsubj` sygnalizuje focus z emfazą → `rheme='Wykonawca'` (odwrócenie domyślne); `SemanticMapper` zapisuje `theme_topic` jako atrybut węzła `EventFrame` — podstawa analizy spójności dokumentu?
- Jak zintegrować moduł koreferencji z łańcuchem agent-akcja-obiekt — `CorefAwareSemanticMapper(SemanticMapper)` override `map(doc)`: po ekstrakcji AAO sprawdza czy `frame.agent` jest zaimkiem; `resolver=PolishCoreferenceResolver.resolve(doc)`; `frame.agent = resolver[pronoun_idx]` gdy token w `POLISH_PRONOUNS`; Cypher: `MERGE (e1:EventFrame)-[:SAME_AGENT]->(e2:EventFrame)` gdy `e1.resolved_agent == e2.resolved_agent`; query `MATCH (e1)-[:SAME_AGENT*1..3]->(e2) RETURN e1.id, e2.id` — łańcuchy agenta przez wiele zdań; `COREREF-01`: `MATCH (e:EventFrame) WHERE e.agent IS NULL AND e.has_pronoun=True AND NOT (e)-[:SAME_AGENT]->() RETURN e.id`; `SemanticMapper.__init__(coref_resolver=None)` — `None`-safe: gdy brak resolvera `frame.agent` zostaje NULL zamiast wyjątku?
- Jak rozbudować NLPExtractor o detekcję wymiarów intencji jako komponent spaCy — `@Language.component('intent_clf') def create_clf(nlp, name): clf=IntentClassifier(); return lambda doc: _apply_clf(clf, doc)`; `_apply_clf(clf, doc)`: `doc._.speech_acts=[clf.classify_span(sent) for sent in doc.sents]; return doc`; `Doc.set_extension('speech_acts', default=[], force=True)`; `nlp.add_pipe('intent_clf', after='parser')`; `NLPPipeline.with_intent_clf()` = `@classmethod def with_intent_clf(cls, nlp, proxy): nlp.add_pipe('intent_clf', after='parser'); return cls(nlp, proxy)`; `NLPPipeline.enrich()` automatycznie zwraca `doc._.speech_acts` razem z `token._.hypernyms`; jeden pass przez `nlp(text)` = dep-parse + morph + intencja + hiperonimia — zero dodatkowych wywołań?
- Jak zintegrować Słowosieć z procesem koreferencji semantycznej — `SemanticCoreferenceEnhancer.enhance(np_a, np_b, proxy)->float`: `hyps_a=set(proxy.get_hypernyms(np_a.root.lemma_,depth=2))`; `hyps_b=set(proxy.get_hypernyms(np_b.root.lemma_,depth=2))`; `sim=len(hyps_a&hyps_b)/max(len(hyps_a),1)`; gdy `sim>0.6` i pozycje składniowe kompatybilne → para koreferencji; przykład: `'system informatyczny'`↔`'platforma'` — wspólny hiponim `'oprogramowanie'` → `sim=0.7` → co-referent; `PolishCoreferenceResolver._semantic_fallback(np_a, np_b, proxy)` wywoływany gdy heurystyka składniowa zawiedzie; `SemanticRelationType.SEMANTICALLY_COREFERENT` jako opcjonalna relacja w grafie; redukcja `COREREF-01` naruszeń o ~30% dla dokumentacji gdzie synonimy używane zamiennie (`Wykonawca`/`Dostawca`/`Strona`)?
- Jak obsłużyć anafory i zaimki w NLPExtractor — `NLPExtractor._resolve_anaphora(doc)` jako pre-processing przed `SemanticMapper.map(doc)`: wywołuje `PolishCoreferenceResolver.resolve(doc)→Dict[int,int]`; `Doc.set_extension('coref_resolved', default={}, force=True)` przechowuje mapę `{pronoun_idx: antecedent_idx}`; `_apply_coref(doc)`: dla każdego tokenu w `POLISH_PRONOUNS` podmienia `token._.resolved_lemma = antecedent.lemma_`; zaimki zwrotne `'się'` — nie resolvowane ale flagowane `token._.is_reflexive=True` → `COREREF-02` gdy `'się'` bez `nsubj` w tym samym zdaniu; kolejność komponentów spaCy: `dep-parse → coref_resolver → intent_clf` — resolver musi widzieć dep-tree zanim ustali antecedenta; `nlp.add_pipe('coref_resolver', after='parser')` → `nlp.add_pipe('intent_clf', after='coref_resolver')`; test: `'Wykonawca dostarczył SRS. On zatwierdził.'` → `on.resolved_lemma='Wykonawca'` → `frame2.agent='Wykonawca'` po `_apply_coref`?
- Pokaż jak zintegrować Morfeusza z parserem przez MorphFeatures — `@dataclass MorphFeatures(mood:str='Ind', tense:str='Pres', aspect:str='Imp', gender:str='m1', case:str='Nom')`; `MorphFeatureExtractor.extract(morph_token:MorphToken)->MorphFeatures`: parsuje tag string `'verb:sg:past:m1:perf'` przez `tag.split(':')` — indeksowane pola zamiast regex; `IntentClassifier.classify_span(sent)` używa `feats=MorphFeatureExtractor.extract(verb)` → bezpośrednie `feats.mood`, `feats.tense`, `feats.aspect` zamiast `t.morph.get('Mood')`; fallback dla OOV: `MorphFeatures()` = domyślne Ind+Pres = ASSERT; `MorphFeatureExtractor` jako `@staticmethod` — zero stanu, testowalny z `MorphToken`-stubami bez Morfeusza; `TokenizerAdapter.tokenize()` zwraca `List[EnrichedToken(morph=MorphFeatures, dep=str, lemma=str)]` jako jednorodna struktura przez cały pipeline — `IntentClassifier` i `SemanticMapper` korzystają z tego samego obiektu?
- Napisz regułę NER dla spaCy scalającą terminy techniczne — `EntityRulerFactory.create_tech_ruler(nlp)->EntityRuler`: dodaje wzorce przed modelem NER; `TECH_TERMS: List[Dict] = [{'label':'TECH_STANDARD', 'pattern':[{'LOWER':'tei'},{'UPPER':'P5'}]}, {'label':'TECH_CORPUS', 'pattern':[{'UPPER':'NKJP'}]}, {'label':'TECH_CORPUS', 'pattern':[{'UPPER':'KPWr'}]}, {'label':'TECH_TOOL', 'pattern':[{'LOWER':'spacy'}]}, {'label':'TECH_DB', 'pattern':[{'LOWER':'neo4j'}]}, {'label':'TECH_TOOL', 'pattern':[{'LOWER':'morfeusz'},{'IS_DIGIT':True}]}]`; `ruler = nlp.add_pipe('entity_ruler', before='ner'); ruler.add_patterns(TECH_TERMS)`; pułapka: `before='ner'` konieczne — entity_ruler musi wchodzić PRZED modelem NER, nie po; multi-token: `'pl_core_news_lg'` → wzorzec `[{'LOWER':'pl'},{'LOWER':'_'},{'LOWER':'core'}...]` lub `LOWER: {'REGEX':'pl_core.*'}` z flagą `is_regex:True`; `token.ent_type_ == 'TECH_STANDARD'` → `SemanticMapper` przypisuje `role='INSTRUMENT'` zamiast `UNKNOWN`?
- Jak zintegrować NER z istniejącym potokiem NLPExtractor — kolejność komponentów spaCy: `Morfeusz2 → dep-parse → entity_ruler → ner → intent_clf → directive_matcher`; `nlp.add_pipe('entity_ruler', before='ner')` + `nlp.add_pipe('intent_clf', after='ner')` — NER wzbogaca tokeny przed klasyfikacją intencji; `EnrichedToken.ner_type: str = ''` jako pole; `SemanticMapper.extract_roles()`: `if token.ner_type in {'TECH_TOOL','TECH_DB'}: role='INSTRUMENT'`; `if token.ner_type in {'ORG','PERSON'} and not nsubj_found: role='AGENT'` — fallback gdy dep-parse nie dał `nsubj`; `NLPExtractor.extract(text:str)->List[EventFrame]` = jeden `nlp(text)` przez wszystkie 6 komponentów — zero oddzielnych wywołań API; metryka: `NERCoverageMetric = ner_tokens / total_tokens` — cel >0.4 dla dokumentacji technicznej?
- Jak napisać moduł GraphEmitter dla struktury Document-Section-Paragraph — `GraphEmitter.emit_document_structure(doc_ast:dict, doc_id:str)->List[Triple]`: buduje hierarchię `(:Document)-[:CONTAINS]->(:Section {heading, level})-[:CONTAINS]->(:Paragraph {idx})-[:CONTAINS]->(:Sentence {text})`; `MarkdownASTReader(mistune.ASTRenderer()).read_milestone_table(table_ast)->List[EventFrame]`: kolumny `| Kamień milowy | Data | Odpowiedzialny |` → `EventFrame(predicate='kamień_milowy', TIME=date_cell, AGENT=responsible_cell, INSTRUMENT=tool_cell)`; każdy wiersz tabeli = osobny EventFrame; trigger TEMPORAL-01 automatycznie gdy `date(date_cell) < date()`; `(:Paragraph)-[:GENERATES]->(e:EventFrame)` łączy DSP hierarchię z EventFrame — pełna śledzalność skąd pochodzi zdarzenie; `MarkdownASTReader.read(text)` jako wejście do `MarkdownGraphParser.parse()` zamiast czystego `mistune.markdown()`?
- Jak rozbudować MarkdownASTReader o obsługę tabel i ich semantyki — `MarkdownASTReader.read_table(table_ast)->List[EventFrame]` rozpoznaje 4 typy tabel przez `TableTypeClassifier.classify(headers:List[str])->TableType`: (1) milestone (`| Kamień milowy | Data |`) → EventFrame z TIME; (2) requirement (`| Wymaganie | Priorytet | Status |`) → `SpeechAct('REQUIREMENT')` + `Violation('PROC-01')` gdy `Status='OTWARTE'`; (3) decision (`| Decyzja | Uzasadnienie | Data |`) → `SpeechAct('ASSERT')` + `AUDIT_EVIDENCE` node; (4) RACI (`| Zadanie | Odpowiedzialny | Konsultowany |`) → AGENT + PATIENT_INDIRECT bez TIME; `TableType('RACI')` — EventFrame bez TIME nie triggeruje TEMPORAL-01; test: `def test_requirement_table_extracts_status(): frames=ast.read_table({'headers':['Wymaganie','Priorytet','Status'],'rows':[['REQ-01','HIGH','OTWARTE']]}); assert frames[0].speech_act=='REQUIREMENT'`; `TableTypeClassifier` używa `difflib.SequenceMatcher` do dopasowania nagłówków po normalizacji polskich znaków?
- Jak zintegrować bibliotekę Stanza do walidacji fleksji w potoku — `StanzaFlexionValidator(lang='pl')`: `import stanza; stanza.download('pl'); nlp_stanza=stanza.Pipeline('pl', processors='tokenize,pos,lemma,depparse')`; `StanzaFlexionValidator.validate(text:str)->List[FlexionError]`: uruchamia potok Stanza i porównuje lematy z Morfeuszem — gdy `stanza_lemma != morfeusz_lemma` → `FlexionError(form, morfeusz_lemma, stanza_lemma, pos)`; zastosowanie: cross-validation dla OOV — gdy obaj zgadzają się → pewność wysoka; gdy różnią się → `KnowledgeGapTracker.capture_ign(token, conflict='stanza_vs_morfeusz')`; `NLPPipeline.with_stanza_validation()` jako wariant `deep_level` — overhead 2× spaCy uzasadniony tylko przy wysokiej liczbie OOV tokenów; `StanzaFlexionValidator` opcjonalny: `NLPPipeline.__init__(stanza_validator=None)` → None-safe?
- Pokaż jak zintegrować Morfeusza z potokiem NLP dla zaawansowanej fleksji — `morfeusz2.Morfeusz(expand_tags=True)` zwraca pełne tagi morfologiczne zamiast skrótów; `morf.analyse_text_xml(text)→xml_str` jako alternatywne API: `ET.fromstring(xml_str)` → `<seg><tok><orth>dostarczył</orth><lex><base>dostarczyć</base><ctag>praet:sg:m1:perf</ctag></lex></tok></seg>`; `MorphToken.from_xml_element(el)` parsuje bezpośrednio z XML zamiast tuple; `expand_tags=True`: `perf` → `Aspect=Perf` — bezpośrednia kompatybilność z `MorphFeatures(aspect='Perf')`; `analyse_text_xml()` 20% wolniejsze niż `analyse_text()` ale self-documenting format; `TokenizerAdapter.tokenize_xml(text)` jako opcjonalna metoda; wybór przez `PipelineConfig.tokenizer_format='xml'|'tuple'` — `xml` w deep_level, `tuple` w fast_level?
- Jak wdrożyć reguły wyciągające okoliczniki czasu i miejsca oraz koreferencję zaimka 'go' — adverbials TIME: dep-rel `obl` + NER `DATE`/`TIME` lub lemma in `{'data','rok','godzina','termin','dzień'}` → `role='TIME'`; adverbials LOCATION: dep-rel `obl` + NER `LOC`/`GPE` → `role='LOCATION'`; `AdverbialExtractor.extract(sent)->Dict[str,Token]` jako metoda `SemanticMapper`; zaimek `'go'` = forma `ppron3:sg:gen:m1` lub `ppron3:sg:acc:m1` od `'on'`; `PolishCoreferenceResolver.POLISH_PRONOUNS` pełna paradigma: `{'on','go','mu','jego','jemu','nim','ń'}` — formy zależne przypadkowe; resolver: `go` szuka antecedenta `subst:sg:m1` identycznie jak `on` ale formy zależne; test: `'Wykonawca dostarczył SRS. Zamawiający go zatwierdził.'` → `go.resolved_lemma='SRS'` (PATIENT antecedent, nie AGENT); `obl+DATE`: `'dostarczyć dnia 2026-01-15'` → `HAS_ROLE {role:'TIME'}->(:Token {lemma:'2026-01-15', type:'DATE'})`?
- Jak zintegrować Morfeusza z potokiem spaCy dla obsługi polskiej fleksji — `MorphCompatibilityLayer.convert(morfeusz_tag:str)->Dict[str,List[str]]` mostkuje format tagów Morfeusza (`subst:sg:nom:m1`) na Universal Dependencies (`{'Number':['Sing'],'Gender':['Masc'],'Case':['Nom'],'POS':['NOUN']}`); mapowanie przez słownik: `'subst'→'NOUN'`, `'sg'→Number=Sing`, `'nom'→Case=Nom`, `'m1'→Gender=Masc`, `'perf'→Aspect=Perf`; `TokenizerAdapter.apply_morph(doc:Doc, morph_tokens:List[MorphToken])->Doc`: iteruje tokeny spaCy i wywołuje `token.set_morph(layer.convert(mt.tag))` dla każdego `is_oov`; wynik: `token.morph.get('Aspect')` zwraca `['Perf']` zamiast `[]` → `MorphFeatureExtractor.extract(token)` korzysta z `.morph.get()` bez warunkowania na źródle; formularz `'dostarczylibyśmy'` — 84 form tego samego lemu — po `apply_morph()` sprowadzony do `lemma='dostarczyć', Mood=Cnd, Number=Plur, Person=1`; test: `adapter.apply_morph(doc,[mt_dostarcz]); assert doc[0].morph.get('Aspect')==['Perf']`; `MorphCompatibilityLayer` jako `@staticmethod` — zero stanu, testowalny bez Morfeusza przez string-stuby?
- Pokaż jak spaCy-pl z Morfeuszem wspiera analizę struktury Temat-Rema — `ThemeRhemeAnalyzer.analyze(sent:Span)->Tuple[Span,Span]` używa spaCy dep-parse + Morfeusz dla podziału informacji; Temat (informacja dana) = `nsubj` + `det` po lewej stronie verbu + `case=Nom`; Remat (informacja nowa) = `obj/dobj` + `obl` + `xcomp` po prawej stronie verbu; Morfeusz dostarcza `case` i `number` dla grupowania fraz — nominatyw → Temat, akuzatyw/instrumentalny → Remat; zapis w grafie: `(e:EventFrame)-[:HAS_THEME]->(t:Token {lemma:'Wykonawca'})` + `(e)-[:HAS_RHEME]->(r:Token {lemma:'dostarczyć'})`; spójność: `CohesionAnalyzer` sprawdza typical progression — `THEME[n].lemma IN RHEME[n-1].lemmas` → `COHESION+1`; test: `'Wykonawca dostarczył SRS. SRS został zatwierdzony.'` → `theme[1].lemma='SRS'` = `rheme[0].lemma='SRS'` → `cohesion_score += 1`; `ThemeRhemeAnalyzer` jako `@Language.component('theme_rheme')` po `dep-parse` i przed `intent_clf`?
- Pokaż kod integracji Morfeusza z potokiem spaCy-pl jako jednorodną metodę — `TokenizerAdapter.tokenize_and_apply(text:str, nlp:Language)->Doc` jako jedyna metoda publiczna: `(1) doc=nlp.make_doc(text)` — spaCy tokenizacja bez pipeline; `(2) morf_toks=morf.analyse_text(text)` — 1 call na cały dokument = 40× szybciej; `(3) morfeusz_map: Dict[int,MorphToken]={mt.offset:mt for mt in morf_toks}` — O(1) lookup; `(4) for token in doc: mt=morfeusz_map.get(token.idx); if mt and token.is_oov: token.lemma_=mt.lemma; layer.apply_morph(token,mt.tag)`; `(5) return nlp(doc)` — reszta pipeline na wzbogaconym doc; pułapka: Morfeusz i spaCy mogą różnie tokenizować aglutynacje — remedium: `TokenizerAdapter._align_tokens(morf_toks, spacy_toks)` przez `difflib.SequenceMatcher` dla tokenów z `offset` niezgodnym; `BatchProcessor`: `tokenize_and_apply(text, nlp)` wywoływany z `ThreadPoolExecutor(max_workers=4)` dla równoległego przetwarzania dokumentów?
- Jak wdrożyć Morfeusza z potokiem spaCy dla lepszej fleksji i zmierzyć wydajność — `PipelineProfiler` mierzy każdy krok: `{morfeusz: 12ms, spacy_dep: 45ms, ner: 8ms, morph_apply: 3ms, total: 68ms}` per dokument; pułapki wydajnościowe: (1) `spacy.load()` jednorazowo — nie w pętli; (2) `analyse_text()` na pełnym tekście zamiast `analyse()` per token = 40× szybciej; (3) `morfeusz_map[offset]` O(1) vs linear scan O(n²); `NLPPipeline.benchmark(texts:List[str])->ProfilerReport` raportuje `p50/p95/p99` latency dla 100 przykładów; `PipelineConfig.fast_mode=True` wyłącza StanzaFlexionValidator + ThemeRheme + DomainDictionary lookup → 3× szybszy dla prototypu; `ProfilerReport` do `pipeline_perf.json` w CI dla śledzenia regresji wydajności; `NLPPipeline.profile_component(name)->ContextManager` jako `@contextmanager` owijający dowolny krok?
- Pokaż konfigurację Stanza pod kątem walidacji parametrów fleksyjnych — `stanza.Pipeline('pl', processors='tokenize,pos,lemma,depparse', tokenize_pretokenized=True, use_gpu=False)` — `tokenize_pretokenized=True` re-używa Morfeusz tokenizacji; `stanza.download('pl', package='pdb')` — model PDB (Polish Dependency Bank) ma wyższe pokrycie techniczne niż domyślny; `lemma_use_identity=False` — Stanza nigdy nie zwraca oryginalnej formy jako lemat dla OOV; `StanzaFlexionValidator.Config(lang='pl', processors='tokenize,pos,lemma,depparse', min_confidence=0.7, conflict_log='stanza_conflicts.jsonl', validation_mode='lenient'|'strict')`; `validation_mode='strict'` blokuje pipeline gdy rozbieżność Stanza vs Morfeusz > `min_confidence`; `validation_mode='lenient'` loguje do `stanza_conflicts.jsonl` bez blokowania; cross-validation: `tokenize_pretokenized=True` umożliwia Stanza dep-parse na Morfeusz tokenizacji — porównanie dep-tree dla redundancji?
- Jak zintegrować Stanza do walidacji cech morfologicznych per forma — `StanzaFlexionValidator.validate_features(doc:Doc)->List[FlexionError]` sprawdza cechy przez `token.feats: Dict[str,str]` ze Stanza `pos` procesora; cross-validation: `FlexionError.compare(stanza_feat, morfeusz_feat)` → `{feature, stanza_val, morfeusz_val}`; przykład zgodności: czas przeszły `praet:sg:m1:perf` (Morfeusz) vs `Tense=Past,Aspect=Perf,Number=Sing` (Stanza UD) — różne formaty, ta sama treść; przykład niezgodności: `'dostarczylibyśmy'` — Stanza `Mood=Cnd`, Morfeusz tag `tryb_warunkowy` (starszy NKJP) → `FlexionError(form='dostarczylibyśmy', feature='Mood', stanza='Cnd', morfeusz='tryb_warunkowy')`; `conflict_log.jsonl`: `{'form','feature','stanza','morfeusz','doc_id'}` per wpis; `StanzaFlexionValidator.report_conflicts()->Counter[str,int]` = `{form: conflict_count}` top-10 problematycznych form; `StanzaFeatureNormalizer.to_ud(morfeusz_tag)->Dict[str,str]` unifikuje oba formaty przed porównaniem?
- Pokaż przykład analizy Temat-Rema dla polskiej fleksji zdania technicznego — `ThemeRhemeAnalyzer.analyze()` dla `'Wykonawca dostarczył dokumentację techniczną dnia 15 marca.'`; Temat = `Wykonawca` (dep `nsubj`, `case=Nom`, lemma `wykonawca`); Remat = `dostarczył dokumentację techniczną dnia 15 marca` (predykat + obj + obl); wyzwanie fleksyjne: `dokumentację techniczną` → `MorphFeatures(case='Acc', gender='f1')` z Morfeusz — grupuje oba tokeny jako jeden span `doc[2:4]`; `TIME` wyodrębniony przez `AdverbialExtractor`: `'dnia 15 marca'` → `HAS_ROLE {role:'TIME'}`; zapis grafu: `(e)-[:HAS_THEME]->(t{lemma:'wykonawca', case:'Nom'})`, `(e)-[:HAS_RHEME]->(r{lemma:'dostarczyć'})`, `(e)-[:HAS_ROLE{role:'TIME'}]->(tp{lemma:'15_marca'})`; typical progression: zdanie 2 `'Dokumentację zatwierdzył zamawiający.'` → `THEME[2].lemma='dokumentacja'` ∈ `RHEME[1].lemmas` → `CohesionAnalyzer.score += 1`; test: `assert analyzer.analyze(sent)[0][0].text == 'Wykonawca'`?
- Jak Stanza waliduje polskie przypadki gramatyczne w potoku NLP — `StanzaFlexionValidator.validate_cases(doc:Doc)->List[CaseError]` sprawdza polskie przypadki: Stanza `pos` procesor (model PDB) rozpoznaje `Case=Nom|Gen|Dat|Acc|Ins|Loc|Voc`; reguły dep-rel→przypadek: `dep='nsubj'` → `Case MUST BE Nom` (mianownik), `dep='obj'` → `Case IN {Acc, Gen}` (biernik lub dopełniacz przy negacji), `dep='iobj'` → `Case='Dat'` (celownik); najczęstszy konflikt: biernik vs narzędnik — `'pisać coś'` (Acc) vs `'pisać czymś'` (Ins) — Stanza `depparse OBJECT` vs `INSTRUMENT`; `CaseConsistencyRule.validate(token)`: gdy `dep='nsubj' AND feats['Case']!='Nom'` → `FlexionError(form, dep='nsubj', expected='Nom', actual=feats['Case'])`; integracja z potokiem: `validate_cases()` jako opcjonalny krok po `validate_features()` aktywowany w `PipelineConfig(level='deep')`; `conflict_log.jsonl` rejestruje przypadki rozbieżności Stanza vs Morfeusz dla ręcznej inspekcji; test: `assert validator.validate_cases(nlp('Zamawiający zatwierdził SRS.'))==[]` (brak błędów dla poprawnego zdania)?
- Jak zaimplementować testy neutralizacji retorycznej dla zdań czynnych i biernych — `RetoricalNeutralizationTester` weryfikuje że `NLPExtractor` zwraca identyczny `EventFrame` dla zdań czynnych i biernych; test RED: `def test_active_passive_same_eventframe(): active='Wykonawca dostarczył SRS.'; passive='SRS zostało dostarczone przez Wykonawcę.'; frame_a=extractor.extract(active)[0]; frame_p=extractor.extract(passive)[0]; assert frame_a.agent.lemma==frame_p.agent.lemma=='wykonawca'; assert frame_a.patient.lemma==frame_p.patient.lemma=='srs'`; detekcja pasywu: `PassiveVoiceDetector.is_passive(sent)->bool`: `any(t.dep_=='nsubjpass' for t in sent)` lub `t.lemma_=='zostać' AND t.head.dep_=='auxpass'`; mapowanie ról pasywnych: `SemanticMapper.map_passive(sent)` — `nsubjpass→PATIENT`, `obl:agent→AGENT` (inwersja ról względem czynnego); reguła `NLP_PIPELINE_NORM-01`: naruszenie gdy `frame_active.agent != frame_passive.agent` dla par zdań z tego samego dokumentu; `NormalizationTestSuite.run(pairs:List[Tuple[str,str]])` weryfikuje wszystkie pary czynna/bierna w zbiorze testowym; pułapka: `'SRS zostało dostarczone.'` bez agenta — `frame_passive.agent=None` = dozwolone, `NORM-01` nie narusza?
- Jak zaimplementować testy neutralizacji retorycznej w pytest — `@pytest.mark.parametrize('active,passive,agent,patient', [('Wykonawca dostarczył SRS.','SRS zostało dostarczone przez Wykonawcę.','wykonawca','srs'), ('System zapisał dane.','Dane zostały zapisane przez system.','system','dane')])` def `test_norm_parity(active,passive,agent,patient,extractor)`: `frame_a=extractor.extract(active)[0]; frame_p=extractor.extract(passive)[0]; assert frame_a.agent.lemma==frame_p.agent.lemma==agent; assert frame_a.patient.lemma==frame_p.patient.lemma==patient`; `@pytest.fixture(scope='session') def extractor(): return NLPExtractor(nlp=spacy.load('pl_core_news_sm'))`; zestaw testowy `test_pairs.json` z 50 parami czynna/bierna z domeny kontraktowej; `NormalizationTestSuite.from_json(path)->List[TestPair]` ładuje pary dla pytest parametrize; pułapka: zdania bierne bez agenta `'Dane zostały zapisane.'` → `frame_p.agent=None` = PASS; `pytest -m norm -v --tb=short`; `conftest.py addopts = --norm-report` generuje raport pokrycia NORM-01?
- Jak obsłużyć zaimki zwrotne w grafie pamięci kontekstu — `PolishCoreferenceResolver` rozszerza `POLISH_PRONOUNS_MAP` o zaimki zwrotne: `'się':{'case':'Acc','refl':True}`, `'sobie':{'case':'Dat','refl':True}`, `'siebie':{'case':'Gen/Acc','refl':True}`; strategia: `się` → antecedent = podmiot zdania (dep `nsubj`), NIE szukamy w `DocumentContextMemory`; `ReflexivePronounResolver.resolve(token)->Token`: `return [t for t in token.sent if t.dep_=='nsubj'][0]`; zapis grafu: `MERGE (refl:Token {form:'się'})-[:REFLEXIVE_OF]->(subj:Token {lemma:'wykonawca'})` — oddzielna relacja od `COREFERENCE_OF`; `DocumentContextMemory.skip_reflexive(token)->bool`: `token.lemma_ in {'się','sobie','siebie'}` → `True` — nie aktualizuje okna antecedentów; `COREREF-02`: naruszenie gdy `refl:Token` bez `[:REFLEXIVE_OF]` w grafie; `ReflexivePronounResolver` jako osobny komponent przed `PolishCoreferenceResolver` w pipeline — pułapka: `'System sam się naprawił.'` — `się` = reflexive do `system`, NIE do zewnętrznego kontekstu?
- Pokaż przykład analizy wymiarów dla zdarzenia z zaimkiem — `EventDimensionMatrix` pełny cykl dla `'Wykonawca dostarczył SRS. Zamawiający go zatwierdził dnia 15.03.'`: `e1` → `coverage_score=2/8`; `e2` po koreferencji → `coverage_score=4/8`; `EventDimensionMatrix.merge(e1,e2)` gdy `e1.patient==e2.patient` → `merged.coverage_score=5/8`; `GapFiller.fill_from_context(frame, memory:DocumentContextMemory)`: `e1.time=None` + `memory.lookup('time')='15.03'` z e2 → propagacja wstecz; `EventDimensionMatrix.propagate_shared_dims([e1,e2])` = batch fill dla powiązanych eventów; `ROLE-06`: `MATCH (e1)-[:CAUSED_BY|FOLLOWS]->(e2) WHERE e2.timeDimension IS NOT NULL AND e1.timeDimension IS NULL RETURN e1.id` = zdarzenia dziedziczące TIME z przyczyny?
- Jakie są techniki rekonstrukcji semantycznej dla tekstów technicznych — `SemanticReconstructionEngine` — 4 techniki: (1) **ElipsisResolver** — `'Wykonawca dostarczył. Zamawiający zaakceptował.'` → brakujący patient zdania 2 = `srs` z zdania 1 przez `DocumentContextMemory`; (2) **ImplicitAgentReconstructor** — `'Należy przetestować moduł.'` → AGENT = `ORG_responsible` z `memory.lookup('last_org')`; (3) **VaguePhraseNormalizer** — `'odpowiednie testy'` → `TestFrame(type='unit', coverage='?')` + `Violation('VAGUE-01')`; (4) **NominalEventDecoder** — nominalizacje: `'realizacja dostawy'` → `EventFrame(action='dostarczyć', patient='dostawa')` — sufiksy `['acja','anie','enie','ość']`; `SemanticReconstructionEngine.reconstruct(doc)->Doc`: pipeline 4 kroków po `NLPExtractor`; `RECON-01`: `MATCH (e:EventFrame) WHERE e.agentDimension IS NULL AND e.speechAct IN ['POLECENIE','ZAKAZ'] RETURN e.id` = zdania z obowiązkiem bez agenta → kandydaci do rekonstrukcji?
- Pokaż przykład implementacji testu neutralizacji dla pary czynna-bierna w pytest — pełna implementacja z `conftest.py`: `@pytest.fixture(scope='session') def extractor(nlp_model): return NLPExtractor(nlp_model)` + `@pytest.fixture(scope='session') def nlp_model(): return spacy.load('pl_core_news_sm')`; `@pytest.mark.parametrize('active,passive,agent,patient', NormalizationTestSuite.from_json('test_pairs.json'))` → 50 par z domeny kontraktowej; naturalne rozszerzenie: Hypothesis property-based `@given(st.sampled_from(NormalizationTestSuite.all_pairs())) def test_active_passive_hypothesis(pair, extractor): frame_a=extractor.extract(pair.active)[0]; frame_p=extractor.extract(pair.passive)[0]; assert frame_a.agent.lemma==frame_p.agent.lemma` — losowe pary z `test_pairs.json`; `pytest.mark.norm` + `conftest.py addopts = --norm-threshold=0.90` → fail gdy `pass_rate < 0.90`; pułapka: `spacy.load()` w `scope='function'` = 100× wolniejszy — zawsze `scope='session'`; `NormalizationReport(coverage=n_pairs/total_pairs, failures)` jako artifact CI?
- Jak skonfigurować NKJPCorpusReader, aby pobrać wzorcowe tagi morfologiczne do walidacji — `reader=NKJPCorpusReader(root='/data/nkjp', fileids='.*')`; `NKJPGoldTagExtractor.extract_for_lemma(lemma='dostarczyć', n=100)->List[str]` = wszystkie tagi NKJP dla lemmatu; porównanie: Morfeusz `analyse_text('dostarczylibyśmy')` vs `reader.tags_for_form('dostarczylibyśmy')` — gold standard; `NKJPTagMatcher.match(morfeusz_tag, nkjp_tag)->bool`: `'verb:sg:past:m1:perf' ~ 'praet:sg:m1:perf'` przez mapowanie tagsetów; `NKJPValidationConfig(min_coverage=0.85, strict_aspect=True, strict_gender=True)` — strictness; `reader.sents(categories=['law'])` dla zdań prawnych; `NKJPCorpusValidator.build_tag_matrix(lemma)->DataFrame` = matrix form×tag dla analizy odchyleń; CI: `pytest --nkjp-morph-validate` → fail gdy `coverage < min_coverage`?
- Jakie słowniki kolokacji pomogą w walidacji języka technicznego — `CollocateValidator` — słownik kolokacji dla języka technicznego: `CollocateDict.load('tech_collocations.json')` = `{('dostarczyć','dokumentację'):0.95, ('zatwierdzić','SRS'):0.88}` — pary `(verb,obj)` z PMI (Pointwise Mutual Information); `CollocateValidator.validate(triple:Triple)->CollocateScore`: gdy `(action.lemma, patient.lemma) ∉ CollocateDict` → `Violation('COLLOC-01','Nieznana kolokacja','LOW')`; `PMICalculator.from_corpus(CoNLLUReader.read_directory('kpwr/'))` oblicza PMI dla wszystkich par `(verb,obj)` w KPWr; `DomainDictionary.add_collocates(lemma, collocates)` rozszerza słownik; przykład: `'wykonać backup'` (PMI=0.91) OK, `'zrealizować backup'` (PMI=0.12) → `COLLOC-01`; źródła: KPWr (CC), PELCRA N-grams, PWN Słownik Kolokacji; `COLLOC-02`: `MATCH (e:EventFrame)-[:HAS_ROLE{role:'ACTION'}]->(a)-[:HAS_ROLE{role:'PATIENT'}]->(p) WHERE NOT exists{MATCH (:Collocate {verb:a.lemma, obj:p.lemma})} RETURN e.id`; `CollocateValidator` wyłączony w `fast_mode`?
- Pokaż jak Stanza weryfikuje mianownik i biernik w testach neutralizacji — `StanzaFlexionValidator.validate_cases_for_neutralization(active:str, passive:str)->List[CaseMismatch]`: zdanie czynne: `nsubj` dep → `Case MUST BE Nom` (mianownik); zdanie bierne: `nsubjpass` → `Case='Nom'`, `obl:agent` (`przez kogo`) → `Case IN {Acc,Ins}`; konkretny przykład: `'Wykonawca[Nom] dostarczył SRS[Acc].'` vs `'SRS[Nom] zostało dostarczone przez Wykonawcę[Ins].'` — pułapka: `Wykonawcę` może być `Acc` lub `Ins` zależnie od Stanza modelu; `CaseConsistencyRule.for_active_passive(active_doc, passive_doc)`: `assert active.nsubj.case=='Nom' and passive.obl_agent.case in {'Acc','Ins'}`; `CaseMismatchReport(active_doc, passive_doc)->Dict[str,str]` per token; integracja z `NormalizationTestSuite`: `validate_cases_for_neutralization(pair.active, pair.passive)` jako dodatkowa asercja w teście parametrycznym; `NORM-02`: `Case[nsubjpass]!='Nom'` → naruszenie zasady neutralizacji?
- Jak zintegrować Morfeusza z potokiem walidacji korpusowej NKJP — `NKJPMorfeuszPipeline(reader:NKJPCorpusReader, adapter:TokenizerAdapter)`: `reader.tagged_words()` → NKJP tagi → `NKJPToUDConverter.convert()` → UD feats → `MorphCompatibilityLayer.to_ud()` → Morfeusz UD → porównanie; `MorphCompatibilityLayer.validate_against_nkjp(form:str, nkjp_tag:str)->CompatResult`: `morfeusz_ud=adapter.apply_morph(form)`, `nkjp_ud=NKJPToUDConverter.convert(nkjp_tag)`, `match=morfeusz_ud==nkjp_ud`; raport: `{form:'dostarczył', morfeusz:{Tense:'Past',Aspect:'Perf'}, nkjp:{Tense:'Past',Aspect:'Perf'}, match:True}`; 500 tokenów prawnych → `coverage=0.87` (87% zgodność); `MORPH-NKJP-01`: naruszenie gdy `coverage < 0.80`; `NKJPMorfeuszPipeline.run(n_samples=500)->PipelineReport` → `pytest --morph-nkjp` CI?
- Jak zautomatyzować sprawdzanie reguł kolokacyjnych w potoku CI/CD — `CollocateCI` — `audit.py --colloc-check` aktywuje `CollocateValidator` w pipeline; `CollocateReport.violations_count` → `sys.exit(1)` gdy `> --max-colloc-violations=5`; GitHub Actions: `- name: collocation-check\n  run: python -m audit --colloc-check --max-colloc-violations=5`; `CollocateDict.update(new_terms)` hook: `DomainDictionary.on_approve(term)` → auto-dodaje kolokacje przy zatwierdzeniu nowego terminu przez `RuleWizard`; `CollocateCI.diff_report(baseline, current)->DiffReport` = nowe naruszenia vs HEAD~1; `--colloc-strict`: każde `COLLOC-01` = FAIL; pułapka: zbyt agresywne `--colloc-strict` blokuje PR dla terminów specjalistycznych OOV z KPWr → `collocate_whitelist.json` jako bypass; `collocate_whitelist.json` commitowany do repo + review w PR?
- Pokaż jak napisać test jednostkowy dla neutralizacji strony czynnej — `NormalizationTestSuite.split_by_domain(domain:str)->List[TestPair]` filtruje pary per domenę; `pytest -m norm --domain=contract` = 15 par kontraktowych, `--domain=law` = 20 par prawnych; `DomainNormReport(domain, pass_rate, failed_pairs)` per domena; pułapka: zdania z domeny prawnej mają odmienną pasywizację — `PassiveVoiceDetector.PASSIVE_VERBS={'zostać','być','dać się'}`; `NormalizationTestSuite.add_pair(active, passive, domain='contract')` dynamiczne rozszerzanie; `NormalizationCI.run_domain_matrix()->Dict[str,float]` = pass_rate per domena → ogólny wynik = średnia ważona; pułapka: `spacy.load()` musi być `scope='session'` — `conftest.py` + `pytest-xdist -n auto` dla równoległych domen?
- Jak weryfikować chronologiczny porządek zdarzeń TIME w grafie — `TemporalOrderChecker` — `CYPHER 25 MATCH (e1:EventFrame)-[:PRECEDES]->(e2:EventFrame) WHERE e1.timeDimension > e2.timeDimension RETURN e1.id, e1.timeDimension, e2.id, e2.timeDimension` = sprzeczność czasowa; `TEMPORAL-03`: `time(e1) > time(e2)` przy `e1 PRECEDES e2` → `Violation('TEMPORAL-03','HIGH')`; `TemporalOrderChecker.build_timeline(doc_id)->List[EventFrame]` sortuje po `timeDimension`; `TimeNormalizer.to_date(str)->Optional[date]`: `'15 marca'` → `date(2026,3,15)` przez `dateutil.parser.parse`; `check_contract_milestones(doc_id)`: weryfikacja sekwencji `delivery < acceptance < payment < close`; `TEMPORAL-04`: brakujący `PRECEDES` między etapami kontraktu → `Violation('TEMPORAL-04','MEDIUM')`; `TemporalOrderChecker` jako krok po `TimeDimensionExtractor` w pipeline?
- Jak zmapować tagi z NKJP na role semantyczne w grafie — `NKJPSemanticRoleInducer.induce(form, nkjp_tag)->Optional[str]`: `subst:sg:nom:m1` → `'AGENT'`, `subst:sg:acc:f` → `'PATIENT'`, `subst:sg:dat:*` → `'PATIENT_INDIRECT'`, `subst:sg:ins:*` → `'INSTRUMENT'`, `subst:sg:loc:*` → `'LOCATION'`; `CASE_TO_ROLE={'nom':'AGENT','acc':'PATIENT','dat':'PATIENT_INDIRECT','ins':'INSTRUMENT','loc':'LOCATION','gen':None}`; `NKJPTagMapper.to_triple_with_role(form,tag)->List[Triple]` emituje zarówno `HAS_MORPH_FEATURE` jak i opcjonalny `HAS_ROLE`; `ROLE-08`: `MATCH (t:Token)-[:HAS_MORPH_FEATURE{name:'Case',value:'Nom'}]->() WHERE NOT (e:EventFrame)-[:HAS_ROLE{role:'AGENT'}]->(t) RETURN t.lemma` = mianownik bez roli AGENT; `NKJPSemanticRoleInducer` jako pre-processor uzupełniający `SemanticMapper` gdy brakuje dep-parse?
- Jak zautomatyzować CI dla weryfikacji nowych kolokacji przed merge — `CollocateCI.baseline_update(branch='main')` pobiera bieżący `CollocateDict` jako punkt odniesienia; `CollocateCI.pre_merge_check(pr_branch)->DiffReport`: porównuje naruszenia HEAD~1 vs PR — blokuje gdy `DiffReport.new_violations > 0`; `--colloc-report-only` tryb: raportuje bez blokowania (informacyjny); GitHub Actions `on: pull_request` + `paths: ['dokumentacja/**']`; `CollocateCI.suggest_whitelist(diff_report)->List[str]` = propozycje do `collocate_whitelist.json` dla terminów OOV; webhook `POST /collocate/approve` + `DomainDictionary.on_approve(term)` → `CollocateDict.add_seed(term)` — zamknięta pętla review→approve→update; monitoring: `CollocateCI.trend(window=10)->TrendReport` = trajektoria naruszeń na 10 ostatnich PR?
- Jak obsłużyć zmianę przypadku z biernika na dopełniacz przy negacji — `NegationCaseShifter` — polskie zjawisko: `'nie dostarczył dokumentacji'` (gen) vs `'dostarczył dokumentację'` (acc); `NegationCaseShifter.detect(token)->bool`: `token.dep_=='obj' AND negation_in_ancestors(token.head)`; `NegationCaseShifter.shift_role(token, case)`: `acc→gen` przy negacji → rola pozostaje `PATIENT` ale `CASE_TO_ROLE` wymaga pominięcia `gen:None`; `MORPH-02`: `MATCH (t:Token{case:'Gen'})-[:HAS_ROLE{role:'PATIENT'}]->() WHERE NOT (n:Token{dep:'neg'})-[:MODIFIES]->(t.head) RETURN t` = gen bez negacji = anomalia; `NegationCaseShifter.as_component(nlp)` = spaCy custom component po `morfeusz` w pipe; pułapka: podwójna negacja `'nie mógł nie dostarczyć'` — `negation_depth > 1` → `shift_role` idempotentny?
- Jak walidować Morfeusz na złotym standardzie NKJP w Pythonie — `NKJPMorfeuszPipeline.validate_against_nkjp()` skrypt: `gold=NKJPGoldTagExtractor.extract(doc_id)` + `pred=MorfeuszAnalyser.analyse(text)` + `CompatResult=NKJPTagMatcher.match(gold,pred)`; `MorfeuszValidator.run_on_nkjp(corpus_path, sample_size=200)->ValidationReport`: pętla per zdanie: `sentence=reader.next()`, `predicted=morfeusz.analyse(sentence.text)`, `f1=F1Scorer.score(sentence.gold_tags, predicted)`; `ValidationReport(coverage=0.87, f1_per_case={'nom':0.93,'acc':0.89,'gen':0.81,'ins':0.91})` — gen najsłabszy przez alternację z biernikiem przy negacji → `NegationCaseShifter` uzupełnienie; `MorfeuszValidator.export_mismatches(report)->CSV` = eksport błędów do analizy; `audit.py --morph-validate` uruchamia pełną walidację jako krok CI?
- Jak zaimplementować testy neutralizacji retorycznej w potoku CI/CD — `NormalizationCI` — pełny przepływ: `NormalizationCI.run(pairs_file='test_pairs.json')` → `NormalizationTestSuite.split_by_domain()` per domenę → `RetoricalNeutralizationTester.evaluate(active, passive)->NormResult`; `NormalizationCI.run_domain_matrix()->Dict[str,float]` = pass_rate per domena; `sys.exit(1)` gdy `mean_pass_rate < 0.85`; `pytest -m norm -q --tb=short` w `pre-commit`; pułapka: `spacy.load('pl_core_news_lg')` wolny start → `conftest.py` `scope='session'`; integracja z `CollocateCI` i `MorfeuszValidator` — wspólny raport `LinguisticCIReport(norm_rate, colloc_violations, morph_f1)`; `audit.py --full-linguistic` = wszystkie 3 sprawdzenia jako jeden krok?
- Jak stworzyć plik konfiguracyjny GitHub Actions dla testów lingwistycznych — `linguistic-tests.yml`: `on: [push, pull_request]` + `paths: ['dokumentacja/**','nlp/**']`; `jobs.linguistic-tests.steps`: (1) `actions/checkout` + `actions/setup-python@v5` Python 3.11; (2) `pip install -r requirements-nlp.txt` (spaCy pl, Stanza, morfeusz2); (3) `python -m pytest -m norm -q` = NormalizationCI; (4) `python -m audit --colloc-check --max-colloc-violations=5`; (5) `python -m audit --morph-validate`; `if: github.event.pull_request.draft == false`; `continue-on-error: false`; artefakty: `uses: actions/upload-artifact` dla `LinguisticCIReport.json`; `timeout-minutes: 15` na cały job; pułapka: model `pl_core_news_lg` 700 MB → `actions/cache` key `spacy-pl-${{ hashFiles('requirements-nlp.txt') }}`?
- Pokaż skrypt iterparse() do strumieniowego wyciągania wiedzy z tabel XML — `XMLKnowledgeExtractor.stream(path)->Iterator[EventFrame]`: `for event, elem in iterparse(path, events=['start','end'])` — `tag=='{tei}cell'` → bufor kolumny; `tag=='{tei}row' AND event=='end'` → `yield build_frame(row_buffer)`; pułapka: `elem.clear()` po `yield` — bez tego RAM rośnie liniowo z rozmiarem pliku; `TEITableReader(schema=ColumnSchema)` mapuje kolumny na wymiary: `col['agent']→AGENT, col['action']→predicate, col['deadline']→timeDimension`; `XMLKnowledgeExtractor.to_frames(path, schema)->List[EventFrame]` = zmaterializowana wersja; pułapka: `encoding='utf-8-sig'` dla plików z BOM z polskich edytorów; `GraphEmitter.emit_from_xml_stream(path, schema, session)` = pipeline streaming → Neo4j bez pośrednich struktur?
- Jak użyć lxml.etree.iterparse() do strumieniowego przesyłania danych do Neo4j w paczkach — `XMLStreamNeo4jWriter.write(path, session, batch_size=100)`: akumuluje `EventFrame` w `buffer: List`, gdy `len(buffer)==batch_size` → `session.run('UNWIND $frames AS f CREATE (:EventFrame{id:f.id,predicate:f.predicate})', frames=buffer)`; `buffer.clear()`; finalnie `flush(buffer)` dla reszty; zysk: pojedyncza transakcja per 100 ramek zamiast N osobnych `CREATE`; `elem.clear()` + `while elem.getprevious() is not None: del elem.getparent()[0]` = pełne czyszczenie drzewa; `XMLStreamNeo4jWriter.stats()->WriteStats(frames_written, batches, elapsed_ms, frames_per_sec)`; pułapka: zbyt duże `batch_size` → timeout Neo4j; rekomendacja: `batch_size=50` dla relacji z wieloma atrybutami?
- Jak wdrożyć fraktalną analizę dokumentów w systemie audytowym — `FractalDepthAnalyser.analyse(doc_id, session)->FractalReport`: rekurencyjnie rozkłada dokument na warstwy `Document→Section→Paragraph→Sentence→EventFrame`; `FractalDepth.compute(node)->int` = głębokość semantyczna węzła w grafie (max path do liścia); korzyść w audycie IT: identyfikacja sekcji `depth > 4` = nadmierna złożoność → `Violation('FRAC-01','LOW')`; `FractalReport.hotspots()->List[Section]` = sekcje z najgłębszą strukturą — kandydaci do refaktoryzacji dokumentacji; `FractalDepthCI.assert_below(doc_id, max_depth=5, session)` = CI gate → blokuje commit gdy sekcja zbyt rozbudowana; korzyść strategiczna: fraktalna metryka jako proxy dla comprehensibility — im głębiej zagnieżdżone zdarzenia, tym trudniejszy audyt manualny; `FractalDepthTrend.track(doc_id)->List[Tuple[version,avg_depth]]` = historyczny wzrost złożoności?
- Pokaż reguły Drools do wyodrębniania aktów mowy z dokumentacji — `SpeechActExtractor` reguły: `rule 'DIRECTIVE' when sent:Sentence(modalWord in {'należy','trzeba','musisz','powinien'}) then speechActs.add(new SpeechAct(sent.getId(),'DIRECTIVE',sent.getText())); end`; `rule 'COMMISSIVE' when sent:Sentence(subject=='Wykonawca', verbLemma in {'dostarczyć','zobowiązać się','zapewnić'}) then speechActs.add(new SpeechAct(sent.getId(),'COMMISSIVE',sent.getText())); end`; `rule 'ASSERTIVE' when sent:Sentence(verbLemma in {'jest','wynosi','posiada'}, negation==false) then speechActs.add(new SpeechAct(sent.getId(),'ASSERTIVE',sent.getText())); end`; `SpeechActReport(directives, commissives, assertives, declarations)` = statystyki per sekcja; `SPEECH-01`: sekcja z 0 DIRECTIVE i 0 COMMISSIVE = martwa sekcja bez zobowiązań → `Violation('SPEECH-01','LOW')`; `SpeechActPlugin` jako krok pipeline po `IntentClassifier`?
- Jak zaimplementować śledzenie referencji zaimkowych (koreferencję) w Pythonie — `PolishCoreferenceResolver.resolve(doc)->CorefChain`: (1) `EntityMentionDetector.detect(doc)` → lista kandydatów `{text, span, features}`; (2) `AntecedentScorer.score(mention, candidates)->Dict[str,float]`: cechy: `gender_match, number_match, distance_sentences, is_subject`; (3) `CorefChain.link(mention, best_antecedent)`; `DocumentContextMemory.window=3` — przechowuje ostatnie 3 zdania jako kontekst; `ReflexivePronounResolver.resolve(się/sobie/siebie)`: `REFLEXIVE_OF` do bieżącego podmiotu; `PolishCoreferenceResolver.update_graph(chain, session)`: `MERGE (a:CorefMention)-[:REFERS_TO]->(b:CorefMention)` per para; `CorefCI.assert_coverage(doc_id, min_resolved=0.7, session)` = CI gate — min 70% zaimków rozwiązanych; test: `resolver.resolve(sent_with_pronoun); assert len(chain.links) >= 1`?
- Jak śledzić referencje zaimków w tekście za pomocą koreferencji — nowy kąt: `CorefGraphEnricher.enrich(chain, session)`: dla każdego `CorefChain.link(mention, antecedent)` → `EventFrame` z `agent=mention.text` aktualizowany: `MATCH (e:EventFrame)-[:HAS_ROLE{role:'AGENT'}]->(m:CorefMention{text:$mention}) MERGE (e)-[:HAS_ROLE{role:'AGENT'}]->(a:Concept{lemma:$antecedent.lemma})`; `CorefPropagator.propagate_agent(chain, session)`: wszystkie ramki z zaimkiem AGENT → zastąp lemmatem antecedenta; `CorefEnrichmentReport(chains_processed, agents_updated, unmapped:List[str])` = raport uzupełnienia; `CorefCI.enrichment_coverage(doc_id, session)->float` = % ramek z AGENT po propagacji (cel ≥ 0.85 vs 0.73 przed); `CorefDependencyGraph.build(doc_id, session)` = graf zależności koreferencyjnych dla wizualizacji; pułapka: cykliczne referencje `'on → system → on'` → `CorefCycleDetector.detect(chain)->bool` + fallback do `EllipsisNode`?
- Jak wyciągnąć hiperonimy ze Słowosieci za pomocą Pythona — `SlowosiecHypernymExtractor.extract(lemma)->List[Synset]`: `client = SlowosiecClient(api_url='https://ws.clarin-pl.eu/slowosiec')` → `GET /synsets?lemma={lemma}&lang=pl`; `response.synsets[0].hypernyms` = lista synsetów nadrzędnych; `SlowosiecHypernymPath.trace(lemma, depth=3)->List[str]`: BFS po relacjach `hypernym` do głębokości 3: `['wykonawca' → 'podmiot_prawny' → 'organizacja' → 'byt']`; `HypernymCache.get(lemma)->Optional[List[Synset]]` — LRU cache 1000 wpisów; `HypernymGraphEmitter.emit(lemma, session)`: `CREATE (:Concept{lemma})-[:IS_A*]->` per krok ścieżki hiperonomii; pułapka: Słowosieć ma ~155k synsety — `SlowosiecProxy.batch_lookup(lemmas)->Dict` zamiast per-lemma HTTP; `SlowosiecHypernymExtractor.to_ontology_fragment(lemma)->str` = Cypher fragment dla `ZasóbDanych` hierarchii?
- Jak wyciągnąć hiperonimy ze Słowosieci za pomocą Pythona — kąt klasyfikacyjny: `HypernymBasedClassifier.classify(lemma, target_domains=['IT','LEGAL','ADMIN'])->str`: porównuje ścieżkę hiperonomii z profil domenowy; `'awaria' → 'zdarzenie_negatywne' → 'stan'` pasuje do domain `IT`; `DomainHypernymProfile(domain, key_hypernyms:List[str])` per domena — `IT={'system','usługa','procedura','zasób'}`, `LEGAL={'podmiot_prawny','zobowiązanie','naruszenie'}`; `HypernymClassificationPlugin` jako krok po `NEREntityRuler` — klasyfikuje nowe terminy; `SlowosiecHypernymExtractor.classify_batch(lemmas, profiles)->Dict[str,str]` = masowa klasyfikacja; `ClassificationCoverage.run(session)->float` = % konceptów z przypisaną domeną; `AuditITSynsetDictionary.auto_extend(classified:Dict)` = rozszerzenie słownika przez klasyfikację hiperonomiczną?
- Jak allReduce optymalizuje event reasoning w dużych dokumentach — `REDUCE(score=0, e IN nodes(path) | score + e.chain_score)` = agregacja oceny łańcucha przyczynowego w jednym przebiegu; dla dokumentu 150 stron: bez `REDUCE` — N osobnych zapytań per EventFrame; z `REDUCE` — 1 zapytanie z `{1,n}` QPP + `REDUCE` = 4× szybciej; `allReduceDimensions(e)->float`: `REDUCE(s=0, dim IN ['AGENT','ACTION','PATIENT','TIME','LOCATION','INSTRUMENT','ENVIRONMENT','PATIENT_INDIRECT'] | s + (CASE WHEN e[dim] IS NOT NULL THEN 1 ELSE 0 END)) / 8.0` = `dim_coverage` 0–1 per ramka; `AllReduceReport(doc_id, avg_coverage, min_coverage_frames:List[str])` = identyfikacja najsłabiej uzupełnionych ramek; `REDUCE` + `UNWIND` + `apoc.periodic.iterate` = kompletny stack wysokowydajny dla dużych dokumentów; `EventReasoningEngine.apply_with_reduce(doc_id, session)->AllReduceReport`?
- Jak allReduce optymalizuje event reasoning w dużych dokumentach — kąt alertów i threshold: `AllReduceReport.below_threshold(min_coverage=0.5)->List[str]` = ramki z `dim_coverage < 0.5` = priorytet uzupełnienia; `DimCoverageAlerter.check(report)`: gdy `avg_coverage < 0.6` → `Violation('DIM-01','MEDIUM')` per dokument; `ReduceParallelExecutor.run(doc_id, session)`: `apoc.periodic.iterate` + `REDUCE(s=0, ...)` = równoległa agregacja per batch; `ReduceParallelExecutor.benchmark(doc_id)->BenchmarkResult`: sekwencyjny 45s → równoległy 11s (4 wątki); `AllReduceReport.export_markdown(path)` = sekcja `## Pokrycie wymiarów` z tabelą per EventFrame; `DIM-01`: `MATCH (e:EventFrame) WHERE size([(e)-[:HAS_ROLE]->() | 1]) < 3 RETURN e.id` = ramki z < 3 wymiarami; CI: `audit.py --dim-coverage --min=0.6`?
- Jak zintegrować koreferencję z analizą intencji i przypisać nakaz do aktora — `CorefIntentLinker.link_actor(chain:CorefChain, intent_frame:IntentFrame)->ActorBinding`: dla każdego `CorefChain` z `mention.role=='AGENT'` → wyszukuje `IntentFrame` w tym samym `doc_id` z `speech_act in {'DIRECTIVE','COMMISSIVE'}` → `ActorMandateAssigner.assign(binding)`: `MATCH (i:IntentFrame{id:$intent_id}) MERGE (i)-[:ACTOR_IS]->(a:Concept{lemma:$actor_lemma})`; `CorefContextWindow(doc_id, window_size=5)` → bufor ostatnich 5 zdań dla rozwiązywania anafor; `CorefZaimkiTracker.track(text)->List[PronounMention]`: skanuje `on/ona/oni/ono/je/go` + taguje `PronounMention(form, sent_idx, coref_chain_id)`; `CorefIntentReport(actors_assigned, unresolved_mandates:List[str])` = % nakazów z przypisanym aktorem (cel ≥0.9); pułapka: nakaz w stronie biernej `'procedura powinna być wykonana'` → brak jawnego AGENT → `CorefIntentLinker.fallback_to_ellipsis()` wstrzykuje `EllipsisNode` jako proxy aktora?
- Jak programistycznie wyciągnąć hiperonimy ze Słowosieci dla konkretnego słowa — kąt API słowa: `SlowosiecWordQuery.fetch_for(word:str, depth:int=3)->List[HypernymRecord]`: inicjuje `PlWordNetClient(endpoint='http://wordnet.pwr.edu.pl/api')` → `GET /synsets?lemma={word}&lang=pl` → parsuje JSON → `HypernymPath.extract(synset, depth)`: dla `'awaria'` zwraca `['zdarzenie_negatywne','zdarzenie','stan','byt']`; `HypernymCache.warm(lemma, session)`: `MERGE (:HypernymCache{lemma:$lemma}) SET cache.path=$path, cache.refreshed_at=timestamp()`; `SlowosiecWordQuery.batch_fetch(lemmas:List[str], depth=3)->Dict[str,List]` = masowe pobieranie; `HypernymPathVisualizer.to_mermaid(word, path)->str` = schemat Mermaid dla łańcucha hiperonomii; `CacheMissDetector.stale(threshold_days=30, session)->List[str]` = lematy z nieświeżą ścieżką → trigger `SlowosiecWordQuery.batch_fetch` w CI?
- Pokaż przykład wnioskowania na wymiarach intencji dla tekstu o incydencie — `IncidentIntentAnalyzer.analyse(text:str, session)->IncidentIntentReport`: 1) `SpeechActExtractor.extract(text)` → breakdown: `DIRECTIVE=3, PROHIBIT=1, ASSERTIVE=5`; 2) `IntentClassifier.classify_all(sentences)->List[IntentFrame]` z wymiarami: `ACTOR='System_Monitorowania', ACTION='wykryto_naruszenie', PATIENT='serwer_prod', TIME='2025-03-12T03:41', CAUSE='brak_aktualizacji', INTENT='NOTIFY'`; 3) Cypher: `MATCH (e:EventFrame{doc_id:$incident_id})-[:HAS_ROLE{role:'INTENT'}]->(i:Concept) RETURN i.value, count(*) as cnt ORDER BY cnt DESC` = rozkład intencji; `DIM-INTENT-01`: `EventFrame` z `INTENT IN ['DIRECTIVE','PROHIBIT']` bez `ACTOR` → `Violation('DIM-INTENT-01','HIGH','Nakaz bez aktora')`; `IncidentIntentReport.summary()->str` = Markdown: `DIRECTIVE×3, PROHIBIT×1, aktorzy_przypisani=3/4`?
- Jakie są korzyści z fraktalnej analizy dokumentów w audycie IT — kąt wartości biznesowej: (1) **wykrywalność złożoności**: `FractalDepthCI` blokuje commit gdy `avg_depth > 5` → wymusza modularność sekcji zanim dokument stanie się nieutrzymywalny; (2) **porównywalność z wzorcami**: `FractalDepthComparator` vs `StandardTemplateLibrary(ISO-27001)` = delta +1.3 = obiektywna miara odchylenia; (3) **trend historyczny**: `FractalDepthTrend.track()` → `v2.1:3.2→v2.3:4.1` = rosnąca złożoność = sygnał ryzyka przed audytem zewnętrznym; (4) **rekomendacje podziału**: `FractalNormalizationSuggester` generuje konkretne akcje (`'podziel §4.1.2 na a+b, max 3 EventFrame'`); (5) CI gate `FractalCI.compare_to_template(max_delta=1.0)` = automatyczna blokada regresji strukturalnej; `FractalAuditBenefit.report(doc_id)->str` = sekcja `## Złożoność` w raporcie końcowym z trendami, deltamiISO i listą hotspotów; `FractalROI.estimate(violations_prevented)->float` = szacowany koszt unikniętych błędów strukturalnych?
- Jak zaimplementować testy parametryczne dla kolokacji w pytest — `@pytest.mark.parametrize('pair,expected', [('wdrożyć system',True), ('system wdrożyć',False), ('wykonać procedurę',True), ('procedura wykonać',False)])` w `test_collocation_validator.py`; `CollocateValidator.validate(pair)->bool` = sprawdza PMI ≥ threshold + kolejność lemmatów zgodna z wzorcem kolokacyjnym; `@pytest.fixture def corpus_sample()` → `NKJPCorpusReader.load_sample(n=500)` = mini-korpus; `@pytest.mark.parametrize('tag,expected_role', [('Case=Nom','AGENT'), ('Case=Gen','PATIENT_NEG'), ('Case=Acc','PATIENT')])` → test mapowania tagów morfologicznych na role; `CollocateCI.run_parametric_suite(corpus_sample)` jako krok `pytest -m collocation` w CI; `CollocationTestReport(passed, failed, coverage_pct, worst_pairs:List)` = raport per batch kolokacji; pułapka: kolokacje wielowyrazowe `'przeprowadzić audyt bezpieczeństwa'` → `MultiWordCollocateValidator.validate(triple)->bool` z PMI₃?
- Jak sklasyfikować zdarzenie jako kłusownictwo (domenę prawną) w grafie — `LegalDomainEventClassifier.classify(event_lemma:str, session)->ClassificationResult`: 1) `SlowosiecWordQuery.fetch_for('kłusownictwo', depth=3)` → ścieżka `['przestępstwo_łowieckie','przestępstwo','czyn_zabroniony','zdarzenie_prawne']`; 2) `HypernymBasedClassifier.classify('kłusownictwo', target_domains=['LEGAL','CRIMINAL'])` → `domain='LEGAL', confidence=0.91`; 3) `DomainHypernymProfile('LEGAL', key_hypernyms=['podmiot_prawny','zobowiązanie','naruszenie','czyn_zabroniony'])`; `AuditITSynsetDictionary.lookup('kłusownictwo')` → `synset_id='pl-174823-n'`; `LegalEventTagger.tag(event_frame, session)`: `MATCH (e:EventFrame{id:$eid}) SET e.legal_domain='CRIMINAL', e.synset_id='pl-174823-n', e.confidence=0.91`; `ClassificationCoverage.run(session, domain='LEGAL')->float` = % EventFrame z przypisaną domeną prawną; pułapka: homonim `'kłusownictwo'` vs `'kłusować'` (bieganie) → `DisambiguationFilter.resolve(lemma, context_window)->synset_id` przed klasyfikacją?
- Jak zintegrować moduł Bartek do koreferencji w pipeline NLP — **NEW** `BartekCoreferenceResolver`: polskie narzędzie do koreferencji (Bartek — IPIPAN, model BERT-based dla PL); `BartekCoreferenceResolver.resolve(text:str)->List[CorefCluster]`: wywołuje `bartek.predict(text)` → `CorefCluster(mention_spans:List[Span], representative:str, cluster_id:int)`; integracja z pipeline: po `StanzaMorphValidator` → `BartekCoreferenceResolver.resolve()` → `CorefGraphEnricher.enrich(clusters, session)`; `BartekVsPCCEvaluator.compare(bartek_chains, pcc_gold)->CorefMetrics`: benchmark vs CorefGraphEnricher: `bartek_conll=0.87 vs prev_conll=0.80` (+8.75%); `BartekPipelineStep` jako opcjonalny krok z `use_bartek=True` flag; `BartekModelLoader.load(model_path='bartek-pl-base')->BartekModel` = lazy loading z cache; pułapka: Bartek wymaga całego tekstu naraz → `BartekDocumentBatcher.batch(doc, max_tokens=512)` = sliding window z overlap 50 tokenów?
- Jak radzić sobie z idiomami i frazeologią w potoku NLP — **NEW** `IdiomPhraseologyDetector`: wykrywa idiomy, które nie powinny być dosłownie analizowane jako EventFrame; `IdiomPhraseologyDetector.detect(sent:Span)->Optional[IdiomMatch]`: 1) `IdiomLexicon.lookup(sent.text)` = słownik frazeologiczny (`{'wykonać krok':None, 'wziąć pod uwagę':None, 'na pierwszym planie':'PRIORITY'}`) — wyrażenia transparentne semantycznie (None) vs idiomy z sensem domenowym; 2) dla transparentnych: `EventFrame` nie tworzony → `IdiomNode{text, type:'TRANSPARENT'}` jako stub; dla idiomów z sensem: mapowanie na predykat: `'na pierwszym planie'→'prioritize'`; `IdiomPhraseologyDetector.add_to_lexicon(phrase, mapped_predicate, session)` = rozszerzanie słownika; `IdiomFilter.filter_before_emit(doc_id, session)`: usuwa transparentne idioomy z kolejki `GraphEmitter`; `IdiomReport(detected, transparent, mapped, unknown:List[str])` = raport per dokument; pułapka: `'przeprowadzić audyt'` = kolokacja (PMI≥3) nie idiom → `CollocateValidator.is_collocation(phrase)->bool` PRZED `IdiomLexicon.lookup()`?
- Pokaż przykład testu pytest dla mapowania Case=Gen przy negacji — `@pytest.mark.parametrize('sent,expected_role,expected_case', [('nie wdrożył systemu', 'PATIENT_NEG', 'Case=Gen'),('nie dostarczono raportu', 'PATIENT_NEG', 'Case=Gen'),('nie wykryto naruszenia', 'PATIENT_NEG', 'Case=Gen'),('wdrożył system', 'PATIENT', 'Case=Acc'),('dostarczono raport', 'PATIENT', 'Case=Acc')])` w `test_negation_case.py`; `NegationCaseShifter.shift(sent)->MorphToken` = testowana funkcja; `assert result.role == expected_role and result.morph.case == expected_case`; `@pytest.fixture def morph_tagger()` = `Morfeusz2() + StanzaMorphValidator()`; `NegationCaseCI.run(test_negation_case, corpus_sample)` = krok `pytest -m negation` w CI; edge case: `'nie ma systemu'` → `'mieć'` + Case=Gen ale rola = `EXISTENCE_NEG` nie PATIENT_NEG → `NegationRoleClassifier.classify(verb, case)->str` przed przypisaniem roli?
- Pokaż przykład neutralizacji wariancji retorycznej dla pary zdań — kąt konkretnej pary z kodem: para: `A='Administrator wdrożył system 12.03.2025'` vs `B='System został wdrożony przez Administratora 12.03.2025'`; `VoiceNeutralizer.normalize(A)` → `NormalizedFrame(AGENT='Administrator', ACTION='wdrożyć', PATIENT='system', TIME='2025-03-12')`; `VoiceNeutralizer.normalize(B)` → ta sama ramka; `assert NormalizedFrame(A) == NormalizedFrame(B)` = test deterministyczności; trzecia para: `C='Wdrożenie systemu nastąpiło 12.03.2025'` (bezosobowa) → `AGENT=None, ACTION='wdrożyć', PATIENT='system', TIME='2025-03-12'`; w tym przypadku `EllipsisInFlightReconstructor` próbuje wypełnić AGENT; `RhetoricalVarianceTestSuite.run(pairs:List[Tuple])` = batch test wszystkich par; `RhetoricalVarianceReport(pairs_equal, pairs_mismatch, bezosobowe_reconstructed:List)` = raport z parami, które nie osiągnęły równoważności?
- Jak wstrzyknąć wykryte hiperonimy jako atrybuty węzłów w grafie — kąt bezpośredniego kodu Python: `HypernymNodeAttributeInjector.inject(lemma:str, session)`: `path = SlowosiecWordQuery.fetch_for(lemma, depth=3)` → `['system','artefakt_techniczny','byt']`; `with session.begin_transaction() as tx:` `  tx.run('MATCH (c:Concept{lemma:$l}) SET c.hypernym_l1=$h1, c.hypernym_l2=$h2, c.hypernym_l3=$h3, c.hypernym_path=$path, c.hypernyms_injected_at=datetime()', l=lemma, h1=path[0], h2=path[1] if len(path)>1 else None, h3=path[2] if len(path)>2 else None, path=path)`; `HypernymAttributeValidator.validate(session)`: `MATCH (c:Concept) WHERE c.hypernym_l1 IS NULL RETURN count(c)` = brakujące; `HypernymInjectionReport(injected, failed, avg_depth, coverage_pct)` per batch; optymalizacja: `tx.run('CREATE INDEX concept_hypernym FOR (c:Concept) ON (c.hypernym_l1)')` = szybkie `MATCH` po hiperonimiach L1; pułapka: `depth=3` dla rzadkich lematów może zwrócić `[]` → `HypernymFallback.use_domain_default(lemma, domain)->str`?
- Jak zintegrować NKJPCorpusReader z potokiem przetwarzania — kąt step-by-step integracji: (1) `NKJPCorpusReader(root=nkjp_path, fileids='.*\.xml')` = inicjalizacja; (2) `reader.tagged_sents(fileids=selected)` → `List[List[Tuple[str,str]]]` = tagowane zdania; (3) `Morfeusz.lemmatize_tagged(tagged_sent)` → lematy z weryfikacją tagów NKJP; (4) `CollocateValidator.validate_batch(lemma_pairs)` = weryfikacja kolokacji vs NKJP; (5) `HypernymNodeAttributeInjector.inject_batch(lemmas, session)` = wstrzyknięcie; (6) `NKJPDocumentValidator.validate(doc_id, session, nkjp_profile)` = spójność domenowa; `NKJPPipelineStep` jako opcjonalny etap `AuditPipeline` z flagą `use_nkjp=True`; `NKJPPipelineConfig(root_path, categories:List[str], max_docs:int=200)` = konfiguracja; `NKJPIntegrationReport(tagged_sents, lemmas_injected, collocation_verified, domain_validated, duration_ms)` = raport integracji?
- Jak napisać zapytanie Cypher automatycznie klasyfikujące zdarzenie dla domeny prawnej — `MATCH (e:EventFrame)-[:HAS_ROLE{role:'ACTION'}]->(a:Concept) WHERE NOT (e)-[:LEGAL_CLASS]->() CALL { WITH a MATCH (a)-[:IS_A*1..4]->(h:Concept) WHERE h.lemma IN ['czyn_zabroniony','naruszenie','przestępstwo','wykroczenie'] RETURN h.lemma AS legal_class, count(*) AS depth ORDER BY depth ASC LIMIT 1 } MERGE (e)-[:LEGAL_CLASS{auto:true, confidence:0.85}]->(lc:LegalConcept{lemma:legal_class}) RETURN e.id, legal_class`; `LegalClassAutoTagger.run(session)` = wrapper batch: `apoc.periodic.iterate` po 100 ramek; `LegalClassificationReport(classified, unclassified, top_classes:List[Tuple])` = top 5 klas z licznością; `LEGAL-CLASS-01`: `EventFrame` z `speech_act='DIRECTIVE'` bez `LEGAL_CLASS` → `Violation('LEGAL-CLASS-01','LOW','Nakaz bez klasyfikacji prawnej')`; CI: `audit.py --legal-class-coverage --min=0.75` = blokada gdy < 75% nakazów sklasyfikowanych?
- Jak wdrożyć pętlę sprawdzania statusu KIE Server w Pythonie — `KieStatusPoller.poll(url, interval_s=5, max_retries=12)->PollerResult`: `for attempt in range(max_retries): resp=requests.get(f'{url}/server', timeout=3); if resp.status_code==200 and resp.json()['result']['kie-server-info']['version']: return PollerResult(ready=True, attempt=attempt); time.sleep(interval_s)`; `KieConnectionError` gdy wyczerpane próby; `KieStatusPoller.poll_until_ready(url, timeout_s=60)` = retry z exponential backoff: `interval=min(interval*1.5, 30)`; `KieHealthCheck.run(url)->HealthReport(status, version, containers:List[str], latency_ms)`: `GET /server/containers` → lista aktywnych KContainerów; `KiePollerCI.assert_ready(url, timeout_s=120)` = krok pre-deploy w CI — blokuje jeśli KIE Server nie odpowie w 2 min; `KieSessionPool.warm(url, container_id)` = pre-warm session po potwierdzeniu gotowości → redukuje cold-start latency pierwszego `fireAllRules()` z 800ms do 120ms?
- Jak zautomatyzować weryfikację spójności między dokumentacją a testami jednostkowymi — `DocTestConsistencyChecker.run(doc_path, test_dir)->ConsistencyReport`: 1) `RuleIdExtractor.from_doc(doc_path)->Set[str]` = zbiór rule ID z pliku MD (regex `r'[A-Z]+-\d+'`); 2) `RuleIdExtractor.from_tests(test_dir)->Set[str]` = `grep -r 'rule_id' tests/`; 3) `missing_tests = doc_ids - test_ids`; `missing_docs = test_ids - doc_ids`; `ConsistencyReport(missing_tests, missing_docs, coverage_pct)` = raport; `CI step`: `pytest tests/test_doc_consistency.py` — `assert len(missing_tests)==0`; `RuleTestGenerator.generate_stub(rule_id)->str` = auto-generuje `def test_{rule_id}(): pass` dla brakujących testów; `DocCoverageGate.assert_full(doc_path, test_dir)` = fail gdy `coverage_pct < 1.0`; integracja: `linguistic-tests.yml` + krok `doc-test-consistency` jako osobny job w GitHub Actions?
- Jak przetestować cały potok NLP end-to-end — `NLPPipelineE2ETest.run(text, session)->E2EResult`: etapy: (1) `MorfeuszTokenizer.tokenize(text)` → tokeny; (2) `spaCy-pl NER+deps`; (3) `StanzaMorphValidator.validate(tokens)` → cechy; (4) `GraphEmitter.emit(doc_id, session)` → EventFrame w Neo4j; (5) `GraphDroolsBridge.run_cycle(doc_id, session, kie_session)` → Violations; (6) `AllReduceAuditPlugin.run(doc_id, session)` → DIM-AUDIT-01; fixture: `@pytest.fixture def sample_incident_text()` = zdanie testowe z AGENT+ACTION+TIME; `assert e2e.event_frames >= 1 and e2e.violations_by_rule.get('DIM-AUDIT-01',0) == 0`; `NLPPipelineE2ETest.benchmark(text, n=10)->BenchmarkResult(avg_ms, p95_ms)` = pomiar latencji; cel: p95 < 2000ms per dokument ~50 zdań; `e2e_test.yml` = osobny workflow GitHub Actions uruchamiany na push do `main`?
- Jak wdrożyć DeepEREntityDetector do wykrywania obiektów wykraczających poza nazwy własne — **NEW** `DeepEREntityDetector(spacy_model, hypernym_profiles:Dict[str,DomainHypernymProfile])`: rozszerza standardowy `EntityRuler` o rozpoznawanie semantyczne przez hiperonimy; pipeline: (1) `spaCy NER` → standardowe ORG/PERSON/LOC; (2) dla tokenów `POS in ['NOUN','PROPN']` bez NER label → `SlowosiecWordQuery.fetch_for(lemma, depth=3)` → `HypernymBasedClassifier.classify(lemma)`; (3) gdy `domain in ['IT','LEGAL','ADMIN','FINANCE']` → `DeepEREnt(text, label=domain, synset_id, confidence)`; `DeepEREntityDetector.register_profile(domain, key_hypernyms)` = nowa domena; przykład: `'procedura_awaryjna'` → NER miss → hypernym `'procedura→działanie→byt'` → `DeepEREnt(label='PROC_IT', confidence=0.88)`; `DeepERReport(standard_ner, deeper_ner, coverage_gain_pct)` = wzrost pokrycia NER; `DeepERPlugin` jako krok spaCy po `NEREntityRuler` w `AuditPipeline`; pułapka: rzeczowniki ogólne `'element','część'` → fałszywe wykrycia → `DeepERStopList.filter(lemma)->bool` + próg `confidence >= 0.75`?
- Jakie są najczęstsze błędy przy modelowaniu ontologii dla auditów IT — kąt katalogu pułapek: (1) **eksplozja dziedzin** — `OntologyBuilder.infer_domain()` bez `DOMAIN_WHITELIST` → `OntologyDriftMetric` alarmuje; fix: `max_domains_per_document=5`; (2) **płytkie hierarchie** — brak `IS_A` dla terminów pochodnych → `SynsetPropagator` nie propaguje; fix: `PropagationDepthGuard.check()` po każdym `inject_batch()`; (3) **polisemia bez WSD** — `AuditITSynsetDictionary` mapuje `'wymaganie'` do złego synsetu bez `DisambiguationFilter`; fix: `DisambiguationFilter.resolve(lemma, context_window)`; (4) **cykl IS_A** — ręczna edycja tworzy `A→IS_A→B→IS_A→A`; fix: `CorefCycleDetector.detect(chain)` + `QPPCycleDetector.detect()`; (5) **brak normalizacji lemmatów** — `'System'` vs `'system'` = 2 węzły; fix: `ConceptNormalizer.normalize(lemma)->str` = lowercase + `Morfeusz.lemmatize()`; `OntologyHealthReport(pitfall_counts:Dict[str,int], risk_level:str)` = raport per dokument?
- Jak przetestować koreferencję używając Polskiego Korpusu Koreferencyjnego (PCC) — `PCCCorpusValidator.load(pcc_path)->List[CorefDocument]`: parsuje PCC XML/JSONL → `CorefDocument(doc_id, sentences, gold_chains:List[CorefChain])`; `CorefEvaluator.evaluate(predicted_chains, gold_chains)->CorefMetrics`: MUC, B³, CEAFₑ, CoNLL-F1 (średnia z 3); przykład: `gold={'on':'System_Monitorowania'}, predicted={'on':'system'}` → `MUC_F1=0.82, B3_F1=0.79, CoNLL=0.80`; `PCCBenchmarkSuite.run(CorefGraphEnricher, pcc_path)->BenchmarkReport`: per łańcuch koreferencyjny: precision, recall, F1; `@pytest.mark.parametrize('doc_id', PCC_SAMPLE_IDS)` = 20 dokumentów testowych; `CorefCI.assert_conll_f1(min=0.75, pcc_path)` = blokada CI gdy CoNLL-F1 < 0.75; pułapka: PCC używa id-based chains → `ChainIDMapper.map(pcc_chain, our_chain)` normalizuje przed porównaniem?
- Wyjaśnij różnicę między prostym tagowaniem a pełnym Event Reasoning — kąt inżynieryjny: **tagowanie** (`spaCy NER + Morfeusz`) = etykiety na tokenach: `[System_Monitorowania/ORG] [wykrył/VB] [naruszenie/NOUN]` — brak relacji, brak przyczyny, brak intencji; **Event Reasoning** = `EventFrame{id, predicate='wykryć', AGENT='System_Monitorowania', PATIENT='naruszenie', TIME='2025-03-12', CAUSE='brak_aktualizacji', INTENT='NOTIFY', LOCATION='serwer_prod'}` — 8 wymiarów; różnica praktyczna: tagowanie → `grep -c 'naruszenie' doc.txt = 3`; Event Reasoning → `MATCH (e:EventFrame)-[:HAS_ROLE{role:'CAUSE'}]->(c:Concept{lemma:'brak_aktualizacji'}) RETURN e.id, e.predicate` = kauzalne zapytanie niemożliwe w tagowaniu; `EventReasoningCoverageMetric.compare(tagged_doc, reasoned_doc)->CoverageGap`: `tagged_entities=12, event_frames=5, dims_filled=37/40, causal_chains=2` = raport wartości dodanej; reguła: tagowanie = prerequisite; Event Reasoning = cel docelowy potoku?
- Jak napisać testy parametryczne dla kolokacji w pytest — kąt rozszerzony z edge cases: `@pytest.mark.parametrize('lemma_pair,expected_pmi,expected_valid', [('wdrożyć_system', 4.2, True), ('system_wdrożyć', -0.3, False), ('przeprowadzić_audyt', 5.1, True), ('audyt_przeprowadzić', -1.2, False), ('wykryć_naruszenie', 3.8, True), ('brak_aktualizacji', 6.0, True)])` w `test_collocation_pmi.py`; `assert abs(CollocateValidator.pmi(pair) - expected_pmi) < 0.5`; `@pytest.mark.parametrize('context,neg_word,expected_case', [('nie wykonał', 'nie', 'Case=Gen'), ('nie wdrożył', 'nie', 'Case=Gen'), ('wykonał', None, 'Case=Acc')])` → `NegationCaseShifter` edge-case tests; `@pytest.fixture(scope='session') def nkjp_bigrams()` → `NKJPCorpusReader.load_bigrams(n=10000)` = zbiór referencyjny; `CollocationCoverageTest.assert_pmi_coverage(nkjp_bigrams, min_coverage=0.80)` = % znanych kolokacji z PMI ≥ 2.0 w NKJP; `pytest -m collocation -v` w `linguistic-tests.yml`?
- Wyjaśnij różnicę między prostym tagowaniem a pełnym Event Reasoning — kąt porządku temporalnego i łańcuchów: tagowanie = zbiór niezależnych etykiet bez kolejności; Event Reasoning = graf z relacjami `[:FOLLOWS]` i `[:CAUSE]` między ramkami; Cypher temporalny niemożliwy przy tagowaniu: `MATCH p=(e1:EventFrame)-[:FOLLOWS*1..3]->(e3:EventFrame{predicate:'eskalacja'}) WHERE e1.doc_id=$doc RETURN [n IN nodes(p) | n.predicate] AS chain`; `EventChainBuilder.build(doc_id, session)->EventChain`: tworzy ordered sequence EventFrame po `sent_idx` + łączy `[:FOLLOWS]`; `EventTemporalReport.gaps(doc_id, session)->List[TemporalGap]`: wykrywa `e1.TIME > e2.TIME` przy `e1-[:FOLLOWS]->e2` = cofnięcie w czasie → `Violation('TEMPORAL-02','MEDIUM','Anachronizm w sekwencji zdarzeń')`; `TEMPORAL-02`: nowy rule ID — błąd kolejności temporalnej w łańcuchu zdarzeń?
- Jakie są etapy analizy dokumentacji w potoku NLP — kompletna lista `AuditPipeline.stages`: (1) `MarkdownASTReader.read()` → sekcje/paragrafy/tabele; (2) `MorfeuszTokenizer.tokenize()` → tokeny z tagami morfologicznymi; (3) `spaCy-pl NER+deps` → encje + zależności składniowe; (4) `StanzaMorphValidator.validate()` → cechy Case/Mood/Voice; (5) `NegationCaseShifter` → Case=Gen przy negacji; (6) `DeepEREntityDetector` → obiekty poza standardowym NER; (7) `CollocateValidator` → walidacja PMI kolokacji; (8) `EllipsisInFlightReconstructor` → rekonstrukcja brakujących AGENT; (9) `CorefIntentLinker` → koreferencja → intencja → aktor; (10) `GraphEmitter.emit()` → EventFrame w Neo4j; (11) `WordNetWSDIntegrator` → WSD dla Concept; (12) `GraphDroolsBridge.run_cycle()` → Drools violations; (13) `AllReduceAuditPlugin` → DIM coverage; (14) `CascadeAuditPlugin` → kaskady braków; (15) `AuditReportGenerator.generate()` → Markdown/HTML raport; `AuditPipeline.run(doc_path, session)->AuditResult` = cały potok; `AuditPipeline.benchmark(doc_path)->PipelineProfile(stage_ms:Dict)` = profil per etap?
- Jak system automatycznie wyciąga intencje (akty mowy) z surowego tekstu — kąt end-to-end pipeline: `SpeechActPipeline.extract(raw_text:str)->List[SpeechAct]`: (1) `SentenceSegmenter.split(raw_text)` → `List[str]`; (2) `MorfeuszTokenizer.tokenize(sent)` → tokeny z `Mood/Voice/Case`; (3) `ModalWordDetector.detect(tokens)` → `modal in {'należy','trzeba','musi','powinien'}` → klasa wstępna `DIRECTIVE`; (4) `ConditionalIntentExtractor.detect(tokens)` → markery `{'jeżeli','o ile'}` → `CONDITIONAL`; (5) `VoiceNeutralizer.neutralize(sent)` → strona bierna → `ActorMandateAssigner` uzupełnia AGENT; (6) `SpeechActClassifier.classify(sent, modal, conditional)->SpeechAct(type, confidence, sent_id)`; `SpeechActPipeline.batch(doc_id, session)->SpeechActReport(directives, commissives, assertives, conditionals, declarations, unclassified_pct)` = statystyki per typ; cel: `unclassified_pct < 0.1` = 90% zdań z przypisanym typem aktu mowy; `SpeechActCI.assert_coverage(min=0.9, doc_id)` = gate CI?
- Przygotuj testy parametryczne w pytest weryfikujące mapowanie zdań na wymiary grafu — `@pytest.mark.parametrize('sentence,expected_dims', [('Administrator wdrożył system 12.03.2025', {'AGENT':'Administrator','ACTION':'wdrożyć','PATIENT':'system','TIME':'2025-03-12'}),('System wykrył naruszenie z powodu braku aktualizacji', {'AGENT':'System','ACTION':'wykryć','PATIENT':'naruszenie','CAUSE':'brak_aktualizacji'}),('Procedura powinna być wykonana przez Wykonawcę', {'AGENT':'Wykonawca','ACTION':'wykonać','PATIENT':'Procedura','INTENT':'OBLIGATION'}),('Nie dostarczono raportu', {'ACTION':'dostarczyć','PATIENT_NEG':'raport','negated':True})])` w `test_eventframe_mapping.py`; `EventFrameMapper.map(sentence, session)->Dict[str,str]` = testowana funkcja; `assert set(result.keys()) >= set(expected_dims.keys())`; `assert all(result[k]==v for k,v in expected_dims.items())`; `@pytest.fixture def morph_pipeline()` = Morfeusz+spaCy+Stanza gotowy do testów; `EventFrameMappingCI.run(test_file, session)` = krok `pytest -m eventframe` w CI; cel: 100% zdań testowych z poprawnym mapowaniem ≥ 3 wymiarów?
- Jak działa neutralizacja wariancji retorycznej mapująca czynną i bierną na deterministyczny wzorzec — kąt algorytmu: `VoiceNeutralizer.normalize(sent:Span)->NormalizedFrame`: (1) `Stanza.morph` → `Voice=Pass` gdy `nsubj:pass + aux:pass`; (2) strona bierna: `'System został wdrożony przez Administratora'` → swap: `nsubj:pass='System'→PATIENT`, `obl:agent='Administratora'→AGENT`; (3) strona czynna: `'Administrator wdrożył system'` → `nsubj='Administrator'→AGENT`, `obj='system'→PATIENT`; wynik identyczny: `NormalizedFrame(AGENT='Administrator', ACTION='wdrożyć', PATIENT='System')`; `DeterministicPatternValidator.validate(active_sent, passive_sent, session)`: `assert NormalizedFrame(active) == NormalizedFrame(passive)` = test deterministyczności; `RhetoricalVarianceCI.run(sentence_pairs:List[Tuple], session)` = test dla zbioru par czynna↔bierna → `assert all_equal`; `VoiceNormalizationReport(pairs_tested, normalized_equal, mismatches:List)` = raport?
- Jak zintegrować NKJPCorpusReader z nltk do nakładania hiperonimów na teksty korpusowe — `NKJPHypernymOverlay.run(nkjp_path, session)`: 1) `NKJPCorpusReader(nkjp_path).words(categories='')` → tokeny z NKJP; 2) `Morfeusz.lemmatize(token)` → lemma per token; 3) `SlowosiecWordQuery.batch_fetch(lemmas, depth=2)` → hiperonimy; 4) `HypernymBasedClassifier.classify(lemma, target_domains=['IT','LEGAL'])` → domain; 5) `WordNetSynsetInjector.inject_batch(classified_lemmas, session)` → atrybuty Neo4j; `NKJPHypernymValidationReport(tokens_processed, classified_pct, domain_distribution:Dict)`: przykład: `IT=34%, LEGAL=21%, ADMIN=18%, unclassified=27%`; `NKJPHypernymCI.assert_coverage(min=0.73, session)` = gate CI; `NKJPHypernymOverlay.compare_domains(doc_id, nkjp_sample)->DomainComparisonReport`: weryfikuje czy hiperonimy dokumentu są spójne z rozkładem w NKJP; pułapka: NKJP zawiera teksty z lat 1945–2012 → archaiczne formy → `LemmaAgeFilter.filter(lemma, max_age=30)` przed klasyfikacją?
- Jakie są korzyści z mapowania tagów fleksyjnych na relacje semantyczne — uzupełnienie ROLE-09: metryki benchmarkowe na NKJP 200-zdaniowym złotym standardzie: `dep_only_precision=0.87, dep_only_recall=0.79` vs `morph_augmented_precision=0.88, morph_augmented_recall=0.91` — recall +12% głównie przez zdania OSV i passivum; `DualSignalRoleMapper(dep_weight=0.6, morph_weight=0.4)->EventDimensionMatrix` = ważony konsensus; `MorphRoleConflictDetector.resolve(token)->str`: tiebreaker: gdy konflikt → morph wins dla `nom/acc/gen`, dep wins dla `obl/iobj`; `RoleConfidenceMatrix.compute(token)->Dict[str,float]` = rozkład prawdopodobieństwa per rola; `ROLE-09` Cypher monitoring: `MATCH (t:Token) WHERE t.dep_role <> t.morph_role RETURN t.lemma, t.dep_role, t.morph_role, t.sent_id ORDER BY t.sent_id` — eksport do CSV dla ręcznej weryfikacji?
- Jak automatycznie mapować nagłówki tabel na relacje ontologiczne — `TableHeaderOntologyMapper.auto_map(headers:List[str])->Dict[str,RelationType]`: reguły priorytetowe: (1) **exact match** `HEADER_EXACT={'agent':'AGENT','wykonawca':'AGENT','zamawiający':'PATIENT','termin':'timeDimension','narzędzie':'INSTRUMENT'}`; (2) **lemma match** przez Morfeusz: `morfeusz.analyse(header)[0].lemma` → lookup w `HEADER_EXACT`; (3) **embedding similarity** `cos(embed(header), embed(known_role)) > 0.8`; `TableHeaderOntologyMapper.explain(header)->MappingReason`: `{method:'exact'|'lemma'|'embedding', confidence:float, matched_to:str}`; `ColumnSchema.from_headers(headers, mapper)->ColumnSchema` = auto-konfiguracja `TEITableReader`; `STRUCT-07`: brak zmapowanego nagłówka dla żadnej roli → `Violation('STRUCT-07','LOW')`; pułapka: header `'Podmiot'` → Morfeusz lemma `'podmiot'` → `AGENT` — ale `podmiot` może być AGENT lub PATIENT zależnie od kontekstu?
- Jakie są korzyści ze stosowania tagów fleksyjnych w mapowaniu relacji — tagi morfologiczne z Morfeusza/NKJP jako disambiguation layer: `NKJPSemanticRoleInducer` poprawia AGENT/PATIENT recall o ~12% vs dep-parse `nsubj/obj` samodzielnie (wg NKJP benchmark); pułapka dep-parse: `'Dokumentację dostarczył wykonawca'` (OSV) — `nsubj` wskazuje `dokumentację` (acc) błędnie jako AGENT; tag `acc` z Morfeusza koryguje → `NegationCaseShifter` + `NKJPSemanticRoleInducer` jako dual-signal; `ROLE-09`: `MATCH (t:Token) WHERE t.dep_role != t.morph_role AND t.morph_role IS NOT NULL` = konflikt rola-dep vs rola-morph → wymagana ręczna weryfikacja; `MorphRoleConflictDetector.scan(session)->List[ConflictToken]`; beneficjent: Drools facts — `EventFrame` z poprawnym AGENT umożliwia `AI-LIABILITY-01` firing dla zdań WSO/OSV?

### 4. Testowanie

- Jak napisać test własnościowy (Hypothesis) dla `gap_detector.py`, który gwarantuje, że `completeness_score ∈ [0,1]`?
- Jak przetestować regresję po zmianie progów podobieństwa — złoty wzorzec dla 10 dokumentów?
- Jak zmierzyć pokrycie mutacyjne (Mutation Score) dla W0 — jakie są minima dla projektu zarobkowego?
- Jakie testy integracyjne należy napisać dla subkomendy `doc-audit` w `compliance_check.py`?
- Napiszmy pierwszy RED test dla `CrossReferenceEngine` na poziomie unit — `def test_cross_ref_engine_no_violations_for_empty_docs(): engine = CrossReferenceEngine([]); result = engine.check_cascade(Violation('AUTH-01','node_x')); assert result == []`; RED bo konstruktor `CrossReferenceEngine([])` nie istnieje — zakłada co najmniej jeden dokument; unit test bez fixtury Neo4j?
- Napiszmy RED test dla bazy danych bez backupu — `def test_backup01_rule_fires_on_db_without_policy(): session = Mock(); session.run.return_value = [{'db': {'id': 'db_prod'}}]; rule = Backup01Rule(); violations = rule.run_cypher_rule(session); assert violations[0].rule_id == 'BACKUP-01'; assert violations[0].severity == 'CRITICAL'`; RED bo klasa `Backup01Rule` jeszcze nie istnieje w katalogu reguł?
- Napiszmy RED test integracyjny dla reguły SEC-01 — `@pytest.mark.integration def test_sec01_rule_integration(neo4j_session): neo4j_session.run("MERGE (:EventFrame {id:'e_no_threat', domain:'API'})"); rule = SEC01Rule(); violations = rule.run_cypher_rule(neo4j_session); assert len(violations) == 1; assert violations[0].node_id == 'e_no_threat'`; RED bo `neo4j_session` fixture nie jest jeszcze zarejestrowana w `conftest.py`; test wymaga prawdziwej bazy — `@pytest.mark.integration` odseparowuje od unit testów?
- Dodajmy przypadek testowy dla reguły TEST-01 — `def test_test01_rule_fires_on_system_without_approved_plan(): session = Mock(); session.run.return_value = [{'e': {'id': 'sys_checkout'}}]; rule = Test01Rule(); violations = rule.run_cypher_rule(session); assert violations[0].rule_id == 'TEST-01'; assert violations[0].severity == 'MEDIUM'`; drugi test: `def test_test01_no_violation_when_plan_approved(): session.run.return_value = []; assert rule.run_cypher_rule(session) == []`; RED bo `Test01Rule` klasa jeszcze nie istnieje — otwiera TDD cycle dla TEST-01?
- Zdefiniujmy więcej testów RED dla reguły TEST-01 — `def test_test01_fires_when_plan_draft(): session.run.return_value = [{'e':{'id':'sys_reg'}}]; assert Test01Rule().run_cypher_rule(session)[0].rule_id == 'TEST-01'`; `def test_test01_returns_medium_severity(): v = Test01Rule().run_cypher_rule(session)[0]; assert v.severity == 'MEDIUM'; assert v.domain == 'SYSTEM'`; `def test_test01_query_checks_approved_status(): rule=Test01Rule(); assert "status:'APPROVED'" in rule.QUERY`; 3 nowe testy + 2 z B46 = kompletna specyfikacja TEST-01 przed Green?
- Pokaż test RED symulujący kaskadę bezpieczeństwa dla modułu logowania — `def test_login_module_security_cascade(): login_frame = Violation('SEC-01','e_login', domain='AUTH', severity='HIGH'); engine = CrossReferenceEngine([doc_session_mgmt, doc_sso, doc_audit_log]); analysis = engine.analyze_cascades([login_frame]); assert len(analysis.cascaded) >= 2; cross = engine.aggregate_cross_domain(analysis.direct+analysis.cascaded); assert any(c.node_id=='e_login' for c in cross); assert cross[0].severity_escalated == 'CRITICAL'`; RED bo `severity_escalated` pole nie istnieje jeszcze w `CrossDomainIssue`?
- Pokaż test RED dla `resolve_conflicts` ze słowem "klucz" — `def test_resolve_conflicts_klucz_homograph(): engine = CrossReferenceEngine(docs, use_wordnet=True); v_auth = Violation('AUTH-01','klucz', domain='SECURITY'); v_role = Violation('ROLE-01','klucz', domain='GRAPH'); conflicts = engine.detect_conflicts([v_auth], [v_role]); assert len(conflicts) == 1; resolved = engine.resolve_conflicts(conflicts); assert resolved[0].resolved == True; assert resolved[0].resolution == 'WORDNET_DOMAIN'; assert resolved[0].winner_rule == 'AUTH-01'`; RED bo `resolve_conflicts()` i `RuleConflict.winner_rule` jeszcze nie istnieją?
- Czy zaczynamy od Fazy RED dla reguły `OPS-04` w Neo4j — `def test_ops04_fires_on_db_without_backup(): session=Mock(); session.run.return_value=[{'node_id':'db_prod','rto':None}]; rule=Ops04Rule(); violations=rule.run_cypher_rule(session); assert violations[0].rule_id=='OPS-04'; assert violations[0].severity=='HIGH'`; `def test_ops04_fires_when_rto_exceeds_4h(): session.run.return_value=[{'node_id':'db_prod','rto':8}]; v=Ops04Rule().run_cypher_rule(session)[0]; assert v.details=={'rto_hours':8}`; `def test_ops04_no_violation_when_compliant(): session.run.return_value=[]; assert Ops04Rule().run_cypher_rule(session)==[]`; 3 testy RED — kompletna specyfikacja przed Green?
- Faza Green dla `OPS-04` — `class Ops04Rule(LinterRule): id='OPS-04'; QUERY="MATCH (db:Database) WHERE NOT (db)-[:HAS_BACKUP_PROCEDURE]->(:BackupProcedure {tested:True,frequency:'DAILY'}) RETURN db.id AS node_id, null AS rto UNION MATCH (db:Database)-[:HAS_BACKUP_PROCEDURE]->(bp:BackupProcedure) WHERE bp.rto_hours > 4 RETURN db.id AS node_id, bp.rto_hours AS rto"; def run_cypher_rule(self,session): return [Violation(self.id,r['node_id'],severity='HIGH',domain='OPS',details={'rto_hours':r['rto']} if r['rto'] else {}) for r in session.run(self.QUERY)]`; UNION w Cypher obsługuje oba warunki; wszystkie 3 testy RED przechodzą?
- Jak przetestować `AMBIGUOUS_RESOLUTION` w `resolve_conflicts` — `def test_resolve_conflicts_ambiguous_when_low_confidence(): adapter=Mock(); adapter.get_synsets.return_value=[Synset(similarity_to_domain=lambda d: 0.55)]; engine=CrossReferenceEngine([], wordnet_adapter=adapter); c=RuleConflict('AUTH-01','ROLE-01','klucz',conflict_type='HOMOGRAPH_AMBIGUITY'); resolved=engine.resolve_conflicts([c]); assert resolved[0].resolution=='AMBIGUOUS_RESOLUTION'; assert resolved[0].resolved==False`; `resolved=False` oznacza że wniosek wymaga ręcznego przeglądu — nie jest usuwany z raportu, lecz oznaczony flagą `needs_review=True`?
- Uruchommy testy integracyjne dla reguły SEC-01 w Neo4j — rozbuduj fixture: `@pytest.fixture def neo4j_session(): driver = GraphDatabase.driver('bolt://localhost:7687', auth=('neo4j','test')); session = driver.session(); yield session; session.run("MATCH (n) DETACH DELETE n"); session.close()`; test: `def test_sec01_fires_and_clears_on_mitigated(): neo4j_session.run("MERGE (:EventFrame {id:'e_pay', domain:'API'})-[:HAS_THREAT_MODEL]->(:ThreatModel {mitigated:True})"); assert SEC01Rule().run_cypher_rule(neo4j_session) == []`; GREEN po dodaniu fixture; wymaga lokalnego Neo4j lub testcontainers?
- Jak uruchamiać testy integracyjne SEC-01 bez lokalnego Neo4j — `pip install testcontainers[neo4j]`; fixture: `@pytest.fixture(scope='session') def neo4j_session(): from testcontainers.neo4j import Neo4jContainer; c=Neo4jContainer('neo4j:5.18'); c.start(); driver=GraphDatabase.driver(c.get_connection_url(), auth=('neo4j','password')); with driver.session() as s: yield s; c.stop()`; kontener startuje w CI automatycznie, teardown usuwa dane; `pytest -m integration` uruchamia tylko testy z prawdziwą bazą; `@pytest.mark.skipif(not os.getenv('NEO4J_TESTCONTAINERS'), reason='requires docker')` jako guard dla środowisk bez Dockera?
- Jak przetestować SEC-01 parametrycznie by chronić przed regresją domenową — `@pytest.mark.parametrize('domain,expected_count', [('API',1),('OPS',0),('GRAPH',0)])` weryfikuje że SEC-01 odpala TYLKO dla domeny API; `def test_sec01_domain_selectivity(neo4j_session, domain, expected_count): neo4j_session.run(f"MERGE (:EventFrame {{id:'e_t',domain:'{domain}'}})");\u00a0assert len(SEC01Rule().run_cypher_rule(neo4j_session))==expected_count`; `scope='function'` z `DETACH DELETE` teardown między przypadkami; 3 parametry = 3 sub-testy RED w jednym `@pytest.mark.integration`?
- Napiszmy plik `conftest.py` z fixture'ami dla testów Neo4j — minimalna struktura: `import pytest; from unittest.mock import Mock; import sys; from pathlib import Path; sys.path.insert(0, str(Path(__file__).parent.parent/'src'))`; fixture mock (unit): `@pytest.fixture def neo4j_session(): s=Mock(); s.run.return_value=[]; return s`; fixture real (integracyjny): `@pytest.fixture(scope='session') def neo4j_session_real(): from testcontainers.neo4j import Neo4jContainer; ...`; rejestracja markerów: `def pytest_configure(config): config.addinivalue_line('markers','integration: wymaga Neo4j'); config.addinivalue_line('markers','slow: ponad 10s')`; `conftest.py` w `tests/` jako single source of truth dla fikstur i markerów?
- Napiszmy test RED dla `LinterRule.evaluate()` (TDD cycle #10) — `def test_sec01_evaluate_returns_evaluation_result(): rule=SEC01Rule(); session=Mock(); session.run.return_value=[{'node_id':'e_test'}]; result=rule.evaluate(session); assert isinstance(result, EvaluationResult); assert result.is_clean==False; assert 'SEC-01' in result.summary`; RED bo `evaluate()` i `EvaluationResult` nie istnieją w `LinterRule`; Green = dodanie `EvaluationResult(NamedTuple)` + domyślna `evaluate(session,engine=None)` do base class; domknięcie TDD cycle #10?
- Faza Green dla TDD cycle #10: `LinterRule.evaluate()` + `EvaluationResult` — `from typing import NamedTuple, List; class EvaluationResult(NamedTuple): violations: List; is_clean: bool; summary: str`; do `LinterRule` base class: `def evaluate(self, session, engine=None): v=self.run_cypher_rule(session); all_v=engine.analyze_cascades(v).direct+engine.analyze_cascades(v).cascaded if engine else v; return EvaluationResult(violations=all_v, is_clean=not all_v, summary=f'{self.id}: {len(all_v)} naruszeń')`; test `test_sec01_evaluate_returns_evaluation_result` przechodzi GREEN: `isinstance(result, EvaluationResult)==True`, `result.is_clean==False`, `'SEC-01' in result.summary`; `SEC01Rule`, `Test01Rule`, `Ops04Rule` dziedziczą `evaluate()` bezpłatnie — TDD cycle #10 zamknięty?
- Jak uruchomić testy integracyjne SEC-01 i OPS-04 w CI — GitHub Actions job: `services: neo4j: image: neo4j:5.18; env: NEO4J_AUTH: neo4j/test; ports: ['7687:7687']; options: --health-cmd 'cypher-shell -u neo4j -p test RETURN 1'`; krok: `pytest -m integration -k 'sec01 or ops04' --tb=short -v`; oczekiwane wyniki: `test_sec01_fires PASSED`, `test_ops04_fires_when_rto_exceeds_4h PASSED`, `test_ops04_no_violation_when_compliant PASSED`; marker `@pytest.mark.integration` wyklucza te testy z pre-commit hook?
- Jak zaktualizować `conftest.py` o fixture łańcucha przyczynowego dla Wymiaru 6 — `@pytest.fixture def causal_chain_w6(neo4j_session_real): neo4j_session_real.run("MERGE (e:EventFrame {id:'e_deadline',domain:'CONTRACT'})-[:HAS_ROLE {role:'TIME'}]->(:Token {lemma:'2024-12-31',type:'DATE'}) MERGE (e)-[:HAS_ROLE {role:'AGENT'}]->(:Token {lemma:'Wykonawca'}) MERGE (e)-[:HAS_ROLE {role:'ACTION'}]->(:Token {lemma:'dostarczyć'})"); yield neo4j_session_real; neo4j_session_real.run("MATCH (n) DETACH DELETE n")`; kompletny EventFrame z przeterminowaną datą TIME testuje jednocześnie TEMPORAL-01 i ROLE-01?
- Napiszmy test E2E z Neo4j dla reguły OPS-04 — `def test_ops04_e2e_full_pipeline(neo4j_session_real): neo4j_session_real.run("MERGE (:Database {id:'db_prod'})-[:HAS_BACKUP_PROCEDURE]->(:BackupProcedure {tested:True, frequency:'DAILY', rto_hours:8})"); linter=LinterRuleEngine(rules=[Ops04Rule()]); engine=CrossReferenceEngine([], depth_limit=3); generator=AuditReportGenerator(); pipeline=AuditPipeline(linter, engine, generator); report=pipeline.run(neo4j_session_real, docs=[]); assert report['violations'][0]['rule_id']=='OPS-04'; assert report['violations'][0]['details']=={'rto_hours':8}; assert 'cascade_summary' in report`; test E2E weryfikuje pełny łańcuch Linter→CrossRef→Generator jako kontrakt Facade?
- Napiszmy testy dla `SlowosiecProxy` sprawdzające relacje hiperonimii — `def test_proxy_returns_hypernyms(): mock=Mock(spec=WordnetAdapter); mock.get_hypernyms.return_value=['urządzenie','artefakt']; proxy=SlowosiecProxy(mock, cache_ttl=60); result=proxy.get_hypernyms('klucz',depth=2); assert result==['urządzenie','artefakt']; mock.get_hypernyms.assert_called_once_with('klucz',2)`; `def test_proxy_caches_result(): proxy.get_hypernyms('klucz',2); proxy.get_hypernyms('klucz',2); mock.get_hypernyms.assert_called_once()`; `def test_proxy_none_adapter_returns_empty(): proxy=SlowosiecProxy(None); assert proxy.get_hypernyms('klucz')==[]`; 3 przypadki: poprawność, cache hit, None-safe fallback?
- Jak przygotować Seeding i Teardown dla reguły OPS-04 w `conftest.py` — dedykowana fixture: `@pytest.fixture def ops04_seed(neo4j_session_real): neo4j_session_real.run("MERGE (:Database {id:'db_prod'})-[:HAS_BACKUP_PROCEDURE]->(:BackupProcedure {tested:True, frequency:'DAILY', rto_hours:8, rpo_hours:2})")`; seed: rto=8>4h i rpo=2>1h → oba naruszenia DORA; teardown: `yield neo4j_session_real; neo4j_session_real.run("MATCH (n:Database {id:'db_prod'}) DETACH DELETE n")`; wzorzec `{rule_id}_seed` jako standard dla wszystkich reguł w `conftest.py`?
- Pokaż test E2E dla reguły OPS-04 z użyciem fixture — `@pytest.mark.integration def test_ops04_e2e_with_fixture(ops04_seed): linter=LinterRuleEngine(rules=[Ops04Rule()]); report=AuditPipeline(linter, CrossReferenceEngine([]), AuditReportGenerator()).run(ops04_seed, docs=[]); rule_ids=[v['rule_id'] for v in report['violations']]; assert 'OPS-04' in rule_ids; assert any(v['details'].get('rto_hours')==8 for v in report['violations']); assert any(v['details'].get('rpo_hours')==2 for v in report['violations'])`; test weryfikuje oba naruszenia DORA (rto+rpo) w jednym przebiegu przez `ops04_seed` — brak `neo4j_session_real` inline, tylko fixture?
- Jak wstrzyknąć testowy graf Database bez procedury backupu — fixture `backup01_no_procedure_seed`: `neo4j_session_real.run("MERGE (:Database {id:'db_no_backup'})")`; bez `[:HAS_BACKUP_PROCEDURE]` → `BACKUP-01` fires: `MATCH (db:Database) WHERE NOT (db)-[:HAS_BACKUP_PROCEDURE]->() RETURN db.id`; teardown: `MATCH (n:Database {id:'db_no_backup'}) DETACH DELETE n`; test: `def test_backup01_fires_when_no_procedure(backup01_no_procedure_seed): violations=BackupRule().evaluate(backup01_no_procedure_seed); assert violations[0].rule_id=='BACKUP-01'`; odróżnienie: `BACKUP-01`=brak BackupPolicy/Procedure, `OPS-04`=Procedure istnieje ale niespełnia RTO/RPO?
- Jak zaktualizować `test_ops04_report_pipeline.py` do obsługi prawdziwej bazy — zamień `mock_session=Mock()` na `@pytest.mark.integration` + `neo4j_session_real` fixture; `mock_session.run.return_value=[{'db':{'id':'x'}}]` → realny MERGE w `ops04_seed`; `assert violations[0].rule_id=='OPS-04'` pozostaje bez zmian — kontrakt testu niezmieniony; plik konfiguracyjny: `pytest.ini` z `addopts = --ignore=tests/integration` domyślnie pomija testy integracyjne; CI uruchamia przez `pytest -m integration` jako osobny krok po unit testach?
- Jak wstrzyknąć do Neo4j bazę z poprawnym backupem (test Green) — fixture `ops04_compliant_seed`: `neo4j_session_real.run("MERGE (:Database {id:'db_compliant'})-[:HAS_BACKUP_PROCEDURE]->(:BackupProcedure {tested:True, frequency:'DAILY', rto_hours:2, rpo_hours:0.5})")`; rto=2≤4h i rpo=0.5≤1h → oba warunki DORA spełnione; teardown: `MATCH (n:Database {id:'db_compliant'}) DETACH DELETE n`; test: `def test_ops04_no_violation_when_compliant(ops04_compliant_seed): assert Ops04Rule().evaluate(ops04_compliant_seed)==[]`; para `ops04_seed` (RED) + `ops04_compliant_seed` (GREEN) zamyka pełny cykl TDD dla OPS-04 — kontrakt reguły udowodniony dwustronnie?
- Uruchommy test E2E dla OPS-04 na żywej bazie Neo4j — krok po kroku: (1) `docker run -d --name neo4j-test -p 7687:7687 -e NEO4J_AUTH=neo4j/test neo4j:5.18`; (2) `pytest tests/integration/test_ops04_e2e.py -m integration -v --neo4j-uri bolt://localhost:7687 --neo4j-auth neo4j/test`; (3) oczekiwane wyniki: `test_ops04_e2e_with_fixture PASSED`, `test_ops04_no_violation_when_compliant PASSED`, `test_ops04_rto_violation PASSED`; (4) teardown: `docker stop neo4j-test && docker rm neo4j-test`; alternatywa: `testcontainers` `Neo4jContainer` w `conftest.py` zastępuje kroki 1 i 4 automatycznie — `scope='session'` = jeden kontener na cały run pytest?
- Jak stworzyć `SlowosiecProxy` do weryfikacji hiperonimii w testach i grafie — test helper: `class SlowosiecProxyStub(SlowosiecProxy): def __init__(self): self._data={'klucz':[('urządzenie',1),('artefakt',2)],'backup':[('procedura',1),('zasób',2)]}`; `get_hypernym_path(lemma)→self._data.get(lemma,[])`; `is_ancestor(a,b)→b in {h for h,_ in self.get_hypernym_path(a)}`; użycie w teście: `proxy=SlowosiecProxyStub(); engine=CrossReferenceEngine(rules, proxy=proxy); assert engine.resolve_conflicts(...).resolution_type=='HYPERNYMY_SPECIFICITY'`; w grafie: `SlowosiecProxy.from_neo4j(session)` zastępuje Stub w testach integracyjnych — te same asercje, różny backend?
- Jak testować wyniki audytu SQLite, żeby sprawdzić idempotentność skanowania?
#### Kompletna hierarchia TDD
- Napisz czerwony test TDD dla `GapDetector` — `detect_gaps(doc)` z brakującą sekcją → `[GapFinding(section='Wymagania', severity=HIGH)]`.
- Jak zaimplementować minimalną logikę `GapDetector` żeby przejść z fazy RED do GREEN — który warunek sprawdzać jako pierwszy?
- Jak zrefaktoryzować `GapDetector` po uzyskaniu zielonego testu — wydzielić `SectionValidator`, `RelationChecker`, `DuplicateScorer` jako osobne klasy?
- Zrefaktoryzuj `GapDetector` do strategii per-rule — każda reguła (ARCH-01, SEC-01) jako osobna klasa walidatora.
- Jak napisać test jednostkowy dla `DuplicateDetector.score_similarity()` — izolacja od zewnętrznych zależności?
- Jak zbudować oracle dataset dla W0 — 30 dokumentów z ręcznie oznaczonymi lukami, duplikatami i zerowanymi relacjami?
- Jak zapewnić że zmiana algorytmu `DuplicateDetector` nie obniży Precision poniżej 85% na corpus testowym?
- Stwórz test regresyjny dla `GapDetector` — baseline snapshot raportów luk zapisany jako golden file.
- Jak napisać test E2E: wgraj dokument SRS → przejdź przez W0 → W8 → sprawdź że `GapAnalysisReport` ma ≥ 1 naruszenie ARCH-01?
- Napiszmy RED test dla `EventReasoningEngine.apply()` — `def test_event_reasoning_detects_missing_agent(): session=Mock(); session.run.side_effect=[[], [{'node_id':'e_no_agent'}], [], []]; engine=(DeductionEngine().register(RoleCompletenessRule()).register(TemporalConstraintRule()).register(CausalChainRule()).register(DomainConsistencyRule())); violations=engine.apply(session); assert any(v.rule_id=='ROLE_COMPLETENESS' and v.node_id=='e_no_agent' for v in violations)`; `side_effect=[[], [...], [], []]` symuluje: ROLE_COMPLETENESS odpala, pozostałe 3 nie; RED bo `DeductionEngine` i `RoleCompletenessRule` nie istnieją jeszcze; domknięcie przez `DeductionEngine.register()` chain builder?
- Napiszmy RED test dla `IntentClassifier._classify_conditional()` — `def test_conditional_obligation_detected(): nlp=spacy.blank('pl'); sent=nlp('Wykonawca musi dostarczyć SRS o ile Zamawiający potwierdzi budżet'); clf=IntentClassifier(); result=clf.classify_span(sent); assert result=='CONDITIONAL_OBLIGATION'`; `'o ile'` jako marker warunkowy powinien nadpisać czysty `DIRECTIVE` (`musi`); RED bo `_classify_conditional()` i `classify_span(Span)` nie istnieją; Green: dodaj `_classify_conditional` PRZED branżą DIRECTIVE w łańcuchu; kontrprzykład: `'Należy dostarczyć SRS'` → nadal `DIRECTIVE` (brak markera warunkowego)?
- Napiszmy RED test dla `NLPPipeline.fast_mode()` wydajności — `def test_fast_mode_disables_ner(): pipeline=NLPPipeline.fast_mode(); assert 'ner' not in pipeline.nlp.pipe_names; assert 'senter' not in pipeline.nlp.pipe_names`; `def test_fast_mode_batch_throughput(): texts=['Należy dostarczyć SRS.']*100; docs=list(pipeline.nlp.pipe(texts, batch_size=64)); assert len(docs)==100`; RED bo `NLPPipeline.fast_mode()` nie istnieje; Green: `@classmethod def fast_mode(cls, proxy=None): nlp=spacy.load('pl_core_news_lg', disable=['ner','senter','attribute_ruler']); return cls(nlp, proxy or SlowosiecProxy(None))`; `fast_mode` bezpieczny na środowiskach CI bez modelu — `spacy.blank('pl')` jako fallback gdy model niedostępny?
- Zbudujmy generator raportu audytowego z Macierzą Pokrycia — `CoverageMatrix = TypedDict('CoverageMatrix', {'rules_total':int, 'rules_triggered':int, 'rules_clean':int, 'coverage_pct':float, 'top_violated':List[str]})`; `AuditReportGenerator.generate_coverage_matrix(violations, all_rules)->CoverageMatrix`: `triggered={v.rule_id for v in violations}`; `pct=len(triggered)/len(all_rules)*100`; `top_violated=Counter(v.rule_id for v in violations).most_common(5)`; Markdown sekcja `## Macierz Pokrycia`: tabela `|Reguła|Status|Naruszeń|` — ✅ zielona gdy `rule_id not in triggered`, 🔴 czerwona gdy >0; `generate_json()` zwraca `report['coverage_matrix']` jako pole; CI bramka: `assert report['coverage_matrix']['coverage_pct'] <= 20.0` (≤20% reguł z naruszeniami = dobry audyt)?
- Napiszmy RED test dla reguły `SEC-03` — `def test_sec03_detects_unencrypted_db(): session=Mock(); session.run.return_value=[{'node_id':'db_prod'}]; rule=Sec03Rule(); violations=rule.evaluate(session); assert violations[0].rule_id=='SEC-03'; assert violations[0].severity=='CRITICAL'`; RED bo `Sec03Rule` i `(:EncryptionConfig)` nie istnieją; Green: `class Sec03Rule(LinterRule): id='SEC-03'; QUERY="MATCH (db:Database) WHERE NOT (db)-[:ENCRYPTED_WITH]->(:EncryptionConfig {algorithm:'AES-256'}) RETURN db.id AS node_id"`; seed: `MERGE (:Database {id:'db_prod'})` bez `[:ENCRYPTED_WITH]` → violation; complement: `MERGE (db)-[:ENCRYPTED_WITH]->(:EncryptionConfig {algorithm:'AES-256', key_length:256})` → brak violation?
- Zaprojektuj generator raportu audytowego z Macierzą Pokrycia — `generate_coverage_matrix_markdown(violations, all_rules, prev_violations=None)->str`: tabela `| Reguła | Status | Naruszeń | Severity | Trend |`; `Status` = ✅ gdy 0 naruszeń, 🔴 gdy ≥1 CRITICAL, 🟡 gdy ≥1 HIGH/MEDIUM; `Trend` = `↑`/`↓`/`=` vs `prev_violations`; wiersz sumaryczny: `| **TOTAL** | | {total} | | {pct:.1f}% |`; wynik: sekcja `## Macierz Pokrycia` osadzona w `generate_markdown()` przed sekcją `## Naruszenia`; CI: `report['coverage_matrix']['rules_red_pct'] <= 20.0` jako bramka release; `generate_coverage_matrix_markdown(violations=[],all_rules=ALL_RULES)` → wszystkie ✅ = baseline raport dla nowego projektu?
- Pokaż jak wygenerować Macierz Pokrycia w Generatorze Raportu Audytowego — pełna integracja: `AuditReportGenerator.generate(violations, all_rules, prev_run:Optional[AuditReport]=None)->{json, markdown}`; `generate_json()` zwraca `{'violations':..., 'coverage_matrix':CoverageMatrix, 'cohesion':CohesionReport, 'release_blocked':bool}`; `generate_markdown()` = `## Streszczenie` + `## Macierz Pokrycia` (z emoji + Trend) + `## Spójność Dyskursu` + `## Naruszenia`; `release_blocked = any(v.severity=='CRITICAL' for v in violations)` → sekcja `## ⛔ RELEASE ZABLOKOWANY` gdy True; `AuditPipeline.run(doc)->AuditReport`: `extractor.extract(doc)→frames → engine.run(frames)→violations → generator.generate(violations, ALL_RULES)` = 3 kroki od tekstu do raportu; `@pytest.fixture prev_audit` symuluje poprzedni przebieg dla testów kolumny Trend?
- Przetestujmy MarkdownParser i NLPExtractor na dokumentach technicznych — RED tests: `def test_markdown_milestone_table_parsed(): parser=MarkdownGraphParser(); frames=parser.parse('| Kamień milowy | Data |\n|---|---|\n| SRS dostarczone | 2026-01-15 |'); assert len(frames)==1; assert frames[0].predicate=='kamień_milowy'; assert frames[0].time_dimension=='2026-01-15'`; `def test_nlpextractor_causal_event(): extractor=NLPExtractor(); frames=extractor.extract('System nie przetwarza żądań ponieważ baza nie jest dostępna.'); causal=[f for f in frames if f.predicate=='przetwarzać']; assert causal[0].cause_dimension is not None`; oba RED — `MarkdownGraphParser.parse()` nie obsługuje tabel; `NLPExtractor` nie wyciąga `cause_dimension`; GREEN wymaga `mistune.ASTRenderer` + `causal_marker_detection(['ponieważ','dlatego że','bo'])`?
- Jak skonfigurować potok CI/CD dla automatycznego audytu dokumentacji — kompletny workflow `.github/workflows/doc-audit.yml`: `on: push: paths: ['dokumentacja/**/*.md']`; jobs: `audit: runs-on: ubuntu-latest; services: neo4j: image: neo4j:5.18; env: NEO4J_AUTH: neo4j/test; ports: ['7687:7687']`; kroki: `pip install -r requirements.txt` → `pytest -m 'not slow' -q` (unit) → `pytest -m integration -v` (z Neo4j) → `python -m audit --input dokumentacja/ --output report.json --exit-on-failure`; artefakt: `uses: actions/upload-artifact@v4 with: {name: audit-report, path: report.json}`; PR komentarz: `gh pr comment --body "$(cat report_summary.md)"`; bramka release: `if report['release_blocked']: sys.exit(1)` → GitHub Actions fail → blokada merge; `RULES_CATALOGUE.md` jako source of truth wersjonowania reguł — hash reguł w artefakcie dla pełnej odtwarzalności?
- Pokaż jak napisać test E2E integrujący wszystkie warstwy systemu — `@pytest.mark.e2e def test_e2e_markdown_to_audit_report(seeded_session, tmp_path): md_text='# SRS\n| Kamień milowy | Data |\n|---|---|\n| Dostawa | 2024-01-15 |'; frames=MarkdownGraphParser().parse(md_text); enriched=NLPExtractor().enrich(frames); triples=[t for f in enriched for t in GraphEmitter.emit_from_doc(f)]; GraphEmitter().flush(seeded_session, triples); violations=LinterRuleEngine([Temporal01Rule(), Role01Rule(), Sec03Rule()]).run_all(seeded_session); report=AuditReportGenerator().generate(violations, ALL_RULES); assert report['violations'][0]['rule_id']=='TEMPORAL-01'`; jeden test weryfikuje: Markdown → EventFrame → NLP → Neo4j → Linter → Report; `seeded_session` z `nlp/conftest.py`; marker `@pytest.mark.e2e` → pomijany w pre-commit, uruchamiany przez `pytest -m e2e` w CI; `conftest.py` z `pytest.ini: markers = e2e: End-to-end tests requiring Neo4j`?
- Pokaż przykład raportu audytowego dla dokumentacji PANTERA w formacie Markdown — `AuditReportGenerator.generate_markdown(violations, doc_id='PANTERA')` → plik `audit_PANTERA_2026-03-16.md`; sekcje: `# Raport Audytowy: PANTERA`, `## Macierz Pokrycia` (tabela reguły × domeny: `| Reguła | INFRA | CONTRACT | BACKUP | Pokrycie |`), `## Naruszenia` — wiersze: `- [HIGH] TEMPORAL-01 — EventFrame#e042: brak wymiaru TIME (§3.2)`, `## Rekomendacje`, `## Historia zapytań QA` (skrót `QueryAuditLog.jsonl`); `MdReportWriter.write_violation_table(violations)->str`: nagłówki `| Reguła | Ważność | Opis | Węzeł |`; `generate_markdown()` vs `generate_json()` — Markdown dla analizy manualnej, JSON dla CI/CD pipeline; `AuditReportGenerator.generate_pantera_summary(doc)` hook dla PANTERA-specyficznych metadanych (`doc.classification`, `doc.version`); test: `@pytest.mark.e2e def test_pantera_report(seeded_session): report=AuditReportGenerator().generate_markdown(violations, doc_id='PANTERA'); assert '## Macierz Pokrycia' in report and '[HIGH]' in report`?
- Zaprojektuj tabelę Markdown dla Macierzy Pokrycia dokumentacji PANTERA — `generate_coverage_matrix(rules, domains)->str`: nagłówek `| Reguła | INFRA | CONTRACT | BACKUP | TESTING | Pokrycie |`; komórki: `✓` gdy `RuleRegistry.applies_to_domain(rule_id, domain)`, `✗` gdy reguła nie pokrywa domeny, `N/A` gdy domena nieistotna; przykład: `| TEMPORAL-01 | ✓ | ✓ | ✗ | N/A | 50% |`, `| BACKUP-01 | ✓ | ✗ | ✓ | N/A | 50% |`, `| TEST-01 | ✗ | ✗ | ✗ | ✓ | 25% |`; wiersz `| **Suma** | 3/5 | 2/5 | 1/5 | 1/5 | 35% |`; `CoverageCell.value: Literal['✓','✗','N/A']` — `@applies_to('PANTERA','INFRA')` dekorator na klasie reguły; `MdReportWriter.write_matrix_footer(totals)` oblicza kolumnowe sumy; `generate_coverage_matrix()` jako sekcja `## Macierz Pokrycia` wewnątrz `generate_markdown()` przed `## Naruszenia`?
- Jak stworzyć skrypt Python do automatycznego audytu dokumentacji w CI/CD — `audit.py --input docs/ --output report.json --rules ALL --exit-on-failure` jako moduł `python -m audit`; `AuditCLI.main(args)`: `(1) files=glob('**/*.md', recursive=True)` → `(2) frames=MarkdownGraphParser().parse_all(files)` → `(3) GraphEmitter().flush(session, triples)` → `(4) violations=LinterRuleEngine(rules).run_all(session)` → `(5) json.dump(report, open('report.json','w'))`; `--exit-on-failure`: `if any(v.severity=='HIGH' for v in violations): sys.exit(1)` — blokuje CI; `--since HEAD~1` delta mode: `git diff --name-only HEAD~1` → przetwarza tylko zmienione pliki; `BatchProcessor.process(files)` z `ThreadPoolExecutor(max_workers=4)` dla równoległego parsowania; `--rules ALL` ładuje `RULES_CATALOGUE.yml`; test: `def test_audit_cli_exit_code(): result=subprocess.run(['python','-m','audit','--input',tmp_dir,'--exit-on-failure']); assert result.returncode==1` gdy HIGH violations?
- Jak skonfigurować GitHub Actions do blokowania PR przy błędach audytu — workflow `on: pull_request: paths: ['dokumentacja/**/*.md']`; `permissions: pull-requests: write; statuses: write`; krok status: `github.rest.repos.createCommitStatus({owner, repo, sha:context.sha, state:'failure', description:'HIGH violations found', context:'audit/compliance'})` przez `actions/github-script@v7`; Branch protection: `Settings → Branches → Required status checks: audit/compliance` → merge zablokowany do czasu przejścia; `--github-summary` flag w `audit.py` → `echo "$(cat summary.json)" >> $GITHUB_STEP_SUMMARY` — tabela naruszeń w PR UI; `if: failure()` krok: `gh pr comment --body "$(cat audit_summary.md)"` dodaje tabelę do PR komentarza; `RULES_CATALOGUE.yml` hash w opisie statusu — reviewer wie dokładnie jakie wersje reguł były aktywne przy każdej weryfikacji?
- Pokaż jak zintegrować moduł nltk.corpus.reader.nkjp do walidacji z korpusem narodowym — `nltk.corpus.reader.nkjp.NKJPCorpusReader(root, fileids='.*')` ładuje NKJP (Narodowy Korpus Języka Polskiego); `reader.tagged_words(fileids)` → `[('dostarczył','verb:sg:past:m1:perf'), ...]` tokeny z tagami NKJP tagset; `NKJPCorpusValidator` porównuje tagowanie Morfeusza z `NKJPCorpusReader` dla tych samych tokenów — walidacja pipeline na złotym standardzie; `NKJPSampleExtractor.extract_domain(domain='law', n=500)->List[str]` wylosowuje zdania z domeny prawnej do fine-tuningu `IntentClassifier`; pułapka: NKJP tagset ≠ UD tagset — `NKJPToUDConverter.convert('verb:sg:past:m1:perf')->'VERB Tense=Past,Number=Sing,Gender=Masc,Aspect=Perf'` przez mapowanie tablicowe; `KnowledgeGapTracker.capture_ign()` loguje tokeny OOV w NKJP (nieznane w złotym standardzie); alternatywa: korpus KPWr (Creative Commons) jako zamiennik NKJP przy problemach licencyjnych IPI PAN; integracja CI: `nltk.download('nkjp')` w `setup.cfg [tool:pytest] testpaths` — automatyczne pobranie przed testami?
- Jak zintegrować moduł nltk.corpus.reader.nkjp do walidacji wyników pipeline — `NKJPCorpusValidator.validate_pipeline(extractor:NLPExtractor, n=200)->ValidationReport`: losuje 200 zdań → `frame_pred=extractor.extract(text)[0]` vs `frame_gold=NKJPGoldFrameBuilder.from_tagged_words(nkjp_tagged)`; metryki F1 per rola: `{agent_f1:0.82, patient_f1:0.77, time_f1:0.91}`; aproksymacja gold frames: `nsubj→AGENT`, `obj→PATIENT`, `obl+ner=DATE→TIME`; CI: `pytest --nkjp-validate` → `ValidationReport.assert_above_threshold(min_f1=0.75)` blokuje merge gdy F1<0.75; `ValidationReport.save('nkjp_validation.json')` jako artefakt GitHub Actions; diff `HEAD~1` vs `HEAD` w `nkjp_validation.json` — monitoring regresji per PR?

### 5. Obsługa błędów

- Co się dzieje, gdy `doc_auditor.py` napotka plik UTF-8 z niepoprawnym kodowaniem?
- Jak moduł zachowuje się, gdy baza SQLite jest zajęta (concurrent access)?
- Co zwraca `gap_detector.py` dla pustego dokumentu (0 słów)?
- Jak logować błędy parsowania bez przerywania całego skanu?
- Jakie są progi błędów, po których przekroczeniu `completeness_score = 0`?
- Jak `relation_mapper.py` obsługuje cykl w grafie relacji dokumentów (A wymaga B, B wymaga A)?
- Co się dzieje gdy plik Markdown ma niepoprawny YAML front matter (np. brakujący cudzysłów) — czy cały audyt pada czy plik jest skipowany?

### 6. Integracja z innymi warstwami

- Jak W0 będzie konsumować wyniki z W1 (tokenizacja, lematyzacja) po implementacji W1?
- Jak wyniki `relation_mapper.py` (relacje między dokumentami) zostaną przekazane do W4 (Neo4j)?
- Jak `doc_auditor.py` będzie korzystać z W3 (Słowosieć) do lepszego wykrywania synonimicznych duplikatów?
- Jak W8 (AuditEngine) rozszerza W0, nie duplikując jego funkcji?
- Czy W0 (doc audit) może być uruchamiany niezależnie od innych warstw jako standalone narzędzie?
- Jak W0 raportuje zidentyfikowane luki do W8 (compliance audit) — zdarzenie, callback, czy polling?
- Jak aktualizacje W1 (nowe tokenizatory) wpływają na wyniki W0 — czy audyt musi być ponownie uruchomiony?

### 7. Pułapki i ryzyka

- **Pułapka 1:** TF-IDF bez lematyzacji W1 daje fałszywe duplikaty dla fleksji polskiej (np. "testy" vs "testów") — do naprawy w W1.
- **Pułapka 2:** Progi podobieństwa (0.85/0.65/0.40) są stałe — zmiana w jednym projekcie łamie audyt innego; rozwiązanie: konfigurowalne progi per projekt.
- **Pułapka 3:** SQLite audit.db commitowany do repo ujawnia historię dokumentów — decyzja: `.gitignore` vs `audit.db` jako artefakt CI.
- **Pułapka 4:** `duplicate_detector` porównuje dokumenty przez TF-IDF bez lematyzacji — "wymagania" i "wymaganie" to różne tokeny; duplikat może nie zostać wykryty.
- **Pułapka 5:** SQLite jako backend audytu jest single-writer — równoległe uruchomienia `doc_auditor.py` na tym samym `.db` powodują `database is locked`.
- **Pułapka 6:** Reguły ARCH-01, SEC-01 są zdefiniowane per-projekt — bez wersjonowania zbioru reguł wyniki audytu dla tego samego dokumentu różnią się między datami.
- **Pułapka 7 (bariery projektowe):** 3 fundamentalne bariery które zatrzymały podobne projekty NLP+compliance: (1) **Ontologia vs. corpus drift** — ontologia projektowana na początku nie nadąża za ewolucją dokumentacji; rozwiązanie: `OntologyBuilder.infer_domain()` auto-rozszerza domenę; (2) **Skalowalność grafu** — Neo4j Community bez partycjonowania zatrzymuje się przy ~1M węzłów; rozwiązanie: domenowe podgrafy + `depth_limit`; (3) **Brak definicji done** — projekt nie ma granicy "kiedy audyt jest kompletny"; rozwiązanie: reguły z numerowanymi wersjami (`SEC-01 v1.2`) + `coverage_pct` jako metryka zamknięcia?
- **Pułapka 8 (bariery projektów lingwistycznych):** 3 bariery specyficzne dla projektów lingwistycznych: (1) **Pokrycie leksykalne** — Słowosieć plWordNet pokrywa ~150k synsetów, ale dokumentacja techniczna IT zawiera do 30% leksemów spoza korpusu; `KnowledgeGapTracker.UNKNOWN_WORD` mierzy tę lukę; (2) **Drift morfologiczny** — polskie fleksje mnożą formy (np. "dostarczylibyśmy"); Morfeusz bez aktualizacji do nowych neologizmów zwraca `ign` tag dla ~5% zdań technicznych; (3) **Konflikt standardów anotacji** — TEI P5 vs CoNLL-U vs własny EventFrame: każda zmiana wymaga migracji całego korpusu; rozwiązanie: adapterowa warstwa konwersji?
- **Pułapka 9 (lock-in grafu):** Lock-in schematu Neo4j — węzły `:EventFrame`, `:ThreatModel`, `:BackupProcedure`, `:TestPlan` zaprojektowane pod Cypher; migracja do AuraDB cloud, Amazon Neptune (Gremlin) lub Apache AGE (PostgreSQL) wymaga przepisania wszystkich reguł Lintera; rozwiązanie: `GraphQueryAdapter` jako interfejs abstrakcji `run_query(q: Query)→List[Row]`; `CypherQueryAdapter` i `GremlinQueryAdapter` jako implementacje; `LinterRule.run_query(adapter)` nie wie jakiego backendu używa — zero-cost swap bazy grafowej bez zmiany kodu reguł?
- Jak mierzyć drift ontologiczny (remedium dla Pułapki 7 bariera 1) — `OntologyDriftMetric` porównuje snapshot domeny `t0` vs `t1`; `new_domains = domains(t1) - domains(t0)`; próg alertu: `len(new_domains) > 3` w jednym kwartale → `OntologyDriftWarning`; `OntologyBuilder.infer_domain()` zapisuje każdą domenę z datą `MERGE (:Domain {name:d, inferred_at:date()})`; historia przez Cypher: `MATCH (d:Domain) RETURN d.name, d.inferred_at ORDER BY d.inferred_at`; `KnowledgeGapTracker.DOMAIN_DRIFT` jako nowy typ luki gdy nowa domena pojawia się bez pokrycia żadną regułą?
- **Pułapka 10 (eksplozja reguł):** Katalog rośnie bez granic — 13 aktywnych reguł AUTH-01→DATA-01; bez governance każdy nowy wymóg dodaje regułę; 3 zasady hamowania eksplozji: (1) **reguła tylko jeśli mierzalna** — nowa reguła wymaga ≥1 testu RED zanim wejdzie do katalogu; (2) **reguła tylko jeśli unikalna** — `RuleCoverageChecker.overlaps(new_rule, existing_rules)` wykrywa nakładające się klauzule MATCH; (3) **reguła wycofana gdy jałowa** — brak naruszeń przez 3 audyty z rzędu → `status='DEPRECATED'`; `RULES_CATALOGUE.md` z polem `status:'ACTIVE'|'DEPRECATED'|'PROPOSED'` jako single source of truth?
- **Pułapka 11 (wąskie gardło spaCy+Neo4j pod obciążeniem):** 4. bariera projektów lingwistycznych specyficzna dla stacku spaCy+Słowosieć+Neo4j: `spaCy.pipe()` z pełnym modelem `pl_core_news_lg` + `SlowosiecProxy` (HTTP do Neo4j) + `session.run()` per token = 3 warstwy I/O serializujące pipeline; przy 10k zdań dokumentacja czas rośnie O(n×3) zamiast O(n); rozwiązania: (1) `fast_mode(disable=['ner','senter'])` — 3× przyspieszenie; (2) `SlowosiecProxy` z TTL cache — 90% cache hit rate po pierwszych 500 tokenach; (3) `UNWIND $roles` batch Cypher zamiast N+1 `session.run()` — 10× mniej round-tripów; łączny efekt: 80% redukcja czasu przy tej samej semantycznej poprawności?
- **Pułapka 12 (bariery komercyjnych silników NLP dla polskiego):** 3 bariery które zatrzymały projekty komercyjne NLP+compliance dla języka polskiego: (1) **cold-start corpus** — brak anotowanego polskiego korpusu kontraktowego; Google/Microsoft NLU trenowane na angielskim prawie = F1<0.40 dla `Wykonawca`/`Zamawiający`; remedium: ręczna anotacja 500 zdań kontraktowych → prototyp F1>0.75; (2) **eksplozja kombinatoryczna fleksji** — 7 przypadków × 2 liczby × 3 rodzaje × 2 aspekty = 84 formy per lemma; silnik bez Morfeusza traktuje każdą formę jako osobny token → 84× więcej węzłów bez lematyzacji; remedium: `TokenizerAdapter(Morfeusz2)` normalizuje do lemmy przed zapisem; (3) **koszt utrzymania ontologii domenowej** — ontologia projektowana na początku nie nadąża za ewolucją wymagań kontraktowych; remedium: `OntologyBuilder.infer_domain()` auto-rozszerza + `RuleVersion('SEC-01','v1.2')` dla wersjonowania; koszt ~2h/tydzień przegląd nowych domen vs ~40h jednorazowej migracji ontologii bez automatyki?
- **Pułapka 13 (optymalizacja zrywająca rozszerzalność):** 80% redukcja złożoności przez selektywne wyłączanie faz NIE zabija rozszerzalności przy wzorcu `PipelineProfile`: `fast_mode()` = `disable=['ner','senter','attribute_ruler']` — wyłączone fazy nadal zarejestrowane w `nlp.pipe_names`; `NLPPipeline.upgrade(doc, to_level='deep')` re-uruchamia wyłączone fazy na istniejącym `Doc` — zero ponownego parsowania tekstu; `@dataclass PipelineConfig(level:str, disabled:List[str], features:Set[str])`; tabela trade-off: `fast`(3000 zdań/s, −15% accuracy) / `standard`(800 zdań/s, baseline) / `deep`(150 zdań/s, +8% accuracy); nowy komponent: `nlp.add_pipe('new_component', after='intent_clf')` automatycznie aktywny na wszystkich 3 poziomach o ile nie jest w `PipelineConfig.disabled`; reguła: optymalizacja = selektywne wyłączanie, NIE usuwanie — system zawsze rozszerzalny przez `add_pipe()` bez zmiany kodu profili?
- **Pułapka 14 (bariery symbolicznych projektów NLP):** 3 bariery specyficzne dla systemów rule-based + graf (vs Pułapka 12 = bariery komercyjne): (1) **Brittleness reguł** — każda nowa konstrukcja językowa wymaga nowej reguły; przy 1000 zdań ~300 reguł, przy 10k zdań ~2000 reguł → eksponencjalny wzrost; remedium: `RuleGeneralizer` abstrahuje konkretne reguły do wzorców przez `dep-rel + MorphFeatures` zamiast literalnych lemmatów — 10k zdań → 200 wzorców; (2) **Anotatorzy domenowi** — brak osób łączących prawo polskie + NLP + Neo4j; remedium: `RuleWizard` UI — ekspert prawny definiuje reguły w języku naturalnym, konwertowane do DRL/Python bez znajomości kodu; (3) **Degradacja ontologii** — po 6 miesiącach stare węzły `(:Concept)` bez krawędzi, zduplikowane relacje, nieaktualna `DomainDictionary`; remedium: `OntologyHealthChecker.run()` jako miesięczne zadanie CI: `MATCH (c:Concept) WHERE NOT (c)-[]-() RETURN count(c)` = izolowane węzły; `STRUCT-03`: `MATCH (e:EventFrame)-[:HAS_ROLE{role:'AGENT'}]->(t:Token) WHERE t.lemma IS NULL RETURN e.id` = zdarzenia z pustym agentem?
- **Pułapka 15 (eksplozja ontologii i kompresja słowników):** `OntologyCompressor` stosuje 4 techniki: (1) **scalanie synonimów** — `SlowosiecProxy.synonyms('wykonawca') ∩ synonyms('dostawca') ≠ ∅` → `MERGE (:Concept {lemma:'wykonawca'})` + DELETE `(:Concept {lemma:'dostawca'})` + przepianie relacji; (2) **przycinanie łańcuchów IS_A** — `OntologyCompressor.prune_hypernym_chain(max_depth=5)`: IS_A łańcuch > 5 hopów → zachowanie bezpośredniej krawędzi root→leaf, usunięcie pośrednich węzłów; (3) **deduplicacja relacji** — `MATCH (a)-[r1:IS_A]->(b),(a)-[r2:IS_A]->(b) WHERE id(r1)<id(r2) DELETE r2`; (4) **archiwizacja OOV** — `DomainDictionary.archive_unused(days=90)` → przenosi nieużywane terminy do `archived_concepts.json`; metryka: `{nodes:4200→1800, relations:12000→5100, duplicates_removed:320}` = `OntologyHealthReport.compression_ratio`; `OntologyCompressor.run(session)` jako cron CI `0 2 1 * *`; `STRUCT-04`: `MATCH (c:Concept)-[:IS_A*6..]->(root) RETURN c.lemma` = zbyt głęboka hierarchia IS_A?
- Jak radzić sobie z wieloznacznością słów (polisemią) w ontologii — `PolysemyResolver` — `'system'` w NKJP ma 3 synsetty: `system:IT`, `system:ORG`, `system:PHYS`; `SlowosiecProxy.disambiguate(token, context:List[Token])->SynsetId`: algorytm Lesk — nakładanie definicji synsetu z kontekstem zdania; `OntologyPolysemyManager.detect(session)`: `MATCH (c:Concept) WHERE size((c)-[:SYNONYM_OF]-()) > 3 RETURN c.lemma` = potencjalnie wieloznaczne węzły; `ContextualSynsetPin(lemma, doc_id, synset_id)` — per-dokument pinning synsetu; przykład: `'backup systemu'` → `system:IT` dzięki `backup` w kontekście; `OntologyCompressor` nie scala wieloznacznych: `PolysemyGuard.is_polysemous(lemma)->bool` blokuje scalanie; `DomainDictionary.add_disambiguation_rule(lemma, trigger_word, synset_id)` — ręczna reguła ekspertowa; `POLY-01`: `MATCH (c:Concept {polysemy_score:>3}) WHERE NOT (c)-[:HAS_DISAMBIGUATION_RULE]->() RETURN c` = wieloznaczne bez reguły disambiguacji?

## Kryteria akceptacji

| Metryka | Minimum |
|---|---|
| Completeness Score dla wzorcowego doc-zestawu 10 plików | ≥ 0.7 |
| False Positive Rate dla duplikatów | ≤ 5% |
| Czas skanowania 50 plików .md | < 5 s |
| Mutation Score (testy jednostkowe) | ≥ 60% |
| Pokrycie testów linii | ≥ 90% |

## Pytania o idempotentność i deterministyczność

- Czy `doc_auditor.py scan` na tych samych plikach dwukrotnie daje identyczny wynik w SQLite?
- Czy `similarity_engine.py` daje identyczny cosine score dla identycznych wejść niezależnie od kolejności wywołań?
- Jak zapewnić, że zmiana kolejności plików wejściowych nie zmienia wyników?

## Pytania o migrację i wersjonowanie

- Jak migrować schemat SQLite (`ddl_audit.sql`) po dodaniu nowej kolumny bez utraty historii audytów?
- Jak wersjonować szablony gap-detektora, gdy zmienia się standard dokumentacji projektu?
- Jak obsłużyć backwards-compatibility dla CLI `doc-audit`, gdy dodajemy nowe flagi?

## Pytania o audytowalność

- Jak każde wykrycie luki jest powiązane z konkretnym plikiem, linią i regułą w SQLite?
- Jak wygenerować raport "dlaczego dokument X dostał score 0.6?" z szczegółowym uzasadnieniem?
- Jak przechowywać historię audytów (który commit = jaki wynik) dla celów dowodowych?

---

## Rozszerzalność i skalowanie

### Stopniowe rozszerzanie analizy dokumentów

- Jak W0 obsługuje dokumenty w nowych formatach (DOCX, PDF, RST) bez zmiany kontraktu `AuditResult`?
- Jak dodać nową regułę audytu (np. wykrywanie duplikatów między plikami) bez modyfikacji istniejących reguł?
- Jak skalować W0 do analizy repo z 1000+ plikami — limit czasu, paginacja, cache wyników?
- Jak testować, że dodanie nowej reguły nie zmienia wyników dla dokumentów niepodlegających tej regule?
- Jak wersjonować zbiór reguł audytu — żeby wynik z reguła v1.0 i v1.1 był porównywalny historycznie?
- Jak W0 obsługuje dokumenty wielojęzyczne gdy W1 (Morfeusz) jest dostępny tylko dla polskiego?
- **EXISTS↗ `CaseGenNegationTestMatrix`** — pytest parametric dla Case=Gen + neutralizacja retoryczna: `@pytest.mark.parametrize('sent,voice,role', [(zdanie_czynne, 'act', 'AGENT'), (zdanie_bierne, 'pass', 'PATIENT')])` → `VoiceNeutralizer.normalize(sent)` → assert `frame.agent == expected_actor`; `CaseGenNegationTestMatrix` = siatka 2D: negation×case (Gen/Acc/Nom) × role (AGENT/PATIENT/THEME) — 18 kombinacji; `NegationCaseReport(coverage_pct, failed_pairs:List)` eksportowany do CI.
- **NEW `RulerBartekComparator`** — EntityRuler vs Bartek: Ruler = wzorcowy, bezpośredni, bez morfologii, 5× szybszy (≈0.8 ms/sent); Bartek = koref-aware, morfologia polska (Morfeusz-backed), +18% recall na pronominach i rzeczownikach odczasownikowych; `RulerBartekComparator(precision_ruler:float, recall_bartek:float, f1_delta:float)`; benchmark: Ruler wygrywa dla NER terminologicznego, Bartek niezbędny gdy zdania bezosobowe lub elipsy aktorów.
- **EXISTS↗ `EllipsisVirtualNodeInjector`** — walidacja virtual-Ellipsis z NKJPCorpusReader: iteracja NKJP → detekcja null-subject (brak nominativu przy czasowniku) → `MERGE (:Ellipsis {sent_id, default_subj: last_actor, source:'NKJP'})`; `NKJPEllipsisValidationReport(injected:int, validated_against_corpus_pct:float)` porównuje wirtualny podmiot z adnotacją NKJP; threshold ≥ 0.82.
- **EXISTS↗ `HypernymAttributeQuerySet`** — pytest parametric dla injekcji hiperonimów: `@pytest.mark.parametrize('lemma,expected_l1,expected_l2', słowosiec_oracle)` → `HypernymNodeAttributeInjector.inject(lemma)` → `MATCH (c:Concept {lemma}) RETURN c.hypernym_l1, c.hypernym_l2` → assert equals; `HypernymAttributeQuerySet(lemma, expected_l1, expected_l2)` jako dataclass; `HypernymInjectionValidationReport(passed, failed_lemmas:List, mismatch_l1_pct:float)`.
- **EXISTS↗ `NKJPValidationReportGenerator`** — pełny raport walidacji z NKJPCorpusReader: `NKJPValidationReportGenerator(reader, graph_client).run()` → `compare_lemmas(nkjp_set, graph_set)` → `ValidationDiff(missing_in_graph:Set, extra_in_graph:Set, mismatch_tags:List)`; eksport JSON do CI: `diff.to_json('nkjp_validation.json')`; threshold: `missing_in_graph_pct < 0.05`; integracja z `NKJPPipelineStep` — walidacja po każdym batch-imporcie.
- **NEW `NativePythonDocumentParser`** — natywne biblioteki Python zamiast Apache Tika: `pdfplumber` (PDF z warstwą tekstu), `python-docx` (DOCX), `docutils` (RST); `NativePythonDocumentParser(format:str).parse(path) -> RawDocument`; brak JVM dependency; `ParseCapabilityMatrix(pdf:pdfplumber, docx:python-docx, rst:docutils, html:bs4, xml:lxml)`; `DocumentParserReport(parsed:int, failed:int, format_dist:Counter)` eksportowany do pipeline; integracja z `AuditResult` — bez zmiany kontraktu.
- **EXISTS↗ `NKJPGoldStandardMetrics`** — metryki Accuracy dla Złotego Standardu NKJP: `Precision = TP/(TP+FP)`, `Recall = TP/(TP+FN)`, `F1 = 2PR/(P+R)`, `ExactMatch = identical_frames/total`; `NKJPGoldStandardMetrics(gold_path:str, pred_path:str).evaluate()` → `MetricsReport(precision, recall, f1, exact_match, by_category:Dict)`; thresholds CI: `f1 ≥ 0.85`, `exact_match ≥ 0.72`; porównanie z PCC (Polish Coreference Corpus) dla modułu Bartek.
- **EXISTS↗ `FractalAuditROIMatrix`** — rozszerzenie korzyści: `FractalAuditROIMatrix(depth_levels:int, doc_count:int)` → ROI per poziom fraktalny: `L1=doc-level (szybki overview)`, `L2=section-level (spójność tematyczna)`, `L3=paragraph-level (kolizje intencji)`, `L4=sentence-level (elipsy, koreferencja)`; `ROIReport(hours_saved_per_level, defect_detection_rate_per_level, recommended_max_depth:int)` — rekomendacja `max_depth=3` dla audit IT (kompromis czas/coverage).
- **EXISTS↗ `DocLinterPearlQARCBridge`** — integracja modelu Pearla z wymiarami QARC: `DocLinterPearlQARCBridge(causal_graph:CausalGraph, qarc_dims:QARCDimensions)` → mapuje wymiary `Q(Who)/A(Action)/R(Resource)/C(Condition)` na węzły grafu Pearla `(Cause)-[:CAUSES]->(Effect)`; `PearlQARCScorer(doc) -> QARCCausalScore(q_score, a_score, r_score, c_score, causal_completeness:float)`; reguła: `causal_completeness < 0.70` → insert `RuleViolation('QARC-CAUSAL-01')`.
- **EXISTS↗ `DocLinterComplexityReducer`** — 80% redukcja złożoności: `DocLinterComplexityReducer` = strategia trójwarstwowa: (1) normalizacja retoryczna `VoiceNeutralizer` eliminuje warianty czynna/bierna/bezosobowa → 3 formy → 1 wzorzec; (2) `IdiomPhraseologyDetector` kolapsuje idiomy do atomów semantycznych; (3) `MorfeuszAmbiguityStateMatrix` redukuje wieloznaczność fleksyjną przez CONTEXT_WINDOW; `ComplexityReductionReport(original_patterns:int, normalized_patterns:int, reduction_pct:float)` — benchmark: 80% dla technicznego PL.
- **NEW `IncrementalParsingTestSuite`** — scenariusze testowe dla analizy przyrostowej: `@pytest.mark.parametrize('doc_chunk,expected_delta', incremental_oracle)` — 5 scenariuszy: (1) dodanie zdania → nowy EventFrame bez reparse całości; (2) modyfikacja zdania → delta tylko dla affected_sentences; (3) usunięcie aktora → propagacja przez StateMatrix; (4) nowy idiom → `IdiomPhraseologyDetector.hot_reload()`; (5) zmiana ontologii → `HypernymNodeAttributeInjector.incremental_update()`; `IncrementalParsingReport(delta_ms, full_parse_ms, speedup_ratio:float)` — target `speedup_ratio ≥ 4.0`.
- **EXISTS↗ `NKJPDependencyTreeMetrics`** — metryki NKJP dla precyzji drzew zależności: `UAS` (Unlabeled Attachment Score = poprawne łuki / wszystkie łuki), `LAS` (Labeled Attachment Score = poprawna etykieta + łuk / wszystkie), `EM` (Exact Match drzewa); `NKJPDependencyTreeMetrics(gold_conllu:str, pred_conllu:str).evaluate()` → `DepMetricsReport(uas, las, em, by_deprel:Dict)`; thresholds CI: `LAS ≥ 0.88`, `UAS ≥ 0.91`; integracja z `NKJPGoldStandardMetrics` — oba raporty łączone w `NKJPFullValidationReport`.
- **EXISTS↗ `QPPBenefitCatalog`** — katalog korzyści QPP: (1) `path_quantification` — `{2..6}` zastępuje 5 osobnych zapytań UNION; (2) `inline_predicates` — warunki bezpośrednio na krawędzi redukują post-filter o 60%; (3) `elementId()` safe-ID eliminuje błędy migracji; (4) `ALL(n IN nodes(p))` = zero false-positives w domain-filter; (5) `PROFILE` pokazuje index-hit-rate per hop; `QPPBenefitCatalog.benchmark(doc_count=1000)` → `QPPBenchmarkReport(queries_replaced:int, avg_speedup_x:float, index_coverage_pct:float)` — benchmark: `avg_speedup ≥ 3.2×`.
- **EXISTS↗ `FractalDocAnalysisDefinition`** — Fraktalna Analiza Dokumentów w DocLinter: samopowtarzalna struktura hierarchiczna `L1(doc) → L2(section) → L3(paragraph) → L4(sentence)`; każdy poziom stosuje te same operacje: `EventFrameExtractor → CorefResolver → IntentClassifier → AuditRuleEngine`; `FractalDepthComparator(l1_result, l4_result)` mierzy spójność między poziomami; metryka `FractalCoherenceScore(doc) = avg_similarity(L1_intent, L4_intents)`; DocLinter uruchamia analizę fraktalną gdy `doc.section_count > 3`.
- **EXISTS↗ `QARCSemanticRoleIntegrator`** — integracja QARC z ontologią ról semantycznych: `QARCSemanticRoleIntegrator` mapuje `Q(Who)→AGENT`, `A(Action)→PREDICATE`, `R(Resource)→THEME/INSTRUMENT`, `C(Condition)→CONDITION_NODE`; `SemanticRoleOntology(pl_roles:Dict)` = rozszerzenie PropBank PL o role IT (EXECUTOR, TARGET_SYSTEM, PROCEDURE, PREREQUISITE); `QARCRoleMappingReport(mapped_pct:float, unmapped_roles:List, ontology_coverage:float)` — target `ontology_coverage ≥ 0.90`; każde `QARCDimension` → `EventFrame.role` w Neo4j.
- **EXISTS↗ `QARCKnowledgeCompressor`** — koncepcja QARC i kompresja wiedzy: QARC = 4-wymiarowy filtr semantyczny `(Q, A, R, C)` redukujący zdanie do atomu wiedzy; `QARCKnowledgeCompressor(doc).compress()` → `KnowledgeAtom(who:str, action:str, resource:str, condition:str, confidence:float)`; kompresja: przeciętne zdanie techniczne (18 tokenów) → 4-pole atom (4 wartości) = **78% redukcja rozmiaru reprezentacji**; `KnowledgeAtomStore` = cache atomów z deduplikacją `(who, action, resource)` jako klucz; `QARCCompressionReport(atoms:int, dedup_savings_pct:float)`.
- **EXISTS↗ `WSDChallengeMatrix`** — wyzwania Word Sense Disambiguation: `WSDChallengeMatrix` = 4 bariery: (1) brak dużego annotowanego korpusu PL (SemCor PL < 10k zdań vs angielski 234k); (2) polisemia techniczna (np. `'interfejs'` = GUI / API / protokół); (3) domain shift — Słowosieć ogólna vs IT; (4) kontekst wielozdaniowy (sense zmienia się po 3+ zdaniach); `WSDMitigationStrategy` per bariera: `(1)→transfer_learning_from_EN, (2)→TechnicalLexicon, (3)→DOMAIN_PRIORITY, (4)→StateMatrix_window5)`; `WSDChallengeReport(barrier_id, severity:HIGH|MED, mitigation, residual_error_pct:float)`.
- **EXISTS↗ `EllipsisZeroAnaphoraResolver`** — elipsa i anafora zerowa: `EllipsisZeroAnaphoraResolver` = połączenie `EllipsisVirtualNodeInjector` (null-subject) + `CataphoraDelayedResolver` (anafora wyprzedzająca); algorytm: (1) wykryj brak nominativu przy czasowniku (Morfeusz); (2) sprawdź `StateMatrix.last_active_actor()` w oknie ±5 zdań; (3) jeśli anafora zerowa (podmiot domyślny = 'się/my/oni') → `ZeroAnaphoraFact(default_subj, certainty:'IMPLICIT')`; `MERGE (:ZeroAnaphora {sent_id})-[:RESOLVED_BY_ZERO_SUBJ]->(:Concept {lemma})`; `ZeroAnaphoraReport(implicit_resolved:int, explicit_resolved:int, failed:int)`.
- **EXISTS↗ `NKJPDoDChecklist`** — mierzalne DoD dla walidacji NKJP: `NKJPDoDChecklist` = 7 kryteriów: (1) `LAS ≥ 0.88` (dep-tree); (2) `F1 ≥ 0.85` (NER); (3) `ExactMatch ≥ 0.72` (EventFrame); (4) `missing_in_graph_pct < 0.05` (pokrycie grafu); (5) `nkjp_coverage_query > 10_000 sentences`; (6) `NKJP-COV-01 not fired` (wszystkie domeny ≥ threshold); (7) `IterparsePerformanceReport.peak_memory_mb < 256`; `NKJPDoDValidator.run_all()` → `DoDReport(passed:int, failed:List[str], ready_to_merge:bool)` — CI gate: `ready_to_merge == True`.
- **EXISTS↗ `DocLinterCorefSolutionMap`** — mapa rozwiązań koreferencji w DocLinter: warstwa 1 `EntityRuler` (NER terminologiczny, wzorce regex); warstwa 2 `BartekCoreferenceResolver` (morfologia PL, elipsy); warstwa 3 `CorefPipelineOrchestrator` (łączy warstwy, buduje `CorefChain`); warstwa 4 `CorefKnowledgeGraphResolver` (zapis `[:REFERS_TO]` w Neo4j); warstwa 5 `CorefIntentLinker` (przypisanie aktu mowy do aktora); `CorefSolutionMapReport(chain_count:int, avg_chain_length:float, unresolved_pct:float, intent_linked_pct:float)` — target `unresolved_pct < 0.05`.
- **EXISTS↗ `NVRDocLinterIntegration`** — wdrożenie NVR w DocLinter: `NVRDocLinterIntegration` = etap 2a w 15-etapowym pipeline po tagowaniu Morfeuszem; `VoiceNeutralizer.normalize(sent) → NormalizedFrame`; 3 ścieżki: czynna `subj→AGENT`, bierna `nsubj:pass→PATIENT + obl:agent→AGENT`, bezosobowa `się→ZeroAgent`; wynik → `RhetoricalVarianceNeo4jWriter.write(frame)` = idempotentny MERGE; `NVRPipelineReport(active_pct, passive_pct, impersonal_pct, normalized_total:int)` — wbudowany w `AuditResult`.
