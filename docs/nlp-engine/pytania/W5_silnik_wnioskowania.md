---
layer: W5
title: "Warstwa 5 — Silnik Wnioskowania (InferenceEngine / Drools)"
phase: 5
status: planned
docs_version: 1.0.0
tags: [InferenceEngine, Drools, DRL, wnioskowanie, dedukcja, lancuchy_przyczynowe, StateMatrix]
---

# Warstwa 5 — Silnik Wnioskowania (InferenceEngine / Drools)

## Przegląd

Warstwa 5 implementuje logiczne wnioskowanie na grafie wiedzy z W4.
Przetwarza `EventRoleDict` przez reguły IF-THEN (Python lub Drools DRL),
buduje łańcuchy przyczynowe i inferencje (stan obiektu, posiadanie, lokalizacja, naruszenie_zobowiązania).

Kluczowe klasy: `InferenceEngine`, `StateMatrix`, `IntentClassifier`.

## Uzasadnienie istnienia warstwy

**Dlaczego ta warstwa jest potrzebna:**
W5 istnieje bo compliance wymaga logiki warunkowej która kaskaduje — zdarzenie A może triggerować wniosek B który triggeruje regułę C. Tego nie da się wyrazić prostym filtrowaniem. Drools DRL pozwala deklaratywnie zapisać "IF agent EXISTS AND instrument IS_A narzędzie_ostre AND NOT policy(bezpieczeństwo) THEN flag RISK-01" — i ta reguła działa dla każdego przyszłego przypadku bez zmiany kodu. `StateMatrix` deduplicuje wnioski (ten sam fakt nie może być flagowany dwukrotnie) i "zamraża" stan po każdym przebiegu (idempotentność — krytyczna dla projektu zarobkowego).

**Co się sypie bez tej warstwy:**
- Reguły compliance muszą być zakodowane jako if-else w Pythonie — każda zmiana wymaga deploymentu nowego kodu; klient nie może edytować reguł bez programisty
- Brak kaskadowania: "RISK-01 + CONS-02 jednocześnie" nie może triggerować eskalacji severity
- Brak `StateMatrix`: ten sam dokument audytowany dwukrotnie daje zduplikowane wyniki — raport compliance jest nieaudytowalny

**Zależności:**
- Wchodzi z W4: graf wiedzy (Cypher zapytania, zwracające fakty)
- Wchodzi z W2: `EventRoleDict` (bezpośrednio lub przez W4)
- Wychodzi do W7: `List[AuditFinding]` przez REST API
- Wychodzi do W8: te same fakty jako wejście dla `AuditEngine` i `GapAnalysisReport`

## Diagram przepływu danych

```
Graf Neo4j (W4) + EventRoleDict (W2)
       │
  InferenceEngine
  ┌────────────────────────────────────────────┐
  │  _rule_location(event) → LOCATED_AT        │
  │  _rule_possession_transfer(event) → OWNS   │
  │  _rule_state_change(event) → state=dead    │
  │  _rule_classify_legal(event) → naruszenie_zobowiązania│
  │  _rule_negation(event) → NOT_OCCURRED      │
  └────────────────────────────────────────────┘
       │
  StateMatrix         ← deduplikacja wniosków, "zamrażanie"
       │
  IntentClassifier    ← QUESTION / COMMAND / ASSERT / NEGATE
       │
       ▼
  InferenceResult {facts: [...], intents: [...], state: {...}}
       │
       ▼
  W7 (FastAPI) / W8 (AuditEngine)
```

## Pytania źródłowe — sklasyfikowane

### 1. Architektura
- Jak załadować Słowosieć do modułu wnioskowania w Pythonie?
- Jak połączyć GraphDatabaseAdapter z modułem wnioskowania Drools?
- Jaki jest podział odpowiedzialności między InferenceEngine, Drools KieSession, i StateMatrix?
- Jak wyglądają granice W5 — co dostarcza W4 (baza grafowa) a co W5 dodaje przed przekazaniem do W8?
- Jaki wzorzec stosuje W5 dla rejestrowania reguł DRL — plugin, convention-over-configuration, czy explicit register?
- Jak W5 obsługuje wiele aktywnych sesji KieSession równolegle — pool sesji, jedna sesja globalna, czy per-request?
- Jak zdefiniować architekturę DocumentClassifier w W5 — oddzielna klasa czy metoda KlasyfikatorKontekstu.classify_by_metadata()?
- Jak DocumentClassifier współpracuje z InferenceEngine — wynik klasyfikacji pliku filtruje zestaw reguł DRL przed wnioskowniem?
- Zaimplementujmy klasę DocumentClassifier jako Krok 0 pipeline — wywołanie DocumentClassifier.classify(file_path) przed W1→W8 wybiera zestaw reguł DRL dla danego typu dokumentu?
- Jak DocumentClassifier.classify(file_path) → DocumentType z confidence — następnie InferenceEngine.set_active_ruleset(document_type) aktywuje właściwe reguły?
- Jak wyeksponować wynik DocumentClassifier w logach pipeline'u — linia "Krok 0: UMOWA (confidence=0.92)" przed przetwarzaniem W1→W8?

### 2. Kontrakty danych
_brak pytań źródłowych w tej kategorii_
- Jaki jest format wejściowy reguły DRL — plik tekstowy, JSON z metadanymi reguły, czy DSL Drools?
- Jak zdefiniować kontrakt dla wyniku wnioskowania — lista obiektów Inference z polami confidence, rule_id, evidence?
- Jakie pola są wymagane w StateMatrix przekazywanej między sesjami Drools — id, version, facts_hash?
- Jak wersjonować reguły DRL — SemVer (1.2.3), hash zawartości, czy timestamp modyfikacji?
- Jak wygląda przykładowy JSON dla wyniku InferenceEngine dla naruszenia CONS-02 — pokaż schemat?

### 3. Implementacja
- Jak w grafie wiedzy oznaczyć intencję działania aktora?
- Jakie reguły logiczne w Drools obsłużą synkretyzm form?
- Wyjaśnij różnicę między modelem lingwistycznym a modelem zdarzenia..
- Jakie reguły logiczne wdrożyć dla automatycznej ontologii?
- Pokaż przykład implementacji reguł Drools dla grafu..
- Jak stworzyć model ontologii zdarzeń dla domeny prawnej?
- Jakie reguły weryfikacji zastosować dla ontologii zdarzeń w fazie 2?
- Jak wdrożyć model danych grafu (Neo4j) dla analizy zdarzeń?
- Czy system może sam wywnioskować intencję na podstawie narzędzia?
- Wyjaśnij barierę eksplozji reguł i ontologii w projektach symbolicznych..
- Jakie reguły w silniku Drools obsłużą relacje agent-patient?
- Pokaż przykład wnioskowania logicznego w systemie symbolicznym..
- Jak wdrożyć walidację reguł w Drools dla relacji nsubj i obj?
- Pokaż przykład wnioskowania na grafie zdarzeń w Neo4j..
- Jakie reguły Drools najlepiej obsłużą synkretyzm w DRL?
- Jakie reguły Drools najlepiej obsłużą eliminację ról przy wieloznaczności?
- Jak zapisać reguły dedukcyjne IF-THEN w silniku Drools?
- Pokaż przykład reguły DRL dla wnioskowania o istnieniu obiektu..
- Jak uniknąć eksplozji ontologii przy modelowaniu zdarzeń?
- Jak stworzyć reguły Drools klasyfikujące zdarzenie prawne?
- Jak połączyć ontologię z analizą intencji (speech acts)?
- Jak w Drools zgłosić lukę w dokumentacji?
- Pokaż przykład reguły Drools rozwiązującej synkretyzm..
- Pokaż przykład reguły Drools mapującej składnię na rolę agent..
- Pokaż przykład reguły Drools dla relacji AGENT i PATIENT..
- Jak połączyć relację HAS_TRIGGER z wnioskowaniem w silniku Drools?
- Jak zmapować relacje przyczynowe w grafie zdarzeń?
- Jak rozszerzyć ontologię o relacje przyczynowe w Neo4j?
- Jak zdefiniować regułę dedukcji dla lokalizacji w grafie?
- Czy Drools to dobry wybór dla polskiego silnika wnioskowania?
- Jakie jeszcze reguły poza lokalizacją warto teraz zaimplementować?
- Czy do wnioskowania semantycznego lepiej użyć Drools czy Pythona?
- Jakie reguły dedukcji przestrzennej dodać do InferenceEngine?
- Jak połączyć reguły logiczne z ontologią Słowosieci?
- Czy silnik wnioskowania powinien obsługiwać relacje przyczynowe?
- Czy Drools obsłuży reguły oparte na cechach obiektu i środowisku?
- Jak system powinien reagować na wykrycie przestępstwa w grafie?
- Pokaż jak zapisać regułę lokalizacji w pliku .drl dla Drools.
- Jakie inne reguły przestrzenne warto dodać do silnika wnioskowania?
- Jak połączyć wnioski z Drools z bazą grafową Neo4j?
- Pokaż implementację logiki wnioskowania dla lokalizacji.
- Jak zdefiniować regułę dla wnioskowania o intencjach?
- Jak zapisać regułę w pliku .drl dla Drools?
- Jak zapisać regułę lokalizacji w natywnym formacie Drools .drl?
- Pokaż logikę pętli w natywnym pliku .drl dla Drools.
- Jakie reguły wnioskowania dodać dla domeny prawnej i medycznej?
- Pokaż przykład pliku .drl dla reguły lokalizacji.
- Jak połączyć Walentego z naszym modelem zdarzeń i ról?
- Jakie reguły wnioskowania najlepiej obsłużą hierarchię gatunków ze Słowosieci?
- Jak zaimplementować wykrywanie ramy subkategoryzacyjnej dla czasownika 'dostarczyć'?
- Pokaż regułę wnioskowania o posiadaniu dla Wykonawcy i Zamawiającego.
- Jakie są najtrudniejsze ramy walencyjne w języku polskim?
- Pokaż przykład złożonej ramy walencyjnej dla nowej domeny..
- Jak rozbudować model zdarzenia o wymiar narzędzia i intencji?
- Pokaż jak zaimplementować regułę _rule_possession_transfer w kodzie..
- Pokaż logikę wstrzymania zapisu dla intencji QUESTION w InferenceEngine..
- Pokaż regułę wnioskowania: 'Maria posiada książkę' po akcji 'dać'..
- Jak rozbudować InferenceEngine o tryb odpytywania grafu?
- Jak rozszerzyć reguły wnioskowania o wymiar intencji i przyczyny?
- Pokaż kod metody _rule_possession_transfer dla silnika..
- Jak wykryć negację dla reguły _rule_possession_transfer?
- Czy możemy rozbudować tę regułę o wymiar intencji?
- Jak wdrożyć logikę odpytywania grafu dla pytań o lokalizację?
- Pokaż implementację reguły _rule_possession_transfer z obsługą negacji..
- Jak dodać wymiar konsekwencji dla akcji sprzedaży w InferenceEngine?
- Zaimplementujmy mechanizm przeszukiwania wzorca w InferenceEngine dla pytań..
- Jak rozbudować ontologię o relacje przyczynowe i skutkowe?
- Pokaż jak dodać wymiar narzędzia do reguł wnioskowania..
- Pokaż jak rozbudować graf o łańcuchy przyczynowo-skutkowe..
- Czy system obsłuży pytania o relacje przyczynowe?
- Jak dodać relację negacji do silnika wnioskowania?
- Pokaż implementację relacji NEGATION dla zaprzeczonych faktów..
- Jak zmodyfikować _rule_possession_transfer, aby uwzględniała status NOT_OCCURRED?
- Pokaż jak wykryć relację NEGATION za pomocą parsera zależności..
- Jak zaimplementować relację NEGATION dla zaprzeczonych faktów?
- Jak wdrożyć State Matrix, by uniknąć fałszywych alarmów w audycie?
- Jak silnik powinien wnioskować na podstawie przyczyn i skutków?
- Pokaż przykładowe reguły logiczne dla domeny prawnej i medycznej..
- Jak rozszerzyć CausalChainBuilder o wnioskowanie semantyczne z użyciem synsetów z W3 (Słowosieć)?
- Jak CausalChainBuilder buduje łańcuch przyczynowy — które EventFrame stają się węzłami a które krawędziami?
- Jak wzbogacić CausalChainBuilder o synonimy — np. "dostarczyć" i "przekazać" traktowane jako ta sama akcja?
- Jakie reguły DRL sterują CausalChainBuilder przy wykrywaniu związku przyczyna→skutek w dokumentach prawnych?
- Jak CausalChainBuilder obsługuje łańcuchy wielopoziomowe — A→B→C gdy B jest pośrednim warunkiem?
- Pokaż implementację Soft Matching w CausalChainBuilder z użyciem hiperonimii — predykaty są dopasowywane semantycznie nie leksykalnie?
- Jak Soft Matching sprawdza czy :EventFrame.predicate należy do synsetu 'dostarczyć' (i synonimów) przed emisją krawędzi :CAUSES?
- Jak zdefiniować próg Soft Matching — minimalne jaccard@synsets między predykatem a wzorcem reguły (threshold = 0.5)?
- Jak testować Soft Matching — asercja że 'przekazać' pasuje do reguły zdefiniowanej dla 'dostarczyć' przez wspólny synset?
- Zintegrujmy SlowosiecAdapter z CausalChainBuilder do łączenia zdarzeń — CausalChainBuilder.link_events(e1, e2) sprawdza SlowosiecAdapter.get_synonyms(e1.predicate) zanim emituje krawędź :CAUSES?
- Jak CausalChainBuilder decyduje czy dwa EventFrame połączyć :CAUSES bazując na Słowosieci — e1.predicate ∈ get_synonym_set(rule.predicate) → MERGE (e1)-[:CAUSES]->(e2)?
- Jak wywołać SlowosiecAdapter wewnątrz CausalChainBuilder — inicjalizacja `self._lexicon = SlowosiecAdapter(db_path)` w konstruktorze, wywołanie `self._lexicon.get_synonyms(predicate)` w metodzie `link_events()`?
- Jak obsłużyć wyjątek LookupError gdy predykat nie istnieje w Słowosieci — fallback do dopasowania leksykalnego (exact match) zamiast semantycznego?
- Jak zaktualizować CausalChainBuilder._infer() o wnioskowanie semantyczne — dla każdej pary EventFrame sprawdź `_lexicon.get_synonyms(e.predicate)` i dopasuj do synsetów wzorca reguły przed emisją :CAUSES?
- Jak buforować wyniki SlowosiecAdapter w CausalChainBuilder — `@lru_cache(maxsize=512)` na metodzie `_get_synonym_set(predicate)` aby uniknąć powtórnych zapytań SQLite podczas analizy dokumentu?
- Jak skonfigurować próg semantycznego soft matchingu w CausalChainBuilder — parametr `threshold: float = 0.5` w konstruktorze, porównanie jaccard@synsets między predykatem a wzorcem?
- Jak przetestować integrację SlowosiecAdapter+CausalChainBuilder dla soft matchingu — asercja że dokument z 'przekazać' aktywuje regułę CONS-02 zdefiniowaną dla 'dostarczyć'?
- Jak wpiąć DependencyParserAdapter do CausalChainBuilder — `CausalChainBuilder.__init__(self, parser: DependencyParser, lexicon: SlowosiecAdapter)` umożliwia re-parsowanie zdania źródłowego gdy EventFrame.agent jest None przy budowaniu łańcucha?
- Jak CausalChainBuilder używa DependencyParser gdy EventFrame nie ma wypełnionych ról — fallback `parser.parse(event.raw_sentence)` wydobywa nsubj i uzupełnia EventFrame.agent zamiast odrzucania zdarzenia z łańcucha?
- Jak CausalChainBuilder propaguje SituationalContext przez łańcuch kauzalny — węzeł B dziedziczy `context.temporal` od A gdy zdarzenie B następuje bezpośrednio po A (AFTER); `context.causal.cause_id = e_prev.id` ustawiany automatycznie po emisji krawędzi :CAUSES?
- Jak modelować wielowymiarowy kontekst zdarzenia w grafie przyczynowym — każdy EventFrame w CausalChainBuilder nosi `context.causal: {cause_id, effect_id}` aktualizowany po MERGE :CAUSES; `context.temporal` dziedziczony z relacji temporalnych wnioskowanych przez parser?
- Jak rozszerzyć `_soft_match` o relacje hiperonimii — oprócz synonimów sprawdzaj `_lexicon.get_hypernyms(predicate)` i porównaj hiperonim ze wzorcem reguły (np. 'przekazać' → 'dostarczyć' → 'działanie')?
- Jak ustalić głębokość przeszukiwania hiperonimii w `_soft_match` — parametr `max_depth: int = 3` w konstruktorze, BFS po ścieżce hiperonimów do korzenia ontologii przez `get_hypernym_path()`?
- Zaimplementujmy klasę KnowledgeGapTracker do logowania nieznanych zdarzeń i słów — jakie typy luk rozróżnia (UNKNOWN_PREDICATE, MISSING_SYNSET, UNMATCHED_RULE)?
- Jak KnowledgeGapTracker rejestruje zdarzenie nierozpoznane przez żadną regułę DRL — hook after_rule_evaluation gdy activated_rules.is_empty()?
- Jak KnowledgeGapTracker eksportuje dane do kolejki aktywnego uczenia — JSONL z polami predicate, context, doc_id, timestamp?
- Jak zintegrować KnowledgeGapTracker z InferenceEngine — rejestracja jako listener Drools WorkingMemoryEventListener?
- Zaimplementujmy KnowledgeGapTracker do logowania nieznanych struktur składniowych — jak tracker wykrywa zdanie bez parsowania dep_rel (brak węzła root w CoNLL-U z UDPipe)?
- Jak KnowledgeGapTracker kategoryzuje typy luk — UNKNOWN_WORD (tag ign), UNKNOWN_STRUCTURE (brak root), UNMATCHED_PREDICATE (brak synsetu), UNMATCHED_RULE (brak reguły DRL)?
- Jak tracker loguje nieznane struktury do bazy aktywnego uczenia — wpis UNKNOWN_STRUCTURE zawiera: raw_sentence, conllu_partial, doc_id, timestamp?
- Jak zintegrować KnowledgeGapTracker z potokiem analizy zdarzeń — w EventFrame extraction loop: dla każdego EventFrame wywołaj `tracker.check_predicate(frame.predicate)` zanim trafi do InferenceEngine?
- Jak KnowledgeGapTracker wchodzi w potok W2→W5 — po SemanticMapper.extract() `tracker.check(event)` przed przekazaniem EventFrame do InferenceEngine?
- Napiszmy kod KnowledgeGapTracker — metoda `capture_ign(token)` gdy `token.tag == 'ign'`: `self._gaps.append({'type': 'UNKNOWN_WORD', 'form': token.form, 'doc_id': self._doc_id})`?
- Jak KnowledgeGapTracker loguje nieznane lematy z tagiem ign — pełny wpis: `{'type':'UNKNOWN_WORD', 'form': token.form, 'context': [prev.form, next.form], 'pos_in_sentence': token.id, 'doc_id': self._doc_id, 'timestamp': datetime.utcnow().isoformat()}`; kontekst sąsiednich tokenów ułatwia ręczne etykietowanie w kolejce aktywnego uczenia?
- Jak KnowledgeGapTracker przechwytuje osierocone zdarzenia — metoda `capture_orphan(event: EventFrame)` gdy event nie ma żadnej krawędzi :CAUSES w grafie?
- Jak eksportować zebrane luki do pliku JSONL — metoda `dump_jsonl(path)` iteruje `self._gaps` i zapisuje każdy wpis przez `json.dumps(gap, ensure_ascii=False)`?
- Wdróżmy pętlę KnowledgeGapTracker w pipeline W2→W5 — `for event in event_frames: tracker.check_predicate(event.predicate); tracker.capture_orphan(event) if not graph.has_edges(event); inference_engine.run(event)`?
- Jak pętla KnowledgeGapTracker obsługuje wyjątki na poziomie zdarzenia — try/except per event: wyjątek → `tracker.capture_error(event, exc)`, pipeline kontynuuje następne zdarzenie zamiast przerywać sesję?
- Jak wdrożyć Klasyfikator Kontekstu aby system rozumiał typ (jestestwo) dokumentu — umowa, SRS, raport, specyfikacja?
- Jak KlasyfikatorKontekstu pobiera sygnały z W3 (leksyka), W2 (role) i W6 (koreferencja) do klasyfikacji dokumentu?
- Jak zdefiniować enum klas dokumentów w KlasyfikatorKontekstu — UMOWA, SRS, RAPORT_AUDYTU, SPECYFIKACJA_TECHNICZNA?
- Jak KlasyfikatorKontekstu wpływa na InferenceEngine — czy aktywowane reguły DRL są filtrowane per typ dokumentu?
- Jak InferenceEngine odpytuje graf przyczynowy w Neo4j — Cypher query wbudowany w regułę DRL?
- Jak CausalChainBuilder tworzy krawędź :CAUSES między dwoma :EventFrame po aktywacji reguły?
- Jak InferenceEngine weryfikuje spójność grafu przyczynowego — czy krawędź :CAUSES nie tworzy cyklu?
- Jakie dane treningowe są potrzebne dla KlasyfikatorKontekstu — etykietowane przykłady per typ dokumentu?
- Jak zdefiniować próg pewności (confidence threshold) KlasyfikatorKontekstu — co zwrócić gdy wynik < 0.6?
- Jak obsłużyć fallback KlasyfikatorKontekstu gdy dokument nie pasuje do żadnej klasy — UNKNOWN czy heurystyka?
- Jak aktualizować model KlasyfikatorKontekstu bez restartu serwisu — hot reload reguł klasyfikacji?
- Jak ekstrahować cechy z nagłówków i tytułów sekcji dokumentu dla KlasyfikatorKontekstu — regex + tf-idf?
- Jak obsłużyć dokument wieloklasowy (jednocześnie UMOWA i SPECYFIKACJA_TECHNICZNA) — multi-label vs. priorytetyzacja?
- Jak wyjaśnić decyzję klasyfikatora — które cechy zadecydowały o klasie UMOWA (SHAP lub lista feature importances)?
- Jak walidować KlasyfikatorKontekstu na polskich dokumentach prawnych — stratified cross-validation, metryki F1 per klasa?
- Jak zautomatyzować klasyfikację dokumentów na podstawie YAML front matter — pola title, tags, layer jako sygnały KlasyfikatorKontekstu?
- Jak wyekstrahować cechy z tytułów sekcji (## nagłówki) dla KlasyfikatorKontekstu — TF-IDF na słowach kluczowych nagłówków?
- Jak połączyć sygnały YAML metadata (layer: W_x, tags: [ARCH-01]) z sygnałami leksykalnymi w jednym wektorze cech klasyfikatora?
- Jak klasyfikować dokument bez YAML front matter — fallback na analizę nagłówków i pierwszych 200 tokenów?
- Jak zaimplementować szablony walidacyjne per typ dokumentu — dataclass DocumentTemplate z listą wymaganych sekcji i reguł compliance?
- Jak KlasyfikatorKontekstu generuje szablony walidacyjne — mapowanie UMOWA→[CONS-02, RISK-01], SRS→[ARCH-01, SEC-01]?
- Jak wdrożyć Klasyfikator Kontekstu i Złotych Standardów łącznie — KlasyfikatorKontekstu.classify() → GoldenStandardProfile → DocumentTemplate z regułami?
- Jak przekazać wynik KlasyfikatorKontekstu do GoldenStandardProfile.get_template(document_type) w AuditEngine?
- Jak testować połączenie KlasyfikatorKontekstu z GoldenStandardProfile — mock document_type=UMOWA → oczekiwany template zawiera CONS-02?
- Jak wykryć że dokument nie spełnia szablonu walidacyjnego — diff wymaganych sekcji vs. nagłówków wykrytych przez Linter?
- Zaimplementujmy KlasyfikatorKontekstu i szablony walidacyjne dla typów dokumentów — pipeline: classify() → select_template() → validate_against_template() → lista luk?
- Jak zdefiniować dataclass DocumentTemplate z listą required_sections, mandatory_rules i optional_rules per typ dokumentu (UMOWA/SRS/RAPORT)?
- Jak DocumentTemplate.validate(document) zwraca listę ValidationGap — brakujące sekcje i niespełnione reguły jako strukturyzowane wyniki audytu?
- Jak InferenceEngine korzysta ze synsetów w Neo4j (Wariant A) do rozszerzenia reguł o synonimy?
- Jak InferenceEngine odpytuje SlowosiecAdapter on-demand (Wariant B) w regule DRL bez blokowania sesji?
- Jak porównać skuteczność Wariantu A vs. B dla reguły CONS-02 — F1 score na zbiorze testowym?
- Zaimplementujmy eksport łańcuchów kauzalnych do formatu Mermaid.js — metoda CausalChainBuilder.to_mermaid()?
- Jak zserializować graf przyczynowy jako Mermaid flowchart LR — węzły to EventFrame.id, krawędzie to :CAUSES z rule_id?
- Jak obsłużyć łańcuchy z rozgałęzieniami (jeden :EventFrame powoduje dwa skutki) w formacie Mermaid.js?
- Jak escapować znaki specjalne (cudzysłowy, polskie litery) w etykietach węzłów Mermaid.js?
- Jak testować serializer Mermaid.js — golden file test z oczekiwanym outputem dla łańcucha A→B→C?
- Jak ograniczyć głębokość wyświetlanego łańcucha kauzalnego parametrem MAX_DEPTH w to_mermaid()?
- Jak kodować kolorem węzły Mermaid.js według ważności — CRITICAL: style fill:#f66, WARNING: style fill:#fa0?
- Jak osadzić diagram Mermaid.js w raporcie PDF/HTML — inline w Markdown czy eksport do SVG przez mermaid CLI?
- Jak linkować węzeł Mermaid.js z referencją do zdania źródłowego — click callback z document_id i sentence_id?
- Wygeneruj przykładowy kompletny diagram Mermaid.js dla raportu audytu CONS-02 — flowchart LR z węzłami zdarzenia i krawędziami :CAUSES?
- Jak wybrać poziom szczegółowości przykładu Mermaid.js — 3-węzłowy dla prezentacji vs. pełny dla eksportu technicznego?
- Jak zaktualizować metodę to_mermaid() aby wyświetlała relacje semantyczne — :IS_A, :HAS_SYNSET obok :CAUSES w tym samym diagramie?
- Jak kodować typ relacji w etykiecie krawędzi Mermaid.js — A -->|IS_A| B vs. A -->|CAUSES rule_id| B?
- Jak filtrować które relacje semantyczne trafiają do to_mermaid() — parametr include_relations: List[str]?
- Jak generować oddzielne sekcje `subgraph` w Mermaid.js dla każdego typu relacji (kauzalne vs. ontologiczne)?
- Zaktualizujmy logikę CausalChainBuilder o analizę intencji aktów mowy — jak rozpoznać czy :EventFrame jest ZOBOWIĄZANIEM, OSTRZEŻENIEM czy WYKONANIEM?
- Jakie cechy morfosyntaktyczne i leksykalne wskazują na akt mowy ZOBOWIĄZANIE (shall, zobowiązuje się) vs. POTWIERDZENIE (oświadcza, potwierdza)?
- Jak CausalChainBuilder tworzy krawędź :CAUSES gdy akt mowy ZOBOWIĄZANIE nie jest spełniony — link do EventFrame naruszenia?
- Stwórzmy wizualizację grafu łańcuchów przyczynowych w Mermaid.js — flowchart LR: niedostarczenie→brak_odbioru→naruszenie_CONS02→kara_umowna z etykietami severity?
- Jak dodać etykiety identyfikatorów reguł compliance na krawędziach Mermaid.js — `A -->|CAUSES:CONS-02| B` z linkiem do definicji reguły?
- Przygotujmy metodę to_html() w CausalChainBuilder — serializuje łańcuch kauzalny jako fragment HTML z osadzonym diagramem Mermaid.js przez `<div class="mermaid">` + inicjalizację mermaid.initialize()?
- Jak to_html() różni się od to_mermaid() — to_mermaid() zwraca str z flowchart LR, to_html() opakowuje go w kompletny fragment HTML gotowy do wstawienia w raport?
- Jak testować to_html() — asercja że wynik zawiera `<div class="mermaid">flowchart LR` i wywołanie `mermaid.initialize()`?

### 4. Testowanie
- Napiszmy test jednostkowy dla soft matchingu synonimów — `@pytest.mark.parametrize` z parami ('przekazać','dostarczyć'), ('wręczyć','dostarczyć') potwierdzającymi aktywację reguły CONS-02?
- Jak przetestować próg soft matchingu — asercja że pary z jaccard@synsets < 0.5 NIE aktywują reguły, a pary z jaccard ≥ 0.5 aktywują?
- Jak testować `_soft_match` w izolacji — mock SlowosiecAdapter zwracający zdefiniowane synsets dla konkretnych lematów bez zapytania do SQLite?
- Jak w Drools połączyć rozpoznaną akcję z wymogiem testu?
- Pokaż regułę Drools dla testu integracyjnego.
- Stwórzmy czerwony test dla nowej klasy InferenceEngine.
- Pokaż pętlę dedukcji, która zapali test na zielono..
- Stwórzmy test dla reguły skutku akcji 'dostarczyć z opóźnieniem' w InferenceEngine.
- Przetestujmy ramy walencyjne dla nowych czasowników..
- Napiszmy czerwony test dla nowej reguły posiadania w InferenceEngine..
- Napiszmy funkcję build_event_graph w Fazie GREEN..
- Jak zbudować dataset testowy dla reguły CONSTRAINT_VIOLATION — 10 zdań triggering i 10 non-triggering?
- Jak napisać test własnościowy (Hypothesis) dla CONSTRAINT_VIOLATION sprawdzający że kolejność słów nie zmienia wyniku?
- Jak mierzyć Precision i Recall reguły CONSTRAINT_VIOLATION na zbiorze zdań z korpusu NKJP?
- Pokaż strukturę pliku JSONL z danymi testowymi dla CONSTRAINT_VIOLATION — pola: sentence, expected_fired, rule_id?

### 5. Obsługa błędów
- Jak obsłużyć zaprzeczenia w regule _rule_possession_transfer?
- Co się dzieje gdy żadna reguła DRL nie pasuje do faktu — pusta lista Inference czy wyjątek NoRuleMatched?
- Jak obsłużyć nieoczekiwany wyjątek Java w KieSession — izolacja sesji czy restart całego serwisu?
- Jak logować każdą decyzję wnioskowania do celów audytu i traceability dla klienta?
- Jak obsłużyć zbyt długi czas wnioskowania — czy jest zdefiniowany timeout per session?
- Jak system obsłuży brakujące pojęcia OOV w InferenceEngine — gdy predykat EventFrame nie istnieje w Słowosieci: fallback do exact match, log KnowledgeGapTracker.capture_ign(token), kontynuacja pipeline zamiast wyjątku?
- Jak propagować informację o OOV przez pipeline W1→W5 — token `ign` z Morfeusza staje się UNKNOWN_WORD w KGT, InferenceEngine traktuje go jako low-confidence event bez emisji :CAUSES?

### 6. Integracja z innymi warstwami
- Jak zintegrować reguły Drools z wynikami parsera lxml?
- Czy Drools wymaga osobnego modułu w pipeline?
- Pokaż jak zintegrować Słowosieć z regułami wnioskowania o gatunkach.
- Jak zintegrować Słowosieć z regułami wnioskowania o lokalizacji?
- Jak zintegrować Słowosieć z ontologią pojęć w silniku wnioskowania?

### 7. Pułapki i ryzyka
_brak pytań źródłowych w tej kategorii_
- Jak uniknąć konfliktów reguł DRL gdy dwie reguły mają tę samą salience i sprzeczne akcje dla tego samego faktu?
- Co się dzieje gdy InferenceEngine wchodzi w pętlę — cykl zależności reguła A aktywuje B aktywuje A?
- Jak obsłużyć stare reguły DRL które nie pasują do nowego schematu EventFrame po migracji struktury danych?
- Jaka jest konsekwencja gdy StateMatrix jest niespójna między dwiema instancjami serwisu za load balancerem?
- Czy KieSession Drools jest thread-safe przy równoległym przetwarzaniu wielu dokumentów?
- Jak ograniczyć czas wnioskowania gdy reguły prowadzą do rozległego forward chaining powyżej limitu czasowego?
- Jakie jest ryzyko błędnej klasyfikacji CONS-02 gdy w dokumencie brakuje daty lub strony umowy?

## Pytania uzupełniające
- **Pułapka 3:** Drools `KieSession` nie jest thread-safe — każdy wątek musi mieć własną sesję; reużycie sesji między wątkami powoduje `ConcurrentModificationException`.
- **Pułapka 4:** Reguły DRL z `salience` (priorytet) — zmiana kolejności plików `.drl` może zmienić wyniki gdy dwie reguły mają ten sam salience; wyniki są niedeterministyczne bez jawnego salience.
- **Pułapka 5:** Drools `Working Memory` akumuluje wszystkie facts — bez `retract(fact)` po przetworzeniu zdania sesja rośnie w pamięci bez ograniczeń przy batch processingu.
- **Pułapka 6:** IntentClassifier wytrenowany na jednej domenie (np. prawna) nie generalizuje na inną (wojskowa, medyczna) — F1 może spaść z 0.90 do 0.45 bez domeny-specyficznego fine-tuningu.

### 1. Architektura

- Czy `InferenceEngine` powinien używać Drools (JVM) czy natywnego Pythona — trade-off?
- Jak zarządzać eksplozją reguł przy >1000 reguł DRL — partycjonowanie, agenda groups?
- Jak podzielić `InferenceEngine` na plugins (jeden plugin = jedna domena: prawna, medyczna, techniczna)?
- Jaka jest granica odpowiedzialności między W5 (wnioskowanie) a W4 (persystencja grafu)?
- Jak `StateMatrix` deduplikuje sprzeczne fakty — first-write-wins czy last-write-wins?

### 2. Kontrakty danych

- Jaki jest schemat `InferenceResult` — pola facts, intents, state?
- Jak reprezentować łańcuch przyczynowy w JSON: lista par `(cause, effect)` czy drzewo?
- Jaki format ma plik `.drl` dla reguły lokalizacji — minimalny przykład?
- Jak kodować pewność wnioskowania (confidence) w `InferenceResult`?
- Jak przechowywać `StateMatrix` między wywołaniami (stateful vs. stateless engine)?

### 3. Implementacja

- Jak zaimplementować `_rule_location(event)` — kiedy `EventRoleDict` ma LOCATION → zapisz `LOCATED_AT`?
- Jak zaimplementować `_rule_possession_transfer(give_event)` → `Maria OWNS książka`?
- Jak wykrywać negację (`nie dał`) i zapisać `NOT_OCCURRED` w StateMatrix?
- Jak zaimplementować `_rule_classify_legal(event)` wykrywającą naruszenie_terminowe (AGENT + dostarczyć + MANNER:opóźnienie)?
- Jak zaimplementować `IntentClassifier` — detekcja QUESTION przez słowa pytajne?
- Napiszmy minimalny kod klasy `IntentClassifier` aby testy przeszły — `class IntentClassifier: QUESTION_WORDS = {'jak','czy','kiedy','gdzie','co','kto'}; def classify(self, sentence: str) → str: return 'QUESTION' if sentence.split()[0].lower() in self.QUESTION_WORDS else 'ASSERT'`; faza GREEN dla testu `assert clf.classify('Jak działa system') == 'QUESTION'`?
- Pokaż przykład klasyfikacji intencji dla dokumentacji technicznej w Pythonie — `clf = IntentClassifier(); clf.classify('Wykonawca dostarczył SRS')` → `'ASSERT'`; `clf.classify('Czy SRS spełnia wymagania?')` → `'QUESTION'`; `clf.classify('Należy dostarczyć dokumentację')` → `'REQUIREMENT'` (rozszerzony klasyfikator z listą leksemów modalnych)?
- Jakie są najczęstsze luki wykrywane przez KnowledgeGapTracker w tekstach — typ UNMATCHED_RULE (brak reguły DRL dla predykatu) dominuje (~60%), UNKNOWN_WORD/ign (~25% dla neologizmów i skrótów technicznych), UNKNOWN_STRUCTURE/brak root (~10% dla zdań eliptycznych), UNMATCHED_PREDICATE/brak synsetu (~5%); proporcje wskazują że priorytetem ML jest uzupełnianie reguł DRL?
- Wdróżmy klasę `IntentClassifier` w Fazie Green — po napisaniu czerwonego testu `assert clf.classify('Jak działa system') == 'QUESTION'`, implementacja z `QUESTION_WORDS` przechodzi; następny krok: dodaj `MODAL_WORDS = {'musi','powinien','należy'}` i test `'Należy dostarczyć' → 'REQUIREMENT'` jako kolejna iteracja RED→GREEN?
- Pokaż implementację fazy Green dla `IntentClassifier` — minimalna klasa: `class IntentClassifier: QUESTION_WORDS={'jak','czy','kiedy','co','kto'}; MODAL_WORDS={'musi','powinien','należy','wymaga'}; def classify(self, s): words=s.lower().split(); return 'REQUIREMENT' if any(w in self.MODAL_WORDS for w in words) else 'QUESTION' if words[0] in self.QUESTION_WORDS else 'ASSERT'`; wszystkie 3 testy RED przechodzą?
- Jak wdrożyć logikę FACT/REQUIREMENT w metodzie `classify()` — `if any(w in sentence for w in self.MODAL_WORDS): return 'REQUIREMENT'`; `elif sentence.split()[0].lower() in self.QUESTION_WORDS: return 'QUESTION'`; `else: return 'ASSERT'`; kolejność warunków ważna: modal check przed pytajnym?
- Jak zrefaktoryzować `IntentClassifier.classify()` z użyciem tagów morfologicznych Morfeusza — zamiast leksykalnych list słów kluczowych: `morfeusz.analyse(sentence)` → sprawdź `feats['Mood']=='Imp'` lub predykat w MODAL_LEMMAS → REQUIREMENT; `feats['Tense']=='Past'` → ASSERT; dodaj fallback do leksykalnego klasyfikatora gdy `feats` niedostępne?
- Jak rozbudować `IntentClassifier` o obsługę trybu przypuszczającego — tag Morfeusza `Mood=Cond` (np. "dostarczyłby", "mógłby") → nowa kategoria `'HYPOTHESIS'`; dodaj `elif verb and verb.feats.get('Mood')=='Cond': return 'HYPOTHESIS'` po REQUIREMENT; HYPOTHESIS pozwala Linterowi flagować warunki hipotetyczne jako potencjalne luki w wymaganiach?
- Jak rozbudować `IntentClassifier` o kolejne tagi Morfeusza — poza `Imp`/`Past`/`imps` dodaj: `Mood=Ind + Tense=Pres` → stan aktualny → `'ASSERT'`; `'qub' in t.tag` (partykuła pytajna: "czy") → `'QUESTION'`; `Aspect=Perf + Tense=Fut` → zobowiązanie przyszłe → `'REQUIREMENT'`; każdy tag mapuje na jeden z 4 typów intencji bez regex?
- Pokaż logikę Refactor dla `IntentClassifier` — wydziel prywatną metodę `_morph_classify(tokens) → Optional[str]`: zwraca klasę gdy feats jednoznaczne, `None` gdy brak pewności; `classify()` staje się `_morph_classify(tokens) or _lexical_fallback(sentence)`; metoda `_morph_classify` jest niezależnie testowalna z gotowymi listami tokenów-stubów?
- Zrefaktoryzujmy `IntentClassifier` strukturalnie: wydziel `MorphClassifier(MorphAnalyser)` + `LexicalClassifier` jako dwie strategii; `IntentClassifier.__init__(primary: MorphClassifier, fallback: LexicalClassifier)`; `classify()` próbuje `primary.classify()` — jeśli zwróci `None` (brak feats) → `fallback.classify()`; wzorzec Strategy umożliwia niezależne testowanie obu warstw?
- Zaktualizujmy metodę `classify()` o tagi `praet` i `imps` z Morfeusza — `praet` = czas przeszły (Tense=Past, Aspect=Perf/Imp) → ASSERT; `imps` = forma bezosobowa (`'imps' in t.tag`) → często REQUIREMENT (np. "dostarcza się", "podaje się"); dodaj `elif any('imps' in t.tag for t in tokens): return 'REQUIREMENT'` przed fallback lexical?
- Jak zintegrować `IntentClassifier` z modelem DeepER — po rozpoznaniu przez DeepER encji `TECHNOLOGIA` lub `ORGANIZACJA`, wzbogać `context_hint` dla `classify()`: `clf.classify(sentence, context_hint=ner_label)` → jeśli `ner_label=='TECHNOLOGIA'` i `speech_act=='REQUIREMENT'` → dodaj `domain='technical_requirement'` do EventFrame; NER wzbogaca klasyfikację intencji o domenę?
- Zaktualizujmy kod `IntentClassifier` o morfologię Morfeusza — pełna implementacja: `tokens = self._morph.analyse(sentence)`; `verb = next((t for t in tokens if 'verb' in t.pos), None)`; `if verb and verb.feats.get('Mood')=='Imp' or verb and verb.lemma in MODAL_LEMMAS: return 'REQUIREMENT'`; `elif verb and verb.feats.get('Tense')=='Past': return 'ASSERT'`; `else: return self._lexical_fallback(sentence)`?
- Czy implementujemy `IntentClassifier` z analizą tagów Morfeusza — tak: kolejność implementacji: 1) `MorphClassifier._classify_by_mood()` (Imp/Cond/Ind), 2) `._classify_by_tense()` (Past/Fut), 3) `._classify_by_pos()` (imps/qub), 4) `LexicalClassifier` jako fallback; każda sub-metoda testowana oddzielnie z `Token`-stubami bez rzeczywistego Morfeusza?
- Czy zrefaktoryzować `IntentClassifier` pod tagi Morfeusza — tak, dodaj `_classify_by_aspect(tokens)`: `Aspect=Perf+Tense=Past` → silny ASSERT (zakończony fakt); `Aspect=Imp+Tense=Pres` → stan trwający → ASSERT; `Aspect=Perf+Tense=Fut` → przyszłe zobowiązanie → REQUIREMENT; aspekt jako czwarty wymiar klasyfikacji po Mood/Tense/POS?
- Jak testować `_classify_by_aspect()` niezależnie — `def test_perf_fut_is_requirement(): tokens = [Token(lemma='dostarczyć', feats={'Aspect':'Perf','Tense':'Fut'})]; clf = MorphClassifier(None); assert clf._classify_by_aspect(tokens) == 'REQUIREMENT'`; stub tokena bez Morfeusza — izolowany test jednotkowy sub-metody?
- Jak zrefaktoryzować `IntentClassifier`, aby uniknąć fałszywych dopasowań — dodaj `confidence_threshold=0.7`; `_morph_classify()` zwraca `(label, confidence)`; gdy `confidence < threshold` → fallback do LexicalClassifier; fałszywe dopasy często: słowo `musi` w nazwie własnej (np. "firma Musi-Tech") → sprawdź POS: `label=='verb'` przed MODAL_LEMMAS check?
- Zrefaktoryzujmy `IntentClassifier` krok po kroku z tagami Morfeusza — Refactor phase po GREEN: 1) wydziel `_classify_by_mood()`, `_classify_by_tense()`, `_classify_by_aspect()`, `_classify_by_pos()` jako prywatne metody; 2) `_morph_classify()` wywołuje je w łańcuchu z `or`; 3) każda metoda zwraca `Optional[str]` — None gdy tag niedostępny; 4) `classify()` = `_morph_classify(tokens) or _lexical_fallback(sent)`?
- Jak zintegrować `IntentClassifier` z głównym pipeline'em — `SemanticMapper.__init__(intent_clf: IntentClassifier)`, po `map_roles()` wywołaj `event.speech_act = intent_clf.classify(sentence)` zanim EventFrame trafi do InferenceEngine; klasyfikator operuje na surowym zdaniu przed parsowaniem dep_rel?
- Jak KnowledgeGapTracker raportuje braki w Słowosieci — `dump_jsonl()` filtruje wpisy `type=='MISSING_SYNSET'` i eksportuje do `wordnet_gaps.jsonl`; format: `{predicate, pos, doc_id, sentence_fragment}`; plik stanowi wkład do procesu rozszerzania plWordNet o nowe leksemy techniczne?
- Jak sformatować raport luk dla błędów `MISSING_SENSE` — różnica: `MISSING_SYNSET` = brak całego synsetu dla lematu; `MISSING_SENSE` = synset istnieje ale brakuje konkretnego sensu (znaczenia) w danym kontekście domenowym; format wpisu: `{predicate, synset_id, missing_sense_gloss, domain_hint, doc_id}`; wyeksportuj jako `sense_gaps.jsonl` osobno od `wordnet_gaps.jsonl`?
- W jaki sposób silnik zamraża wniosek w macierzy stanu — `StateMatrix.freeze(fact_hash)` oznacza wniosek jako rozliczony: `self._frozen: Set[str]`; przed każdym `InferenceEngine.run()`: `if fact_hash in state_matrix._frozen: skip`; `freeze()` wywoływany po pomyślnym zapisie `AuditFinding` do Neo4j; gwarancja idempotentności — ten sam dokument audytowany dwukrotnie nie generuje duplikatów; `fact_hash = sha256(agent+action+patient+speech_act)` jako klucz deduplikacji?
- Jak zmapować taksonomię Austina na klasy `IntentClassifier` — 5 typów aktów mowy: ASSERTIVE (opis stanu → `'ASSERT'`), DIRECTIVE (nakaz → `'REQUIREMENT'`), COMMISSIVE (zobowiązanie → `'COMMITMENT'`), EXPRESSIVE (ocena → `'OPINION'`), DECLARATIVE (zmiana stanu prawnego → `'DECLARATION'`); `COMMITMENT_LEMMAS={'zobowiązuje się','dostarczy','zapewni'}`; `DECLARATIVE` gdy predykat `'niniejszym'` + orzeczenie w Pres; `COMMITMENT` kluczowy w dokumentach kontraktowych — Linter wykrywa brak `DECLARATION` potwierdzającej `COMMITMENT`?
- Jakie reguły składniowe są kluczowe dla polskiej dokumentacji technicznej — 5 wzorców dep-rel istotnych dla `DependencyParserAdapter`: (1) **nsubj** — podmiot wykonawcy (`Wykonawca dostarczył`); (2) **obj** — dopełnienie bliższe (`dostarczył dokumentację`); (3) **nmod** — modyfikator rzeczownika (`dokumentacja z opóźnieniem`); (4) **mark+advcl** — zdanie podrzędne warunkowe (`jeżeli Zamawiający potwierdzi`); (5) **aux:pass** — strona bierna (`dokumentacja została dostarczona` → agent niejawny → `Violation('ROLE-01')` gdy brak AGENT); `DepRelExtractor.extract_passive(doc)` wykrywa aux:pass i emituje `Violation` gdy `EventFrame.agent IS NULL`?
- W jaki sposób graf znaczeń rozwiązuje problem elastycznego szyku zdania — w polszczyźnie `Dokumentację dostarczył Wykonawca` ≡ `Wykonawca dostarczył dokumentację` — różny szyk, to samo znaczenie; graf koduje relacje przez etykiety krawędzi (HAS_ROLE {role:'AGENT'/'PATIENT'}) niezależnie od pozycji tokenu w zdaniu; `DependencyParserAdapter` wyciąga dep-rel (nsubj, obj) przez spaCy — pozycja nieistotna; wynik: `EventFrame.agent='Wykonawca', patient='dokumentacja'` dla obu wariantów szyku; porównaj z modelem bag-of-words: `TF-IDF('Wykonawca','dostarczył','dokumentację')` identyczny dla obu zdań, ale bez ról; graf > BoW dla tekstu prawno-kontraktowego gdzie podmiot=odpowiedzialność?
- Jakie reguły weryfikacji domenowej dodać do silnika wnioskowania — `DomainVerificationRule` jako kategoria równoległa do katalogu Lintera: `DOMAIN-01` (`MATCH (e:EventFrame) WHERE NOT e.domain IN ['API','CONTRACT','SYSTEM','INFRA','AUTH','OPS','GRAPH'] RETURN e.id`) — nieznana domena; `DOMAIN-02` (`MATCH (e1)-[:CAUSES]->(e2) WHERE e1.domain <> e2.domain AND NOT exists((e1)-[:HAS_BRIDGE_RULE]->()) RETURN e1.id, e2.id`) — cross-domain CAUSE bez reguły mostu; `DOMAIN-03` wykrywa `EventFrame.domain=NULL`; `DomainValidator(rules=[DOMAIN-01,DOMAIN-02,DOMAIN-03]).validate(session)→List[DomainViolation]`; każda nowa domena wymaga wpisu w `DOMAIN_WHITELIST` i nowego `DOMAIN-0N` testu — zamknięty katalog domen?
- Pokaż przykład reguły Freeze dla wnioskowania w `CrossReferenceEngine` — circuit-breaker dla kaskad: `@dataclass class FreezeRule: condition: Callable[[CascadeState], bool]; reason: str`; dwie gotowe instancje: `depth_freeze = FreezeRule(lambda s: s.depth >= s.depth_limit, 'DEPTH_LIMIT_REACHED')` i `cycle_freeze = FreezeRule(lambda s: s.current_node in s.visited, 'CYCLE_DETECTED')`; `CascadeEngine.step(state)` sprawdza `any(r.condition(state) for r in self.freeze_rules)` → gdy True: `state.frozen=True; Violation(cascade_type='FROZEN_CASCADE', reason=r.reason, node_id=state.current_node, severity='LOW')`; Frozen violations trafiają do osobnej sekcji raportu — nie blokują HIGH/CRITICAL; `FreezeRule` serializowalny do YAML → konfiguracja `depth_limit` i `cycle_detection` bez zmiany kodu?
- Jak zdefiniować klasy intencji dla modelu mowy — taksonomia Austin: (1) `ASSERTIVE` — opis stanu: `'Wykonawca dostarczył SRS'` → `SpeechAct.type='ASSERT'`; (2) `DIRECTIVE` — nakaz/wymóg: `'Należy dostarczyć SRS do 30.06'` → `SpeechAct.type='REQUIREMENT'`; (3) `INTERROGATIVE` — pytanie: `'Jak działa system?'` → `SpeechAct.type='QUESTION'`; (4) `COMMISSIVE` — zobowiązanie: `'Zamawiający zapewni dostęp'` → `SpeechAct.type='COMMITMENT'`; `IntentClassifier.MODAL_WORDS={'należy','musi','zobowiązuje','zapewni'}→DIRECTIVE`; fallback `'UNKNOWN'` gdy żaden wzorzec nie pasuje; `SpeechAct(type, speaker, sentence_id)` jako węzeł `(:SpeechAct)` połączony `[:HAS_SPEECH_ACT]` z `EventFrame` — rozdziela intencję od treści zdarzenia?
- Jakie reguły wnioskowania dodać do Silnika Zależności — 4 kategorie `DeductionRule`: (1) `CAUSAL_CHAIN`: `MATCH (e1)-[:CAUSES]->(e2) WHERE e1.domain=e2.domain RETURN e2.id AS derived` — propagacja naruszeń po łańcuchu przyczynowym; (2) `TEMPORAL_CONSTRAINT`: `MATCH (e)-[:HAS_ROLE {role:'TIME'}]->(t) WHERE date(t.lemma)<date() RETURN e.id` — przeterminowane zobowiązania; (3) `ROLE_COMPLETENESS`: `MATCH (e:EventFrame) WHERE NOT (e)-[:HAS_ROLE {role:'AGENT'}]->() RETURN e.id` — brak aktora; (4) `DOMAIN_CONSISTENCY`: `MATCH (e1)-[:CAUSES]->(e2) WHERE e1.domain<>e2.domain RETURN e1.id` — cross-domain bez bridge; `DeductionEngine.register(rule)→chain`; kolejność wykonania: ROLE→TEMPORAL→CAUSAL→DOMAIN?
- Jak zbudować `DeductionEngine` z metodą `register()` i łańcuchowym wykonaniem — `class DeductionEngine: def __init__(self): self._rules:List[DeductionRule]=[]; def register(self, rule:DeductionRule)->'DeductionEngine': self._rules.append(rule); return self`; metoda `apply(session)->List[Violation]`: `return [v for rule in self._rules for v in rule.run(session)]`; builder pattern: `engine = (DeductionEngine().register(RoleCompletenessRule()).register(TemporalConstraintRule()).register(CausalChainRule()).register(DomainConsistencyRule()))`; `AuditPipeline` wstrzykuje `DeductionEngine` obok `LinterRuleEngine` — osobny runner, ten sam `session`; `rule.run(session)→List[Violation]` jako interfejs — każda reguła to 1 Cypher query?
- Jak zdefiniować regułę `COMPLIANCE-01` dla zakazów bez podstawy prawnej — `class Compliance01Rule(LinterRule): id='COMPLIANCE-01'; QUERY="MATCH (s:SpeechAct {subtype:'ZAKAZ'}) WHERE NOT (s)-[:HAS_LEGAL_BASIS]->(:LegalReference) RETURN s.id AS node_id"`; `Violation('COMPLIANCE-01', node_id, severity='HIGH', domain='LEGAL')`; `(:LegalReference {id, article, act})` jako węzeł podstawy prawnej; `MERGE (s)-[:HAS_LEGAL_BASIS]->(ref)` wstawiany przez `DocumentLogicParser` gdy parser wykryje `'zgodnie z art.'` w kontekście zakazu; `COMPLIANCE-01` zamyka pętlę: `classify_directive_subtype()→ZAKAZ → COMPLIANCE-01 sprawdza podstawę → AuditReport`?
- Pokaż regułę Freezing dla `CrossReferenceEngine` w Pythonie — integracja w konstruktorze: `class CrossReferenceEngine: def __init__(self, docs, depth_limit=3, freeze_rules=None): self.freeze_rules=freeze_rules or [FreezeRule(lambda s: s.depth>=depth_limit,'DEPTH_LIMIT'),FreezeRule(lambda s: s.current_node in s.visited,'CYCLE_DETECTED')]`; użycie w `check_cascade`: `state=CascadeState(current_node=v.node_id,depth=0,visited=set()); while not state.frozen: fr=next((r for r in self.freeze_rules if r.condition(state)),None); if fr: state.frozen=True; violations.append(Violation(cascade_type='FROZEN',reason=fr.reason,severity='LOW')); break; state.visited.add(state.current_node); self._propagate(state)`; `CascadeState(current_node,depth,visited,frozen=False)` jako mutable `@dataclass`?
- Jak zdefiniować klasy intencji dla polskiego modelu mowy — polskie markery leksykalne per klasa: `DIRECTIVE` → `{'należy','musi','powinien','wymaga się','zakazuje się'}`; `COMMISSIVE` → `{'zobowiązuje się','dostarczy','zapewni','gwarantuje'}`; `DECLARATIVE` → `{'niniejszym','stwierdza się','postanawia'}`; `ASSERTIVE` → brak markera modalnego — czysty orzecznik przeszły lub teraźniejszy; wykrywanie: `IntentClassifier._match_polish(sent:str)->SpeechActType`: `for marker in DIRECTIVE_MARKERS: if marker in sent.lower(): return DIRECTIVE`; kolejność sprawdzania: DECLARATIVE → DIRECTIVE → COMMISSIVE → ASSERTIVE (malejący priorytet); `pl_core_news_lg` morfologia `Mood=Imp` jako dodatkowy sygnał DIRECTIVE?
- Jakie są 4 kluczowe reguły Event Reasoning dla grafu — reguły operujące na wzorcach `EventFrame`: (1) `OBLIGATION_CHAIN`: gdy `(e1 {type:'OBLIGATION'})-[:CAUSES]->(e2)` i `e2` bez `AGENT` → `Violation('CAUSAL-01')`; (2) `DEADLINE_PROPAGATION`: gdy `e1` ma TIME przeterminowany i `[:CAUSES]->e2` → `e2.severity` eskaluje do `HIGH`; (3) `ACTOR_CONSISTENCY`: gdy dwa `EventFrame` w tym samym łańcuchu mają różny `AGENT` bez relacji `[:DELEGATES_TO]` → `Violation('ROLE-02')`; (4) `DOMAIN_BRIDGE_CHECK`: gdy `e1.domain != e2.domain` i `(e1)-[:CAUSES]->(e2)` bez `[:HAS_BRIDGE_RULE]` → `Violation('DOMAIN-02')`; `EventReasoningEngine(rules).apply(session)→List[Violation]` — oddzielny runner od `LinterRuleEngine`?
- Stwórzmy klasyfikator intencji dla poleceń i zakazów — sub-klasyfikacja w ramach `DIRECTIVE`: `POLECENIE` gdy `Mood=Imp` lub marker `{'należy','musi','powinien'}` + brak negacji; `ZAKAZ` gdy marker `{'zakazuje się','nie wolno','zabrania się'}` lub `Mood=Imp` + `negation_token=True`; implementacja: `def classify_directive_subtype(sent:Span)->str: neg=[t for t in sent if t.dep_=='neg']; imp=[t for t in sent if t.morph.get('Mood')==['Imp']]; prohib=[t for t in sent if t.lemma_ in {'zakazywać','zabraniać','zabroniać'}]; return 'ZAKAZ' if (neg and imp) or prohib else 'POLECENIE'`; wynik wzbogaca `SpeechAct.subtype`; `ZAKAZ` generuje `Violation('COMPLIANCE-01')` gdy brak dokumentu wyjaśniającego podstawę zakazu?
- Jak zapisać intencję zdania jako węzeł `(:SpeechAct)` w Neo4j — pełny wzorzec Cypher: `MERGE (sa:SpeechAct {id:$sentence_id}) ON CREATE SET sa.type=$type, sa.subtype=$subtype, sa.speaker=$speaker, sa.confidence=$conf MERGE (e:EventFrame {id:$frame_id}) MERGE (e)-[:HAS_SPEECH_ACT]->(sa) MERGE (s:Sentence {id:$sentence_id, text:$text}) MERGE (sa)-[:EXPRESSED_IN]->(s)`; `IntentClassifier.save_to_neo4j(session, frame_id, sentence_id)` wywołuje powyższe z `{'type': self._last_type, 'subtype': self._last_subtype, 'confidence': self._last_conf}`; `confidence` = wynik `_morph_classify()` (0.0–1.0), dla leksykalnego fallback = 0.6; query agregujący: `MATCH (sa:SpeechAct) RETURN sa.type, count(*) AS n ORDER BY n DESC` — rozkład intencji w dokumencie jako metryka pokrycia klasyfikatorem?
- Jak przekazać zamrożone wnioski `FreezeRule` do `AuditReportGenerator` — `AuditReportGenerator.add_frozen_section(frozen:List[Violation])` zbiera `Violation(cascade_type='FROZEN', reason=...)` w osobną listę; `generate_markdown()` dodaje sekcję `## Wnioski zamrożone (LOW)` pod sekcją `## Kaskady grafowe`, format: tabela `|node_id|reason|depth_at_freeze|`; `generate_json()` zwraca `report['frozen_violations']` oddzielnie od `report['violations']` i `report['cascades']`; `AuditPipeline.run()` aktualizowany: `frozen=[v for v in all_violations if v.cascade_type=='FROZEN']; generator.add_frozen_section(frozen)` przed `generate_markdown()`; operatorzy widzą gdzie silnik zatrzymał kaskadę i dlaczego (`DEPTH_LIMIT_REACHED` vs `CYCLE_DETECTED`) — przezroczystość zamrażania jako audit trail?
- Pokaż kod klasyfikatora intencji dla zdań trybu warunkowego — `Mood=Cond` (`'dostarczyłby'`) + leksykalne markery `{'jeżeli','o ile','pod warunkiem','w przypadku gdy'}` → klasa `CONDITIONAL_OBLIGATION`; implementacja: `def _classify_conditional(sent:Span)->Optional[str]: markers=['jeżeli','o ile','pod warunkiem','w przypadku gdy']; has_marker=any(m in sent.text.lower() for m in markers); has_cond_mood=any(t.morph.get('Mood')==['Cond'] for t in sent); return 'CONDITIONAL_OBLIGATION' if has_marker or has_cond_mood else None`; wywołanie w łańcuchu PRZED `DIRECTIVE` — `'musi dostarczyć o ile...'` → CONDITIONAL, nie DIRECTIVE; `SpeechAct(type='CONDITIONAL_OBLIGATION', condition_text=subclause_span.text)` gdzie `subclause_span` to `mark+advcl` zależność; Linter reguła `COND-01`: każdy `CONDITIONAL_OBLIGATION` musi mieć węzeł `(:TriggerCondition)` powiązany relacją `[:ACTIVATED_BY]`?
- Jak zmapować intencję 'prewencja' na regułę walidacyjną w silniku wnioskowania — reguła `MEDICAL-01`: `class Medical01Rule(LinterRule): id='MEDICAL-01'; QUERY="MATCH (e:EventFrame {intent:'PREVENTION'})-[:TRIGGERS_PROTOCOL]->(p:MedicalProtocol) WHERE NOT (e)-[:HAS_AUTHORIZATION]->(:AuthorizationDocument) RETURN e.id AS node_id"`; `Violation('MEDICAL-01', node_id, severity='HIGH', domain='MEDICAL')`; `(:AuthorizationDocument {type:'MEDICAL_APPROVAL', issuer:$issuer})` analogiczne do `LegalReference`; wzorzec reużywalny: każda dziedzina (MEDICAL/LEGAL/SECURITY) ma własny `(:ComplianceDocument)` subtype i regułę `{DOMAIN}-01` sprawdzającą obecność autoryzacji; `DeductionEngine.register(Medical01Rule())` dodaje nową domenę bez zmiany rdzenia silnika?
- Czy klasyfikator intencji obsługuje polskie formy trybu rozkazującego — tak, ale wymagane 3 wzorce poza `Mood=Imp`: (1) `niech + 3p verb` (np. "niech Wykonawca dostarczy") — `t.lemma_=='niech'` + następny token `Mood=Ind, Tense=Pres, Person=3` → `DIRECTIVE`; (2) `proszę + infinitiv` (np. "proszę dostarczyć SRS") — `t.lemma_=='proszić'` + następny `Verbform=Inf` → `DIRECTIVE` (polite imperative, wyższy formalizm); (3) `precz z + noun` → `EXPRESSIVE_COMMAND`; `_classify_by_mood()` sprawdza te wzorce PRZED leksykalnym fallbackiem; fałszywy alarm: "musi-Tech dostarcza" — POS guard `t.pos_=='VERB'` przed sprawdzeniem `Mood=Imp` odfiltruje `'musi-'` w nazwie własnej?
- Jak zmniejszyć złożoność analizy polszczyzny o 80% przez dekompozycję pipeline'u — strategia 3-poziomowa: (1) **Poziom fast** (`fast_mode`): `disable=['ner','senter','attribute_ruler']` + `SlowosiecProxy(None)` (brak wordnet) — dla wstępnej klasyfikacji DIRECTIVE/ASSERTIVE, ~3000 zdań/s; (2) **Poziom standard**: pełne `pl_core_news_lg` + `SlowosiecProxy(Neo4jAdapter)` z TTL cache — dla EventFrame extraction, ~800 zdań/s; (3) **Poziom deep**: standard + `Neo4jWordnetAdapter([:HYPERNYM*1..3])` + `DeductionEngine.register(4 reguły)` — dla pełnego audytu, ~150 zdań/s; `NLPPipeline.select_level(doc_complexity)` automatycznie wybiera poziom na podstawie liczby zdań i domeny dokumentu; eliminuje zbędne wywołania Słowosieci dla prostych dokumentów operacyjnych?
- Pokaż jak zdefiniować regułę SEC-03 dla wymogu szyfrowania bazy — `class Sec03Rule(LinterRule): id='SEC-03'; SEVERITY='CRITICAL'; QUERY="MATCH (db:Database) WHERE NOT (db)-[:ENCRYPTED_WITH]->(:EncryptionConfig) OR (db)-[:ENCRYPTED_WITH]->(c:EncryptionConfig) WHERE NOT c.algorithm IN ['AES-256','ChaCha20-Poly1305'] RETURN db.id AS node_id"`; `Violation('SEC-03', node_id, severity='CRITICAL', domain='SECURITY')`; `(:EncryptionConfig {algorithm, key_length:int, rotation_days:int})` jako węzeł konfiguracji szyfrowania; seed fixture: `MERGE (:Database {id:'db_prod'})-[:ENCRYPTED_WITH]->(:EncryptionConfig {algorithm:'AES-256', key_length:256, rotation_days:90})`; SEC-03 `CRITICAL` blokuje release — `AuditPipeline.run()` ustawia `report['release_blocked']=True` gdy violation `severity=='CRITICAL'`?
- Jakie reguły wnioskowania dodać dla domeny bezpieczeństwa — triadę SEC-03/04/05: `SEC-03` (baza nieszyfrowana — CRITICAL); `SEC-04`: `MATCH (e:EventFrame {domain:'SECURITY'})-[:HAS_ROLE {role:'INSTRUMENT'}]->(t:Token) WHERE t.lemma_ IN ['HTTP','FTP','Telnet'] RETURN e.id` — nieszyfrowany kanał transmisji → `Violation('SEC-04', severity='HIGH')`; `SEC-05`: `MATCH (e:EventFrame) WHERE e.domain='SENSITIVE_DATA' AND NOT (e)-[:HAS_AUDIT_LOG]->() RETURN e.id` — brak logu audytu dla danych wrażliwych → `Violation('SEC-05', severity='HIGH')`; `DeductionEngine.register(Sec03Rule()).register(Sec04Rule()).register(Sec05Rule())` zamyka domenę SEC; SEC-06 proaktywnie: brak `[:ROLE_BASED_ACCESS]` na węźle `(:Database {classification:'SENSITIVE'})` → CRITICAL?
- Stwórzmy listę reguł dla klasy intencji Polecenie z priorytetami — `POL_RULES: OrderedDict = {'Mood=Imp+no_neg': ('POLECENIE', 1.0), 'modal_pos+no_neg': ('POLECENIE', 0.8), 'niech_3p': ('POLECENIE', 0.7), 'proszę_inf': ('POLECENIE', 0.6), 'Mood=Imp+neg': ('ZAKAZ', 1.0), 'zakaz_marker': ('ZAKAZ', 0.95), 'prohib_verb': ('ZAKAZ', 0.9)}`; `classify_directive_subtype(sent)`: iteruje od najwyższego priorytetu, zwraca pierwszy match z confidence; macierz testowa: 7 reguł × 2 wyniki = 14 przypadków testowych; priorytet MORFOLOGICZNY przed LEKSYKALNYM — `Mood=Imp` zawsze wygrywa z markerem `'należy'`; `SpeechAct(subtype='POLECENIE', confidence=0.8)` gdy `modal_pos` — raport wyróżnia pewne polecenia (conf≥0.9) od probabilistycznych (conf<0.9)?
- Pokaż przykład analizy tematyzacji temat-rema w grafie semantycznym — `SemanticMapper` po ekstrakcji AAO dołącza `EventFrame.cohesion_score: float = 0.0`; algorytm: sekwencja `[e1, e2, e3, ...]` — gdy `e_i.theme_topic == e_{i+1}.theme_topic` → `e_{i+1}.cohesion_score += 0.1`; `CohesionAnalyzer.analyze(frames:List[EventFrame])->float` = `mean(f.cohesion_score for f in frames)`; `Violation('COHESION-01', severity='LOW')` gdy `mean_score < 0.3` dla sekwencji ≥5 ramek — dokument bez ciągłości tematycznej; `TOPICALLY_RELATED` chain: `MATCH (e1)-[:TOPICALLY_RELATED*1..5]->(e5) RETURN count(e5) AS chain_length` — długie łańcuchy = spójny dyskurs; `AuditReport` nowa sekcja `## Spójność Dyskursu`: `{chain_count:int, mean_cohesion:float, violations_cohesion_01:int}`; polskie cleft `'To Wykonawca dostarczył SRS'` odwraca tema/remat ale zachowuje spójność dyskursu — `TOPICALLY_RELATED` wykrywa ciągłość nawet przy inwersji tematycznej?
- Jakie reguły wnioskowania dodać dla wymiarów przyczyna i narzędzie — `INSTR-01`: `MATCH (e:EventFrame)-[:HAS_ROLE {role:'INSTRUMENT'}]->(t:Token) WHERE t.ent_type IN ['TECH_TOOL','TECH_DB'] AND NOT (t)-[:HAS_VERSION]->() RETURN t.lemma AS node_id` → `Violation('INSTR-01', severity='LOW', details={'reason':'tool_without_version'})` — narzędzie techniczne bez wersji w dokumentacji; `CAUSAL-01`: `MATCH (e:EventFrame) WHERE e.speech_act='ASSERT' AND e.predicate IN ['awaria','nie działać','nie przetwarza'] AND NOT (e)-[:CAUSED_BY]->() RETURN e.id` → `Violation('CAUSAL-01', severity='MEDIUM')` — zdarzenie awaryjne bez przyczyny w grafie; `CAUSAL-02`: `MATCH (e1)-[:CAUSED_BY]->(e2) WHERE e1.domain != e2.domain RETURN e1.id` → CROSS_DOMAIN_CAUSE → `severity='HIGH'` gdy przyczyna z domeny INFRA dla zdarzenia CONTRACT; `DeductionEngine.register(Instr01Rule()).register(Causal01Rule()).register(Causal02Rule())`; `e.cause_dimension = causal_marker_detection(sent, ['ponieważ','bo','dlatego że'])` w `SemanticMapper` ustawia `[:CAUSED_BY]` przy zapisie?
- Jakie reguły w spaCy pomogą wykrywać tryb rozkazujący i zakazy — `DIRECTIVE_MATCHER = Matcher(nlp.vocab)`; wzorzec rozkazujący: `DIRECTIVE_MATCHER.add('POLECENIE_IMP', [[{'MORPH': {'IN': ['Mood=Imp']}}]])`; wzorzec modalny: `DIRECTIVE_MATCHER.add('POLECENIE_MODAL', [[{'LEMMA': {'IN': ['należeć','musieć','powinien']}}, {'DEP':'xcomp', 'MORPH':{'IN':['VerbForm=Inf']}}]])`; wzorzec zakazu: `DIRECTIVE_MATCHER.add('ZAKAZ', [[{'LEMMA': {'IN': ['zakazywać','zabraniać']}}, {'OP':'?'}], [{'LOWER':'nie'},{'LOWER':'wolno'}]])`; `DirectiveMatcher.match(doc)->List[SpeechAct]` = `DIRECTIVE_MATCHER(doc)` → `SpeechAct(subtype=match_id, confidence=POL_RULES[match_id][1])`; `@Language.component('directive_matcher') def apply_dm(doc): doc._.speech_acts.extend(dm.match(doc)); return doc`; `nlp.add_pipe('directive_matcher', after='intent_clf')` — Matcher + morfologia = dwa niezależne sygnały potwierdzające tę samą intencję; `confidence = max(morph_conf, pattern_conf)` gdy oba matchują?
- Jak dodać własną regułę NER dla terminów technicznych do istniejącego potoku — `EntityRulerUpdater.add_patterns(nlp, patterns:List[Dict])`: `ruler=nlp.get_pipe('entity_ruler') if 'entity_ruler' in nlp.pipe_names else nlp.add_pipe('entity_ruler', before='ner')` — idempotentna inicjalizacja; `ruler.add_patterns(patterns)` — duplikaty ignorowane; hot-reload: `EntityRulerUpdater.reload_from_yaml(nlp, path)` = `ruler.patterns=[]` + `ruler.add_patterns(load_yaml(path))` — zmiana reguł bez restartu serwisu; `tech_ner_rules.yml` z polami `{label, pattern, id}`: `- {label: TECH_STANDARD, pattern: TEI P5, id: tei_p5}`; weryfikacja: `assert 'TECH_STANDARD' in ruler.labels`; `EntityRulerUpdater` jako adapter między YAML-config a spaCy — operator może rozszerzyć NER bez znajomości kodu; reload idempotentny: `reload_from_yaml()` wywołany 2× nie duplikuje wzorców dzięki `{id}` kluczowi?
- Dodajmy słownik domenowy korygujący błędy Słowosieci dla terminów AI — `DomainDictionaryPL`: `AI_TERMS: Dict[str,SynsetOverride] = {'transformer': SynsetOverride(domain='AI', hypernyms=['model_językowy','sieć_neuronowa'], correct_synset='transformer.1'), 'embedding': SynsetOverride(domain='AI', hypernyms=['reprezentacja_wektorowa']), 'fine-tuning': SynsetOverride(domain='AI', hypernyms=['uczenie_maszynowe'], pl_equiv='strojenie')}`; `SlowosiecProxy.get_hypernyms(lemma)`: gdy `lemma in DomainDictionaryPL.AI_TERMS` → zwraca `override.hypernyms` bez pytania Neo4j; `DomainDictionaryPL.correct_synset(lemma)→str` nadpisuje `disambiguate()` dla znanych terminów; ładowany z `domain_dict_ai.yml` — edytowalny przez specjalistów bez zmiany kodu; `KnowledgeGapTracker.capture_ign()` loguje tokeny które NIE są w słowniku i mają tag `ign` — priorytetyzuje rozszerzanie; `DomainDictionaryPL.merge(other:DomainDictionaryPL)` pozwala łączyć słowniki per-projekt (AI + LEGAL + MEDICAL)?
- Pokaż przykładowy kod NER do łączenia pojęć technicznych TEI NKJP przez PhraseMatcher — `PhraseMatcher(nlp.vocab, attr='LOWER')` dla multi-token exact match: `TECH_PHRASES=['TEI P5','NKJP','KPWr','pl_core_news_lg','morfeusz2','neo4j aura']`; `patterns=[nlp.make_doc(p) for p in TECH_PHRASES]`; `matcher.add('TECH_STANDARD', patterns)`; `TechPhraseNER.apply(doc)→doc`: `matches=matcher(doc); spans=[Span(doc,s,e,label='TECH_STANDARD') for _,s,e in matches]; doc.ents=filter_spans(doc.ents+tuple(spans))`; `filter_spans` rozwiązuje nakładanie z modelem NER; `PhraseMatcher` vs `EntityRuler`: PhraseMatcher szybszy dla dużych list (>1000 fraz) przez AHO-CORASICK; `EntityRuler` lepszy dla wzorców z morfologią; `TechPhraseNER` jako `@Language.component('tech_phrase_ner')` lub zintegrowany w `entity_ruler`; `nlp.add_pipe('tech_phrase_ner', before='ner')` — kolejność krytyczna?
- Pokaż zapytanie Cypher wykrywające brak testów dla komponentu — `TEST-01`: `MATCH (c:Component) WHERE NOT (c)-[:HAS_TEST]->(:TestCase) RETURN c.id AS node_id` → `Violation('TEST-01', severity='MEDIUM', domain='TESTING')`; `TEST-02` (brak testu integracyjnego): `MATCH (c:Component)-[:DEPENDS_ON]->(dep:Component) WHERE NOT (c)-[:HAS_TEST {type:'integration'}]->() RETURN c.id`; `(:TestCase {id, type:'unit'|'integration'|'e2e', status:'PASS'|'FAIL'|'MISSING'})`; seed: `MERGE (:Component {id:'nlp_extractor'})-[:HAS_TEST]->(:TestCase {id:'tc_01', type:'unit', status:'PASS'})`; coverage metric: `MATCH (c:Component) OPTIONAL MATCH (c)-[:HAS_TEST]->(t) RETURN c.id, count(t) AS test_count ORDER BY test_count ASC` — komponenty z `test_count=0` jako priorytet; `TestCoverageValidator.validate(session)->List[Violation]` odpytuje TEST-01/02 w jednym przebiegu; integracja: `AuditPipeline.run()` wywołuje `TestCoverageValidator` jako ostatni validator po `BackupDomainValidator`?
- Jak wdrożyć analizę domenową dla różnych wariantów faktów przez DomainDictionary — `DomainDictionaryPL.correct_synset_rank(lemma, candidates:List[Synset])->List[Synset]` re-rankuje kandydatów zamiast całkowicie zastępować: gdy `lemma in AI_TERMS` → `score += 2.0` dla `domain='AI'` synsetów; `SynsetCandidate(synset_id, score, source:'model'|'domain_dict'|'context_window')`; warianty faktów: `FactVariantClassifier.classify(frame)->FactVariant` rozróżnia `NORMATIVE_FACT` (powinien dostarczyć) vs `EMPIRICAL_FACT` (dostarczył) vs `HYPOTHETICAL_FACT` (gdyby dostarczył) na podstawie `MorphFeatures(mood,tense,aspect)` + `speech_act`; `DomainDictionaryPL.validate_coverage(corpus:List[str])->Dict[str,int]` = `{term: miss_count}` dla terminów które Słowosieć błędnie klasyfikuje; `DomainDictionaryPL.suggest_additions(gap_tracker)->List[SynsetOverride]` = pętla feedback: `KnowledgeGapTracker → słownik → poprawa`; `DomainDictionaryPL.merge(ai_dict, legal_dict, medical_dict)` = projekty wielodomenowe bez konfliktów kluczy?
- Zaproponuj zestaw reguł Cypher do weryfikacji spójności intencji w dokumentach — `IntentConsistencyRuleEngine` z 3 regułami: (1) **INTENT-01** sprzeczność POLECENIE+ZAKAZ: `MATCH (e1)-[:HAS_SPEECH_ACT {type:'POLECENIE'}]->(), (e2)-[:HAS_SPEECH_ACT {type:'ZAKAZ'}]->() WHERE e1.action_lemma=e2.action_lemma AND e1.doc_id=e2.doc_id RETURN e1.id, e2.id` → `Violation('INTENT-01','CONTRADICTORY_DIRECTIVES','HIGH')`; (2) **INTENT-02** warunek bez następnika: `MATCH (e)-[:HAS_SPEECH_ACT {type:'WARUNEK'}]->() WHERE NOT (e)-[:CAUSES|ACTIVATES]->() RETURN e.id` → `Violation('INTENT-02','CONDITION_WITHOUT_EFFECT','MEDIUM')`; (3) **INTENT-03** ASSERT się zaprzecza: `MATCH (e1 {speech_act:'ASSERT', negated:False}), (e2 {speech_act:'ASSERT', negated:True}) WHERE e1.action_lemma=e2.action_lemma AND e1.agent_lemma=e2.agent_lemma AND e1.doc_id=e2.doc_id RETURN e1.id,e2.id` → `Violation('INTENT-03','SELF_CONTRADICTORY_ASSERT','HIGH')`; `IntentConsistencyRuleEngine.run(session)->List[Violation]` wywoływany w `AuditPipeline` po `CrossReferenceEngine`; każda reguła jako `@staticmethod` → testowalny izolowany?
- Jakie reguły wtyczki audytowej najlepiej wykryją sprzeczności w intencjach — `IntentContradictionPlugin` implementuje `AuditPlugin` interface: `detect(session)->List[Contradiction]`, `report(contradictions)->str`; `@dataclass Contradiction(id1:str, id2:str, type:'POLECENIE_ZAKAZ'|'ASSERT_NEGATED'|'CONDITION_ORPHAN', severity:str, context:str)`; detection query: `MATCH (doc:Document)-[:CONTAINS*]->(e1:EventFrame), (doc)-[:CONTAINS*]->(e2:EventFrame) WHERE e1.id<e2.id AND e1.action_lemma=e2.action_lemma AND e1.intent<>e2.intent RETURN e1,e2,doc.id`; `ContradictionSeverityClassifier.classify(c)->str`: `POLECENIE_ZAKAZ→HIGH`, `ASSERT_NEGATED→MEDIUM`, `CONDITION_ORPHAN→LOW`; `AuditPipeline.register_plugin(IntentContradictionPlugin())` → `plugins.append(plugin)`; `plugin.report(contradictions)` generuje sekcję Markdown `## Sprzeczności Intencji` z tabelą `| Doc | Zdarzenie 1 | Zdarzenie 2 | Typ | Ważność |`; `plugin.detect()` wywoływany po `IntentConsistencyRuleEngine` — dopełnia pełny widok konfliktu intencji w dokumencie?
- Jak zaimplementować słownik domenowy korygujący błędy Słowosieci dla AI przez YAML workflow — lifecycle `DomainDictionaryPL`: (1) `suggest_additions(gap_tracker)->List[SynsetOverride]` → zapisuje `domain_dict_proposals.yml` z polami `{lemma, suggested_hypernym, frequency, source_doc, status:'proposed'}`; (2) ekspert zatwierdza: zmienia `status:'proposed'→'approved'` lub `'rejected'`; (3) hot-reload: `DomainDictionaryPL.reload(path)` — zero restart serwisu; (4) wersjonowanie: `version=sha256(yaml_content)[:8]` w logach; `DomainDictionaryPL.export_to_graph(session)`: `MERGE (:DomainTerm {lemma:$l, domain:$d})-[:OVERRIDES_SYNSET]->(:Synset {id:$s})` — słownik jako węzły grafu; CI/CD test: `def test_approved_terms_have_valid_synsets(domain_dict): assert all(proxy.synset_exists(t.correct_synset) for t in domain_dict.approved_terms())`; `DomainDictionaryPL.merge(ai, legal, medical)` — słowniki wielodomenowe bez konfliktów kluczy przez namespace `domain:lemma`?
- Pokaż kod parsera tabel Markdown dla struktury Poziom A dokumentu technicznego — `MarkdownASTReader.read_level_a(ast)->List[DocumentSection]` parsuje H1 jako jednostkę Poziomu A; `DocumentSection(heading:str, level:int, type:SectionType, children:List[DocumentSection], tables:List[TableAST])`; `SectionTypeClassifier.classify(heading:str)->SectionType`: `'Wymagania'→REQUIREMENT`, `'Procedury'→PROCEDURE`, `'Harmonogram'→TIMELINE`, `'Słownik'→GLOSSARY`, `'Ryzyko'→RISK` przez `difflib.SequenceMatcher` po normalizacji; Cypher: `MERGE (:Section {heading:'Wymagania', level:2, type:'REQUIREMENT', doc_id:$id})-[:PART_OF]->(:Document {id:$id})`; `StructureValidator.validate_level_a(sections)->List[Violation]`: brak sekcji `REQUIREMENT` → `Violation('STRUCT-01','MISSING_REQUIREMENT_SECTION','MEDIUM')`; `MarkdownGraphParser.parse()` wywołuje `read_level_a()` przed `read_table()` — hierarchia sekcji determinuje domenę semantyczną parsowanych tabel (tabela w `REQUIREMENT` → SpeechAct.REQUIREMENT)?
- Jak zaimplementować model Agent-Akcja-Obiekt w katalogu nlp — `nlp/models/aao.py` jako centrum dataclasses: `@dataclass AnnotatedToken(lemma:str, pos:str, dep:str, ner_type:str='', morph:MorphFeatures=None, role:str='')` + `@dataclass EventFrame(id:str, predicate:str, agent:AnnotatedToken|None, action:AnnotatedToken, patient:AnnotatedToken|None, time:AnnotatedToken|None, location:AnnotatedToken|None, instrument:AnnotatedToken|None, environment:AnnotatedToken|None, speech_act:str='ASSERT', negated:bool=False, domain:str='', source_doc_id:str='')`; `nlp/extractors/aao_extractor.py`: `AaoExtractor.extract(doc:Doc)->List[EventFrame]`; `nlp/rules/aao_rules.py`: `ROLE_01=RoleRule('ROLE-01', lambda f: f.agent is None and f.speech_act=='POLECENIE')`; `nlp/__init__.py`: `from nlp import NLPPipeline, EventFrame, AaoExtractor, LinterRuleEngine`; `pyproject.toml`: `[tool.setuptools.packages.find] where=['nlp']`; test: `from nlp import NLPPipeline; frames=NLPPipeline.fast_mode().extract('Wykonawca dostarczył SRS.'); assert frames[0].agent.lemma=='Wykonawca'`?
- Jak wdrożyć silnik reguł Drools do wnioskowania z grafu NLP — `DroolsRuleEngine` jako `AuditPlugin` dla organizacji z Java stack: reguła `.drl`: `rule "TEMPORAL-01" when e: EventFrame(timeDimension == null, domain == "CONTRACT") then violations.add(new Violation("TEMPORAL-01", e.getId(), Severity.HIGH)); end`; integracja Python→JVM: `DroolsAdapter(rules_drl_path)` przez `jpype.startJVM()` (cache jako singleton — startup 2-5s jednorazowo); `EventFrame.to_java()` konwertuje do Java object przez JPype; `KieSession.insert(event_frame_java)` → `session.fireAllRules()` → `violations.getViolations()`; zalety Drools vs Python rules: `salience` priorytet reguł, `agenda-group` grupowanie, `no-loop` zapobiega rekursji; prostszy alternative: `durable_rules` (Python-native, składnia podobna do Drools BRL): `@when('EventFrame(timeDimension == None, domain == "CONTRACT")')` → `def temporal_01(c): c.assert_fact(Violation('TEMPORAL-01', HIGH))`; `RULES_ENGINE=drools|durable|python` z env var — `AuditPlugin` factory ładuje właściwy backend?
- Jak modelować relacje Instrument i Patient w rozszerzonym modelu 8-wymiarowym — `InstrumentSchemaBuilder` rozszerza INSTRUMENT o podtypy i zależności z PATIENT; `(:Token {role:'INSTRUMENT'})-[:IS_INSTANCE_OF]->(:InstrumentType {name:'SOFTWARE', requires_version:True})`; `INSTR-01`: `MATCH (e)-[:HAS_ROLE {role:'INSTRUMENT'}]->(t) WHERE NOT (t)-[:HAS_VERSION]->() AND t.ner_type='TECH_TOOL' RETURN t.lemma` → brak wersji = naruszenie; PATIENT+INSTRUMENT w jednym zdaniu: `'system szyfruje plik kluczem AES'` → `PATIENT='plik'`, `INSTRUMENT='klucz AES'`; query: `MATCH (e)-[:HAS_ROLE {role:'PATIENT'}]->(p),(e)-[:HAS_ROLE {role:'INSTRUMENT'}]->(i) RETURN e.id, p.lemma, i.lemma`; PATIENT bez INSTRUMENT → `ROLE-03` potencjalne naruszenie; kompletny 8-wymiarowy model: AGENT + ACTION + PATIENT + PATIENT_INDIRECT + TIME + LOCATION + INSTRUMENT + ENVIRONMENT — `EventFrame` dataclass po B86; `SemanticMapper.REQUIRED_DIMS=['AGENT','ACTION']` + `OPTIONAL_DIMS=['PATIENT','INSTRUMENT','TIME','LOCATION','PATIENT_INDIRECT','ENVIRONMENT']`?
- Jak wdrożyć moduł coreference resolution dla polskich tekstów technicznych — `PolishCoreferenceResolver.resolve(doc:Doc)->CorefMap` łączy zaimki z antecedentami; `CorefMap: Dict[int,int]` = `{pronoun_token_idx: antecedent_token_idx}`; algorytm: `(1) find_mentions(doc)→List[Mention]` = `nsubj` + `ppron3`; `(2) score_pairs(m1,m2)` = `gender_match + number_match + proximity_bonus`; `(3) argmax per pronoun` = wybrany antecedent; polskie wyzwania: `'on/go/mu'` = różne formy tego samego zaimka — `POLISH_PRONOUNS_MAP={'go':'on','mu':'on','jego':'on','jemu':'on','nim':'on'}` mapuje na lemma; Graf: `MERGE (p:Token {form:'go'})-[:COREFERENCE_OF]->(a:Token {lemma:'Wykonawca'})`; `SAME_AGENT` chain: `MERGE (e1)-[:SAME_AGENT]->(e2)` gdy `e1.resolved_agent==e2.resolved_agent`; `COREREF-01`: `MATCH (e) WHERE e.has_pronoun=True AND NOT (e)-[:SAME_AGENT]->() RETURN e.id`; test: `'Wykonawca dostarczył SRS. Zamawiający go zatwierdził.'` → `CorefMap={token_idx('go'): token_idx('SRS')}`?
- Jakie są korzyści z fraktalnej analizy dokumentów — fraktalna = self-similar: sekcja `# SRS` → `## Wymagania` → `### Wymagania funkcjonalne` → `#### F-01` — każdy poziom odwzorowuje strukturę całości; `FractalDepthAnalyzer.depth_score(doc)->Dict[str,int]` = `{H1:3, H2:12, H3:45, H4:120}` — typowy rozkład 1:4:15:40 dla dobrze zorganizowanej dokumentacji technicznej; prawo Pareto w dokumentach: 80% treści semantycznej w 20% sekcji → `FractalDepthAnalyzer.top_sections(pct=0.20)->List[Section]` identyfikuje kluczowe węzły; dokumenty prawne: Artykuł → Ustęp → Punkt → Litera = 4 poziomy; `STRUCT-02`: `MATCH p=(d:Document)-[:CONTAINS*]->(s:Section) WITH length(p) AS depth WHERE depth > 4 RETURN d.id` → `Violation('STRUCT-02','EXCESSIVE_DEPTH','LOW')`; `FractalDepthAnalyzer.distribution_chart()` jako wykres głębokości w raporcie PANTERA; self-similar property: sekcje z `coverage_score < 0.5` na każdym poziomie = systemowa luka dokumentacji, nie losowa?
- Jak zintegrować PolishCoreferenceResolver z modułem pamięci kontekstu dokumentu — `DocumentContextMemory` persystuje antecedenty między zdaniami: `ContextMemory.update(mention:Mention, frame:EventFrame)` rejestruje ostatni agent/patient per `entity_type`; `ContextMemory.lookup(pronoun:str, gender:str, case:str)->Optional[Mention]` szuka antecedenta w oknie ostatnich N zdań (`window_size=10` — starsze wygasają); `PolishCoreferenceResolver.resolve(doc, memory:DocumentContextMemory)->CorefMap` używa `memory` zamiast skanowania całego dokumentu; cross-sentence chain: `Wykonawca` (zdanie 1) → `go` (zdanie 3) → `mu` (zdanie 7) = 3-hop koreferencja; `memory.persist(doc_id)` → `context_memory.jsonl` dla trwałości między sesjami; `CohesionAnalyzer.coref_chain_length(doc)->int` = max hop distance — wyższy = trudniejszy do zrozumienia dokument; `ContextMemory.stats()->Dict` = `{mentions_tracked, pronoun_resolved_pct, avg_distance}`; test: `mem=DocumentContextMemory(window_size=10); resolver.resolve(doc,mem); assert mem.stats()['pronoun_resolved_pct'] > 0.80`?
- Jakie reguły Drools obsłużą wnioskowanie po wymiarach zdarzenia, blokowanie architektury i spójność intencji — kompletny zestaw 3 typów: (1) **DIMENSION-01** wymiarowanie: `rule "DIMENSION-01" when e:EventFrame(agentDimension==null, domain=="CONTRACT", speechAct=="POLECENIE") then session.insert(new Violation("ROLE-01",e.getId(),Severity.HIGH)); end`; (2) **ARCH-01** blokowanie architektury: `rule "ARCH-01" salience 100 when e:EventFrame(domain=="ARCH", predicate in {"circular_dependency","missing_interface"}) then session.halt(); violations.add(new Violation("ARCH-01",e.getId(),Severity.CRITICAL)); end` — `salience 100` = najwyższy priorytet, `session.halt()` = stop; (3) **INTENT-CONFLICT** spójność intencji: `rule "INTENT-CONFLICT" when e1:EventFrame(speechAct=="POLECENIE",$action:predicate), e2:EventFrame(speechAct=="ZAKAZ",predicate==$action,docId==e1.docId) then violations.add(new Violation("INTENT-01","HIGH")); end` — `$action` pattern binding; `agenda-group: "BLOCKING"` dla ARCH-01 + `agenda-group: "CONSISTENCY"` dla INTENT-CONFLICT = kontrolowany porządek; `DroolsRuleEngine.run_group("BLOCKING")` przed `run_group("CONSISTENCY")` — architektura sprawdzana przed intencjami?
- Zaprojektuj reguły Drools wykrywające sprzeczne instrukcje w dokumentacji technicznej — zestaw `.drl` dla sprzeczności: (1) **CONTRADICT-01** czynność vs zakaz: `rule "CONTRADICT-01" when d1:Directive(type=="MUST",$action:action), d2:Directive(type=="MUST_NOT",action==$action,docId==d1.docId,section!=d1.section) then violations.add(new Contradiction("CONTRADICT-01",d1,d2)); end`; (2) **CONTRADICT-02** sprzeczność czasowa: `rule "CONTRADICT-02" when e1:EventFrame(predicate=="deadline",$date:timeDimension), e2:EventFrame(predicate=="deadline",timeDimension>$date,docId==e1.docId) then violations.add(new Contradiction("CONTRADICT-02","Dwa deadline w dokumencie")); end`; (3) **ARCH-02** duplikat interfejsu: `rule "ARCH-02" salience 200 when i1:Interface(name==$n), i2:Interface(name==$n,version!=i1.version) then session.halt(); end` — `salience 200` > ARCH-01(100) — blokuje przed sprawdzaniem intencji; test: `drools_session.assert_fact(must_direct); drools_session.assert_fact(must_not_direct); assert any(v.rule=='CONTRADICT-01' for v in drools_session.fire())`?
- Jak zintegrować bibliotekę Drools z systemem audytu dokumentacji — `DroolsRuleEngine` jako `AuditPlugin` w `AuditPipeline.steps[5]`; pipeline: `(1)parse → (2)emit → (3)lint → (4)qarc → (5)drools → (6)report`; `DroolsAuditPlugin.run(session)->List[Violation]`: `facts=[session.execute('MATCH (e:EventFrame) RETURN e').data()]` → `engine.assert_facts(facts)` → `engine.fire_all_rules()` → `violations`; merge: `AuditPipeline.merge_violations(lint_v, drools_v)` deduplication by `(rule_id, event_id)`; `DroolsAuditPlugin.cache_rules(ttl=3600)` — `.drl` cached in memory, reload co 1h; `audit.py --rules-engine drools|lint|all`; `ViolationSummary.by_engine(v)->Dict` = `{'lint':12,'drools':5,'merged_deduplicated':14}`; `DroolsAuditPlugin.on_error(fallback='lint')` — gdy KIE Server niedostępny → `LinterRuleEngine`?
- Pokaż jak Drools wnioskuje o brakujących elementach zdania i wyzwalanie bezpośrednio z Pythona — `StatelessKieSession` dla per-dokument audit: `container.newStatelessKieSession('audit-rules').execute(CommandFactory.newInsertElements(frames))` — bez pamięci między wywołaniami → szybszy; `rule "MISSING-PATIENT" when e:EventFrame(patientDimension==null, speechAct=="POLECENIE", predicate!="istnieć") then session.insert(new GapWarning("PATIENT","Brak dopełnienia",e.getId())); end`; Python batch trigger: `jframes=[JVM.EventFrame(e.id,e.speech_act,e.domain) for e in frames]; session.execute(jframes)` — JPype; `KieServerRestClient.execute_rules(facts)` — REST alternatywa bez JVM; `GapWarning.to_violation()->Violation` konwerter; `MISSING-01` rule set: `agenda-group:"GAPS"` uruchamiany przed `"CONSISTENCY"`; pułapka: `StatelessKieSession` nie może używać `session.insert()` w RHS reguł (brak working memory) — użyj `BatchExecutionCommand`?
- Pokaż jak zintegrować Rulera z Multiservice w nowym emiterze — retry i obsługa błędów: `MultiserviceEmitter(targets, RetryPolicy(max_retries=3, backoff=exponential))`; `EntityRulerMultiserviceCorefBridge.apply_and_emit(doc)`: `entity_ruler.match()` → `resolve()` → `MultiserviceEmitter.emit_coreference()` z `asyncio.gather(*targets, return_exceptions=True)` — wyjątki nie przerywają; `PartialEmitResult(success:List[str], failed:List[str])` per-target status; `DeadLetterQueue(kafka_topic='nlp-dead-letter')` dla trójek po max_retries; `EmitterHealthDashboard.status()->Dict` = `{neo4j:UP, kafka:UP, es:DEGRADED}`; `MultiserviceEmitter.graceful_shutdown()` flush + disconnect; test: `with MultiserviceEmitter([neo4j_target]) as em: em.emit_from_doc(doc); assert neo4j_target.flushed_count > 0`?
- Zaprojektuj regułę .drl dla wielowymiarowego zdarzenia 'zniszczenie mienia' przez AI — `rule "AI-LIABILITY-01" salience 150 when e:EventFrame(domain=="AI_LEGAL", predicate=="zniszczyć", agentDimension.ner_type=="AI_SYSTEM", patientDimension.type=="MIENIE") then violations.add(new LegalViolation("AI-LIABILITY-01","AI jako sprawca zniszczenia mienia",Severity.CRITICAL,e.getAgentDimension().getLemma(),e.getPatientDimension().getLemma())); end`; wielowymiarowość: `LegalViolation` zawiera 8 wymiarów z `EventFrame` (AGENT, ACTION, PATIENT, TIME, LOCATION, INSTRUMENT, ENVIRONMENT, PATIENT_INDIRECT); `AI_SYSTEM` ner_type: `EntityRuler.add_patterns([{label:'AI_SYSTEM', pattern:[{lemma:'AI'},{lemma:'system'}]}])`; `AI-LIABILITY-02` dla niedostarczenia: `predicate=="dostarczyć" AND achieved==false AND agentDimension.ner_type=="AI_SYSTEM"` → `LegalViolation('AILAW-02',"AI nie wywiązało się z dostarczenia")`; `AILAW-01`: `MATCH (e:EventFrame {predicate:'zniszczyć'})-[:HAS_ROLE{role:'AGENT'}]->(a:Token {ner_type:'AI_SYSTEM'}) RETURN e.id`; `LegalViolation.to_report_entry()` → `PANTERAReportSection('AI_LIABILITY')`; `AI-LIABILITY-03`: gdy `timeDimension IS NULL` dla `AI_LEGAL` event → `CRITICAL` (brak dowodu czasu zdarzenia)?
- Jak wdrożyć puste węzły elipsy w kodzie Pythona — `@dataclass class EllipsisFrame: type:Literal['SUBJECT','OBJECT','PREDICATE']; resolved_to:Optional[str]=None; certainty:float=0.9; sentence_idx:int=0; gender:Optional[str]=None`; `EllipsisDetector.detect(sent:Span)->Optional[EllipsisFrame]`: `has_nsubj=any(t.dep_=='nsubj' for t in sent)`; `is_finite=any(t.pos_=='VERB' and t.morph.get('VerbForm')==['Fin'] for t in sent)`; `if is_finite and not has_nsubj: return EllipsisFrame(type='SUBJECT', sentence_idx=sent.start)`; `EllipsisResolver.fill(frames, memory)`: `for ell in frames: candidate=memory.lookup('last_nsubj', gender=ell.gender); ell.resolved_to=candidate.lemma; ell.certainty=0.85`; `GraphEmitter.emit_ellipsis(ell, event_id)->Triple`: `Triple(event_id,'HAS_IMPLICIT_AGENT',f'ell:{ell.resolved_to}',role='ELLIPSIS',certainty=ell.certainty)`; test: `ell=EllipsisDetector.detect(nlp('Zatwierdzył dokumentację.').sents[0]); assert ell.type=='SUBJECT'`?
- EllipsisAwareDroolsPlugin — łączenie ElipsisDetector z DroolsRuleEngine dla pełnego wnioskowania luk: (1) `EllipsisDetector.detect_all(doc)->List[EllipsisFrame]`; (2) `EllipsisResolver.fill(frames, memory)` — uzupełnienie rezolucji; (3) `DroolsRuleEngine.assert_fact(EllipsisAwareFact(event_id, resolved_agent, certainty))` per elipsa; (4) `fire_all_rules()` — Drools widzi jawne `EventFrame` + elipsy jednocześnie; `MISSING-PATIENT + ellipsis`: gdy elipsa podmiotu AND brakuje patienta → `Violation('MISSING-MULTI','HIGH')`; `AuditPipeline.steps[4.5]` między `"GAPS"` a `"CONSISTENCY"` agenda-group; `PluginReport(ellipsis_detected=12, resolved=10, gaps_closed=5, remaining_violations=7)`; test: `plugin=EllipsisAwareDroolsPlugin(); report=plugin.run(doc,session); assert report.gaps_closed >= report.ellipsis_detected * 0.80`?
- Stwórz regułę Drools dla systemu audytu projektów IT — zestaw reguł `IT-PROJECT-AUDIT`: (1) `rule "IT-PROJECT-DEADLINE-01" when sprint:Sprint(status=="COMPLETED", testCoverage<0.80, hasCriticalFeatures==true) then violations.add(new AuditViolation("IT-PROJECT-DEADLINE-01","Sprint krytyczny bez 80% coverage",Severity.HIGH,sprint.getId())); end`; (2) `rule "IT-PROJECT-RISK-01" salience 200 when risk:RiskItem(probability>0.70, impact=="CRITICAL", mitigation==null) then violations.add(new AuditViolation("IT-PROJECT-RISK-01","Ryzyko bez mitygacji",Severity.CRITICAL,risk.getId())); session.halt(); end`; (3) `rule "IT-PROJECT-DEPENDENCY-01" when m1:Module(status=="IN_PROGRESS",$id1:id), m2:Module(depends_on==$id1, status=="RELEASED") then violations.add(new CircularDependencyViolation(m1.getId(),m2.getId())); end`; `ITProjectAuditPlugin` jako `AuditPlugin` w `AuditPipeline`; `ProjectFrame(sprint_id, coverage, risk_score, dependencies)`; Cypher: `MATCH (s:Sprint)-[:HAS_MODULE]->(m:Module {coverage:<0.80}) WHERE s.hasCritical=true RETURN s.id`?
- Jak połączyć KIE Server z Pythonem do obsługi reguł Drools — głębsza integracja: DMN vs DRL: `POST /kie-server/.../dmn` dla tabel decyzyjnych vs `POST /kie-server/.../kie-session/commands` dla reguł DRL; `ContainerManager.deploy(artifact_id, group_id, version)`: `PUT /kie-server/services/rest/server/containers/{id}` = hot-deploy `.drl` bez restartu pipeline; `KieServerRestClient.list_containers()->List[Container]` — `GET /kie-server/services/rest/server/containers`; `DroolsRuleEngine.hot_reload(container_id)` = `ContainerManager.deploy(container_id, new_version)`; konfiguracja: `KIE_SERVER_URL + KIE_SERVER_USER/PASSWORD` przez env (nie hardcode); `docker-compose.yml` + healthcheck: `curl GET /kie-server/... == 200 → healthy`; `pytest --kie-integration` marker; `KieServerRestClient.batch_execute(frames, container_id)->List[Violation]` = 1 HTTP call per dokument?
- Jakie są techniki kompresji ontologii, by uniknąć jej eksplozji — praktyczne uzupełnienie `OntologyCompressor`: (1) **property inheritance pruning**: `MATCH (a)-[:IS_A]->(b)-[:IS_A]->(c) WHERE (a)-[:HAS_PROP]->(p) AND (b)-[:HAS_PROP]->(p) DELETE redundant b→p` — własności dziedziczone nie duplikowane; (2) **edit-distance merge guard**: `Levenshtein.distance('wykonawca','wykonawcy')<=2` → sprawdź czy synonimy przed scaleniem; (3) **resurrect**: `OntologyCompressor.resurrect(concept_id)` gdy termin powraca po `archive_unused(90)`; (4) **lazy loading**: `ConceptNode.load_relations(depth=1)` — nie ładuj całej podgrafii IS_A; `CompressionBenchmark.run(session)` → `{before:4200,after:1800,time:45s}`; `STRUCT-06`: `MATCH (c:Concept)-[:HAS_PROP]->(p) WHERE EXISTS{MATCH (parent)-[:IS_A*]->(c) AND (parent)-[:HAS_PROP]->(p)} RETURN c.lemma` = redundantne dziedziczone własności?
- Zaprojektuj regułę .drl dla zniszczenia mienia przez AI — LegalViolationReport: `LegalViolationReport.generate(violations:List[LegalViolation])->str` — sekcje `## AI jako sprawca`, `## Zdarzenia AILAW-01/02/03/04`, `## Brakujące dowody czasu`; `LegalViolationSummary(total, critical, ai_agent_count, mienie_count)` = statystyki; `AI-LIABILITY-04`: `timeDimension IS NULL AND domain=='AI_LEGAL'` → `Violation('AILAW-04','Brak dowodu czasu','CRITICAL')`; `PANTERALegalSection.append(report)` dodaje do raportu PANTERA; `LegalViolationReport.to_court_format()` — chronologiczna lista zdarzeń z 8 wymiarami per zdarzenie; `ITProjectAuditPlugin.run()` → `DroolsRuleEngine` → `LegalViolationReport` → `PANTERASection` = pełny flow?
- Jak wdrożyć automatyczną kompresję QARC + CompressionScheduler — `CompressionScheduler.auto_trigger(threshold:Dict)`: `if session.count_nodes()>threshold['nodes'] or session.count_rels()>threshold['relations']: CompressionPipeline.run(session)`; domyślne progi: `{nodes:5000, relations:15000}`; `CompressionScheduler.register_hook(event:'post_import', pipeline)` — auto-trigger po `NKJPGraphImporter.import_corpus()`; `CompressionScheduler.status()->SchedulerReport`: `{last_run, next_run, nodes_before, nodes_after, compression_ratio}`; `audit.py --auto-compress` aktywuje `auto_trigger()` podczas audytu; `CompressionScheduler.dry_run()` sprawdza warunek bez uruchamiania; `CompressionReport.broadcast(targets=['slack','github'])` notyfikacja po kompresji?
- Stwórz regułę Drools dla audytu IT — rozszerzenie Sprint Velocity + Definition of Done: `rule "IT-PROJECT-VELOCITY-01" when sprint1:Sprint(velocity>0,$v:velocity), sprint2:Sprint(velocity<$v*0.5, follows==sprint1.getId()) then violations.add(new AuditViolation("IT-PROJECT-VELOCITY-01","Velocity spadło >50%",Severity.MEDIUM,sprint2.getId())); end`; `rule "IT-PROJECT-DOD-01" when story:UserStory(status=="DONE", acceptanceCriteria.isEmpty()==true) then violations.add(new AuditViolation("IT-PROJECT-DOD-01","Story bez kryteriów akceptacji",Severity.LOW,story.getId())); end`; `ProjectAuditReport(sprint_violations, risk_violations, dependency_violations, velocity_violations)` = pełny raport; `ITProjectAuditPlugin.load_rules('it_project_audit.drl')` ładuje rozszerzony zestaw; Cypher: `MATCH (s:Sprint)-[:FOLLOWS]->(prev:Sprint) WHERE s.velocity < prev.velocity*0.5 RETURN s.id` = sprinty z dramatycznym spadkiem?
- Jak Stanza mapuje cechy gramatyczne na role semantyczne w grafie — `StanzaRoleMapper.from_feats(token:StanzaToken)->Optional[str]`: `token.feats['Case']=='Nom' AND token.deprel in {'nsubj','nsubj:pass'}` → `'AGENT'`; `token.feats['Case']=='Acc' AND token.deprel=='obj'` → `'PATIENT'`; `token.feats['Case']=='Ins' AND token.deprel in {'obl','obl:agent'}` → `'INSTRUMENT'`; `StanzaDepLabelBridge`: mapuje `deprel` na dimension gdy `feats` niekompletne — fallback: `nsubj→AGENT, obj→PATIENT, iobj→PATIENT_INDIRECT, obl→LOCATION`; `StanzaRoleMapper.to_event_dimension(token)->EventDimension`: zwraca wypełniony wymiar gotowy dla `EventDimensionMatrix.add()`; integracja: `NLPPipeline.steps=['morfeusz','stanza','stanza_role_map','graph_emit']` → `StanzaRoleMapper` jako most między Stanzą a `SemanticMapper`; pułapka: Stanza `pl` ma niższy F1 dla `obl` niż `nsubj` — `StanzaRoleMapper.confidence_threshold=0.7`?
- Jak połączyć KIE Server z Pythonem do obsługi reguł Drools — `KieServerRestClient` — rozszerzenie: `KieServerHealthCheck.ping(url)->bool`: `GET /services/rest/server` → `200 OK` przed każdym `batch_execute()`; `KieServerRestClient.async_execute(facts, callback)->Future`: `asyncio.create_task(post_to_kie(facts))` → `callback(violations)` gdy gotowe; `ContainerManager.hot_reload()` przy `ContainerStatus.FAILED`; retry policy: `ExponentialBackoff(base=2, max_wait=30, max_retries=5)` + `DeadLetterQueue.push(facts)` po wyczerpaniu; `KieServerRestClient.batch_execute(frames:List[EventFrame])->List[Violation]`: chunk po 50 ramek — unika timeout 30s na KIE Server; `DroolsAuditPlugin.fallback_mode`: gdy KIE Server nieosiągalny → `StatelessKieSession.evaluate(facts)` lokalnie z podstawowymi regułami; monitoring: `KieServerMetrics.latency_p99` w `AuditReport.performance`?
- Jak wdrożyć `OntologyCompressor.preview_compression()` z trybem explain — `OntologyCompressor.preview_compression(session)->CompressionPreview`: `{would_merge:List[Tuple], would_prune:List[Triple], would_archive:List[str]}` — symulacja bez modyfikacji; `--compression-explain` flag: drukuje per-technikę uzasadnienie w formacie `PRUNE: (wykonawca)-[:HAS_PROP]->(name) ← dziedziczone z Podmiot`; `CompressionPreview.risk_score()->float`: ile konceptów z `resurrect_count>0` trafia do archiwum = ryzyko utraty; `OntologyCompressor.explain_merge(c1, c2)->MergeReason`: `{levenshtein:0.12, shared_synonyms:3, is_a_overlap:True}` — pełne uzasadnienie scalenia; `CompressionReporter.generate(preview)->str` = sekcja `## Proponowane zmiany ontologii` w raporcie PANTERA; CI tryb: `--compression-preview-only` = fail gdy `preview.risk_score() > 0.2`?
- Jak stworzyć plik konfiguracyjny GitHub Actions dla testów lingwistycznych — `linguistic-tests.yml`: `on: [push, pull_request]` + `paths: ['dokumentacja/**','nlp/**']`; `jobs.linguistic-tests.steps`: (1) `actions/checkout` + `actions/setup-python@v5` Python 3.11; (2) `actions/cache` key `spacy-pl-${{ hashFiles('requirements-nlp.txt') }}` — cache modeli spaCy 700 MB; (3) `pip install -r requirements-nlp.txt`; (4) `python -m pytest -m norm -q --tb=short`; (5) `python -m audit --colloc-check --max-colloc-violations=5`; (6) `python -m audit --morph-validate`; (7) `actions/upload-artifact` dla `LinguisticCIReport.json`; `timeout-minutes: 15`; `if: github.event.pull_request.draft == false`; integracja: `needs: [linguistic-tests]` w job `merge-gate` → blokuje merge przy błędach lingwistycznych?
- Jakie reguły Drools wykrywają sprzeczności w intencjach dokumentacji technicznej — `CONTRADICT-01` (salience 200): `$e1:EventFrame(predicate=='zakazać'), $e2:EventFrame(predicate=='nakazać', subject==$e1.subject, object==$e1.object)` → `Violation('CONTRADICT-01','HIGH')`; `CONTRADICT-02`: ten sam predicate ale sprzeczne modalne operatory: `$e1(modal=='MUST'), $e2(modal=='MUST_NOT', predicate==$e1.predicate)` → `Violation('CONTRADICT-02','CRITICAL')`; `INTENT-CONFLICT`: `$i1:Intent(type=='REQUIREMENT'), $i2:Intent(type=='PROHIBITION', scope overlaps $i1.scope)` → `Violation('INTENT-CONFLICT','HIGH')`; `DroolsAuditPlugin` zbiera wszystkie 3 typy w `ContradictionReport(total, by_type, critical_pairs)`; Cypher weryfikacja ex post: `MATCH (e1:EventFrame)-[:CONTRADICTS]->(e2:EventFrame) RETURN e1.predicate, e2.predicate, e1.subject` — relacja `CONTRADICTS` dodana przez Drools?
- Jak zintegrować parser zależnościowy Stanza z grafem wiedzy Neo4j — `StanzaDepParser.parse(text)->List[StanzaToken]` → `StanzaRoleMapper.from_feats(token)->Optional[str]` → `EventDimensionMatrix.add(dim, value)`; `StanzaNeo4jBridge.emit(sent, session)`: per token: `CREATE (t:Token {lemma, upos, deprel, feats})`, `(head_token)-[:DEP{label:token.deprel}]->(t)`; `StanzaNeo4jBridge.map_deps_to_roles(sent)->Dict[str,str]`: `nsubj→AGENT, obj→PATIENT, obl→LOCATION/INSTRUMENT` przez `StanzaDepLabelBridge`; `StanzaNeo4jBridge.batch_emit(doc_id, session)` = all sentences → Neo4j transakcja; integracja z `GraphEmitter.emit_from_conllu(path)` — Stanza → CoNLL-U → `GraphEmitter`; `StanzaCacheLayer.cache(text_hash)->Optional[List[StanzaToken]]` — unika re-parsowania identycznych zdań; test: `bridge.emit(sent, mock_session); assert mock_session.run.call_count >= len(sent.words)`?
- Jak stworzyć reguły Drools mapujące tagi fleksyjne na role semantyczne — `TableFlexRuleSet` w Drools: `rule "NKJP-TAG-AGENT" when token:Token(nkjpTag matches 'subst:.*:nom:.*') then token.setRole('AGENT'); end`; `rule "NKJP-TAG-PATIENT-NEG" when token:Token(nkjpTag matches 'subst:.*:gen:.*'), neg:Token(dep=='neg', head==token.head) then token.setRole('PATIENT'); /* negacja — biernik→dopełniacz */ end`; `MorphRoleConflictDetector` jako agenda-group — uruchamiany po `NKJP-TAG-*` regułach gdy `token.dep_role != token.morph_role`; `ROLE-09`: `MATCH (t:Token) WHERE t.dep_role <> t.morph_role AND t.morph_role IS NOT NULL RETURN t` = tokeny wymagające rozstrzygnięcia konfliktu; korzyść: recall AGENT +12% w zdaniach OSV (`'Dokumentację dostarczył wykonawca'`) vs dep-parse z `nsubj` błędnym?
- Jakie protokoły działania uruchomić po wykryciu sprzeczności w intencjach — `ConflictResolutionProtocol.run(violation:Violation)`: switch na `violation.rule_id`: `CONTRADICT-01` → (1) `IssueTracker.create(title='Sprzeczność w sekcji X', severity='HIGH')`; (2) `DocumentEditor.flag_paragraph(violation.location)` = komentarz w pliku Markdown; (3) `PANTERASection.append(conflict_summary)`; `CONTRADICT-02` (CRITICAL) → dodatkowo (4) `BuildBlocker.set()` = blokada merge do rozwiązania; `INTENT-CONFLICT` → (5) `NotificationBroker.broadcast(['slack','github'], message=f'Konflikt intencji: {violation}')`; `ConflictResolutionProtocol.auto_resolve(violation)->bool`: próba auto-naprawy gdy `confidence > 0.9` — usuwa słabszą regułę (niższy `salience`); `ResolutionLog(timestamp, rule, method, resolved_by)` = audit trail; `DroolsAuditPlugin.on_violation(CONTRADICT-*)` rejestruje wszystkie próby rozwiązania?
- Jak wdrożyć pętlę sprawdzania statusu w KIE Server — `KieServerPollingLoop.start(interval_s=30)`: `asyncio.create_task(_poll())` gdzie `_poll()`: (1) `KieServerHealthCheck.ping(url)` — jeśli False → `retries++`; (2) gdy `retries >= 3` → `DroolsAuditPlugin.fallback_mode = True` + `NotificationBroker.broadcast(['slack'],'KIE Server niedostępny')`; (3) gdy znów UP → `ContainerManager.hot_reload()` + `fallback_mode = False`; `KieServerPollingLoop.status()->PollingStatus(last_ping_ms, consecutive_failures, fallback_active)`; `KieServerMetrics.record(latency_ms, status)` → `latency_p99` tracking; `audit.py --kie-monitor` uruchamia pętlę jako sidecar; pułapka: `interval_s=5` na produkcji → flood logów KIE Server; rekomendacja: `interval_s=30` normalnie, `interval_s=5` w trybie recovery?
- Jak QPP optymalizuje wyszukiwanie w sytuacyjnym modelu zdarzeń — QPP variable-hop `{1,n}` dla `EventFrame` modelu sytuacyjnego: `MATCH (doc)-[:CONTAINS*1..4]->(e:EventFrame)-[:HAS_ROLE*1..2]->(concept) WHERE e.domain=$domain RETURN e, concept, length(path) as depth`; `REDUCE(score=0, e IN nodes(path) | score + e.relevance)` = `situational_score` per ścieżka; `SituationalModelQuery.build(situation:str)->str`: mapuje `situation` na `domain + key_predicates` → gotowy QPP; optymalizacja: `WITH e ORDER BY e.relevance DESC LIMIT 10` przed rozwinięciem ścieżki — prune; `QARCQueryOptimizer` skraca `CONTAINS*1..4` po kompresji; `SituationalModelQuery.explain(session)->ProfileResult` = `db_hits` przed/po QPP; zysk: 60–70% redukcja `db_hits` przy `domain='IT_SECURITY'` vs full-scan; `SituationalIndexBuilder.build_for_domain(domain, session)` = indeks per domena?
- Jakie reguły Drools wykrywają kaskadowe braki w dokumentacji IT — `CascadeDefectDetector` reguły: `rule 'CASC-01' when $e:EventFrame(agent IS NULL), $child:EventFrame(parentSection==$e.section, agent IS NULL) then violations.add(new CascadeViolation('CASC-01','Kaskadowy brak AGENT w sekcji',Severity.HIGH,$e.section)); end`; `rule 'CASC-02' when $sect:Section(completeness_score < 0.5), $subsect:Section(parent==$sect, completeness_score < 0.5) then violations.add(new CascadeViolation('CASC-02','Kaskadowy brak kompletności',Severity.HIGH,$sect.getId())); end`; `CascadeViolation(rule_id, message, severity, affected_section, cascade_depth:int)`; `CascadeDefectReport(total_cascades, max_depth, root_cause_section)` = `DroolsAuditPlugin.cascade_report()`; Cypher identyfikacja korzenia kaskady: `MATCH (s:Section)-[:CONTAINS*1..]->(e:EventFrame) WHERE ALL(ef IN collect(e) WHERE ef.agent IS NULL) RETURN s.id, count(e)` = sekcje z wszystkimi brakującymi AGENT?
- Jakie reguły Drools wykrywają kaskadowe braki w dokumentacji IT — kąt naprawy: `CascadeRepairSuggester.suggest(violation:CascadeViolation)->RepairAction`: gdy `CASC-01` → `MATCH (parent:Section)-[:CONTAINS]->(s{id:$section})-[:CONTAINS]->(e:EventFrame) WHERE (parent)-[:CONTAINS]->(:EventFrame{agent IS NOT NULL}) RETURN parent.agent` = propaguj AGENT z sekcji nadrzędnej; `CascadeAutoFixer.fix(violation, session)`: `MATCH (e:EventFrame{section:$sec}) SET e.agent = $propagated_agent, e.auto_fixed=true`; `CascadeFixReport(fixed, unfixed, confidence_avg)` = raport naprawy; `DroolsAuditPlugin.on_cascade_violation(v)`: automatycznie wywołuje `CascadeRepairSuggester`; `CASC-03`: `auto_fixed=true` AND `confidence < 0.7` → `Violation('CASC-03','LOW','Niepewna auto-naprawa')` = wymaga ręcznej walidacji?
- Jak WordNet/Słowosieć pomaga rozwiązywać polisemię w grafie wiedzy — `PolysemyResolver` Lesk + `ContextualSynsetPin` flow: `SlowosiecProxy.get_synsets(lemma)->List[Synset]`; `LeskScorer.score(synset, context_tokens)->int` = overlap słów definicji z tokenami kontekstu; `PolysemyResolver.resolve(lemma, context)->Synset` = synset z max Lesk score; problem `'system'`: `syn_OS=42 overlap, syn_ustrój=3 overlap, syn_metabolizm=0 overlap` → pin `syn_OS`; `PolysemyGuard.warn(lemma, synsety_count)`: gdy `> 2 synsety` i brak `ContextualSynsetPin` → `POLY-01`; `AuditITSynsetDictionary` skraca czas: gdy lemma w local dict → pomiń Lesk; `SynsetConflictDetector.scan(session)`: `MATCH (c:Concept) WHERE c.synset_id IS NULL RETURN c.lemma` = koncepty bez synset = ryzyko polisemii; monitoring: `SynsetCoverage.run(session)->float` cel ≥ 0.85?
- Jak WordNet pomaga rozwiązywać problem polisemii w grafie wiedzy — kąt monitoringu synset drift: `SynsetDriftDetector.detect(session, window=30days)`: gdy `SynsetCoverage.run()` spada o >5% w ciągu 30 dni → `POLY-03`; przyczyna: nowe terminy dodane przez `CollocateStreamUpdater` bez synset_id; `SynsetDomainExpander.expand_it_domain(session)`: `MATCH (c:Concept) WHERE c.domain='IT' AND c.synset_id IS NULL CALL SlowosiecProxy.batch_lookup([c.lemma]) YIELD result SET c.synset_id = result.synset_id` = auto-uzupełnienie; `SynsetDomainExpander.run_scheduled(cron='0 4 * * 1')` = cotygodniowe uzupełnianie; `POLY-03`: `SynsetCoverage < 0.80` → `Violation('POLY-03','MEDIUM')` trigger `SynsetDomainExpander`; `PolysemyDashboard.generate(session)->str` = Markdown raport: pokrycie per domena, top 10 nierozwiązanych, drift 30-dniowy?
- Jak wstrzyknąć wirtualny węzeł Ellipsis dla brakujących podmiotów — `EllipsisInjector.inject(frame, session)`: gdy `frame.agent IS NULL` → `CREATE (en:EllipsisNode{id:'EL_'+frame.id, verb:frame.predicate, reconstruction_status:'UNFILLED'})`, `CREATE (frame)-[:HAS_IMPLICIT_AGENT]->(en)`; `EllipsisResolver.fill(en, context:DocumentContextMemory)`: `candidate=context.last_subject(window=3)` → `en.filled_by=candidate.lemma`, `en.reconstruction_status='FILLED'`, `en.confidence=0.85`; `CREATE (en)-[:RECONSTRUCTED_AS{method:'context_window',confidence:0.85}]->(candidate_token)`; `EllipsisInjector.inject_all(doc_id, session)->int` = liczba wstrzykniętych węzłów; `EllipsisChain.detect(session, doc_id)->List[EllipsisChain]`: sekwencja `EventFrame` z `HAS_IMPLICIT_AGENT` → łańcuch elips; `ELIPS-02`: `EllipsisChain.length > 3` = zbyt długi łańcuch bez wyraźnego antecedenta → `Violation('ELIPS-02','MEDIUM')`?
- Jak wstrzyknąć do grafu wirtualny węzeł Ellipsis dla brakujących podmiotów — kąt walidacji jakości: `EllipsisValidationPipeline.run(doc_id, session)->ValidationReport`: (1) `inject_all()` → (2) `EllipsisResolver.fill_all()` → (3) `QualityScorer.score()`: per węzeł `confidence`, `method`, `antecedent_distance`; `EllipsisQualityReport(filled_pct, avg_confidence, low_confidence_nodes:List[str])` = raport; `EllipsisQualityCI.assert_quality(doc_id, min_filled=0.8, min_confidence=0.7, session)` = CI gate; `LOW_CONF-01`: `en.confidence < 0.6` → `Violation('LOW_CONF-01','LOW')` — wymaga manualnej weryfikacji; `EllipsisValidationPipeline.export_review_csv(report)` = CSV do ręcznej walidacji niskich confidence; `EllipsisAuditPlugin.steps.append(EllipsisValidationPipeline)` jako krok finalizujący po `EllipsisAwareDroolsPlugin`?
- Jakie reguły Drools wykrywają sprzeczność między nakazem a zakazem w tekście — kąt OBLIGATION vs PROHIBITION: `rule "OBLIG_PROHIBIT_CONFLICT"` w pliku `intent-conflict.drl`: `when $o: IntentFact(type=="OBLIGATION", predicate==$pred) $p: IntentFact(type=="PROHIBITION", predicate==$pred) then insert(new Violation("CONFLICT-02", "HIGH", "Sprzeczność: nakaz+zakaz na predykacie: "+$pred)); end`; `CONFLICT-02`: para `OBLIGATION+PROHIBITION` na tym samym predykacie = blokada PR; `ObligProhibConflictChecker.scan(doc_id, kie_session)->List[ConflictPair]`: zbiera wszystkie `IntentFact` z `IntentNode` przez `GraphDroolsBridge` → fire → lista `ConflictPair(oblig_sent_id, prohibit_sent_id, predicate, severity)`; `ConflictResolutionProtocol.resolve(pair)->ResolutionAction`: `DIRECTIVE_WINS` gdy `oblig.confidence > prohibit.confidence + 0.2`, else `FLAG_FOR_REVIEW`; `obligation_prohibit_gap.json` = raport JSON per dokument exportowany przez `ConflictAuditPlugin.export(violations)->str`?
- Jak zaimplementować regułę Drools dla warunków 'jeśli...to' w audycie IT — kąt DSL template: `IfThenAuditRuleTemplate` — szablon DSL w pliku `audit-ifthen.drl.template`: `rule "IF_THEN_AUDIT_${RULE_ID}" salience ${SALIENCE} when ${WHEN_CONDITION} then insert(new Violation("${RULE_ID}", "${SEVERITY}", "${MESSAGE}")); end`; przykład konkretyzacji: `RULE_ID='PROC-CHECK-01'`, `WHEN_CONDITION='ProcedureFact(status=="OPEN", priority=="HIGH", assigned_to==null)'`, `MESSAGE='Otwarta procedura HIGH bez przypisanego aktora'`; `RuleTemplateEngine.generate(template, params:Dict)->str` = gotowy plik `.drl`; `AuditRuleCatalog.register(rule_id, template, params)` = rejestr reguł audytowych; `KieServerRestClient.deploy_rule(drl_content)` → aktualizuje KContainer bez restartu; `IfThenRuleValidator.validate(drl_path)->ValidationResult`: sprawdza składnię DRL + pokrycie faktów z grafu Neo4j przed wdrożeniem?
- Jak stworzyć hybrydowy system łączący graf znaczeń z Drools — kąt silnika wnioskowania: `HybridReasoningEngine.activate_from_graph(pattern:str, session)->ActivationResult`: 1) `execute_cypher(pattern, session)` → `List[GraphFact]`; 2) `GraphFactAdapter.to_drools(facts)->List[DrlFact]`; 3) `KieSessionPool.get_or_create(doc_id)` → `kie_session.insert_all(drl_facts)` → `kie_session.fireAllRules()`; 4) `ViolationCollector.collect(kie_session)->List[Violation]`; `GraphTriggerActivation(pattern_id, facts_count, rules_fired, violations_count, duration_ms)` = log; `HybridReasoningEngine.run_full_pipeline(doc_id, session)`: sekwencja: `IntentConflictDetector → CascadeDefectDetector → EllipsisValidationPipeline → ObligProhibConflictChecker`; `HybridEngineReport.generate(doc_id)->str` = Markdown z podsumowaniem wszystkich aktywacji i naruszeń per cykl?
- Pokaż przykład reguły Drools wyodrębniającej intencję warunku z tekstu — kąt warunkowej intencji: `rule "CONDITIONAL_INTENT"` w `conditional-intent.drl`: `when $s: SentenceFact(conditionWord in {'jeżeli','jeśli','o ile','pod warunkiem że'}, thenClausePresent==true) then ConditionalIntentFact fact = new ConditionalIntentFact(); fact.setCondition($s.getConditionClause()); fact.setConsequent($s.getThenClause()); fact.setType("IF_THEN_OBLIGATION"); insert(fact); end`; `ConditionalIntentFact(condition:str, consequent:str, type:str, confidence:float)` = fakt wstawiany do KIE session; `ConditionalIntentExtractor.extract(text)->List[ConditionalIntentFact]`: `SentenceSegmenter.split(text)` → `ConditionDetector.detect(sent)` → `fireAllRules()`; `rule "UNMET_CONDITION"`: gdy `ConditionalIntentFact(type=='IF_THEN_OBLIGATION')` AND brak `ProcedureFact(id==$consequent.proc_id, status=='FULFILLED')` → `insert(new Violation('COND-01','MEDIUM','Niespełniony warunek: '+fact.condition))`; `COND-01`: nowy rule ID — niespełniony warunek w zdaniu warunkowym; `ConditionalIntentReport(extracted, fulfilled, unmet, cond_01_count)` = raport per dokument?
- Pokaż przykład reguły Drools dla warunku w dokumentacji technicznej — kąt konkretnego wzorca dokumentacyjnego: `rule "DOC_CONDITION_PROC"` w `doc-conditions.drl`: `when $c: ConditionalIntentFact(type=='IF_THEN_OBLIGATION', condition contains 'procedura', consequent contains 'wykonać') $p: ProcedureFact(status!='FULFILLED', name==$c.getConsequentProc()) then insert(new Violation('DOC-COND-01','HIGH', 'Procedura warunkowa niewykonana: '+$p.getName()+' warunek: '+$c.getCondition())); end`; `DOC-COND-01`: nowy rule ID — procedura wskazana w zdaniu warunkowym bez statusu FULFILLED; `rule "PERMISSION_CONDITION"`: gdy `ConditionalIntentFact(type=='IF_THEN_PERMISSION')` AND `ActorFact(role=='UNAUTHORIZED')` → `Violation('DOC-COND-02','MEDIUM','Brak uprawnień do warunkowej akcji')`; `DOC-COND-02`: nieuprawniony aktor przy warunkowej zgodzie; `DocConditionRuleLoader.load(drl_dir='rules/doc-conditions/')`: ładuje wszystkie `*.drl` z katalogu do `KieContainer`; `DocConditionReport(doc_id, doc_cond_01_count, doc_cond_02_count, fulfilled_conditions, total_conditions)` = raport per dokument w CI?
- Jakie reguły Drools wykrywają sprzeczne intencje w dokumentacji technicznej — kąt tech-doc specifics: `rule "DIRECTIVE_PROHIBIT_SAME_SECTION"` w `tech-doc-intent.drl`: `when $d: IntentFact(type=='DIRECTIVE', section==$sec) $p: IntentFact(type=='PROHIBITION', section==$sec, predicate==$d.getPredicate()) then insert(new Violation('INTENT-CONFLICT-01','HIGH', 'Sprzeczność DIRECTIVE+PROHIBITION w sekcji: '+$sec+', predykat: '+$d.getPredicate())); end`; `INTENT-CONFLICT-01`: nowy rule ID — sprzeczność w tej samej sekcji; `rule "COMMISSIVE_VS_PROHIBITION"`: gdy `COMMISSIVE` (zobowiązanie) AND `PROHIBITION` na tym samym AGENT+PREDICATE → `Violation('INTENT-CONFLICT-02','HIGH')`; `INTENT-CONFLICT-02`: zobowiązanie vs zakaz dla tego samego aktora; `TechDocIntentConflictPlugin.scan(doc_id, kie_session)`: ładuje `tech-doc-intent.drl` + wstawia `IntentFact` z `IntentNode` z Neo4j → `fireAllRules()` → zbiera konflikty; `IntentConflictSummary(conflict_01_count, conflict_02_count, sections_affected:List[str])` = raport per sekcja dokumentu?
- Pokaż przykład reguły Drools dla warunków 'jeśli...to' — kąt konkretny IT/security: `rule "ACCESS_CONDITION"` w `access-conditions.drl`: `when $c: ConditionalIntentFact(type=='IF_THEN_PERMISSION', condition contains 'autoryzacja') $a: ActorFact(role=='USER', hasPermission==false, permissionRequired==$c.getRequiredPermission()) then insert(new Violation('ACCESS-COND-01','HIGH', 'Dostęp warunkowy bez autoryzacji: aktor '+$a.getName()+ ' brak uprawnienia: '+$c.getRequiredPermission())); end`; `ACCESS-COND-01`: nowy rule ID — warunek dostępu bez spełnionej autoryzacji; `rule "SECURITY_CONDITION"`: gdy `IF_THEN_OBLIGATION` z `condition contains 'szyfrowanie'` AND `SystemFact(encryption==false)` → `Violation('SEC-COND-01','CRITICAL')`; `SEC-COND-01`: nowy rule ID CRITICAL — nakaz szyfrowania niespełniony; `SecurityConditionPlugin.load(drl='access-conditions.drl')` + `SecurityConditionReport(access_cond, sec_cond, critical_count)` = raport bezpieczeństwa?
- Jak zaimplementować mechanizm rekonstrukcji elips w grafie wiedzy — kąt algorytmu rekonstrukcji: `EllipsisReconstructionAlgorithm.reconstruct(node_id, session)`: 1) `MATCH (e:EllipsisNode{id:$nid})<-[:HAS_ROLE{role:'AGENT'}]-(frame:EventFrame) MATCH (prev:EventFrame{doc_id:frame.doc_id})-[:HAS_ROLE{role:'AGENT'}]->(a:Concept) WHERE prev.sent_idx = frame.sent_idx - 1 RETURN a.lemma AS candidate`; 2) `ReconstructionScorer.score(candidate, frame, session)->float`: TF-IDF podobieństwo + `CorefContextWindow` distance; 3) gdy `score >= 0.7`: `MATCH (e:EllipsisNode{id:$nid}) SET e.reconstructed_agent=$candidate, e.confidence=$score, e.method='PREV_SENT'`; `EllipsisReconstructionPipeline.run(doc_id, session)->ReconstructionReport`: `(injected, reconstructed, failed, avg_confidence)`; `EllipsisAwareDroolsPlugin.on_reconstruct(node)`: trigger `fireAllRules()` po rekonstrukcji → sprawdza czy wypełniony AGENT spełnia reguły `DIRECTIVE_PROHIBIT_SAME_SECTION`; `RECON-01`: `confidence < 0.5` → `Violation('RECON-01','LOW','Niska pewność rekonstrukcji')`?
- Jakie są 3 fundamentalne bariery zatrzymujące projekty symbolicznego NLP — kąt inżynieryjny i implementacyjny: (1) **Eksplozja kombinatoryczna reguł**: każda nowa domena wymaga N² nowych reguł Drools; fix: `IfThenAuditRuleTemplate` + `AuditRuleCatalog` = parametryczne szablony zamiast ręcznych reguł; metrika: `RuleCatalog.size()` powinno rosnąć sub-liniowo względem domen; (2) **Kruchość na zmiany fleksyjne**: reguły oparte na formach `'wykonano'` zamiast lematów → `MorfeuszTokenizer.lemmatize()` + `Stanza.morph` obowiązkowe przed pattern-matching; `FlexionRobustnessTest.run(rule_id, forms:List[str])` = test odporności; (3) **Brak pokrycia kontekstu**: reguła odpalona na izolowanym zdaniu bez `CorefContextWindow` → AGENT=NULL; fix: `CorefStateGate.validate()` przed `fireAllRules()`; `BarrierMitigationReport(barrier, fix_component, coverage_before, coverage_after)` = metryki skuteczności każdego fix?
- Jak inżynieryjnie zaimplementować rekonstrukcję elips w locie — kąt architektury pipeline: `EllipsisInFlightReconstructor` = komponent działający podczas `GraphEmitter.emit()`: gdy `EventFrame.agent IS NULL` → natychmiast trigger `EllipsisReconstructionAlgorithm.reconstruct()`; metody rekonstrukcji z priorytetem: (1) `PREV_SENT` (sent_idx-1) — score >= 0.7; (2) `COREF_CHAIN` — `CorefContextWindow.resolve()` — score 0.5–0.7; (3) `SECTION_DEFAULT` — AGENT z nagłówka sekcji — score 0.4–0.5; (4) `ELLIPSIS_NODE` — wirtualny węzeł gdy wszystkie metody score < 0.4; `EllipsisMethodSelector.select(frame, session)->ReconstructionMethod` = strategia; `EllipsisInFlightReport(method_distribution:Dict, avg_score, fallback_to_ellipsis_pct)` = rozkład metod per dokument; cel: `fallback_to_ellipsis_pct < 0.15` = maksymalnie 15% bez rekonstrukcji; `EllipsisQualityCI.assert_method_distribution(report, max_ellipsis=0.15)` = gate CI?
- Jak zintegrować ujednoznacznianie znaczeń (WSD) z silnikiem Drools — `WSDDroolsPlugin`: po `WordNetWSDIntegrator.disambiguate()` → wstawia `SynsetFact(lemma, synset_id, domain, confidence)` do KIE session; `rule "WSD_DOMAIN_MISMATCH"` w `wsd-rules.drl`: `when $e: EventFact(domain=='IT') $c: SynsetFact(lemma==$e.getPredicate(), domain!='IT', confidence > 0.7) then insert(new Violation('WSD-DOMAIN-01','MEDIUM', 'Predykat '+$e.getPredicate()+' zaklasyfikowany do domeny '+$c.getDomain()+ ' zamiast IT')); end`; `WSD-DOMAIN-01`: niezgodność domeny synsetu z domeną dokumentu; `rule "WSD_LOW_CONFIDENCE"`: gdy `SynsetFact(confidence < 0.6)` → `Violation('WSD-01','LOW')` (integracja z flagą z Neo4j); `WSDDroolsReport(domain_mismatches, low_confidence_count, synsets_verified)` = raport; `AuditPipeline.add_step(WSDDroolsPlugin)` po `WordNetWSDIntegrator`?
- Jakie gotowe reguły Drools wykrywają luki w dokumentacji — lista implementacyjna: (1) `DIM-01` `incomplete-dims.drl`: `when EventFact(dimCount < 3) then Violation('DIM-01','MEDIUM')`; (2) `CASC-01/02` `cascade-defects.drl`: brak AGENT kaskadowo; (3) `CONFLICT-02` `intent-conflict.drl`: OBLIGATION+PROHIBITION na tym predykacie; (4) `COND-01` `conditional-intent.drl`: niespełniony warunek IF_THEN; (5) `DOC-COND-01` `doc-conditions.drl`: procedura warunkowa niewykonana; (6) `ACCESS-COND-01` `access-conditions.drl`: dostęp bez autoryzacji; (7) `SEC-COND-01` CRITICAL `access-conditions.drl`: nakaz szyfrowania niespełniony; (8) `INTENT-CONFLICT-01` `tech-doc-intent.drl`: DIRECTIVE+PROHIBITION w tej samej sekcji; (9) `WSD-DOMAIN-01` `wsd-rules.drl`: predykat w złej domenie; (10) `RECON-01` `ellipsis-rules.drl`: niska pewność rekonstrukcji elipsy; `DroolsRuleCatalog.load_all(drl_dir)->KieContainer` = ładuje wszystkie *.drl z katalogu; `DroolsRuleCatalog.summary()->Dict[str,RuleMeta]` = rule_id → severity/file/description; CI: `DroolsRuleCatalog.verify_coverage(doc_ids, session)` = wszystkie reguły odpalone ≥1 raz?
- Pokaż przykład reguły Drools dla warunku logicznego w tekście — kąt operatora logicznego AND/OR: `rule "COMPOUND_CONDITION"` w `compound-conditions.drl`: `when $c1: ConditionalIntentFact(type=='IF_THEN_OBLIGATION', condition contains 'autoryzacja') $c2: ConditionalIntentFact(type=='IF_THEN_OBLIGATION', condition contains 'weryfikacja', connective=='AND', linkedFact==$c1.getId()) $a: ActorFact(hasPermission==false) then insert(new Violation('COMPOUND-COND-01','HIGH', 'Złożony warunek AND niespełniony: brak autoryzacji I weryfikacji dla: '+$a.getName())); end`; `COMPOUND-COND-01`: nowy rule ID — oba człony warunku logicznego AND niespełnione; `rule "OR_CONDITION_PARTIAL"`: gdy co najmniej jeden człon OR spełniony → brak naruszenia; `CompoundConditionParser.parse(sent)->List[ConditionalIntentFact]` = rozbija zdanie na fakty połączone `connective='AND'|'OR'`; `CompoundConditionReport(and_violations, or_satisfied, compound_count)` = raport?
- Pokaż reguły Drools wyodrębniania aktów mowy z tekstu — kąt kompletnego zestawu w `speech-acts.drl`: `rule 'ASSERTIVE_NEGATED'` gdy `Sentence(verbLemma in {'jest','wynosi'}, negation==true)` → `SpeechAct('ASSERTIVE_NEG')`; `rule 'DECLARATIVE'` gdy `Sentence(verbLemma in {'ogłaszam','zatwierdzam','stanowi'}, subject matches '(Zarząd|Kierownik|Organ)')` → `SpeechAct('DECLARATIVE')`; `rule 'EXPRESSIVE'` gdy `Sentence(verbLemma in {'wyrażam','potwierdzam','akceptuję'}, modality=='VOLUNTARY')` → `SpeechAct('EXPRESSIVE')`; `rule 'REQUEST'` gdy `Sentence(verbLemma in {'proszę','zwracam_się','wnioskuję'})` → `SpeechAct('REQUEST')`; `SpeechActClassificationMatrix`: tabela `type × modal_words × verbal_patterns × example`; `SpeechActRulePriority`: DECLARATIVE > DIRECTIVE > COMMISSIVE > CONDITIONAL > ASSERTIVE > REQUEST > EXPRESSIVE (salience kolejność); `SpeechActExtractionReport(types_distribution:Dict, unclassified_sents:List, top_modal_words:List)` = raport per sekcja dokumentu?
- Jakie gotowe reguły audytowe wykrywają luki — kąt priorytetu salience i macierzy CI: `DroolsRuleSalienceMap`: `SEC-COND-01: salience=100 (CRITICAL)`, `ACCESS-COND-01: salience=90`, `CONFLICT-02: salience=85`, `INTENT-CONFLICT-01: salience=80`, `CASC-01/02: salience=70`, `DIM-01: salience=60`, `COND-01/DOC-COND-01: salience=50`, `COMPOUND-COND-01: salience=45`, `WSD-DOMAIN-01: salience=30`, `RECON-01/LOW_CONF-01: salience=10`; `CIEnforcementMatrix`: `CRITICAL → block_commit`, `HIGH → block_merge`, `MEDIUM → warning_required`, `LOW → report_only`; `DroolsRuleCatalog.enforce(violations, ci_matrix)->CIDecision(action, blocking_rules:List)`: BLOCK gdy ≥1 CRITICAL lub ≥3 HIGH; `AuditCIReport(decision, blocking_rules, warning_rules, info_rules)` = output dla GitHub Actions `check-audit` job?
- Pokaż przykład reguły Drools dla warunku logicznego z negacją w tekście — `rule 'NEGATED_CONDITION'` w `negated-conditions.drl`: `when $c: ConditionalIntentFact(type=='IF_THEN_OBLIGATION', condition contains 'nie', negated==true) $p: ProcedureFact(name==$c.getConsequentProc(), status!='SKIPPED') then insert(new Violation('NEG-COND-01','MEDIUM', 'Warunek NEGATYWNY naruszony: '+$c.getCondition()+ ' procedura '+$p.getName()+' nie powinna być wykonana')); end`; `NEG-COND-01`: nowy rule ID — negacja warunku IF NOT THEN; `NegatedConditionParser.parse(sent)->ConditionalIntentFact(negated=True)`: `ConditionDetector` + `Morfeusz.tag()` → gdy `Negation==True` w condition clause → `fact.setNegated(true)`; `rule 'DOUBLE_NEGATION'`: gdy `ConditionalIntentFact(negated==true)` AND `condition contains 'nie…nie'` → `Violation('NEG-COND-02','LOW','Podwójna negacja: logicznie pozytywny warunek')`; `NEG-COND-02`: podwójna negacja jako fałszywy zakaz; `NegatedConditionReport(neg_cond_01, neg_cond_02, double_negation_count)` = raport?
- Jakie są 3 fundamentalne bariery projektów symbolicznego NLP — kąt zarządczy: (1) **bariery techniczne** znane (eksplozja reguł, kruchość fleksyjna, brak kontekstu — B103); nowy kąt: **bariery organizacyjne**: (1) **knowledge capture bottleneck**: eksperci domenowi nie formalizują wiedzy → fix: `IfThenAuditRuleTemplate` DSL dla non-programistów + `AuditRuleCatalog` GUI; (2) **ontology ownership gap**: nikt nie zarządza `OntologyCompressor` → fix: `OntologyOwnershipPolicy(owner_team, review_cycle_days=90)` + alerting; (3) **test coverage neglect**: reguły Drools bez testów → fix: `DroolsRuleCatalog.verify_coverage()` + `DocTestConsistencyChecker`; `ProjectBarrierAssessment.assess(project_id)->BarrierReport(technical:List, organizational:List, mitigation_status:Dict)` = audit projektu NLP; `BarrierMitigationTracker.track(barrier_id, fix_component, status)` = śledzenie postępu usuwania barier; metrika sukcesu: wszystkie 3 bariery z `mitigation_status='ACTIVE'` przed go-live?
- Pokaż przykład reguły Drools wykrywającej brak opisu procedury fallback — `rule 'MISSING_FALLBACK_PROCEDURE'` w `procedure-completeness.drl`: `when $p: ProcedureFact(type=='CRITICAL', hasFallback==false) then insert(new Violation('PROC-FB-01','HIGH', 'Brak procedury fallback dla: '+$p.getName()+' (CRITICAL)')); end`; `PROC-FB-01`: nowy rule ID — brakująca procedura fallback dla krytycznych procesów; `rule 'FALLBACK_INCOMPLETE'` gdy `ProcedureFact(type=='FALLBACK', hasTrigger==false || hasOwner==false)` → `Violation('PROC-FB-02','MEDIUM','Niekompletna procedura fallback: brak triggeru lub właściciela')`; `PROC-FB-02`: niekompletna fallback (brak trigger/owner); `ProcedureCompletenessChecker.check(doc_id, kie_session)`: ładuje `procedure-completeness.drl` + wstawia `ProcedureFact` z `EventFrame{speech_act='PROCEDURE'}` → `fireAllRules()`; `ProcedureCompletenessReport(missing_fallback, incomplete_fallback, critical_without_fallback:List)` = raport; CI: blokada gdy `missing_fallback > 0`?
- Jak wdrożyć mechanizm zamrażania wniosków (Freezing) w regułach Drools — `rule 'FREEZE_ON_CONFLICT'` w `freezing-rules.drl`: `when $v: Violation(ruleId in {'CONFLICT-02','INTENT-CONFLICT-01'}, severity=='HIGH') $e: EventFact(id==$v.getFrameId()) then insert(new FreezeCommand($e.getId(), $v.getRuleId(), 'HIGH_CONFLICT')); retract($e); end`; `FreezeCommand(frame_id, reason, severity)` = fakt wstawiany do KIE session; `FreezeCommandHandler.handle(cmd, session)`: wywołuje `ConflictFreezer.freeze(cmd.frame_id, cmd.reason, session)` w Neo4j; `rule 'BLOCK_FROZEN_INFERENCE'`: gdy `EventFact(frozen==true)` → `retract(fact)` z KIE session → blokuje dalsze wnioskowanie na tej ramce; `FreezingDroolsPlugin.load(drl='freezing-rules.drl')` + `FreezingDroolsPlugin.on_freeze(cmd)` → callback do `ConflictFreezer`; `ThawCommand(frame_id, approved_by:str)` = odmrożenie po ręcznym review; `FreezingReport.pending_thaw(session)->List[str]` = zamrożone ramki czekające na review?
- Jak zintegrować reguły Drools z pipeline'em CI/CD dla audytu dokumentacji — kąt pełnej integracji krok po kroku: (1) `DroolsRuleCatalog.load_all(drl_dir='rules/')` → `KieContainer` w JVM; (2) `KiePollerCI.assert_ready(url, timeout_s=120)` = pre-check KIE Server; (3) `GraphDroolsBridge.run_cycle(doc_id, session, kie_session)` = bridge Neo4j→Drools; (4) `DroolsRuleCatalog.enforce(violations, CIEnforcementMatrix)` = `CIDecision`; (5) gdy `decision.action=='block_commit'` → exit code 1 w CI; GitHub Actions job `audit-drools.yml`: `steps: [checkout, start-neo4j, start-kie-server, wait-kie-ready, run-audit-pipeline, collect-violations, enforce-ci-matrix, upload-audit-report]`; `DroolsCIReporter.generate(violations, doc_id)->str` = Markdown GitHub Actions Summary; `AuditCIReport.to_github_annotations(violations)->List[Annotation]` = inline annotations w PR diff dla HIGH violations; `DroolsRuleCatalog.verify_coverage(doc_ids, session)` = assert wszystkie *.drl odpalone ≥ 1 raz per release?
- Jak zneutralizować stronę bierną w grafie za pomocą Pythona — kąt kodu implementacyjnego: `def neutralize_passive(sent: spacy.tokens.Span, session) -> NormalizedFrame:` `voice = [t.morph.get('Voice') for t in sent]`; `if 'Pass' in voice:`; `  agent = next((t for t in sent if t.dep_=='obl:agent'), None)`; `  patient = next((t for t in sent if t.dep_=='nsubj:pass'), None)`; `  action = next((t for t in sent if t.pos_=='VERB'), None)`; `  return NormalizedFrame(AGENT=agent.lemma_ if agent else None,`; `    ACTION=action.lemma_, PATIENT=patient.lemma_, voice='PASSIVE')`; `else:` (strona czynna) `agent=nsubj, patient=obj`; `VoiceNeutralizer.neutralize_and_emit(sent, doc_id, session)`: wywołuje powyżej → `GraphEmitter.emit_frame(normalized, doc_id, session)`; `PassiveVoiceCI.assert_neutralization_rate(doc_id, session, min=0.95)`: `MATCH (e:EventFrame{voice:'PASSIVE'}) WHERE e.agent IS NOT NULL` / total_passive ≥ 0.95?
- Jak obsłużyć idiomy i frazeologię w regułach Drools — `rule 'IDIOM_TRANSPARENT_SKIP'` w `idiom-rules.drl`: `when $i: IdiomFact(type=='TRANSPARENT') then retract($i); end` = transparentne idiomy usuwane z KIE session bez naruszenia; `rule 'IDIOM_MAPPED_DIRECTIVE'` gdy `IdiomFact(type=='MAPPED', mappedPredicate in {'prioritize','require','prohibit'})` → `insert(new EventFact(predicate=$i.getMappedPredicate(), source='IDIOM', confidence=0.75))`; `IDIOM-01`: `IdiomFact(type=='UNKNOWN', confidence < 0.5)` → `Violation('IDIOM-01','LOW','Nierozpoznany idiom — możliwa utrata semantyki')` = ostrzeżenie o nieobsłużonym wyrażeniu; `IdiomDroolsPlugin.load(drl='idiom-rules.drl')` + `IdiomDroolsPlugin.register_facts(idiom_matches:List[IdiomMatch], kie_session)`: wstawia `IdiomFact` per wykryty idiom; `IdiomDroolsReport(transparent_skipped, mapped_to_events, unknown_flagged:List)` = raport?
- Zdefiniuj regułę Drools wykrywającą brakujące powiązanie z aktorem — `rule 'MISSING_ACTOR_LINK'` w `actor-completeness.drl`: `when $e: EventFact(speechAct in {'DIRECTIVE','COMMISSIVE','PROCEDURE'}, agentLemma==null || agentLemma=='') then insert(new Violation('ACTOR-LINK-01','HIGH', 'Brak powiązania z aktorem dla: '+$e.getPredicate()+ ' ('+$e.getSpeechAct()+')')); end`; `ACTOR-LINK-01`: nowy rule ID — nakaz/zobowiązanie/procedura bez przypisanego aktora; `rule 'ACTOR_LINK_AFTER_COREF'`: gdy `CorefResolved==true` AND `agentLemma==null` → `Violation('ACTOR-LINK-02','MEDIUM', 'Aktor nieustalony nawet po koreferencji')` — wyższy priorytet niż ACTOR-NULL-01; `ACTOR-LINK-02`: brak aktora mimo próby koref-resolution; `ActorCompletenessPlugin.load(drl='actor-completeness.drl')` + `ActorCompletenessPlugin.check(doc_id, kie_session)`: `ActorCompletenessReport(actor_link_01, actor_link_02, complete_frames_pct)` = % ramek z pełnym AGENT per dokument?
- Jak reguła Drools triggeruje injekcję relacji AGENT dla strony biernej — `rule 'PASSIVE_AGENT_INJECT_TRIGGER'` w `passive-rules.drl`: `when $e: EventFact(voice=='PASSIVE', agentLemma==null) then PassiveAgentInjectCommand cmd = new PassiveAgentInjectCommand($e.getId()); insert(cmd); end`; `PassiveAgentInjectCommand(frame_id)` = fakt wstawiany do KIE; `PassiveAgentInjectHandler.handle(cmd, session)`: wywołuje `PassiveAgentInjector.inject_all(frame_id, session)` → po udanej injekcji: `update($e, e.setAgentLemma(injected))` w KIE; `rule 'PASSIVE_AGENT_FAILED'`: gdy `PassiveAgentInjectCommand` AND `EventFact(agentLemma==null)` po 1 cyklu → `Violation('PASSIVE-AGENT-01','MEDIUM')`; `PassiveAgentDroolsPlugin.register(drl='passive-rules.drl')` = krok po `VoiceNeutralizer` w `AuditPipeline`; `PassiveAgentDroolsReport(injected_via_drools, failed_injection, PASSIVE-AGENT-01_count)` = raport?
- Jak połączyć walidację NKJP z regułami Drools — `rule 'NKJP_DOMAIN_MISMATCH'` w `nkjp-validation.drl`: `when $n: NKJPValidationFact(domainDelta > 0.20, dominantDomain==$e.getDomain()) $e: EventFact(domain!=$n.getDominantDomain()) then insert(new Violation('DOMAIN-DRIFT-01','LOW', 'Odchylenie domenowe: dokument '+$e.getDocId()+' vs NKJP '+$n.getDominantDomain()+ ' delta='+$n.getDomainDelta())); end`; `NKJPValidationFact(doc_id, dominant_domain, domain_delta:float)` = fakt tworzony przez `NKJPDocumentValidator.to_fact(result)->NKJPValidationFact`; `NKJPDroolsPlugin.load(drl='nkjp-validation.drl')` + `NKJPDroolsPlugin.register_facts(validation_results, kie_session)`; `rule 'NKJP_COVERAGE_LOW'`: gdy `NKJPValidationFact(classified_pct < 0.73)` → `Violation('NKJP-COV-01','LOW','Niskie pokrycie klasyfikacji NKJP: '+pct)`; `NKJP-COV-01`: nowy rule ID — niskie pokrycie klasyfikacji domenowej; `NKJPDroolsReport(domain_drift_count, coverage_low_count, validated_docs)` = raport?

### 4. Testowanie

- Jak napisać czerwony test TDD dla `InferenceEngine` — `"Wykonawca dostarczył dokumentację z opóźnieniem"` → `{cons: CONS-02, rule: naruszenie_terminu}`?
- Zdefiniujmy testy RED dla Klasyfikatora Intencji — `def test_question_detected(): assert IntentClassifier().classify('Jak działa system?') == 'QUESTION'`; `def test_requirement_detected(): assert IntentClassifier().classify('Należy dostarczyć SRS') == 'REQUIREMENT'`; `def test_fact_detected(): assert IntentClassifier().classify('Wykonawca dostarczył dokumentację') == 'ASSERT'`; wszystkie RED przed implementacją MODAL_WORDS?
- Jak testować wielowymiarowy model zdarzenia: AGENT + ACTION + PATIENT + INSTRUMENT + LOCATION + TIME?
- Jak testować negację: `"Wykonawca nie dostarczył dokumentacji"` → `dostawa=NOT_OCCURRED`?
- Jak testować łańcuch: zdanie 1 = "Wykonawca złożył ofertę", zdanie 2 = "Wykonawca dostarczył dokumentację z opóźnieniem" → wnioskowanie o naruszeniu?
- Uruchommy testy integracyjne dla całego pipeline'u z klasyfikatorem — `def test_full_pipeline_with_intent_classifier(): clf = IntentClassifier(MorphClassifier(morph_stub), LexicalClassifier()); mapper = SemanticMapper(intent_clf=clf); frame = mapper.process('Wykonawca musi dostarczyć SRS'); assert frame.speech_act == 'REQUIREMENT'`; test integracyjny łączy Morfeusz→IntentClassifier→SemanticMapper→EventFrame w jednym call?
- Uruchommy testy integracyjne dla warstwy pragmatycznej — `@pytest.mark.integration def test_pragmatic_layer_pipeline(): clf = IntentClassifier(MorphClassifier(morph_real), LexicalClassifier()); speech_acts = [clf.classify(s) for s in PRAGMATIC_CORPUS]; assert speech_acts.count('REQUIREMENT') >= 3; assert speech_acts.count('QUESTION') >= 2`; `PRAGMATIC_CORPUS` = 10 zdań kontraktowych + pytań; walidacja rozkładu intencji jako test własnościowy warstwy pragmatycznej; RED bo `morph_real` wymaga uruchomionego Morfeusza?
- Jak testować zapytania o lokalizację: `"Gdzie jest dokumentacja?"` → przeszukanie grafu → odpowiedź?
#### Kompletna hierarchia TDD
- Zaimplementuj Fazę GREEN dla `InferenceEngine` — minimalna reguła: IF AGENT=Wykonawca AND action=dostarczyć AND MANNER=opóźnienie THEN AuditFinding(CONS-02).
- Jak zrefaktoryzować `InferenceEngine` po GREEN — zastąpić hardkodowaną regułę deklaratywnym Drools DRL?
- Zrefaktoryzuj `InferenceEngine` — każda reguła compliance jako osobny plik DRL załadowany przez `RuleLoader`, testowalny w izolacji.
- Jak napisać test jednostkowy dla pojedynczej reguły DRL — mock grafu W4, sprawdzić że jedna reguła odpala się na właściwych faktach?
- Jak napisać test integracyjny W4→W5: załaduj rzeczywiste fakty z Neo4j → sprawdź że `InferenceEngine` generuje `AuditFinding(CONS-02)`?
- Jak zbudować oracle dataset dla W5 — 30 zdań kontraktowych + oczekiwane `AuditFinding[]` per zdanie?
- Jak zmierzyć Mutation Score dla reguł DRL — jak mutować warunki reguły żeby sprawdzić czy testy to wykrywają?
- Jak napisać test własnościowy (Hypothesis) dla `InferenceEngine` — idempotentność: to samo zdarzenie wnioskowane 2× → 1 `AuditFinding` (nie duplikat)?
- Jak wykryć regresję reguł: zmiana hierarchii synsetów W3 może sprawić że CONS-02 przestaje odpalać — baseline snapshot wyników per corpus?
- Stwórz test regresyjny `InferenceEngine` — golden file: 20 zdarzeń kontraktowych + oczekiwane reguły które mają się odpalić; CI fail przy rozbieżności.
- Jak przetestować W1→W5 end-to-end — dokument z 3 naruszeniami → sprawdź że `AuditFinding[]` ma dokładnie 3 wyniki z właściwymi `rule_id`?

### 5. Obsługa błędów

- Co robi `InferenceEngine` gdy EventRoleDict nie ma AGENT (elipsa podmiotu)?
- Jak obsługiwać sprzeczne reguły (reguła A i reguła B dają przeciwne wyniki)?
- Co gdy Drools nie może załadować pliku `.drl` (syntax error)?
- Jak obsługiwać nieskończoną pętlę dedukcji (A → B → A)?
- Jak logować, które reguły zostały aktywowane i w jakiej kolejności?
- Co robi `InferenceEngine` gdy plik `.drl` ma błąd składni Drools — pada całe ładowanie czy tylko zepsuta reguła jest pomijana?
- Jak obsługiwać nieskończoną pętlę reguł (reguła A aktywuje B, B aktywuje A)?

### 6. Integracja z innymi warstwami

- Jak W5 pobiera dane z Neo4j (W4) — czy `InferenceEngine` ma własny `Neo4jAdapter`?
- Jak W5 dostaje `EventRoleDict` z W2 — bezpośrednio czy przez W4?
- Jak W3 (Słowosieć, hiperonimia) zasila reguły W5 — "wróbel IS_A ptak" → reguła dla ptaków?
- Jak W6 (koreferencja) rozwiązuje zaimki ZANIM W5 dostanie EventRoleDict?
- Jak W7 (FastAPI) wywołuje W5 — synchronicznie czy async?

### 7. Pułapki i ryzyka

- **Pułapka 1:** Drools wymaga JVM — niekompatybilność z czystym Python stackiem. Alternatywa: Python rule engine (experta, nools-py) lub własny IF-THEN runner.
- **Pułapka 2:** Eksplozja reguł DRL przy >500 regułach powoduje czas kompilacji >30s. Konieczne: agenda groups, rule salience, partial evaluation.
- **Pułapka 3:** StateMatrix bez "zamrażania wniosków" powoduje wielokrotne re-inferowanie tych samych faktów — każde nowe zdanie re-triggeruje wszystkie reguły.
- **Pułapka 3:** Drools `KieSession` nie jest thread-safe — każdy wątek musi mieć własną sesję; reużycie sesji między wątkami powoduje `ConcurrentModificationException`.
- **Pułapka 4:** Reguły DRL bez jawnego `salience` — gdy dwie reguły mają ten sam priorytet, kolejność aktywacji zależy od kolejności ładowania plików `.drl`; wyniki mogą być niedeterministyczne.
- **Pułapka 5:** `Working Memory` akumuluje fakty bez `retract()` — przy batch processingu sesja rośnie w pamięci bez ograniczeń.
- **Pułapka 6:** IntentClassifier wytrenowany na jednej domenie — F1 może spaść z 0.90 do 0.45 przy zmianie domeny bez fine-tuningu.

## Kryteria akceptacji

| Metryka | Minimum |
|---|---|
| Czas wnioskowania dla 1 zdania (1000 reguł) | < 100 ms |
| Czas wnioskowania batch 100 zdań | < 10 s |
| Precision klasyfikacji naruszenia terminowego (CONS-02) | ≥ 95% |
| F1 detekcji intencji QUESTION vs ASSERT | ≥ 90% |
| Pokrycie testów linii | ≥ 85% |

## Pytania o idempotentność i deterministyczność

- Czy `InferenceEngine.infer(event)` wywołane 3× na identycznym evencie daje identyczny `InferenceResult`?
- Czy kolejność reguł DRL ma wpływ na końcowy wynik?
- Jak zapewnić deterministyczność przy równoległym przetwarzaniu zdań?

## Pytania o migrację i wersjonowanie

- Jak aktualizować reguły `.drl` bez przerywania działania serwisu (hot reload)?
- Jak wersjonować reguły — git tag per wersja reguł, changelog zmian logiki?
- Jak zachować backwards-compatibility gdy zmienia się schemat `InferenceResult`?

## Pytania o audytowalność

- Jak logować pełny ślad wnioskowania: "reguła X aktywowana bo warunek Y spełniony przez fakt Z"?
- Jak przechowywać activation trace per zdanie dla celów dowodowych (odpowiedzialność cywilna)?
- Jak wygenerować raport "dlaczego system sklasyfikował zdarzenie jako naruszenie_zobowiązania"?

---

## Rozszerzalność i skalowanie

### Stopniowe dodawanie reguł wnioskowania

- Jak dodać nową regułę dedukcji bez recompilacji całego zbioru reguł?
- Jak hot-reload reguł Drools bez restartu serwisu (KieScanner, incremental update)?
- Jak zaimplementować `register_rule(name, condition, action)` — dynamiczne reguły Python?
- Jak testować nową regułę w izolacji, nie uruchamiając wszystkich pozostałych?
- Jak mierzyć coverage reguł — ile % zdarzeń z testowego korpusu aktywuje przynajmniej 1 regułę?

### Stopniowe dodawanie domen (prawna → medyczna → wojskowa)

- Jak zorganizować reguły DRL per domena — osobne pliki `.drl` czy jeden globalny?
- Jak zaimplementować `load_domain(name)` — lazy loading reguł domenowych?
- Jak wykrywać konflikty między regułami z różnych domen (reguła prawna vs medyczna dają sprzeczne klasyfikacje)?
- Jak testować, że dodanie domeny medycznej nie zmienia wyników dla domeny prawnej?
- Jak stopniowo rozszerzać `StateMatrix` o nowe typy stanów domenowych?

### Skalowanie liczby reguł

- Jakie są progi wydajności Drools dla 100 / 1000 / 10000 reguł — czas kompilacji i czas ewaluacji?
- Jak zarządzać eksplozją reguł przez Agenda Groups (partycjonowanie per domena)?
- Jak zaimplementować rule salience — priorytetowanie reguł bezpieczeństwa nad regułami informacyjnymi?
- Jak testować, że nowe reguły nie powodują nieskończonej pętli dedukcji?
- Jak profilować, które reguły są najczęściej aktywowane (reguła → liczba aktywacji)?

### Inkrementalne uczenie się

- Jak system uczy się nowych wzorców z corpus feedback — semi-supervised rule induction?
- Jak zaimplementować `suggest_rule(false_negative_examples)` — propozycja nowej reguły?
- Jak weryfikować automatycznie wygenerowane reguły przed wdrożeniem (human-in-the-loop)?

---

## Luki zidentyfikowane przez audyt cross-warstwowy

### Powiadamianie W5 o zmianach ontologii W3 (brak mechanizmu)

- Jak `InferenceEngine` dostaje powiadomienie o nowym synsecie dodanym do W3 bez restartu serwisu?
- Jak zaimplementować `on_lexicon_update(event)` — callback wywoływany gdy W3 dodaje nowy leksem/synset?
- Jakie reguły Drools trzeba recompilować po dodaniu nowego synset, a jakie są odporny na tę zmianę?
- Jak testować, że nowy synset ze Słowosieci (np. neologizm "hackować" IS_A "atakować") jest od razu dostępny dla reguł W5?
- Jak izolować "gorące" reguły (zależne od ontologii W3) od "zimnych" (czysto składniowych) żeby minimalizować zakres recompilacji?

### Brakująca decyzja: W3 before/inside W5

- Czy W5 wywołuje W3 bezpośrednio (np. `SlowosiecAdapter.get_hypernyms(lemma)`) czy dostaje już wzbogacony `EnrichedToken`?
- Jak zdefiniować interfejs `IOntologyProvider` który W5 przyjmuje jako dependency — niezależnie od tego czy to W3 czy wbudowana ontologia?
- **EXISTS↗ `VoiceNormalizationDroolsPlugin`** — Drools triggeruje `VoiceNormalizationCommand` gdy `token.voice=='passive' && token.agent==null`; priorytet `AGENT_INJECT_PRIORITY=100` (przed regułami rolowymi); pipeline: `PassiveDetected → InjectVirtualAgent(StateMatrix.last_actor) → RecomputeEventFrame → update()`; `DroolsVoiceNormalizationReport(normalized:int, fallback_used:int, ci_pass_threshold:0.90)`.
- **EXISTS↗ `NKJPCoverageThresholdMatrix`** — domain-specific thresholds dla NKJPValidationFact: `{it: 0.73, legal: 0.68, medical: 0.80}`; Drools: `when NKJPValidationFact(coverage < threshold_for_domain) then insert new RuleViolation('NKJP-COV-02', domain, delta)`; `NKJPCoverageThresholdMatrix(domain_thresholds:Map<domain,float>, update_from_corpus_release:callable)`; commit-gate: raport musi mieć `all_domains_pass=True`.
- **EXISTS↗ `EllipsisDefaultSubjectPlugin`** + **rule `ELLIPSIS-SUBJ-01`** — `when EllipsisFact(resolution_source=='StateMatrix', confidence < 0.80) then insert RuleViolation('RECON-02', sent_id, actor_candidate)`; `EllipsisDefaultSubjectPlugin` = resolve do `StateMatrix.last_active_actor()`, fallback do `DomainDefaultAgent('system')`; `DefaultSubjectReport(resolved_pct:float, fallback_to_domain_agent_pct:float, recon02_count:int)`.
- **EXISTS↗ `AllReduceInlineFilter`** — filtrowanie błędnych ścieżek w locie: `reduce(valid, [], p -> CASE WHEN p.confidence > 0.70 AND p.depth <= $max_depth THEN valid + [p] ELSE valid END)` inline w Cypher; `AllReduceInlineFilter(min_confidence:0.70, max_depth:6)` → zwraca wyłącznie semantycznie spójne łańcuchy; `FilteredPathReport(total:int, filtered_out:int, avg_confidence_remaining:float)`; pipeline: po `allReduce` → przekazanie do `CausalReasoningEngine` tylko paths z `filtered_out_pct < 0.30`.
- **EXISTS↗ `CataphoraResolutionProcedure`** — implementacja Python krok po kroku: `class CataphoraDelayedResolver: __init__(self, memory_window=5)` → `self.queue = deque(maxlen=5)`; `resolve_on_antecedent_found(cataphora_id, antecedent)` → `StateMatrix.update(cataphora_id, antecedent)`; `confidence_decay_per_sent=0.05` (po 5 zdaniach bez antecedenta → fallback `DomainDefaultAgent`); `CataphoraResolutionProcedure.to_neo4j(session)` → `MERGE (:Cataphora {id})-[:RESOLVED_BY {delay_sents}]->(:Concept {lemma: antecedent})`.
- **EXISTS↗ `DroolsNestedConditionSupport`** — warunki zagnieżdżone AND/OR w Drools 9: `rule 'nested-and-or' when (IntentFact(type=='OBLIG') and (TokenFact(case=='Gen') or TokenFact(negated==true))) then insert new RuleViolation('NEG-COND-03', sent_id)`; `NestedConditionBuilder.and_block([c1, c2]).or_block([c3])` = fluent DSL; `DroolsNestedConditionReport(rules_with_nesting:int, max_depth:int, avg_evaluation_ms:float)`; ograniczenie: `max_nesting_depth=4` (powyżej → rozbij na sub-reguły).
- **NEW `MorfeuszDroolsFact`** + **`MorfeuszDroolsPlugin`** — integracja Morfeusz→Drools: `MorfeuszDroolsFact(form:str, lemma:str, tag:str, case:str, negated:bool)` jako POJO wstrzykiwany do KIE session; `MorfeuszDroolsPlugin.inject_facts(morfeusz_output:List[MorphToken], kie_session)` = `ksession.insert(MorfeuszDroolsFact(...))` dla każdego tokena; reguła wzorcowa: `when MorfeuszDroolsFact(case=='Gen', negated==true) then insert PatientFact(lemma)`; `MorfeuszDroolsIntegrationReport(facts_injected:int, rules_fired:int, avg_latency_ms:float)`.
- **EXISTS↗ `QPPSemanticComplexityOptimizer`** — QPP dla złożonych powiązań: `MATCH p=(:Concept)-[r:AGENT|PATIENT|THEME*2..6 {confidence:>0.65}]->(:Event) WHERE ALL(n IN nodes(p) WHERE n.domain = $domain) RETURN p LIMIT 200`; `QPPSemanticComplexityOptimizer(min_conf:0.65, max_hops:6, domain_filter:str)` = query builder; `INDEX FOR ()-[r:AGENT]->() ON (r.confidence)`; `QPPOptimizationReport(avg_query_ms, paths_returned, index_hit_rate:float)` — target `avg_query_ms < 80`.
- **EXISTS↗ `PearlCausalHallucinationFilter`** — grafy Pearla eliminują halucynacje: `PearlCausalHallucinationFilter(causal_graph)` → każde wnioskowanie musi mieć ścieżkę `do-calculus`: `P(Effect | do(Cause))` weryfikowaną w grafie; jeśli `EventFrame.predicate` nie ma przodka w grafie przyczynowym → `HallucinationViolation('CAUSAL-GHOST-01', predicate, confidence:0.0)`; `CausalHallucinationReport(ghost_events:int, verified_events:int, hallucination_rate:float)` — target `hallucination_rate < 0.05`; integracja z `CausalReasoningEngine`.
- **NEW `TechnicalNeologismFallback`** — wzorzec Fallback dla neologizmów technicznych: `TechnicalNeologismFallback(strategies=[MORFEUSZ_GUESS, HYPERNYM_LOOKUP, DOMAIN_DEFAULT])` → (1) Morfeusz: analiza morfologiczna nieznanego tokenu (np. `'konteneryzacja'` → `subst:nom:sg:f`); (2) Słowosieć hypernym lookup od najbliższego rodzica; (3) fallback `DomainDefaultConcept(domain='IT', type='PROCESS')`; `NeologismFallbackReport(resolved_by_morfeusz_pct, resolved_by_hypernym_pct, fallback_to_default_pct:float)`; rule: `when NeologismFact(strategy=='DEFAULT') then insert RuleViolation('NEOL-01', token)`.
- **EXISTS↗ `PearlDecisionAuditTrail`** — audyt decyzji przez grafy Pearla w DocLinter: `PearlDecisionAuditTrail(causal_graph)` → dla każdej reguły audytowej: `AuditDecision(rule_id, fired:bool, causal_path:List[Node], do_calculus_result:float)`; zapis w Neo4j: `(:AuditDecision)-[:BASED_ON_CAUSAL_PATH]->(:CausalChain)`; `DecisionAuditReport(decisions:int, causally_verified_pct:float, ghost_decisions:int)`; explainability: `DocLinterExplainer.explain(rule_id)` → zwraca łańcuch przyczynowy w Mermaid; DocLinter spełnia wymóg XAI (Explainable AI) dla audytu IT.
- **EXISTS↗ `PolishAmbiguityResolutionPipeline`** — wieloznaczność PL w DocLinter: `PolishAmbiguityResolutionPipeline` = 4 etapy: (1) Morfeusz → `MorfeuszAmbiguityStateMatrix` — wieloznaczność morfologiczna; (2) Słowosieć → `WordNetWSDIntegrator` — polisemia leksykalna; (3) `StateMatrixMorfeuszGuard` — konflikt form fleksyjnych; (4) `TechnicalNeologismFallback` — nieznane terminy; `AmbiguityResolutionSummary(morph_resolved_pct, wsd_resolved_pct, neologism_fallback_pct, unresolved_pct:float)` — target `unresolved_pct < 0.03`.
- **EXISTS↗ `CausalReasoningDroolsRuleset`** — reguły Drools dla wnioskowania przyczynowego: `rule 'causal-chain-fire' when CausalFact(cause_id==$id, confidence >= 0.75) and EventFact(id==$id, resolved==true) then insert EffectFact(effect_id, propagated_confidence: cause.conf * 0.9)`; `CausalRuleChain` = ciąg reguł z salience malejącym (100→10); `DroolsCausalReport(chains_fired:int, avg_propagation_depth:float, low_confidence_chains:int)`; integracja z `PearlCausalHallucinationFilter` — reguły Drools wyzwalają walidację do-calculus.
- **EXISTS↗ `DocLinterHallucinationDefenseStack`** — wielowarstwowa obrona przed halucynacjami: warstwa 1 `PearlCausalHallucinationFilter` (do-calculus); warstwa 2 `ConflictFreezer` (zamrożenie sprzecznych wniosków); warstwa 3 `NKJPCoverageThresholdMatrix` (weryfikacja korpusowa); warstwa 4 `AllReduceInlineFilter` (filtr ścieżek < 0.70 confidence); `HallucinationDefenseReport(layer1_blocked:int, layer2_frozen:int, layer3_flagged:int, layer4_filtered:int, total_hallucination_rate:float)` — target `total_hallucination_rate < 0.02`.
- **EXISTS↗ `CorefKnowledgeGraphResolver`** — koreferencja zaimkowa w grafie wiedzy (zduplikowane pytania 6+10 → jeden kąt: graph-native): `CorefKnowledgeGraphResolver` = zamiast listy mention-spans → bezpośredni zapis w Neo4j: `MERGE (:Mention {form, sent_id})-[:REFERS_TO {confidence}]->(:Concept {lemma})`; query scalający: `MATCH (m:Mention)-[:REFERS_TO]->(c:Concept) WITH c, collect(m.form) AS aliases RETURN c.lemma, aliases`; `CorefKnowledgeGraphReport(mentions_resolved:int, graph_chains:int, avg_chain_length:float)` — teksty techniczne: avg_chain_length ≈ 4.2.
- **EXISTS↗ `SemanticDimensionRoadmapWeek11`** — wymiary semantyczne tygodnia 11: planowane rozszerzenia: (1) `TemporalRelationDimension` — `BEFORE/AFTER/DURING` w pełni zintegrowane z `SpatioTemporalRelationMapper`; (2) `CausalStrengthDimension` — `P(Effect|do(Cause))` jako atrybut krawędzi `[:CAUSES {strength:float}]`; (3) `IntentPolarityDimension` — pozytywna/negatywna/neutralna intencja aktu mowy; (4) `OntologyVersionDimension` — wersjonowanie Słowosieć per węzeł; `SemanticDimensionRoadmap(week:11, planned_dims:4, estimated_new_rule_ids:6, breaking_changes:0)`.
- **EXISTS↗ `QARCArchitectureSpec`** — architektura QARC dla kompresji wiedzy: `QARCArchitectureSpec` = pipeline 4-etapowy: (1) `SpeechActClassifier` → typ aktu mowy; (2) `QARCDimensionExtractor(Q,A,R,C)` → wypełnienie 4 pól; (3) `QARCKnowledgeCompressor` → `KnowledgeAtom` (78% redukcja); (4) `KnowledgeAtomStore.deduplicate()` → unikalne atomy z licznikiem wystąpień; walidacja: `QARCValidator.check_completeness(atom)` → `COMPLETENESS_SCORE = filled_dims / 4`; reguła: `when COMPLETENESS_SCORE < 0.5 then insert RuleViolation('QARC-INCOMPLETE-01', atom_id)`.
- **EXISTS↗ `Cypher25PathOptimizationRules`** — reguły Cypher 25 optymalizujące ścieżki: (1) `QPP {m..n}` zamiast `UNION` — jeden plan zamiast n; (2) `ALL(n IN nodes(p) WHERE n.domain=$d)` — early-termination po pierwszym false; (3) `INDEX FOR ()-[r:AGENT]->() ON (r.confidence)` — relationship index eliminuje full-scan; (4) `LIMIT` przed `ORDER BY` gdy `TOP-K`; (5) `PROFILE` → weryfikacja `db hits` per hop; `PathOptimizationChecklist.verify(cypher)` → `PathOptReport(rule_violations:List, estimated_speedup_x:float, index_recommendations:List)`.
- **NEW `ThemeRhemeGraphUpdater`** — struktura Temat-Rema a aktualizacja grafu: `ThemeRhemeGraphUpdater` = Temat (Given) → istniejący węzeł w grafie (`MATCH`); Rema (New) → nowy węzeł lub atrybut (`MERGE/SET`); algorytm: Morfeusz + pozycja zdania → token na pozycji 0-1 = Temat, reszta = Rema; Cypher: `MATCH (t:Concept {lemma:$theme}) MERGE (r:Concept {lemma:$rheme}) MERGE (t)-[:INTRODUCES {sent_id:$sid, position:'RHEME'}]->(r)`; `ThemeRhemeReport(theme_matched_pct:float, new_rheme_nodes:int, graph_growth_rate:float)` — metryka `graph_growth_rate` = nowe węzły per zdanie.
- **EXISTS↗ `Cypher25DeterminismSuite`** — kompletny zestaw reguł determinizmu: `Cypher25DeterminismSuite` = `Cypher25DeterminismRules` (5 zasad) + `Cypher25PathOptimizationRules` (5 reguł ścieżek) + `Cypher25IdempotentMergeTemplate` (ON CREATE/MATCH) + `GQLStatusErrorHandler` (null-safety) + `GQLSTATUS22N31Handler` (NULL_ELEMENT_ELIMINATION); `DeterminismSuiteRunner.run_all(cypher_dir)` → `DeterminismSuiteReport(total_checks:int, violations:int, auto_fixed:int, manual_needed:int)` — CI gate: `violations == 0 AND manual_needed == 0`.
